### 无穷限积分

#### 定义 1：无穷限积分

设 $f$ 在 $[a, +\infty)$ 上有定义，且 $\forall B > a, f \in R[a, B]$，且极限 $\displaystyle\lim_{B \to +\infty} \int_a^B f(x) \text{d} x$ 存在，则称其值为 $f$ 在 $[a, +\infty)$ 上的无穷限积分，记作：

$$
\int_a^{+\infty} f(x) \text{d} x \triangleq \lim_{B \to +\infty} \int_a^B f(x) \text{d} x
$$

此时称无穷限积分 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛；否则，称 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 发散。

- 注意：无穷限积分收敛不能写成 $f \in R[a, +\infty)$！

同理可以定义无穷限积分 $\displaystyle\int_{-\infty}^a f(x) \text{d} x$。

设 $f$ 在 $\mathbb{R}$ 上有定义，且 $\forall a < b, f \in R[a, b]$，且 $\exists a \in \mathbb{R}, \text{s.t. } \displaystyle\int_{-\infty}^a f(x) \text{d} x, \int_a^{+\infty} f(x) \text{d} x$ 均收敛，则称其和为 $f$ 在 $(-\infty, +\infty)$ 上的无穷限积分，记作：

$$
\int_{-\infty}^{+\infty} f(x) \text{d} x \triangleq \int_{-\infty}^a f(x) \text{d} x + \int_a^{+\infty} f(x) \text{d} x
$$

易知该定义与 $a$ 的选取无关。

------

类比有限区间上的 Riemann 积分，我们有如下基本结论。

#### 定理 2：广义 Newton-Leibniz 公式

内容：已知 $\forall B > a, f \in R[a, B]$，若 $f$ 有原函数 $F$（即 $F' = f$），则：

$$
\int_a^{+\infty} f(x) \text{d} x = \lim_{x \to +\infty} F(x) - F(a) \triangleq F(+\infty) - F(a) \triangleq \left. F(x) \right|_a^{+\infty}
$$

- 即 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 的敛散性与 $\displaystyle\lim_{x \to +\infty} F(x)$ 一致。
- 另外两种无穷限积分也有相似的结论和记号；后面的定理也是一样，从略。

[例 1] 求 $\displaystyle\int_{-\infty}^{+\infty} \frac{\text{d} x}{1 + x^2}$。

$$
\begin{aligned}
\int_{-\infty}^{+\infty} \frac{\text{d} x}{1 + x^2} &= \left. \arctan x \right|_{-\infty}^{+\infty} \\
&= \frac{\pi}{2} - (-\frac{\pi}{2}) \\
&= \pi
\end{aligned}
$$

[例 2] 讨论 $\displaystyle\int_1^{+\infty} \frac{1}{x^p} \text{d} x \ (p \in \mathbb{R})$ 的敛散性。

解：

- 当 $B > 1$，有：

$$
\int_1^B \frac{1}{x^p} \text{d} x = \begin{cases}
\ln B, &\quad p = 1 \\
\dfrac{1}{1 - p} (B^{1 - p} - 1), &\quad p \neq 1
\end{cases}
$$

- 进而易知当 $p > 1$ 时，该无穷限积分收敛，其值为 $\dfrac{1}{p - 1}$；否则，该无穷限积分发散。

#### 定理 3：分部积分法

内容：设 $\forall B > a$，$u, v$ 在 $[a, B]$ 上可导，$u', v' \in R[a, B]$，则：

$$
\int_a^{+\infty} u(x) v'(x) \text{d} x = \left. u(x) v(x) \right|_a^{+\infty} - \int_a^{+\infty} u'(x) v(x) \text{d} x 
$$

[例 3] 求 $\displaystyle\int_1^{+\infty} \frac{\arctan x}{x^3} \text{d} x$。

$$
\begin{aligned}
\int_1^{+\infty} \frac{\arctan x}{x^3} \text{d} x &= -\frac{1}{2} \int_1^{+\infty} \arctan x \text{d} \left( \frac{1}{x^2} \right) \\
&= -\frac{1}{2} \left( \left. \frac{\arctan x}{x^2} \right|_1^{+\infty} - \int_1^{+\infty} \frac{1}{x^2 (x^2 + 1)} \text{d} x \right) \\
&= -\frac{1}{2} \left( -\frac{\pi}{4} - \left. \left( -\frac{1}{x} - \arctan x \right) \right|_1^{+\infty} \right) \\
&= -\frac{1}{2} \left( -\frac{\pi}{4} - \left( \left( -\frac{\pi}{2} \right) - \left( -1 - \frac{\pi}{4} \right) \right) \right) \\
&= \frac{1}{2}
\end{aligned}
$$

#### 定理 4：换元法 I

内容：已知 $\forall B > a, f \in R[a, B]$，设 $\varphi$ 在 $[\alpha, \beta)$ 可导、$\varphi' \in R[\alpha, \beta)$ 且**严格单调递增**（其中 $\beta \in (\alpha, +\infty]$）且 $\displaystyle\lim_{t \to \beta^-} \varphi(t) = +\infty$，若 $\varphi(\alpha) = a, \displaystyle\lim_{x \to \beta^-} \varphi(x) = +\infty$，则：

$$
\int_a^{+\infty} f(t) \text{d} t = \int_{\alpha}^{\beta} f(\varphi(x)) \varphi'(x) \text{d} x
$$

证明：

- 任取 $\beta' \in (\alpha, \beta)$，由通常的换元法可知：

$$
\int_a^{\varphi(\beta')} f(t) \text{d} t = \int_{\alpha}^{\beta'} f(\varphi(x)) \varphi'(x) \text{d} x
$$

- 令 $\beta' \to \beta^-$，则 $\varphi(\beta') \to +\infty$，替换上界即可得证。

[例 4] 讨论 $\displaystyle\int_e^{+\infty} \frac{1}{x \ln^p x} \text{d} x \ (p \in \mathbb{R})$ 的敛散性。

解：

$$
\begin{aligned}
\int_e^{+\infty} \frac{\text{d} x}{x \ln^p x} &= \int_e^{+\infty} \frac{1}{\ln^p x} \text{d} (\ln x) \\
&= \int_1^{+\infty} \frac{1}{t^p} \text{d} t &\quad (\text{换元 } t = \ln x) \\
\end{aligned}
$$

- 由例 2 可知：当 $p > 1$ 时，该无穷限积分收敛，其值为 $\dfrac{1}{p - 1}$；否则，该无穷限积分发散。

类似地，可以讨论 $\displaystyle\int_{e^e}^{+\infty} \frac{\text{d} x}{x \ln x \ln \ln^p x} \ (p \in \mathbb{R})$ 等的敛散性。

------

对普通的 Riemann 积分的换元法而言，我们并不要求 $\varphi$ 严格单调，因此考虑尝试去掉限制。

#### 定理 5：换元法 II

内容：已知 $\forall B > a, f \in R[a, B]$，设 $\varphi$ 在 $[\alpha, \beta)$ 可导（其中 $\beta \in (\alpha, +\infty]$）、$\varphi' \in R[\alpha, \beta)$ 且 $\displaystyle\lim_{t \to \beta^-} \varphi(t) = +\infty$，若 $\varphi(\alpha) = a, \displaystyle\lim_{x \to \beta^-} \varphi(x) = +\infty$，则：

$$
\int_a^{+\infty} f(t) \text{d} t = \int_{\alpha}^{\beta} f(\varphi(x)) \varphi'(x) \text{d} x
$$

证明：

- 记 $\gamma(B) = \displaystyle\sup \left\{ x \in [\alpha, \beta) \mid \varphi(x) = B \right\}$，则：

$$
\forall a \leq B_1 < B_2, \exists x_0 \in (\gamma(B_1), \beta), \text{s.t. } \varphi(x_0) = B_2 \Rightarrow \gamma(B_2) \geq x_0 > \gamma(B_1)
$$

- 故 $\gamma$ 在 $[a, +\infty)$ 上严格单调递增。由通常的换元法可知：

$$
\int_a^B f(t) \text{d} t = \int_{\alpha}^{\gamma(B)} f(\varphi(x)) \varphi'(x) \text{d} x
$$

- 令 $B \to +\infty$，由 $\gamma(B) < \beta$ 和 $B - \gamma(\varphi(B - \epsilon)) < \epsilon$ 可知 $\gamma(B) \to \beta^-$，替换上界即可得证。

#### 定理 6：无穷限积分的加法与数乘

内容：设 $\displaystyle\int_a^{+\infty} f(x) \text{d} x, \int_a^{+\infty} g(x) \text{d} x$ 收敛，则 $\forall \lambda, \mu \in \mathbb{R}, \displaystyle\int_a^{+\infty} (\lambda f(x) + \mu g(x)) \text{d} x$ 收敛，其值为 $\lambda \displaystyle\int_a^{+\infty} f(x) \text{d} x + \mu \int_a^{+\infty} g(x) \text{d} x$。

#### 定义 7：Cauchy 主值积分

设 $f$ 在 $\mathbb{R}$ 上有定义，若 $\forall B > 0, f \in R[-B, B]$，且极限 $\displaystyle\lim_{B \to +\infty} \int_{-B}^B f(x) \text{d} x$ 存在，则称其值为 $f$ 的 Cauchy 主值积分，记作：

$$
\text{(V.P.)} \int_{-\infty}^{+\infty} f(x) \text{d} x \triangleq \lim_{B \to +\infty} \int_{-B}^B f(x) \text{d} x
$$

### 无穷限积分的敛散性判别

#### 定理 1：Cauchy 准则

内容：设 $f$ 在 $[a, +\infty)$ 上有定义，且 $\forall B > a, f \in R[a, B]$，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛的充要条件为：

$$
\forall \epsilon > 0, \exists \Delta \geq a, \text{s.t. } \forall \Delta < x_1 < x_2, \left| \int_{x_1}^{x_2} f(x) \text{d} x \right| < \epsilon
$$

[例 5] 求证：$\displaystyle\int_0^{+\infty} x \sin^8 x \text{d} x$ 发散。

证明：

$$
\begin{aligned}
\forall n \in \mathbb{N}, \int_{n \pi}^{(n + 1) \pi} x \sin^8 x \text{d} x & \geq n \pi \int_{n \pi}^{(n + 1) \pi} \sin^8 x \text{d} x \\
&\geq n \pi \int_{n \pi + \frac{1}{6} \pi}^{n \pi + \frac{5}{6} \pi} \sin^8 x \text{d} x \\
&\geq n \pi \int_{n \pi + \frac{1}{6} \pi}^{n \pi + \frac{5}{6} \pi} \frac{1}{256} \text{d} x \\
&= \frac{n \pi^2}{384}
\end{aligned}
$$

- 令 $\epsilon_0 = \dfrac{\pi^2}{384}$，则 $\forall \Delta \geq 0$，令 $x_1 = \left( \lfloor \dfrac{\Delta}{\pi} \rfloor + 1 \right) \pi, x_2 = x_1 + \pi$，则 $\displaystyle\left| \int_{x_1}^{x_2} f(x) \text{d} x \right| = \left( \lfloor \dfrac{\Delta}{\pi} \rfloor + 1 \right) \frac{\pi^2}{384} \geq \epsilon_0$。
- 由 Cauchy 判别法可知原积分发散。

#### 定义 2：绝对收敛与条件收敛

设 $f$ 在 $[a, +\infty)$ 上有定义，且 $\forall B > a, f \in R[a, B]$。

- 若 $\displaystyle\int_a^{+\infty} |f(x)| \text{d} x$ 收敛，则称 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 绝对收敛。
- 若 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛但 $\displaystyle\int_a^{+\infty} |f(x)| \text{d} x$ 发散，则称 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 条件收敛。

若 $f$ 在 $[A, +\infty)$ 上不变号，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛与绝对收敛等价。

#### 定理 3：绝对收敛则收敛

内容：若 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 绝对收敛，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛。

证明：由 Cauchy 判别法易得。

#### 定理 4：非负函数的收敛条件

内容：设 $f$ 在 $[a, +\infty)$ 上非负，且 $\forall B > a, f \in R[a, B]$，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛当且仅当 $F(B) = \displaystyle\int_a^B f(x) \text{d} x$ 在 $[a, +\infty)$ 上有界。

#### 定理 5：比较判别法

内容：

- 设 $f, g$ 在 $[a, +\infty)$ 上非负，且 $\forall B > a, f, g \in R[a, B]$，若 $\exists c > 0, \exists M \geq a, \text{s.t. } \forall x > M, f(x) \leq cg(x)$，则：
- (1) $\displaystyle\int_a^{+\infty} g(x)$ 收敛 $\Rightarrow \displaystyle\int_a^{+\infty} f(x)$ 收敛。
- (2) $\displaystyle\int_a^{+\infty} f(x)$ 发散 $\Rightarrow \displaystyle\int_a^{+\infty} g(x)$ 发散。

进一步地，可以给出下面的极限形式的比较判别法。

考察广义极限 $l = \displaystyle\lim_{x \to +\infty} \frac{f(x)}{g(x)} \in [0, +\infty]$，则：

- (1) 当 $l = 0$，$\displaystyle\int_a^{+\infty} g(x)$ 收敛 $\Rightarrow \displaystyle\int_a^{+\infty} f(x)$ 收敛。
- (2) 当 $0 < l < +\infty$，$\displaystyle\int_a^{+\infty} f(x)$ 收敛 $\Leftrightarrow \displaystyle\int_a^{+\infty} g(x)$ 收敛。
- (3) 当 $l = +\infty$，$\displaystyle\int_a^{+\infty} g(x)$ 发散 $\Rightarrow \displaystyle\int_a^{+\infty} f(x)$ 发散。

则易得如下推论：

- (1) 若 $\displaystyle\lim_{x \to +\infty} f(x) \neq 0$，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 发散。
- (2) 若 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 发散且 $\displaystyle\lim_{x \to +\infty} f(x)$ 存在，则 $\displaystyle\lim_{x \to +\infty} f(x) = 0$。

[例 6] 讨论 $\displaystyle\int_e^{+\infty} \frac{1}{x^p \ln^q x} \text{d} x \ (p, q \in \mathbb{R})$ 的敛散性。

解：

- (1) 当 $p > 1$，有 $\displaystyle\lim_{x \to +\infty} \frac{\frac{1}{x^p \ln^q x}}{\frac{1}{x^{\frac{p + 1}{2}}}} = \lim_{x \to +\infty} \frac{1}{x^{\frac{p - 1}{2}} \ln^q x} = 0$，而 $\displaystyle\int_e^{+\infty} \frac{1}{x^{\frac{p + 1}{2}}} \text{d} x$ 收敛，故原积分收敛。
- (2) 当 $p = 1$，我们已在例 4 中讨论过：$q > 1$ 时收敛，$q \leq 1$ 时发散。
- (3) 当 $p < 1$，有 $\displaystyle\lim_{x \to +\infty} \frac{\frac{1}{x^p \ln^q x}}{\frac{1}{x}} = \lim_{x \to +\infty} \frac{x^{1 - p}}{\ln^q x} = +\infty$，而 $\displaystyle\int_e^{+\infty} \frac{1}{x} \text{d} x$ 发散，故原积分发散。

[例 7] 讨论 $\displaystyle\int_1^{+\infty} \left( \frac{1}{x^p} - \ln \left( 1 + \frac{1}{x^p} \right) \right) \text{d} x \ (p > 0)$ 的敛散性。

解：

- 当 $x \to +\infty$，有 $\ln \left( 1 + \dfrac{1}{x^p} \right) = \dfrac{1}{x^p} - \dfrac{1}{2x^{2p}} + o \left( \dfrac{1}{x^{2p}} \right)$，故：

$$
\lim_{x \to +\infty} \frac{\frac{1}{x^p} - \ln (1 + \frac{1}{x^p})}{\frac{1}{x^{2p}}} = 2
$$

- 则原积分的敛散性与 $\displaystyle\int_1^{+\infty} \frac{1}{x^{2p}}$ 一致，即当 $p \leq \dfrac{1}{2}$ 发散，当 $p > \dfrac{1}{2}$ 收敛。

#### 定理 6：绝对收敛积分的加法与数乘

内容：设 $\displaystyle\int_a^{+\infty} f(x) \text{d} x, \int_a^{+\infty} g(x) \text{d} x$ 绝对收敛，则 $\forall \lambda, \mu \in \mathbb{R}, \displaystyle\int_a^{+\infty} (\lambda f(x) + \mu g(x)) \text{d} x$ 绝对收敛。

[例 8] 求证：当 $a > 0$，有 $\displaystyle\int_0^{+\infty} P_n(x) e^{-ax} \text{d} x$ 绝对收敛，其中 $P_n(x)$ 为 $x$ 的 $n$ 次多项式。

证明：

- 由定理 6 可知：只需证明 $\forall n \in \mathbb{N}, \displaystyle\int_1^{+\infty} x^n e^{-ax} \text{d} x$ 绝对收敛。
- 由于 $\displaystyle\lim_{x \to +\infty} \frac{x^n e^{-ax}}{\frac{1}{x^2}} = \lim_{x \to +\infty} \frac{x^{n + 2}}{e^{ax}} = 0$，而 $\displaystyle\int_e^{+\infty} \frac{1}{x^2} \text{d} x$ 收敛，故 $\displaystyle\int_1^{+\infty} x^n e^{-ax} \text{d} x$ 绝对收敛，遂得证。

------

当非负连续函数 $f$ 的无穷限积分收敛，$\displaystyle\lim_{x \to +\infty} f(x)$ 是否一定存在？事实上不然—— $f$ 甚至可能是无界的。下面是一个例子。

[例 9] 下面给出的 $f \in C[1, +\infty)$ 无界，$\displaystyle\int_1^{+\infty} f(x) \text{d} x$ 收敛但 $\displaystyle\lim_{x \to +\infty} f(x)$ 不存在。

$$
f(x) = \begin{cases}
2^{n + 1} (1 - 4^{n + 1} (n - x)), &\quad x \in [n - \dfrac{1}{4^{n + 1}}, n), n \in \mathbb{N}_+ \\
2^{n + 1} (1 - 4^{n + 1} (x - n)), &\quad x \in [n, n + \dfrac{1}{4^{n + 1}}], n \in \mathbb{N}_+ \\
0, &\quad \text{otherwise}
\end{cases}
$$

#### 定理 7：Dirichlet 判别法

内容：

- 设 $f$ 在 $[a, +\infty)$ 上有定义，$\forall B > a, f \in R[a, B]$，且 $\exists M > 0, \text{s.t. } \forall B > a, \left| \displaystyle\int_a^B f(x) \text{d} x \right| \leq M$。
- 设 $g$ 在 $[a, +\infty)$ 上**单调**，且 $\displaystyle\lim_{x \to +\infty} g(x) = 0$。
- 则 $\displaystyle\int_a^{+\infty} f(x) g(x) \text{d} x$ 收敛。

证明：由 Cauchy 判别法易得。

[例 10] 讨论 $\displaystyle\int_1^{+\infty} \frac{\sin x}{x^{\alpha}} \text{d} x \ (\alpha > 0)$ 的敛散性和绝对收敛性。

解：

- 由于 $\sin x$ 在有界区间上的积分有界为 $2$，$\dfrac{1}{x^{\alpha}}$ 在 $[1, +\infty)$ 上单调递减且 $\displaystyle\lim_{x \to +\infty} \frac{1}{x^{\alpha}} = 0$，则原积分收敛。
- (1) 当 $\alpha > 1$，由于 $\left| \dfrac{\sin x}{x^{\alpha}} \right| \leq \dfrac{1}{x^{\alpha}}$，而 $\displaystyle\int_1^{+\infty} \frac{1}{x^{\alpha}}$ 收敛，则原积分绝对收敛。
- (2) 当 $\alpha \in (0, 1]$，有：

$$
\begin{aligned}
\forall n \in \mathbb{N}_+, \int_{n \pi}^{(n + 1) \pi} \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x &\geq \int_{n \pi}^{(n + 1) \pi} \frac{|\sin x|}{x} \text{d} x \\
&\geq \frac{1}{(n + 1) \pi} \int_{n \pi}^{(n + 1) \pi} |\sin x| \text{d} x \\
&= \frac{2}{(n + 1) \pi}
\end{aligned}
$$

- 若原积分绝对收敛，则：

$$
\begin{aligned}
\int_1^{+\infty} \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x &= \lim_{B \to +\infty} \int_1^B \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x \\
&= \int_1^{\pi} \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x + \lim_{n \to +\infty} \sum_{i = 2}^n \int_{(i - 1) \pi}^{i \pi} \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x \\
&\geq \int_1^{\pi} \left| \frac{\sin x}{x^{\alpha}} \right| \text{d} x + \lim_{n \to +\infty} \sum_{i = 2}^n \frac{2}{i \pi} \\
&= +\infty
\end{aligned}
$$

- 矛盾！故此时原积分条件收敛。

------

当 $g$ 不单调，Dirichlet 判别法不适用。下面是一个例子。

[例 11] 判断 $\displaystyle\int_1^{+\infty} \frac{\sin x}{x} \cdot \sin x \text{d} x$ 的敛散性。

解：

$$
\begin{aligned}
\int_1^{+\infty} \frac{\sin x}{x} \cdot \sin x \text{d} x &= \frac{1}{2} \int_1^{+\infty} \frac{1 - \cos 2x}{x} \text{d} x \\
&= \frac{1}{2} \left( \int_1^{+\infty} \frac{1}{x} \text{d} x - \int_1^{+\infty} \frac{\cos 2x}{x} \text{d} x \right)
\end{aligned}
$$

- 同例 10 可知后者收敛，但前者发散，故原积分发散。

#### 定理 8：乘非负周期函数不改变敛散性

内容：设 $f$ 在 $[a, +\infty)$ 上单调，非负函数 $g \in C[0, T] \ (T > 0)$ 取值不恒为 $0$，$g$ 以 $T$ 为周期，则 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛 $\Leftrightarrow \displaystyle\int_a^{+\infty} f(x) g(x) \text{d} x$ 收敛。

证明：

- (1) 当 $\displaystyle\lim_{x \to +\infty} f(x) = 0$ 不成立，显然 $\displaystyle\int_a^{+\infty} f(x) \text{d} x, \int_a^{+\infty} f(x) g(x) \text{d} x$ 皆不收敛。
- (2) 当 $\displaystyle\lim_{x \to +\infty} f(x) = 0$ 成立，不妨设 $f$ 单调递减，则 $f \geq 0$。
- $\Rightarrow$：令 $M = \displaystyle\max_{x \in [0, T]} g(x)$，则 $0 \leq f(x) g(x) \leq M f(x)$，由比较判别法即可得证。
- $\Leftarrow$：令 $S = \displaystyle\int_0^T g(x) \text{d} x > 0$，则：

$$
\begin{aligned}
\forall n \in \mathbb{N}_{\geq 2}, \int_a^{a + nT} f(x) \text{d} x &= \int_a^{a + T} f(x) \text{d} x + \sum_{i = 2}^n \int_{a + (i - 1) T}^{a + iT} f(x) \text{d} x \\
&\leq \int_a^{a + T} f(x) \text{d} x + \sum_{i = 2}^n T f(a + (i - 1) T) \\
&= \int_a^{a + T} f(x) \text{d} x + \frac{T}{S} \sum_{i = 2}^n f(a + (i - 1) T) \int_{a + (i - 2) T}^{a + (i - 1) T} g(x) \text{d} x \\
&\leq \int_a^{a + T} f(x) \text{d} x + \frac{T}{S} \sum_{i = 2}^n \int_{a + (i - 2) T}^{a + (i - 1) T} f(x) g(x) \text{d} x \\
&= \int_a^{a + T} f(x) \text{d} x + \frac{T}{S} \int_a^{a + (n - 1) T} f(x) g(x) \text{d} x \\
&\leq \int_a^{a + T} f(x) \text{d} x + \frac{T}{S} \int_a^{+\infty} f(x) g(x) \text{d} x
\end{aligned}
$$

- 于是 $\forall B > a, \displaystyle\int_a^B f(x) \text{d} x \leq \int_a^{a + \lceil \frac{B - a}{T} \rceil T} f(x) \text{d} x$ 有界，故 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛。

[例 12] 讨论 $\displaystyle\int_1^{+\infty} \cos(x^p) \text{d} x \ (p > 0)$ 的敛散性和绝对收敛性。

解：

$$
\begin{aligned}
\int_1^{+\infty} \cos(x^p) \text{d} x &= \int_1^{+\infty} \cos t \cdot \frac{1}{p} t^{\frac{1}{p} - 1} \text{d} t &\quad (\text{换元 } t = x^p) \\
&= \frac{1}{p} \int_1^{+\infty} t^{\frac{1}{p} - 1} \cos t \text{d} t
\end{aligned}
$$

- 同例 10 可知原积分收敛。
- 同例 10 或由定理 8（据 $\cos$ 的周期性和 $\displaystyle\int_1^{+\infty} t^{\frac{1}{p} - 1} \text{d} t$ 发散）可知原积分条件收敛。

[例 13] 讨论 $I = \displaystyle\int_1^{+\infty} \ln \left( 1 + \frac{\sin x}{x^p} \right) \text{d} x \ (p > 0)$ 的敛散性和绝对收敛性。

解：

$$
\begin{aligned}
I &= \int_1^{+\infty} \left( \frac{\sin x}{x^p} - \frac{\sin^2 x}{2x^p} + o \left( \frac{\sin^2 x}{2x^p} \right) \right)  \text{d} x \\
&= \int_1^{+\infty} \left( \frac{\sin x}{x^p} + \frac{\cos 2x}{4x^p} - \frac{1 + 4 \sin^2 x \cdot o(1)}{4x^{2p}} \right) \text{d} x \\
&= \int_1^{+\infty} \frac{\sin x}{x^p} \text{d} x + \frac{1}{4} \int_1^{+\infty} \frac{\cos 2x}{4x^p} \text{d} x - \frac{1}{4} \int_1^{+\infty} \frac{1 + 4 \sin^2 x \cdot o(1)}{x^{2p}} \text{d} x \\
&\triangleq I_1 + \frac{1}{4} I_2 - \frac{1}{4} I_3
\end{aligned}
$$

- $\exists A \geq 1, \text{s.t. } \forall x > A, \dfrac{1}{2} < 1 + 4 \sin^2 x \cdot o(1) < \dfrac{3}{2}$，则 $I_3$ 的敛散性与 $\displaystyle\int_1^{+\infty} \frac{1}{x^{2p}} \text{d} x$ 一致。
- (1) 当 $p \in \left( 0, \dfrac{1}{2} \right]$，$I_1, I_2$ 条件收敛，$I_3$ 发散，则 $I$ 发散。
- (2) 当 $p \in \left( \dfrac{1}{2}, 1 \right]$，$I_1$ 条件收敛，$I_2, I_3$ 绝对收敛，则 $I$ 条件收敛。
- (3) 当 $p > 1$，$I_1, I_2, I_3$ 绝对收敛，则 $I$ 绝对收敛。

#### 定理 9：Abel 判别法

内容：

- 设 $f$ 在 $[a, +\infty)$ 上有定义，$\forall B > a, f \in R[a, B]$，且 $\displaystyle\int_a^{+\infty} f(x) \text{d} x$ 收敛。
- 设 $g$ 在 $[a, +\infty)$ 上单调有界。
- 则 $\displaystyle\int_a^{+\infty} f(x) g(x) \text{d} x$ 收敛。

[例 14] 讨论 $\displaystyle\int_1^{+\infty} \frac{\sin x}{x^p} \cdot \arctan x \text{d} x \ (p > 0)$ 的敛散性。

解：由例 10 可知 $\displaystyle\int_1^{+\infty} \frac{\sin x}{x^p} \text{d} x$ 收敛，而 $\arctan x$ 在 $[1, +\infty)$ 上单调递增且有上界为 $\dfrac{\pi}{2}$，则由 Abel 判别法可知原积分收敛。
