- _部分内容参见上学期 gy 数分的笔记：[第十八讲](https://github.com/Leasier/Mathematical-Analysis-1/blob/main/%E7%AC%AC%E5%8D%81%E5%85%AB%E8%AE%B2%20%E5%AE%9A%E7%A7%AF%E5%88%86%E4%B8%8E%E5%8F%AF%E7%A7%AF%E6%9D%A1%E4%BB%B6.md)、[第十九讲](https://github.com/Leasier/Mathematical-Analysis-1/blob/main/%E7%AC%AC%E5%8D%81%E4%B9%9D%E8%AE%B2%20%E5%AE%9A%E7%A7%AF%E5%88%86%E7%9A%84%E8%AE%A1%E7%AE%97%E4%B8%8E%E5%BA%94%E7%94%A8.md)。_

### 定义：Riemann 积分

设 $f$ 在 $[a, b]$ 上有定义，

- 若 $\exists I \in \mathbb{R}, \text{s.t. }$
- $\forall P : a = x_0 < \cdots < x_n = b$（称之为 $[a, b]$ 的一个**分割**），
- $\forall \{\xi_i \in [x_{i - 1}, x_i]\}_{i = 1}^n$（称之为 $P$ 对应的一个**取点法**），
- 只要 $\|P\| = \displaystyle\max_{i = 1}^n \Delta x_i \to 0$（称之为 $P$ 的**模**，也记作 $\lambda_P$；其中 $\Delta x_i = x_i - x_{i - 1}$），
- 就有 Riemann 和 $S_f(P, \xi) = \displaystyle\sum_{i = 1}^n f(\xi_i) \Delta x_i \to I$，

则称 $f$ 在 $[a, b]$ 上（Riemann）可积，记为 $f \in R[a, b]$，并称 $I$ 为 $f$ 在 $[a, b]$ 上的（Riemann）积分，记为：

$$
I = \int_a^b f(x) \text{d} x = \lim_{\|P\| \to 0} \sum_{i = 1}^n f(\xi_i) \Delta x_i
$$

#### 定义：细化与粗化

对于 $[a, b]$ 的一个分割 $P$，若能向 $P$ 中插入若干分点得到 $P'$，则称 $P'$ 为 $P$ 的一个细化、$P$ 为 $P'$ 的一个粗化。

### 连续函数的可积性

#### 命题 1：Riemann 和的可粗化性

内容：设 $f \in C[a, b]$，任给 $[a, b]$ 的一个分割 $P$、相应的取点法 $\xi$ 和其的一个粗化 $P'$，总存在 $P'$ 对应的取点法 $\xi'$，使得 $S_f(P, \xi) = S_f(P', \xi')$。

证明思路：考虑 $P$ 中相邻分割点 $x_{i - 1}, x_i$ 和 $P'$ 中在 $x_{i - 1}, x_i$ 之间的所有分割点，由连续函数的介质性即得。

#### 引理 2：分割充分细时，同一分割对应的 Riemann 和足够接近

内容：设 $f \in C[a, b]$，则 $\forall \epsilon > 0, \exists \delta > 0, \text{s.t. } \forall [a, b]$ 的分割 $P$，只要 $\|P\| < \delta$，就有 $\displaystyle\sum_{i = 1}^n \omega_i \Delta x_i < \epsilon$，其中 $m_i = \displaystyle\min_{x \in [x_{i - 1}, x_i]} f(x), M_i = \max_{x \in [x_{i - 1}, x_i]} f(x), \omega_i = M_i - m_i$，称 $\omega_i$ 为 $f$ 在 $[x_{i - 1}, x_i]$ 上的振幅。

证明思路：$f$ 在**闭区间** $[a, b]$ 上连续 $\Rightarrow$ 一致连续，故通过控制 $\|P\|$ 可以使得 $\omega_i$ 任意小。

#### 引理 3：分割充分细时，不同分割对应的 Riemann 和足够接近

内容：设 $f \in C[a, b]$，则 $\forall \epsilon > 0, \exists \delta > 0, \text{s.t. } \forall [a, b]$ 的分割 $P_1, P_2$，只要 $\|P_1\|, \|P_2\| < \delta$，就有 $\forall P_1$ 对应的取点法 $\xi_1$ 和 $P_2$ 对应的取点法 $\xi_2$，均满足 $|S_f(P_1, \xi_1) - S_f(P_2, \xi_2)| < \epsilon$。

证明思路：

- _Motivation：找一个中介将 $P_1, P_2$ 相关联。_
- 令 $P = P_1 \cup P_2$，任取 $P$ 对应的取点法 $\xi$。
- 由命题 1 可知：$\exists P_1$ 对应的取点法 $\xi_1'$ 和 $P_2$ 对应的取点法 $\xi_2'$，$\text{s.t. } S_f(P, \xi) = S_f(P_1, \xi_1') = S_f(P_2, \xi_2')$。
- 对 $\dfrac{1}{2} \epsilon$ 应用引理 2 得到对应的 $\delta$，则有 $|S_f(P_1, \xi_1) - S_f(P_2, \xi_2)| \leq |S_f(P_1, \xi_1) - S_f(P_1, \xi_1')| + |S_f(P_2, \xi_2) - S_f(P_2, \xi_2')| < \epsilon$，遂得证。

#### 定理 4：连续函数的可积性

内容：$f \in C[a, b] \Rightarrow f \in R[a, b]$。

证明思路：

- _Motivation：仿照 Cauchy 收敛原理的证明，以一个“可以离目标任意近的好 Riemann 和”作为中介。_
- 构造一列 $[a, b]$ 的分割 $\{P_n\}_{n = 1}^{+\infty}$，其中 $P_n$ 选取了 $[a, b]$ 的 $n + 1$ 等分点作为分割点，则 $n \to +\infty, \|P_n\| \to 0$。
- 任取 $P_n$ 对应的取点法 $\xi_n$，则 $\{S_f(P_n, \xi_n)\}$ 有界，进而存在收敛子列 $\{S_f(P_{n_k}, \xi_{n_k})\}$，设其收敛到 $I$。
- $\forall \epsilon > 0$，控制引理 2 中的 $\delta$ 使得任意充分细 Riemann 和与 $S_f(P_{n_k}, \xi_{n_k})$ 的误差小于 $\dfrac{1}{2} \epsilon$，再控制 $k$ 使得 $S_f(P_{n_k}, \xi_{n_k})$ 与 $I$ 的误差小于 $\dfrac{1}{2} \epsilon$，旋即得证。

### 可积函数类

#### 定理 1：单调函数的可积性

内容：若 $f$ 在 $[a, b]$ 上单调，则 $f \in R[a, b]$。

证明从略。

#### 定理 2：间断点与可积性

内容：设 $f$ 在 $[a, b]$ 上有界，且仅有有限个间断点，则 $f \in R[a, b]$。

证明思路：只需讨论一个间断点的情形。以该间断点为中心切出足够小的邻域即可控制误差充分小，邻域外的部分用连续函数的可积性即可。

#### 推论 3：定理 2 的一些扩展

内容：

- (1) 改变区间上有限个点的值，不改变函数在区间上的可积性；若可积，则积分值不变。
- (2) 设 $f$ 在 $[a, b]$ 上有界，且 $f$ 在 $[a, b]$ 上的间断点仅有有限个聚点，则 $f \in R[a, b]$。

#### 定理 4：可积函数连续点的稠密性

内容：若 $f \in R[a, b] \ (a < b)$，则 $\exists x_0 \in (a, b), \text{s.t. } f$ 在 $x_0$ 处连续。

证明：

- 由 $f \in R[a, b]$ 可知：

$$
\begin{aligned}
& \exists P^{(1)} \text{ 为 } [p_1, q_1] \subset (a, b) \text{ 的分割}, \text{s.t. } \sum_{i = 1}^{n_1} \omega^{(1)}_i \Delta x_i^{(1)} < \frac{q_1 - p_1}{2} \\
\Rightarrow & \exists 1 \leq i_1 \leq n_1, \text{s.t. } \omega^{(1)}_{i_1} < \frac{1}{2}
\end{aligned}
$$

- 由 $f \in R[a_1, b_1] \triangleq [x_{i_1 - 1}^{(1)}, x_{i_1}^{(1)}]$ 可知：

$$
\begin{aligned}
& \exists P^{(2)} \text{ 为 } [p_2, q_2] \subset (a_1, b_1) \text{ 的分割}, \text{s.t. } \sum_{i = 1}^{n_2} \omega^{(2)}_i \Delta x_i^{(2)} < \frac{q_2 - p_2}{4} \\
\Rightarrow & \exists 1 \leq i_2 \leq n_2, \text{s.t. } \omega^{(2)}_{i_2} < \frac{1}{4}
\end{aligned}
$$

- 如此续行, 构造出闭区间套 $\{[a_i, b_i]\}_{i = 1}^{+\infty}$，其中 $\omega(f, [a_i, b_i]) < \dfrac{1}{2^i}$。
- 由闭区间套定理可知 $\displaystyle\bigcap_{i = 1}^{+\infty} [a_i, b_i] \neq \varnothing$。任取 $x_0 \in \displaystyle\bigcap_{i = 1}^{+\infty} [a_i, b_i]$，则易证 $x_0 \in (a, b)$ 且 $\omega(f, x_0) = 0$，即 $f$ 在 $x_0$ 处连续。

#### 推论 5：定理 4 的另一种表述

内容：若 $f \in R[a, b] \ (a < b)$，则 $f$ 的连续点在 $[a, b]$ 上稠密。

#### 定义 6：零测集

对于 $A \subset \mathbb{R}$，若 $\forall \epsilon > 0$，$\exists$ **至多可数**个覆盖 $A$ 的开区间 $\{I_i\}_{i \geq 1}$，$\text{s.t. } \displaystyle\sum_{i \geq 1} |I_i| \leq \epsilon$，则称 $A$ 为零测集。

[例 1] 求证：在零测集的定义中，可以将“开区间”换成“闭区间”。

证明：

- 开区间 $\Rightarrow$ 闭区间：显然。
- 闭区间 $\Rightarrow$ 开区间：$\forall \epsilon > 0$，设覆盖 $A$ 的闭区间集 $\{[a_i, b_i]\}_{i \geq 1}$ 使得 $\displaystyle\sum_{i \geq 1} (b_i - a_i) \leq \dfrac{\epsilon}{3}$。
- 令 $\mathcal{F} = \{(a_i, b_i)\}_{i \geq 1} \cup \left\{ U(a_i, \dfrac{1}{3 \times 2^i} \epsilon) \right\}_{i \geq 1} \cup \left\{ U(b_i, \dfrac{1}{3 \times 2^i} \epsilon) \right\}_{i \geq 1}$，则 $\mathcal{F}$ 覆盖 $A$，且：

$$
\begin{aligned}
\sum_{I \in \mathcal{F}} |I| &= \sum_{i \geq 1} (b_i - a_i) + 2 \sum_{i \geq 1} \frac{1}{3 \times 2^i} \epsilon \\
&\leq \frac{1}{3} \epsilon + \dfrac{2}{3} \epsilon \\
&= \epsilon
\end{aligned}
$$

- 故 $A$ 也满足开区间版的零测集定义。

[例 2] 求证：Cantor 三分集 $C$ 为零测集，其构造如下：

- 令 $C_0 = [0, 1]$。
- $\forall n \in \mathbb{N}$，将 $C_n$ 表示为 $2^n$ 个长为 $\dfrac{1}{3^n}$ 的闭区间 $[l_{n, i}, r_{n, i}]$ 之并表示，令：

$$
C_{n + 1} = \bigcup_{i = 1}^{2^n} \left( \left[ l_{n, i}, \frac{2}{3} l_{n, i} + \frac{1}{3} r_{n, i} \right] \cup \left[ \frac{1}{3} l_{n, i} + \frac{2}{3} r_{n, i}, r_{n, i} \right] \right)
$$

- 令 $C = \displaystyle\bigcap_{n = 0}^{+\infty} C_n$。

证明：

- 从构造过程可以看出，$C_n$ 能被表示为总长为 $\left( \dfrac{2}{3} \right)^n$ 的**闭**区间之并。由于 $C \subset C_n$，$C$ 亦然。
- 故 $\forall \epsilon > 0$，令 $n_0 = \left\lceil \log_{\frac{3}{2}} \left( \dfrac{1}{\epsilon} \right) \right\rceil$ ，则 $C$ 能被表示为总长为 $\left( \dfrac{2}{3} \right)^{n_0} \leq \epsilon$ 的闭区间之并。
- 由例 1 即得 $C$ 为零测集。

------

有趣的是，尽管 $C$ 是零测集，但它与 $[0, 1]$ 是等势的：

- $C$ 事实上恰好包含 $[0, 1]$ 中所有**可以**不用 $1$ 而在三进制下表出的数，这里我们认为 $([\cdots] x222 \cdots)_3 = ([\cdots] \text{(x+1)} 000 \cdots)_3 \ (x \in \{0, 1\})$。
- 构造下列映射 $\varphi$：

$$
\begin{aligned}
\varphi : C & \to [0, 1] \\
\sum_{n \geq 1} \frac{2 a_n}{3^n} & \mapsto \sum_{n \geq 1} \frac{a_n}{2^n} &\quad (a_n \in \{0, 1\}) \\
\end{aligned}
$$

- 不难发现 $\varphi$ 为一一映射，则 $C$ 与 $[0, 1]$ 等势。

[例 3] 求证：至多可数个零测集之并是零测集。

证明：

- 设有一列零测集 $\{A_i\}_{i \geq 1}$，设 $A = \displaystyle\bigcup_{i \geq 1} A_i$。
- $\forall \epsilon > 0, \forall i \geq 1, \exists$ 至多可数个覆盖 $A_i$ 的开区间 $\mathcal{F}_i = \{I_{i, j}\}_{j \geq 1}$，$\text{s.t. } \displaystyle\sum_{j \geq 1} |I_{i, j}| \leq \frac{\epsilon}{2^i}$。
- 令 $\mathcal{F} = \displaystyle\bigcup_{i \geq 1} \mathcal{F}_i$，则 $\mathcal{F}$ 中仍只包含至多可数个开区间，且其并集覆盖 $A$。
- 而 $\mathcal{F}$ 中的开区间长度之和 $\displaystyle\sum_{I \in \mathcal{F}} |I| = \sum_{i \geq 1} \sum_{j \geq 1} |I_{i, j}| \leq \sum_{i \geq 1} \frac{\epsilon}{2^i} = \epsilon$，故 $A$ 为零测集。

[例 4] 已知 $f \in C[a, b]$ 且 $\forall x \in U, f'(x) = 0$，其中 $D \subset [a, b], U = [a, b] \backslash D$。判断下列条件是否足以推出 $f$ 在 $[a, b]$ 上为常函数。若充分，给出证明；若不充分，举出反例。

- (1) $D$ 为至多可数集。
- (2) $D$ 为零测集。

解：

- (1) 充分。证明：
- 只需证明 $\forall x \in [a, b], f(a) = f(x)$。下面讨论 $x = b$ 的情况。
- _Motivation：$f \in C[a, b]$ 和 $\forall x \in U, f'(x) = 0$ 在某种程度上都刻画并限制了“振幅”——前者为“绝对值”、后者为“相对值”。_
- $\forall \epsilon > 0$，首先将 $D$ 列举为 $\{d_n\}_{n \geq 1}$。
- I. 对于 $d_n$，由 $f \in C[a, b]$ 可知 $\exists \delta_n > 0, \text{s.t. } \forall x_1, x_2 \in U(d_n, \delta_n), |f(x_1) - f(x_2)| < \dfrac{\epsilon}{2^{n + 1}}$。
- II. 对于 $x \in U$，由 $f'(x) = 0$ 可知 $\exists \omega_x > 0, \text{s.t. } \forall x' \in \check{U}(x, \omega_x), \left| \dfrac{f(x') - f(x)}{x' - x} \right| < \dfrac{\epsilon}{2(b - a)}$。
- 根据 I、II 构造 $[a, b]$ 的开覆盖 $\mathcal{F} = \{U(d_n, \delta_n) \mid n \geq 1\} \bigcup \{U(x, \omega_x) \mid x \in U\}$。
- 由 Lebesgue 覆盖引理可知 $\exists \delta > 0, \text{s.t. }$ 只要 $[a, b]$ 的子开区间 $I$ 的长度 $< \delta$，就有 $\exists J \in \mathcal{F}, \text{s.t. } I \subset J$。
- 由 $f \in C[a, b]$ 可知在 I、II 的叙述中，可以同时将开邻域换成闭邻域、将 $<$ 换成 $\leq$。
- 构造 $[a, b]$ 的分割 $P : a = x_0 < x_1 < \cdots < x_m = b$，使得 $\|P\| < \delta$。
- 现在将 $P$ 中连续一段 $(x_{i - 1}, x_i), \cdots, (x_{j - 1}, x_j) \subset U(d_n, \delta_n)$ 合并为 $(x_{i - 1}, x_j)$（即删去分割点 $x_i, \cdots, x_{j - 1}$），则新得到的 $P$（_方便起见，保持相关记号不变_）满足：

$$
\forall 1 \leq i \leq m, \exists J \subset \mathcal{F}, \text{s.t. } (x_{i - 1}, x_i) \subset J
$$

- 故有：

$$
\begin{aligned}
|f(b) - f(a)| &\leq \sum_{i = 1}^m |f(x_i) - f(x_{i - 1})| \\
&< \sum_{n \geq 1} \frac{\epsilon}{2^{n + 1}} + \frac{\epsilon}{2(b - a)} \cdot (b - a) \\
&= \epsilon
\end{aligned}
$$

- 由 $\epsilon$ 的任意性即得 $f(a) = f(b)$。
- (2) 不充分。反例：
- 首先易证前面基于 Cantor 三分集构造的 $\varphi$ 单调递增。
- 构造下列映射 $\Phi$（**Cantor 函数**）：

$$
\begin{aligned}
\Phi : [0, 1] & \to [0, 1] \\
x & \mapsto \sup \{\varphi(y) \mid y \leq x, y \in C\}
\end{aligned}
$$

- 则 $\Phi$ 单调递增，$\Phi(0) = 0, \Phi(1) = 1$。
- 考察 $[0, 1] \backslash C = (0, 1) \backslash C$，其为**开集**，则可将其表示为至多可数个**开区间**的并 $U = \displaystyle\bigcup_{i \geq 1} I_i$。_其实就是每次取走的中间 $\dfrac{1}{3}$ 的区间。_
- 由于 $I_i \cap C = \varnothing$，$\Phi$ 在 $I_i$ 上为常函数，则 $\Phi$ 在 $I_i$ 上可导且导函数全 $0$。
- 故可取 $D = C \backslash \{0, 1\}$，则 $D$ 为零测集，但 $\Phi$ 不是常函数。

#### 间断点集的表示

若 $f$ 在 $[a, b]$ 上有界，记 $D_f$ 为 $f$ 在 $[a, b]$ 上的间断点集。

- _Motivation：用单点振幅刻画单点连续性，如此可将连续性与可积性相关联。_

由连续的定义可知：

$$
D_f = \{x \in [a, b] \mid w(f, x) > 0\}
$$

- _Motivation：Riemann 定理考察了 $w_i \geq \eta$ 的子区间，是否可以在 $D_f$ 的表示中加入类似的形式？_

当 $\delta > 0$，记：

$$
D_f(\delta) = \{x \in [a, b] \mid w(f, x) \geq \delta\}
$$

- _Motivation：注意到 $D_f(\delta) \subset D_f$，考虑用**可数**个 $D_f(\delta)$ 的并表征 $D_f$，其中 $\delta \to 0$。_

则可以将 $D_f$ 表示为集合的并：

$$
D_f = \bigcup_{n = 1}^{+\infty} D_f(\frac{1}{n})
$$

#### 定理 7：可积的充要条件 II / Lebesgue 定理

内容：若 $f$ 在 $[a, b]$ 上有界，则 $f \in R[a, b]$ 当且仅当 $D_f$ 为零测集。

证明：

- 充分性：$\forall \epsilon, \eta > 0$，有：
- _Motivation：用总长“任意小”的开区间覆盖 $D_f$，钦定只有包含于这些部分的区间可能使振幅“较大”。_
- (1) 由于 $D_f$ 为零测集，$\exists$ 至多可数个覆盖 $D_f$ 的开区间 $\{I_i\}_{i \geq 1}$，$\text{s.t. } \displaystyle\sum_{i \geq 1} |I_i| \leq \frac{\epsilon}{2} < \epsilon$。
- _Motivation：对于余下部分，由于连续，总能取出一个邻域，使得其中振幅“较小”。_
- (2) $\forall x \in [a, b] \backslash D_f$，由于 $w(f, x) = 0$，$\exists \delta_x > 0, \text{s.t. } \sup \{|f(x_1) - f(x_2)| \mid x_1, x_2 \in U(x, \delta_x) \cap [a, b]\} < \eta$。
- 综上 (1)(2)，开区间族 $\mathcal{F} = \{I_i\} \cup \{U(x, \delta_x) \mid x \in [a, b] \backslash D_f\}$ 构成 $[a, b]$ 的一个开覆盖。
- _Motivation：欲使每个区间都能够至少被上面两类之一的区间包含。_
- 对于 $[a, b]$ 的分割 $P$，由 Lebesgue 覆盖引理可知：$\exists \delta > 0, \text{s.t. }$ 只要 $\|P\| < \delta$，就有 $\forall 1 \leq i \leq n, \exists I \in \mathcal{F}, \text{s.t. } [x_{i - 1}, x_i] \subset I$。
- _Motivation：如此，即可控制振幅“较大”的区间的并为第一类开区间的并的子集，进而总长可以“任意小”。_
- 故 $\displaystyle\sum_{\omega_i \geq \eta} \Delta x_i \leq \sum_{i \geq 1} |I_i| < \epsilon$，由 Riemann 定理可知 $f \in R[a, b]$。
- 必要性：_Motivation：问题转化为证明 $\forall \delta > 0, D_f(\delta)$ 均为零测集。_
- _Motivation：凡 $D_f(\delta)$ 关涉的区间，振幅都“较大”，考虑通过乘积来控制这样的区间总长“不长”。_
- $\forall \delta > 0$，由于 $f \in R[a, b]$，故 $\forall \epsilon > 0, \exists P, \text{s.t. } \|P\| < \delta, \displaystyle\sum_{i = 1}^n \omega_i \Delta x_i < \frac{1}{2} \epsilon \delta$，则 $\displaystyle\sum_{\omega_i \geq \delta} \Delta x_i < \frac{1}{2} \epsilon$。
- $\forall 1 \leq i \leq n$，若 $(x_{i - 1}, x_i) \cap D_f(\delta) \neq \varnothing$，则取 $x \in (x_{i - 1}, x_i) \cap D_f(\delta)$，可得 $\omega_i \geq w(f, x) \geq \delta$。
- _注：这里考察开区间，是因为边界另一侧也可能成为振幅来源，端点振幅与闭区间振幅间的不等关系不再成立。_
- _Motivation：单独把端点用总长“不长”的开区间覆盖住即可。_
- 取开区间族 $\mathcal{F} = \{(x_{i - 1}, x_i) \mid \omega_i \geq \delta\} \cup \{U(x_i, \dfrac{\epsilon}{4(n + 1)}) \mid 0 \leq i \leq n\}$，则其包含有限个能覆盖 $D_f$ 的开区间，且总长为 $\displaystyle\sum_{\omega_i \geq \delta} \Delta x_i + \frac{\epsilon}{2(n + 1)} \cdot (n + 1) < \epsilon$。
- 故对于给定的 $\delta$，由 $\epsilon$ 的任意性，可知 $D_f(\delta)$ 为零测集。
- 又由 $\delta$ 的任意性和 $D_f$ 的并集表示，可知 $D_f$ 为零测集。

#### 定理 8：四则运算下的可积性

内容：

- (1) $f, g \in R[a, b] \Rightarrow \alpha f + \beta g, f \cdot g \in R[a, b]$。
- (2) $f, g \in R[a, b], g \geq c > 0 \Rightarrow \dfrac{f}{g} \in R[a, b]$。

------

从代数的视角看：

- $S = R[a, b]$ 关于逐点加 $+$ 和逐点数乘 $\cdot$ 构成**线性空间**，下面尝试引入一些度量。
- (1) 可积性 $\Rightarrow d(f, g) \triangleq \displaystyle\int_a^b |f(x) - g(x)| \text{d} x$。
- 但若 $f, g$ 只在一个非空零测集上不同，则 $f \neq g$ 但 $d(f, g) = 0$，则 $d$ 不满足正定性。
- 可改令 $S = C[a, b]$ 或让 $S$ 对“至多在一个零测集上不同”取商。
- (2) 只要有一点不同，距离就为正 $\Rightarrow d(f, g) \triangleq \displaystyle\sup_{x \in [a, b]} |f(x) - g(x)|$。
- (3) 双线性性 $\Rightarrow (f, g) = \displaystyle\int_a^b f(x) g(x) \text{d} x$。

#### 反例：可积函数的复合未必可积

[例 5] 已知 Riemann 函数 $R \in R[0, 1]$，符号函数 $\text{sgn} \in R[0, 1]$，但 $\text{sgn} \circ R = D \not\in R[0, 1]$。

[例 6] 已知 $R \in R[0, 1]$，令 $f(x) = \begin{cases} 0, &\quad x = 0 \\ \sin \dfrac{1}{x}, &\quad x \neq 0 \end{cases}$，则 $f \in R[0, 1]$，求证：$f \circ R \not\in R[0, 1]$。

证明：

$$
f(R(x)) = \begin{cases}
\sin q, &\quad x = \dfrac{p}{q} \in (0, 1), p \perp q \\
\sin 1, &\quad x = 0, 1 \\
0, &\quad x \in (0, 1) \backslash \mathbb{Q}
\end{cases}
$$

- (1) 易证 $f \circ R$ 在 $(0, 1) \cap \mathbb{Q}$ 上不连续。
- (2) $\forall x_0 \in (0, 1) \setminus \mathbb{Q}, f(R(x_0)) = 0$ .
- 由实数系连续性, $\exists \{x_n\} \subset (0, 1) \cap \mathbb{Q}, \text{s.t. } \displaystyle\lim_{n \to +\infty} x_n = x_0$。记 $x_n$ 的最简分数表示为 $\dfrac{p_n}{q_n} \ (p_n \perp q_n)$。
- 由于 $x_0$ 为无理数，$\{x_n\}$ 中包含无穷多个真分数，则 $\{q_n\}$ 无界，故可以从 $\{q_n\}$ 中取一个发散于 $+\infty$ 的子列，不妨假设 $\displaystyle\lim_{n \to +\infty} q_n = +\infty$。
- _Motivation: 找两个收敛子列，证明其并不收敛于同一值。_
- 令 $y_n = \dfrac{p_n^2}{p_n q_n + 1}, z_n = \dfrac{p_n^2}{p_n q_n - 1}$，则 $y_n, z_n$ 的这一表示均为既约分数，且 $\displaystyle\lim_{n \to +\infty} y_n = \lim_{n \to +\infty} z_n = x_0$。
- 但 $\{f(R(y_n))\} = \{\sin(p_n q_n + 1)\}, \{f(R(z_n))\} = \{\sin(p_n q_n - 1)\}$，易证这两者不可能同时具有极限且极限值相同。
- 综上 (1)(2)，$f \circ R$ 在 $(0, 1)$ 上处处不连续，与定理 4 矛盾，故 $f \circ R \not\in R[0, 1]$。

#### 定理 9：连续函数复合可积函数仍可积

内容：$g \in R[a, b], A = \displaystyle\inf_{x \in [a, b]} g(x), B = \sup_{x \in [a, b]} g(x), f \in C[A, B] \Rightarrow f \circ g \in R[a, b]$。

证明：

- 设 $g$ 在 $x_0 \in [a, b]$ 处连续。
- 由于 $f$ 在 $g(x_0) \in [A, B]$ 处连续，$\forall \epsilon > 0, \exists \delta_1 > 0, \text{s.t. } \forall y \in U(g(x_0), \delta_1) \cap [A, B], |f(y) - f(g(x_0))| < \epsilon$。
- 由于 $g$ 在 $x_0$ 处连续，$\exists \delta_2 > 0, \text{s.t. } \forall x \in U(x_0, \delta_2) \cap [a, b], |g(x) - g(x_0)| < \delta_1$。
- 故 $\forall x \in U(x_0, \delta_2) \cap [a, b], |f(g(x)) - f(g(x_0))| < \epsilon$，即 $f \circ g$ 在 $x_0$ 处连续，则有 $E_{dc}(f \circ g) \subset E_{dc}(g)$。
- 由 $g \in R[a, b]$ 可知 $D_g$ 零测，进而有 $D_{f \circ g}$ 零测，故 $f \circ g \in R[a, b]$。

#### 反例：可积函数复合连续函数未必可积

[例 7] 记 $\mathbb{Q} \cap [0, 1] = \{r_n\}_{n = 1}^{+\infty}, G = \displaystyle\bigcup_{n = 1}^{+\infty} \left( r_n - \frac{1}{4^n}, r_n + \frac{1}{4^n} \right), E = [0, 1] \backslash G$。

- (1) 令 $f(x) = \text{dist}(x, E) \triangleq \displaystyle\inf_{z \in E} |x - z|$，求证：$f$ 在 $[0, 1]$ 上 Lipschitz 连续。
- (2) 令 $\varphi(x) = [x = 0]$，则 $\varphi \in R[0, 1]$，求证：$\varphi \circ f \not\in R[0, 1]$。

证明：

- (1) $\forall x, y \in [0, 1]$，一方面有 $f(x) \leq \displaystyle\inf_{z \in E} (|x - y| + |y - z|) = |x - y| + f(y)$，另一方面有 $f(x) \geq \displaystyle\inf_{z \in E} (|y - z| - |y - x|) = f(y) - |x - y|$。
- 则 $|f(x) - f(y)| \leq |x - y|$，故 $f$ 在 $[0, 1]$ 上 Lipschitz 连续，且有 Lipschitz 常数为 $1$。
- (2) 易知 $E \subset [0, 1] \backslash \mathbb{Q}$。
- $\forall x_0 \in E$，由实数系的连续性，可知 $\exists \{x_n\} \subset [0, 1] \cap \mathbb{Q}, \text{s.t. } \displaystyle\lim_{n \to +\infty} x_n = x_0$。
- 而 $\varphi(f(x_n)) = 0, \varphi(x_0) = 1$，故 $\varphi \circ f$ 在 $x_0$ 处不连续。由 $x_0$ 的任意性可知 $\varphi \circ f$ 在 $E$ 上不连续。
- 但 $|G| \leq \displaystyle\sum_{n = 1}^{+\infty} 2 \cdot \frac{1}{4^n} = \frac{2}{3}$，则 $|E| \geq 1 - |G| = \dfrac{1}{3}$，故 $E$ 并非零测集，则 $\varphi \circ f \notin R[0, 1]$。

### 将求和转化为 Riemann 积分

[例 8] 已知 $\alpha > 0, \displaystyle\lim_{n \to +\infty} \frac{a_n}{n^{\alpha}} = 1$，求 $\displaystyle\lim_{n \to +\infty} \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n a_i$。

解：

- 首先计算 $a_i = i^{\alpha}$ 对应的结果：

$$
\begin{aligned}
\lim_{n \to +\infty} \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n i^{\alpha} &= \lim_{n \to +\infty} \frac{1}{n} \sum_{i = 1}^n \left( \frac{i}{n} \right)^{\alpha} \\
&= \int_0^1 x^{\alpha} \text{d} x \\
&= \left. \frac{x^{\alpha + 1}}{\alpha + 1} \right|_0^1 \\
&= \frac{1}{\alpha + 1}
\end{aligned}
$$

- $\displaystyle\lim_{n \to +\infty} \frac{a_n}{n^{\alpha}} = 1$ 确保我们可以控制相对误差，即：

$$
\forall \epsilon > 0, \exists N \in \mathbb{N}_+, \text{s.t. } \forall n > N, \left| \frac{a_n}{n^{\alpha}} - 1 \right| < \epsilon
$$

- 令 $S = \displaystyle\sum_{i = 1}^N |a_i - i^{\alpha}|, T(n) = \frac{1}{n^{1 + \alpha}} \sum_{i = N + 1}^n |a_i - i^{\alpha}|$，则：

$$
\begin{aligned}
T(n) &< \frac{1}{n^{1 + \alpha}} \sum_{i = N + 1}^n i^{\alpha} \epsilon \\
&= \frac{\epsilon}{n} \sum_{i = N + 1}^n \left( \frac{i}{n} \right)^{\alpha} \\
&< \frac{\epsilon}{n} \sum_{i = 0}^{n - 1} \left( \frac{i}{n} \right)^{\alpha} + \frac{\epsilon}{n} \\
&< \epsilon \int_0^1 x^{\alpha} \text{d} x + \frac{\epsilon}{n} &\quad (\text{Darboux 下和} \leq \text{定积分}) \\
&< \epsilon \left( \frac{1}{\alpha + 1} + 1 \right) \\
&< 2 \epsilon
\end{aligned}
$$

- 再要求 $\dfrac{S}{n^{1 + \alpha}} < \epsilon$，解得 $n > \left( \dfrac{S}{\epsilon} \right)^{\frac{1}{1 + \alpha}}$。
- 令 $N_0 = \max\left\{ N, \left\lfloor \left( \dfrac{S}{\epsilon} \right)^{\frac{1}{1 + \alpha}} \right\rfloor \right\}$，则：

$$
\forall n > N_0, \left| \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n a_i - \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n i^{\alpha} \right| \leq \dfrac{S}{n^{1 + \alpha}} + T(n) < 3 \epsilon
$$

- 由 $\epsilon$ 的任意性即得：

$$
\lim_{n \to +\infty} \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n a_i = \lim_{n \to +\infty} \frac{1}{n^{1 + \alpha}} \sum_{i = 1}^n i^{\alpha} = \frac{1}{\alpha + 1}
$$

### 微积分基本定理

- 积分 $\Rightarrow$ 越来越“光滑”，微分 $\Rightarrow$ 越来越“不光滑”。

#### 定理 1：Newton-Leibniz 公式

内容：已知 $f \in R[a, b]$，若 $f$ 有原函数 $F$（即 $F' = f$），则：

$$
\int_a^b f(x) \text{d} x = F(b) - F(a)
$$

证明：

- 取 $[a, b]$ 的 $n$ 等分点构造分割 $P_n : a = x_0 < x_1 < \cdots < x_n = b$。
- 由 Lagrange 中值定理可知：

$$
\exists \xi_i \in (x_{i - 1}, x_i), \text{s.t. } F(x_i) - F(x_{i - 1}) = f(\xi_i) (x_i - x_{i - 1}) = f(\xi_i) \Delta x_i
$$

- 则有：

$$
\begin{aligned}
F(b) - F(a) &= \sum_{i = 1}^{n} (F(x_i) - F(x_{i - 1})) \\
&= \sum_{i = 1}^{n} f(\xi_i) \Delta x_i
\end{aligned}
$$

- 令 $n \to +\infty$，此时 $\|P_n\| = \dfrac{b - a}{n} \to 0$，故有：

$$
\begin{aligned}
\int_a^b f(x) \text{d} x &= \lim_{n \to +\infty} \sum_{i = 1}^{n} f(\xi_i) \Delta x_i \\
&= F(b) - F(a)
\end{aligned}
$$

#### 反例：可导不意味着导函数可积

[例 9] 求证：$F(x) = \begin{cases} x^2 \sin \dfrac{1}{x^2}, &\quad x \neq 0 \\ 0, &\quad x = 0 \end{cases}$ 在 $[-1, 1]$ 上可导，但导函数不可积。

证明：

$$
F'(x) = \begin{cases}
2x \sin \dfrac{1}{x^2} - \dfrac{2}{x} \cos \frac{1}{x^2}, &\quad x \neq 0 \\
0, &\quad x = 0
\end{cases}
$$

- 易见 $F'$ 在 $0$ 附近无界，故 $F' \not\in R[-1, 1]$。

------

此时一个 naive 的想法是：

- 那是不是只要限定导函数有界，就能推出其可积了？

事实上不然，下面是一个反例。

##### Volterra 函数

首先构造 $p_a(x) = \sqrt[3]{x - a}$，则 $p'_a(x) = \dfrac{1}{3} (x - a)^{-\frac{2}{3}} \ (x \neq a)$。

将 $[0, 1] \cap \mathbb{Q}$ 列为 $\{r_n\}_{n = 1}^{\infty}$，令 $f(x) = \displaystyle\sum_{n = 1}^{\infty} \frac{p_{r_n}(x)}{10^n}$。

- (1) 由于 $\forall x \in [0, 1], n \geq 1, p_{r_n}(x) \leq 1$，易证 $f$ 在 $[0, 1]$ 上有定义。
- (2) 由于级数中每一项随 $x$ 增大而严格单调递增，故 $f$ 在 $[0, 1]$ 上**严格单调递增**。
- (3) 由于 $g_{r_n}(x)$ 连续，易证 $f \in C[0, 1]$。
- (4) 综合 (2)(3)，可知 $f$ 存在反函数，记作 $g \triangleq f^{-1}$。
- (5) 当 $x \in [0, 1] \backslash \mathbb{Q}$，逐项求导可得 $f'(x) = \dfrac{1}{3} \displaystyle\sum_{n = 1}^{\infty} \frac{p'_{r_n}(x)}{10^n} > 0$，此时有 $g'(f(x)) = \dfrac{1}{f'(x)}$。
- (6) 当 $x \in \mathbb{Q}$，可得 $f'(x) = +\infty$，此时有 $g'(f(x)) = 0$。
- _注：若想严格说明广义极限 $f'(x)$ 的存在性，可使用 $\epsilon-\delta$ 语言 + Lagrange 中值定理。_

综上，由于 $\mathbb{Q}$ 在 $[0, 1]$ 上稠密，$g'$ 在非零测集 $[0, 1] \backslash \mathbb{Q}$ 上不连续，则 $g' \not\in R[0, 1]$。

#### 定理 2：变上限积分的光滑性

内容：

- (1) 若 $f \in R[a, b]$，则 $F(x) = \displaystyle\int_a^x f(t) dt \in C[a, b]$。
- (2) 在 (1) 的基础上，若 $f$ 在 $x_0 \in [a, b]$ 处连续，则 $F$ 在 $x_0$ 处可导，且 $F'(x_0) = f(x_0)$。

证明：

- (1) 设 $f$ 在 $[a, b]$ 上有上界为 $M > 0$，则：

$$
\forall x, y \in [a, b], |F(x) - F(y)| = \left| \int_y^x f(t) dt \right| \le M|x - y|
$$

- 故 $F$ 在 $[a, b]$ 上 Lipschitz 连续，且有 Lipschitz 常数为 $M$。
- (2) 由 $f$ 在 $x_0$ 处连续可知：

$$
\forall \epsilon > 0, \exists \delta > 0, \forall x \in U(x_0, \delta), |f(x) - f(x_0)| < \epsilon
$$

- 则对 $x \in U^+(x_0, \delta)$ 有：

$$
\begin{array}{l}
& \left| \dfrac{F(x) - F(x_0)}{x - x_0} - f(x_0) \right| \\
= & \dfrac{1}{x - x_0} \left| \displaystyle\int_{x_0}^x (f(t) - f(x_0)) dt \right| \\
\leq & \dfrac{1}{x - x_0} \displaystyle\int_{x_0}^x |f(t) - f(x_0)| dt \\
< & \dfrac{1}{x - x_0} \cdot (x - x_0) \epsilon \\
= & \epsilon
\end{array}
$$

- 同理有 $\forall x \in U^-(x_0, \delta), \left| \dfrac{F(x) - F(x_0)}{x - x_0} - f(x_0) \right| < \epsilon$。
- 故 $F$ 在 $x_0$ 处可导，且 $F'(x_0) = f(x_0)$。

#### 定理 3：原函数的构造

内容：若 $f \in C[a, b]$，$F$ 如定理 2 所定义，则 $F$ 为 $f$ 的一个原函数。

证明：由定理 2 可知 $F' = f$，此即原函数的定义。

------

最后解决一个很神秘的问题：如何定义 $\displaystyle\int_a^a f(x) \text{d} x$？

~~一个自然的想法是钦定其为 $0$，但令一个未定义的东西为 $0$ 是不是有点奇怪啊？尤其是当 $f$ 并不在 $a$ 的任何邻域上可积？~~

设 $f$ 在 $a$ 的（左 / 右）邻域上可积，设 $F$ 为 $f$ 的变上限积分，定义：

$$
\int_a^a f(x) \text{d} x \triangleq \lim_{x \to a^{(+/-)}} F(x)
$$

由 $F$ 的连续性可知 $\displaystyle\int_a^a f(x) \text{d} x \equiv 0$，这与我们的直觉一致。

### 有界变差函数

#### 定义 1：有界变差函数

设 $f$ 在 $[a, b]$ 上有定义，$P : a_0 = x_0 < x_1 < \cdots < x_n = b$ 为 $[a, b]$ 的一个分割，称 $\sigma(f, P) \triangleq \displaystyle\sum_{i = 1}^n |f(x_i) - f(x_{i - 1})|$ 为 $f$ 在 $P$ 下的变差。

- _可以发现这个定义与 Riemann 和之类有相似之处，这将导致两者有类似的性质。_

称广义上确界 $\displaystyle\bigvee_a^b f(x) \triangleq \sup_P \sigma(f, P)$ 为 $f$ 在 $[a, b]$ 上的全变差。

若 $\displaystyle\bigvee_a^b f(x) < +\infty$，称 $f$ 为 $[a, b]$ 上的有界变差函数，记作 $f \in BV[a, b]$。

可以通过取广义上确界定义无界闭区间上的全变差，并定义其上的有界变差函数。

#### 命题 2：两类典型的有界变差函数

内容：

- (1) 若定义在 $[a, b]$ 上的函数 $f$ 单调，则 $f \in BV[a, b]$。
- (2) 若定义在 $[a, b]$ 上的函数 $f$ 是两个单调函数 $g, h$ 的差，则 $f \in BV[a, b]$，且 $\displaystyle\bigvee_a^b f(x) \leq |g(b) - g(a)| + |h(b) - h(a)|$。

以上两点可以自然地推广到无界闭区间上的情况。

#### 命题 3：仿照 Riemann 和之类写出的一堆（比较显然正确的）性质

内容：

- (1) $[a, b]$ 上的有界变差函数有界。
- (2) 设 $f$ 定义在 $[a, b]$ 上，$P'$ 为 $P$ 的一个细化，则 $\sigma(f, P) \leq \sigma(f, P')$。
- (3) 设 $f$ 定义在 $[a, b]$ 上，$c \in (a, b)$，则：

$$
\begin{array}{l}
\text{(i) } f \in BV[a, b] \Leftrightarrow f(x) \in BV[a, c] \land f(x) \in BV[c, b] \\
\text{(ii) } \displaystyle\bigvee_a^b f(x) = \bigvee_a^c f(x) + \bigvee_c^b f(x)
\end{array}
$$

- (4) $f, g \in BV[a, b] \Rightarrow \alpha f + \beta g, f \cdot g \in BV[a, b]$。
- (5) $f, g \in BV[a, b], g \geq c > 0 \Rightarrow \dfrac{f}{g} \in BV[a, b]$。
- (6) 设 $f \in BV[a, b]$，则改变 $f$ 在 $[a, b]$ 上有限个点的值不会改变其有界变差的性质。

------

比照先前对可积性的探讨，我们尝试来探讨有界变差的充要条件。一个最自然的想法是考察连续函数，但我们将发现这里的情况与可积性的结论并不一致。

#### 反例：连续不意味着有界变差

[例 10] 求证：$f(x) = \begin{cases} x \sin \dfrac{1}{x}, &\quad x \in \left( 0, \dfrac{2}{\pi} \right] \\ 0, &\quad x = 0 \end{cases}$ 在 $\left[ 0, \dfrac{2}{\pi} \right]$ 上连续但不有界变差。

证明：

- ~~连续性在数分一里就知道了（~~
- _Motivation：$\sin$ 导致 $f$ 震荡，不妨考察有限次震荡的最大“代价”，再令震荡次数 $\to +\infty$。_
- 考察分割 $P_n : 0 < \dfrac{2}{(2n - 1) \pi} < \cdots < \dfrac{2}{\pi}$，则：

$$
\begin{aligned}
\sigma(f, P_n) &= \frac{2}{(2n - 1) \pi} + \sum_{i = 1}^{n - 1} \left( \frac{2}{(2i - 1) \pi} + \frac{2}{(2i + 1) \pi} \right) \\
&= \frac{2}{\pi} \sum_{i = 1}^n \frac{1}{i - \frac{1}{2}} - \frac{2}{\pi} \\
&> \frac{2}{\pi} \sum_{i = 1}^n \frac{1}{i} - \frac{2}{\pi} \\
&= \frac{\ln n + O(1)}{\pi}
\end{aligned}
$$

- 令 $n \to +\infty$，有 $\sigma(f, P_n) \to +\infty$，故 $\displaystyle\bigvee_0^{\frac{2}{\pi}} f(x) = +\infty$，即 $f \not\in BV \left[ 0, \dfrac{2}{\pi} \right]$。

------

究其原因，即便闭区间上连续可以导出一致连续，但与 Riemann 和不同的是，**变差不能被区间长度限制**（丢掉了 $\Delta x_i$ 因子）。

当我们转而寻找一个强于一致连续的限制，自然地期望一个**能用区间长度限制变差**的性质——那 Lipschitz 连续如何？

#### 命题 4：Lipschitz 连续蕴涵有界变差

内容：设 $f$ 在 $[a, b]$ 上 Lipschitz 连续，则 $f \in BV[a, b]$。

证明：

- 设 $f$ 在 $[a, b]$ 上有 Lipschitz 常数为 $M > 0$，则：

$$
\forall P, \sigma(f, P) \leq \displaystyle\sum_{i = 1}^n M (x_i - x_{i - 1}) = M(b - a)
$$

- 故 $\displaystyle\bigvee_a^b f(x) \leq M(b - a)$，即 $f \in BV[a, b]$。

------

对于闭区间连续、开区间可导的函数，配合 Lagrange 定理容易给出下面的推论。

#### 推论 5：导函数有界蕴涵有界变差

内容：设 $f$ 在 $[a, b]$ 上连续、在 $(a, b)$ 上可导，若 $f'$ 有界，则 $f \in BV[a, b]$。

[例 11] 求证：$f(x) = \begin{cases} x^2 \sin \dfrac{1}{x}, &\quad x \in (0, 1] \\ 0, &\quad x = 0 \end{cases}$ 在 $[0, 1]$ 上有界变差。

证明：由 $f \in C[0, 1]$ 和 $f'(x) = 2x \sin \dfrac{1}{x} - \cos \dfrac{1}{x}$ 在 $(0, 1)$ 上有界为 $3$ 即得。

------

仿照我们在构造原函数时构造的变上限积分，构造下列全变差函数。

#### 命题 6：全变差函数单增有界

内容：设 $f \in BV[a, b]$，则 $F(x) = \displaystyle\bigvee_a^x f(t)$ 在 $[a, b]$ 上单调递增且有界。

据此，可以用极限定义无界闭区间上的全变差。

#### 定理 7：有界变差的充要条件 I

内容：$f \in BV[a, b]$ 当且仅当 $\exists F$ 在 $[a, b]$ 上单调递增且有界，$\text{s.t. } \forall a \leq x_1 < x_2 \leq b, |f(x_1) - f(x_2)| \leq F(x_2) - F(x_1)$。此时称 $F$ 为 $f$ 的一个**强函数**。

证明：

- 必要性：命题 6 构造的 $F$ 即满足条件。
- 充分性：

$$
\forall P, \sigma(f, P) \leq \displaystyle\sum_{i = 1}^n (F(x_i) - F(x_{i - 1})) = F(b) - F(a)
$$

- 故 $\displaystyle\bigvee_a^b f(x) \leq F(b) - F(a)$，即 $f \in BV[a, b]$。

#### 定理 8：有界变差的充要条件 II

内容：$f \in BV[a, b]$ 当且仅当 $\exists g, h$ 在 $[a, b]$ 上单调递增，$\text{s.t. } f = g - h$。

证明：

- 充分性：此即命题 2 (2)。
- 必要性：令 $g$ 为命题 6 构造的 $F$，$h = F - f$，下证 $h$ 单调递增。

$$
\forall a \leq x_1 < x_2 \leq b, h(x_2) - h(x_1) = (F(x_2) - F(x_1)) - (f(x_2) - f(x_1)) \geq 0
$$

------

由单调函数的性质，容易给出如下推论。

#### 推论 9：从单调到有界变差

内容：若 $f \in BV[a, b]$，则：

- (1) $f \in R[a, b]$。
- (2) 极限 $f(a^+), f(b^-); \forall x \in (a, b), f(x^-), f(x^+)$ 存在。

------

在先前对变上限积分的探讨中，我们考察了可积函数及其原函数的光滑性的关系。在这里我们可以进行类似的讨论。

#### 命题 10：有界变差函数及其全变差函数的连续性

内容：若 $f \in BV[a, b]$，令 $F(x) = \displaystyle\bigvee_a^x f(t)$，则 $\forall x_0 \in [a, b], f \in C(x_0) \Leftrightarrow F \in C(x_0)$。

证明：

- $\Leftarrow$：由 $|f(x_1) - f(x_2)| \leq F(x_2) - F(x_1)$ 易证。
- $\Rightarrow$：下证当 $f$ 在 $x_0 \in [a, b)$ 处连续，$F$ 在 $x_0$ 处右连续。左连续是同理的。
- $\forall \epsilon > 0$，由全变差 $\displaystyle\bigvee_{x_0}^b f(x)$ 的确界性质可知：

$$
\exists P : x_0 < x_1 < \cdots < x_n = b, \text{s.t. } \sigma(f, P) > \bigvee_{x_0}^b f(x) - \frac{\epsilon}{2}
$$

- 现向 $P$ 中插入 $x' \in (x_0, x_1)$ 得到 $P'$，则：

$$
\sigma(f, P) \leq \sigma(f, P') \leq |f(x') - f(x_0)| + \bigvee_{x'}^b f(x)
$$

- 综合以上两式，由全变差的区间可加性可知：

$$
F(x') - F(x_0) = \bigvee_{x_0}^{x'} f(x) < |f(x') - f(x_0)| + \frac{\epsilon}{2}
$$

- 由 $f$ 在 $x_0$ 处连续可知：

$$
\exists \delta > 0, \text{s.t. } \forall x \in U(x_0, \delta) \cap [a, b], |f(x) - f(x_0)| < \frac{\epsilon}{2}
$$

- 令 $\delta_0 = \min\{x_1 - x_0, \delta\}$，则：

$$
\forall x' \in U^+(x_0, \delta_0), |F(x') - F(x_0)| = F(x') - F(x_0) < \epsilon
$$

- 由 $\epsilon$ 的任意性即得 $F$ 在 $x_0$ 处右连续。
- _本部分的另一个解法是利用定理 8 给出的 $g, h$，先让二者在 $> x_0$ 的部分同时减去 $g(x_0^+) - g(x_0) = h(x_0^+) - h(x_0)$（$f$ 的连续性确保该等式成立和减完后满足条件），再使用全变差函数的三角不等式。_

------

作为本部分的结尾，我们仿照 Riemann 积分的定义，给出有关全变差的如下结论。

#### 定理 11：长得很像 Riemann 积分的定义的等式

内容：设 $f \in BV[a, b] \cap C[a, b]$，则：

$$
\bigvee_a^b f(x) = \lim_{\|P\| \to 0} \sigma(f, P)
$$

证明：

- $\forall \epsilon > 0$，由全变差 $\displaystyle\bigvee_{x_0}^b f(x)$ 的确界性质可知：

$$
\exists P_0 : a = x_0 < x_1 < \cdots < x_n = b, \text{s.t. } \sigma(f, P_0) > \bigvee_{x_0}^b f(x) - \frac{\epsilon}{2}
$$

- 由 $f \in C[a, b]$ 可知 $f$ 在 $[a, b]$ 上一致连续，则：

$$
\exists \delta > 0, \text{s.t. } \forall x_1, x_2 \in [a, b] \land |x_1 - x_2| < \delta, |f(x_1) - f(x_2)| < \frac{\epsilon}{4n}
$$

- $\forall \|P\| < \delta$，将 $P_0$ 的分点逐个插入 $P$ 得到 $P'$，每一步 $\sigma(f, \cdot)$ 的增加量不超过 $\dfrac{\epsilon}{2n}$，则：

$$
\sigma(f, P_0) \leq \sigma(f, P') \leq \sigma(f, P) + \frac{(n - 1) \epsilon}{2n}
$$

- 故有：

$$
\sigma(f, P) \geq \sigma(f, P_0) - \frac{(n - 1) \epsilon}{2n} > \bigvee_{x_0}^b f(x) - \frac{\epsilon}{2}
$$

- 又由于 $\sigma(f, P) \leq \displaystyle\bigvee_{x_0}^b f(x)$，得到：

$$
\bigvee_a^b f(x) = \lim_{\|P\| \to 0} \sigma(f, P)
$$

[例 12] 设 $\varphi \in R[a, b]$，令 $f(x) = \displaystyle\int_a^x \varphi(t) \text{d} t$，求证：$\displaystyle\bigvee_a^b f(x) = \int_a^b |\varphi(t)| \text{d} t$。

证明：

$$
\forall P : a = x_0 < x_1 < \cdots < x_n = b, \sigma(f, P) = \displaystyle\sum_{i = 1}^n |f(x_i) - f(x_{i - 1})| = \sum_{i = 1}^n \left| \int_{x_{i - 1}}^{x_i} \varphi(t) \text{d} t \right|
$$

- (1) 若 $m_i M_i \geq 0$，则 $\varphi(t)$ 在 $[x_{i - 1}, x_i]$ 上同号，故有 $\left| \displaystyle\int_{x_{i - 1}}^{x_i} \varphi(t) \text{d} t \right| = \displaystyle\int_{x_{i - 1}}^{x_i} |\varphi(t)| \text{d} t$。
- (2) 若 $m_i < 0 < M_i$，则有：

$$
0 \leq \left| \displaystyle\int_{x_{i - 1}}^{x_i} \varphi(t) \text{d} t \right| \leq \displaystyle\int_{x_{i - 1}}^{x_i} |\varphi(t)| \text{d} t \leq \max\{-m_i, M_i\} \Delta x_i \leq \omega_i \Delta x_i
$$

- 综上 (1)(2)，总有 $0 \leq \displaystyle\int_{x_{i - 1}}^{x_i} |\varphi(t)| \text{d} t - \left| \displaystyle\int_{x_{i - 1}}^{x_i} \varphi(t) \text{d} t \right| \leq \omega_i \Delta x_i$。
- 对 $i$ 求和并令 $\|P\| \to 0$ 即得 $\displaystyle\bigvee_a^b f(x) = \int_a^b |\varphi(t)| \text{d} t$。

### 积分的计算

#### 定理 1：换元法

内容：设 $f$ 在区间 $I$ 上连续，$\varphi$ 在 $[a, b]$ 上可导，$\varphi' \in R[a, b]$，$\varphi([a, b]) \subset I$，则：

$$
\int_{\varphi(a)}^{\varphi(b)} f(t) \text{d} t = \int_a^b f(\varphi(x)) \varphi'(x) \text{d} x
$$

[例 13] 设 $f \in C(\mathbb{R})$，求证：$\displaystyle\int_{\frac{3 - \sqrt{5}}{2}}^{\frac{3 + \sqrt{5}}{2}} f \left( x + \frac{1}{x} \right) \left( 1 - \frac{1}{x^2} \right) \text{d} x = 0$。

证明：换元 $t = x + \dfrac{1}{x}$，此时被积函数为 $f$ 且积分上下限相等，故积分值为 $0$。

#### 定理 2：分部积分法

内容：设 $u, v$ 在 $[a, b]$ 上可导，$u', v' \in R[a, b]$，则：

$$
\int_a^b u(x) v'(x) \text{d} x = \left. u(x) v(x) \right|_a^b - \int_a^b u'(x) v(x) \text{d} x
$$

[例 14] 令 $I_n = \displaystyle\int_0^{\frac{\pi}{2}} \sin^n x \text{d} x, J_n = \int_0^{\frac{\pi}{2}} \cos^n x \text{d} x \ (n \in \mathbb{N})$。

- (1) 求证：$I_n = J_n$。
- (2) 求 $I_n$ 的值。

解：

- (1) 在 $J_n$ 中换元 $t = \dfrac{\pi}{2} - x$ 即得。
- (2) 首先易知 $I_0 = \dfrac{\pi}{2}, I_1 = 1$。
- 当 $n \geq 2$，考虑对 $I_n$ 使用分部积分法：

$$
\begin{aligned}
I_n &= \int_0^{\frac{\pi}{2}} \sin^{n - 1} x \cdot \sin x \text{d} x \\
&= \left. \sin^{n - 1} x \cos x \right|_0^{\frac{\pi}{2}} - \int_0^{\frac{\pi}{2}} (n - 1) \sin^{n - 2} x \cos x \cdot (-\cos x) \text{d} x \\
&= (n - 1) \int_0^{\frac{\pi}{2}} \sin^{n - 2} x \cos^2 x \text{d} x \\
&= (n - 1) \int_0^{\frac{\pi}{2}} (\sin^{n - 2} x - \sin^n x) \text{d} x \\
&= (n - 1) (I_{n - 2} - I_n)
\end{aligned}
$$

- 化简即得 $I_n = \dfrac{n - 1}{n} I_{n - 2}$，故有：

$$
I_n = \begin{cases}
\dfrac{\pi}{2}, &\quad n = 0 \\
\dfrac{(2k - 1)!!}{(2k)!!} \cdot \dfrac{\pi}{2}, &\quad n = 2k, k \in \mathbb{N}_+ \\
\dfrac{(2k)!!}{(2k + 1)!!}, &\quad n = 2k + 1, k \in \mathbb{N}
\end{cases}
$$

#### 定理 3：Wallis 公式

内容：$\displaystyle\lim_{n \to +\infty} \left( \frac{(2n)!!}{(2n - 1)!!} \right)^2 \cdot \frac{1}{2n + 1} = \frac{\pi}{2}$。

证明：

$$
\begin{array}{l}
& \forall x \in \left( 0, \dfrac{\pi}{2} \right), \sin^{2n + 1} x < \sin^{2n} x < \sin^{2n - 1} x \\
\Rightarrow & I_{2n + 1} < I_{2n} < I_{2n - 1} \\
\Leftrightarrow & \dfrac{(2n)!!}{(2n + 1)!!} < \dfrac{(2n - 1)!!}{(2n)!!} \cdot \dfrac{\pi}{2} < \dfrac{(2n - 2)!!}{(2n - 1)!!} \\
\Leftrightarrow & \dfrac{n \pi}{2n + 1} < \left( \dfrac{(2n)!!}{(2n - 1)!!} \right)^2 \cdot \dfrac{1}{2n + 1} < \dfrac{\pi}{2}
\end{array}
$$

- 据 $\displaystyle\lim_{n \to +\infty} \frac{n \pi}{2n + 1} = \frac{\pi}{2}$，由夹挤原理即得。

[例 15] 求 $\displaystyle\int_0^{\frac{\pi}{2}} \frac{1}{2 + \tan^2 x} \text{d} x$。

解：

- 直接换元 $t = \tan x$ 会遇到一个问题：当 $x \to \left( \dfrac{\pi}{2} \right)^-, t \to +\infty$；而我们目前尚无法处理无穷限积分。
- 但注意到变上限积分的连续性，考虑通过取极限强行避开这一问题：

$$
\begin{aligned}
\int_0^{\frac{\pi}{2}} \frac{1}{2 + \tan^2 x} \text{d} x &= \lim_{\delta \to 0^+} \int_0^{\frac{\pi}{2} - \delta} \frac{1}{2 + \tan^2 x} \text{d} x \\
&= \lim_{\delta \to 0^+} \int_0^{\frac{1}{\tan \delta}} \frac{1}{(t^2 + 1)(t^2 + 2)} \text{d} t &\quad (\text{换元 } t = \tan x) \\
&= \lim_{\delta \to 0^+} \int_0^{\frac{1}{\tan \delta}} \left( \frac{1}{t^2 + 1} - \frac{1}{t^2 + 2} \right) \text{d} t \\
&= \lim_{\delta \to 0^+} \left. \left( \arctan t - \frac{1}{\sqrt{2}} \arctan \frac{t}{\sqrt{2}} \right) \right|_0^{\frac{1}{\tan \delta}} \\
&= \lim_{x \to +\infty} \left( \arctan x - \frac{1}{\sqrt{2}} \arctan \frac{x}{\sqrt{2}} \right) &\quad \left( \lim_{\delta \to 0^+} \frac{1}{\tan \delta} = +\infty \right) \\
&= \frac{2 - \sqrt{2}}{4} \pi &\quad \left( \lim_{x \to +\infty} \arctan x = \frac{\pi}{2} \right)
\end{aligned}
$$

[例 16] 求证：$\displaystyle\sum_{k = 0}^n (-1)^k C_n^k \frac{1}{2k + 1} = \frac{(2n)!!}{(2n + 1)!!}$。

证明：

$$
\begin{aligned}
\text{LHS} &= \sum_{k = 0}^n (-1)^k C_n^k \int_0^1 x^{2k} \text{d} x \\
&= \int_0^1 \sum_{k = 0}^n C_n^k (-x^2)^k \text{d} x \\
&= \int_0^1 (1 - x^2)^n \text{d} x \\
&= \int_0^{\frac{\pi}{2}} \sin^{2n + 1} t \text{d} t &\quad (换元 \ x = \cos t) \\
&= \frac{(2n)!!}{(2n + 1)!!} &\quad (\text{例 14 的结论})
\end{aligned}
$$

------

在分部积分法的式子中，我们注意到只需 $u, v \in C[a, b]$，作为原定理结论的等式两边的积分都有定义。那我们可以将这一限制放宽吗？下面来加以讨论。

#### 引理 4：Riemann 和的扩展形式

内容：设 $f, g \in R[a, b]$，则：

$$
\lim_{\|P\| \to 0} \sum_{i = 1}^n f(\xi_i) \int_{x_{i - 1}}^{x_i} g(x) \text{d} x = \int_a^b f(x) g(x) \text{d} x
$$

- _可以发现，$g = 1$ 的情况就是原始的 Riemann 和。_

证明：

- 设 $g$ 在 $[a, b]$ 上有界为 $M$，则：

$$
\begin{array}{l}
\forall P, & \left| \displaystyle\sum_{i = 1}^n f(\xi_i) \int_{x_{i - 1}}^{x_i} g(x) \text{d} x - \int_a^b f(x) g(x) \text{d} x \right| \\
\leq & \displaystyle\sum_{i = 1}^n \int_{x_{i - 1}}^{x_i} |f(\xi_i) - f(x)| |g(x)| \text{d} x \\
\leq & M \cdot \displaystyle\sum_{i = 1}^n \omega_i \Delta x_i
\end{array}
$$

- 由 $\displaystyle\lim_{\|P\| \to 0} \sum_{i = 1}^n \omega_i \Delta x_i = 0$ 即得：

$$
\lim_{\|P\| \to 0} \sum_{i = 1}^n f(\xi_i) \int_{x_{i - 1}}^{x_i} g(x) \text{d} x = \int_a^b f(x) g(x) \text{d} x
$$

#### 定理 5：广义分部积分公式

内容：设 $f, g \in R[a, b]$，令 $F, G$ 分别为 $f, g$ 在 $[a, b]$ 上的变上限积分，则：

$$
\int_a^b F(x) g(x) \text{d} x = \left. F(x) G(x) \right|_a^b - \int_a^b f(x) G(x) \text{d} x
$$

证明：

$$
\begin{aligned}
\forall P, \left. F(x) G(x) \right|_a^b &= \sum_{i = 1}^n (F(x_i) G(x_i) - F(x_{i - 1}) G(x_{i - 1})) \\
&= \sum_{i = 1}^n F(x_i) (G(x_i) - G(x_{i - 1})) + \sum_{i = 1}^n G(x_{i - 1}) (F(x_i) - F(x_{i - 1})) \\
&= \sum_{i = 1}^n F(x_i) \int_{x_{i - 1}}^{x_i} g(x) \text{d} x + \sum_{i = 1}^n G(x_{i - 1}) \int_{x_{i - 1}}^{x_i} f(x) \text{d} x
\end{aligned}
$$

- 令 $\|P\| \to 0$，由引理 4 可知：

$$
\left. F(x) G(x) \right|_a^b = \int_a^b F(x) g(x) \text{d} x + \int_a^b f(x) G(x) \text{d} x
$$

- 移项即得所求等式。

### 积分中值定理

#### 定理 1：积分第一中值定理

内容：

- 设 $f, g \in R[a, b]$，$g$ **不变号**，$m = \displaystyle\inf_{x \in [a, b]} f(x), M = \sup_{x \in [a, b]} f(x)$，则：

$$
\exists \mu \in [m, M], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = \mu \int_a^b g(x) \text{d} x
$$

- 特别地，当 $f \in C[a, b]$，有：

$$
\exists x_0 \in [a, b], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = f(x_0) \int_a^b g(x) \text{d} x
$$

[例 17] 设 $f \in C^1 [0, 1]$，求证：$\exists \xi \in [0, 1], \text{s.t. } \displaystyle\int_0^1 f(x) \text{d} x = f(0) + \frac{1}{2} f'(\xi)$。

证明：

$$
\begin{aligned}
\int_0^1 f(x) \text{d} x &= \int_0^1 f(x) \text{d} (x - 1) \\
&= \left. f(x) (x - 1) \right|_0^1 - \int_0^1 f'(x) (x - 1) \text{d} x &\quad (\text{分部积分；构造 } f(0)) \\
&= f(0) + \int_0^1 f'(x) (1 - x) \text{d} x \\
&= f(0) + f'(\xi) \int_0^1 (1 - x) \text{d} x &\quad (\text{第一中值定理：} \exists \xi \in [0, 1]) \\
&= f(0) + \frac{1}{2} f'(\xi)
\end{aligned}
$$

[例 18] 设 $f \in C[0, 1]$，求证：$\displaystyle\lim_{n \to +\infty} \int_0^1 \frac{nf(x)}{1 + n^2 x^2} \text{d} x = \frac{\pi}{2} f(0)$。

证明思路：

- 令 $g(x) = \dfrac{n}{1 + n^2 x^2}$，则 $\displaystyle\int g(x) \text{d} x = \arctan nx + C$。
- _Motivation：当 $n$ 充分大，$\arctan nx$ 在 $0$ 的一个充分小的右邻域内就“几乎遍历完了” $0 \sim \dfrac{\pi}{2}$。_
- 由 $f$ 在 $0$ 处连续可知 $\displaystyle\int_0^{\delta} \frac{n |f(x) - f(0)|}{1 + n^2 x^2} \text{d} x, \int_{\delta}^1 \frac{n |f(x)|}{1 + n^2 x^2} \text{d} x, \left| \int_0^1 \frac{nf(0)}{1 + n^2 x^2} \text{d} x - \frac{\pi}{2} f(0) \right|$ 三部分可以被分别控制，进而得证。

------

为便估计积分，我们可以考虑用一些比较“简单”的函数逼近被积函数。

#### 定理 2：阶梯逼近

内容：

- 设 $f \in R[a, b]$，则：
- (1) 设 $m = \displaystyle\inf_{x \in [a, b]} f(x), M = \sup_{x \in [a, b]} f(x)$，则：

$$
\forall \epsilon > 0, \exists \text{ 阶梯函数 } \varphi, \psi, \text{s.t. } \begin{cases}
m \leq \varphi, \psi \leq M \\
\varphi \leq f \leq \psi \\
\displaystyle\int_a^b (\psi(x) - \varphi(x)) \text{d} x < \epsilon
\end{cases}
$$

- (2) 存在每一项满足 (1) 中条件的阶梯函数列 $\{\varphi_n\}, \{\psi_n\}$，使得任给 $g \in R[a, b]$，均有：

$$
\lim_{n \to +\infty} \int_a^b \varphi_n(x) g(x) \text{d} x = \lim_{n \to +\infty} \int_a^b \psi_n(x) g(x) \text{d} x = \int_a^b f(x) g(x) \text{d} x
$$

证明：

- (1) 由 $f \in R[a, b]$ 可知 $\forall n \in \mathbb{N}_+, \exists P, \text{s.t. } \displaystyle\sum_{i = 1}^k \omega_i \Delta x_i < \epsilon$。
- 定义 $[a, b]$ 上的阶梯函数 $\varphi_n, \psi_n$ 如下：

$$
\varphi_n(x) = \begin{cases}
m_i, &\quad x \in [x_{i - 1}, x_i) \\
m_n, &\quad x = b
\end{cases},
\psi_n(x) = \begin{cases}
M_i, &\quad x \in [x_{i - 1}, x_i) \\
M_n, &\quad x = b
\end{cases}
$$

- 故有 $\displaystyle\int_a^b (\psi_n(x) - \varphi_n(x)) \text{d} x = \sum_{i = 1}^k \omega_i \Delta x_i < \epsilon$。
- (2) 设 $g$ 在 $[a, b]$ 上有界为 $K > 0$，构造 $\varphi_n, \psi_n$ 时取 $\epsilon = \dfrac{1}{n}$，则：

$$
\begin{array}{l}
& \left| \displaystyle\int_a^b \varphi_n(x) g(x) \text{d} x - \int_a^b f(x) g(x) \text{d} x \right| \\
\leq & \displaystyle\int_a^b (f(x) - \varphi_n(x)) |g(x)| \text{d} x \\
\leq & \displaystyle\int_a^b (\psi(x) - \varphi(x)) \text{d} x \cdot K \\
< & \dfrac{K}{n}
\end{array}
$$

- 同理有 $\displaystyle\left| \int_a^b \psi_n(x) g(x) \text{d} x - \int_a^b f(x) g(x) \text{d} x \right| < \dfrac{K}{n}$，又由 $\displaystyle\lim_{n \to +\infty} \frac{K}{n} = 0$ 即可得证。

#### Riemann-Lebesgue 引理

##### 引理 3：周期内积分为 $0$ 的函数的积分有界

内容：设 $g \in R[0, T] \ (T > 0)$，$g$ 以 $T$ 为周期，且 $\displaystyle\int_0^T g(x) \text{d} x = 0$。设 $M = \displaystyle\sup_{x \in [0, T]} g(x)$，则：

$$
\forall a < b, \left| \int_a^b g(x) \text{d} x \right| \leq TM
$$

常见的满足上述条件的函数有 $\sin, \cos$ 等。

##### 定理 4：Riemann-Lebesgue 引理

内容：设 $f \in R[a, b]$，$g$ 满足引理 3 的条件，则 $\displaystyle\lim_{k \to +\infty} \int_a^b f(x) g(kx) \text{d} x = 0$。

证明：

- $\forall \epsilon > 0$，对 $f$ 构造精度为 $\dfrac{\epsilon}{2M}$ 的阶梯逼近函数 $\varphi$ 并取相应分割 $P$。
- 设 $f$ 有上界 $K > 0$，则：

$$
\begin{aligned}
\left| \int_a^b f(x) g(kx) \text{d} x \right| &\leq \int_a^b (f(x) - \varphi(x)) |g(kx)| \text{d} x + \left| \int_a^b \varphi(x) g(kx) \text{d} x \right| \\
&< \frac{\epsilon}{2} + \sum_{i = 1}^n m_i \cdot \left| \int_{x_{i - 1}}^{x_i} g(kx) \text{d} x \right| \\
&\leq \frac{\epsilon}{2} + \frac{nKTM}{k}
\end{aligned}
$$

- 取 $k \geq \dfrac{2nKTM}{\epsilon}$ 即可使得 $\left| \displaystyle\int_a^b f(x) g(kx) \text{d} x \right| < \epsilon$，故 $\displaystyle\lim_{k \to +\infty} \int_a^b f(x) g(kx) \text{d} x = 0$。

##### 定义 5：平均值

若 $f \in R[a, b] \ (a < b)$，称 $\bar{f}_{a, b} = \dfrac{1}{b - a} \displaystyle\int_a^b f(x) \text{d} x$ 为其平均值。

##### 推论 6：周期内积分不为零的情况

内容：设 $f \in R[a, b]$，$g \in R[0, T] \ (T > 0)$，$g$ 以 $T$ 为周期，则：

$$
\lim_{k \to +\infty} \int_a^b f(x) g(kx) \text{d} x = \bar{g}_{0, T}  \int_a^b f(x) \text{d} x
$$

[例 19]

- (1) 设 $x \neq 2k\pi$，求证：$\displaystyle\sum_{k = 1}^n \cos kx = \frac{\sin(n + \frac{1}{2}) x - \sin \frac{x}{2}}{2 \sin\frac{x}{2}}$。
- (2) 设 $\alpha \in (0, \pi], S_n(\alpha) = \displaystyle\sum_{k = 1}^n \frac{\sin k \alpha}{k}$，求证：$\displaystyle\lim_{n \to +\infty} S_n(\alpha) = \frac{\pi - \alpha}{2}$。

证明：

- (1) 积化和差即可，略。
- (2) 由于 $\left(\dfrac{\sin kt}{k}\right)' = \cos kt$，有：

$$
\begin{aligned}
S_n(\alpha) &= \sum_{k = 1}^n \int_\pi^\alpha \cos kt \text{d} t \\
&= \int_\pi^\alpha \left( \sum_{k = 1}^n \cos kt \right) \text{d} t &\quad (有限和与积分可交换) \\
&= \int_\pi^\alpha \frac{\sin(n + \frac{1}{2}) t - \sin \frac{t}{2}}{2 \sin \frac{t}{2}} \text{d} t &\quad ((1) \ 的结论) \\
&= \int_\pi^\alpha \frac{\sin(n + \frac{1}{2})t}{2\sin\frac{t}{2}} \text{d}t + \frac{\pi - \alpha}{2} \\
\Rightarrow \lim_{n \to +\infty} S_n(\alpha) &= \lim_{n \to +\infty} \int_\pi^\alpha \frac{\sin(n + \frac{1}{2}) t}{2 \sin \frac{t}{2}} \text{d} t + \frac{\pi - \alpha}{2} \\
&= \frac{\pi - \alpha}{2} &\quad (\text{Riemann-Lebesgue 引理})
\end{aligned}
$$

- _注：这里以 $\displaystyle\int_\pi^\alpha$ 为积分限是为了避开 (1) 的结论中未定义的 $x = 0$。_

#### 定理 7：连续函数逼近 / 分段线性逼近

内容：

- 设 $f \in R[a, b]$，则：
- (1) 设 $m = \displaystyle\inf_{x \in [a, b]} f(x), M = \sup_{x \in [a, b]} f(x)$，则：

$$
\forall \epsilon > 0, \exists \tilde{f} \in C[a, b], \text{s.t. } \begin{cases}
m \leq \tilde{f} \leq M \\
\tilde{f}(a) = f(a), \tilde{f}(b) = f(b) \\
\displaystyle\int_a^b |\tilde{f}(x) - f(x)| \text{d} x < \epsilon
\end{cases}
$$

- (2) 存在每一项满足 (1) 中条件的连续函数列 $\{\tilde{f}_n\}$，使得任给 $g \in R[a, b]$，均有：

$$
\lim_{n \to +\infty} \int_a^b \tilde{f}_n(x) g(x) \text{d} x = \int_a^b f(x) g(x) \text{d} x
$$

证明：

- (1) 由 $f \in R[a, b]$ 可知 $\exists P, \text{s.t. } \displaystyle\sum_{i = 1}^k \omega_i \Delta x_i < \epsilon$。
- 定义 $[a, b]$ 上的分段线性函数 $\tilde{f}$ 如下：

$$
\tilde{f}(x) = \frac{x_i - x}{\Delta x_i} f(x_{i - 1}) + \frac{x - x_{i - 1}}{\Delta x_i} f(x_i), \quad x \in [x_{i - 1}, x_i]
$$

- 则 $\tilde{f} \in C[a, b]$，且有：

$$
\begin{aligned}
|\tilde{f}(x) - f(x)| &\leq \frac{x_i - x}{\Delta x_i} |f(x_{i - 1}) - f(x)| + \frac{x - x_{i - 1}}{\Delta x_i} |f(x_i) - f(x)| \\
&\leq \frac{x_i - x}{\Delta x_i} \cdot \omega_i + \frac{x - x_{i - 1}}{\Delta x_i} \cdot \omega_i \\
&= \omega_i
\end{aligned}
$$

- 故有 $\displaystyle\int_a^b |\tilde{f}(x) - f(x)| \text{d} x \leq \sum_{i = 1}^k \omega_i \Delta x_i < \epsilon$。
- (2) 同定理 2(2) 即得，略。

#### 引理 8：Abel 变换 / “离散分部积分”

内容：

- (1) 设 $S_n = \displaystyle\sum_{i = 1}^n b_i$，则 $\displaystyle\sum_{i = 1}^n a_i b_i = \sum_{i = 1}^{n - 1} (a_i - a_{i + 1}) S_i + a_n S_n$。
- (2) $a_0 b_0 + \displaystyle\sum_{i = 1}^n (a_i - a_{i - 1}) b_{i - 1} = \sum_{i = 1}^n a_i (b_{i - 1} - b_i) + a_n b_n$。

上面的 (2) 也可以理解为下面“离散分部积分”的形式：

$$
\sum \Delta a_i b_{i - 1} = \left. a_i b_i \right|_0^n - \sum a_i \Delta b_i
$$

------

事实上也可以从线性代数的视角来看待 Abel 变换：

- 设 $\alpha = (a_1, \cdots, a_n)^T, \beta = (b_1, \cdots, b_n)^T$，则 $\displaystyle\sum_{i = 1}^n a_i b_i = \alpha^T \beta$。
- 取 $n$ 阶非奇异阵 $M$，则 $\alpha^T \beta = (M^T \alpha)^T (M^{-1} \beta)$。

令 $M = \begin{bmatrix} 1 \\ -1 & 1 \\ & -1 & \ddots \\ & & \ddots & 1 \\ & & & -1 & 1 \end{bmatrix}$，则 $M^{-1} = \begin{bmatrix} 1 \\ 1 & 1 \\ \vdots & & \ddots \\ 1 & 1 & \cdots & 1 \end{bmatrix}$，代入即得经典的 Abel 变换。

#### 定理 9：积分第二中值定理 I / Bonet 定理

内容：设 $f \geq 0, g \in R[a, b]$。

- (1) 若 $f$ 在 $[a, b]$ 上单调递减，则：

$$
\exists \xi \in [a, b], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = f(a) \int_a^{\xi} g(x) \text{d} x
$$

- (2) 若 $f$ 在 $[a, b]$ 上单调递增，则：

$$
\exists \xi \in [a, b], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = f(b) \int_{\xi}^b g(x) \text{d} x
$$

证明：

- 下面证明 (1)，(2) 是同理的。
- 令 $G(x) = \displaystyle\int_a^x g(t) \text{d} t$，则有：

$$
\begin{aligned}
\int_a^b f(x) g(x) \text{d} x &= \lim_{\|P\| \to 0} \sum_{i = 1}^n f(x_{i - 1}) \int_{x_{i - 1}}^{x_i} g(x) \text{d} x &\quad (\text{Riemann 和的扩展形式}) \\
&= \lim_{\|P\| \to 0} \sum_{i = 1}^n f(x_{i - 1}) (G(x_i) - G(x_{i - 1})) \\
&= \lim_{\|P\| \to 0} \left( \sum_{i = 1}^n G(x_i) (f(x_{i - 1}) - f(x_i)) + G(b) f(b) \right) &\quad (\text{Abel 变换}) \\
&\geq \lim_{\|P\| \to 0} \left( \sum_{i = 1}^n (f(x_{i - 1}) - f(x_i)) + f(b) \right) \cdot \inf_{x \in [a, b]} G(x) \\
&= f(a) \cdot \min_{x \in [a, b]} G(x) &\quad (G \in C[a, b])
\end{aligned}
$$

- 同理可知：

$$
\int_a^b f(x) g(x) \text{d} x \leq f(a) \cdot \max_{x \in [a, b]} G(x)
$$

- 由 $G \in C[a, b]$ 即得：

$$
\exists \xi \in [a, b], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = f(a) G(\xi) = f(a) \int_a^{\xi} g(x) \text{d} x
$$

#### 定理 10：积分第二中值定理 II / Weierstrass 定理

内容：设 $f$ 在 $[a, b]$ 上单调，$g \in R[a, b]$，则：

$$
\exists \xi \in [a, b], \text{s.t. } \int_a^b f(x) g(x) \text{d} x = f(a) \int_a^{\xi} g(x) \text{d} x + f(b) \int_{\xi}^b g(x) \text{d} x
$$

证明：将 $f - f(b)$（单调递减）或 $f - f(a)$（单调递增）代入定理 9，整理即得。

[例 20] 设 $f$ 在 $[a, b]$ 上单调递增，求证：$\displaystyle\int_a^b x f(x) \text{d} x \geq \frac{a + b}{2} \int_a^b f(x) \text{d} x$。

证明：

- 由 Weierstrass 定理可知：

$$
\begin{array}{l}
\exists \xi \in [a, b], & \text{s.t. } & \displaystyle\int_a^b \left( x - \frac{a + b}{2} \right) f(x) \text{d} x \\
& = & f(a) \displaystyle\int_a^{\xi} \left( x - \frac{a + b}{2} \right) \text{d} x + f(b) \int_{\xi}^b \left( x - \frac{a + b}{2} \right) \text{d} x \\
& = & (f(b) - f(a)) L &\quad \left( L = \displaystyle\int_{\xi}^b \left( x - \frac{a + b}{2} \right) \text{d} x \right) \\
& \geq & 0 &\quad \left( f(a) \leq f(b), L \geq 0 \right)
\end{array}
$$

- 移项整理即为所求。

[例 21] 设 $f(x) = \begin{cases} \sin \dfrac{1}{x}, &\quad x \neq 0 \\ 0, &\quad x = 0 \end{cases}$，令 $F(x) = \displaystyle\int_0^x f(t) \text{d} t$，求证：$F$ 在 $0$ 处可导，且 $F'(0) = 0$。

证明：

- 由 $f$ 为奇函数可知 $F$ 为偶函数，故只需证明：

$$
\begin{array}{l}
& F'_+(0) = 0 \\
\Leftrightarrow & \displaystyle\lim_{x \to 0^+} \frac{1}{x} \int_0^x g(t) \text{d} t = 0 \\
\Leftrightarrow & \displaystyle\int_0^x g(t) \text{d} t = o(x) \ (x \to 0^+) \\
\Leftrightarrow & \displaystyle\lim_{\delta \to 0^+} \int_{\delta}^x g(t) \text{d} t = o(x) \ (x \to 0^+) &\quad (变限积分的连续性) \\
\Leftrightarrow & \displaystyle\lim_{\delta \to 0^+} \int_{\delta}^x \sin \frac{1}{t} \text{d} t = o(x) \ (x \to 0^+) \\
\end{array}
$$

- _Motivation：造出 $x^2$ 因子不就是 $o(x)$ 的了吗？_
- 而当 $\delta \in (0, x)$，有：

$$
\begin{aligned}
\int_{\delta}^x \sin \frac{1}{t} \text{d} t &= \int_{\frac{1}{x}}^{\frac{1}{\delta}} \frac{\sin u}{u^2} \text{d} u &\quad \left( \text{换元 } u = \frac{1}{x} \right) \\
&= x^2 \int_{\frac{1}{x}}^{\xi} \sin u \text{d} u &\quad (\text{Bonnet 定理；} \xi \in \left[ \frac{1}{x}, \frac{1}{\delta} \right]) \\
&= O(x^2) \ (x \to 0^+)
\end{aligned}
$$

- 故 $\displaystyle\lim_{\delta \to 0^+} \int_{\delta}^x \sin \frac{1}{t} \text{d} t = O(x^2) = o(x) \ (x \to 0^+)$，遂得证。

[例 22] 设 $f$ 在 $[0, 2 \pi]$ 上下凸，$f \in C^1 [0, 2 \pi]$，求证：$\forall n \in \mathbb{N}_+, \displaystyle\int_0^{2 \pi} f(x) \cos nx \text{d} x \geq 0$。

法一（导函数的单调性 + Weierstrass 定理）：

- 由凸性可知 $f'$ 在 $[0, 2 \pi]$ 上单调递增，则有：

$$
\begin{aligned}
\int_0^{2 \pi} f(x) \cos nx \text{d} x &= \left. f(x) \frac{\sin nx}{n} \right|_0^{2 \pi} - \int_0^{2 \pi} f'(x) \frac{\sin nx}{n} \text{d} x \\
&= -\frac{1}{n} \int_0^{2 \pi} f'(x) \sin nx \text{d} x \\
&= -\frac{1}{n} \left( f'(0) \int_0^{\xi} \sin nx \text{d} x + f'(2 \pi) \int_{\xi}^{2\pi} \sin nx \text{d} x \right) &\quad (\text{Weierstrass 定理}) \\
&= \frac{1}{n^2} (f'(2 \pi) - f'(0))(1 - \cos n \xi) \\
&\geq 0
\end{aligned}
$$

法二（凸函数的几何性质）：

- 易见只需证明 $n = 1$ 的情况。
- _Motivation：统一因子的正负。_
- 将 $\cos x$ 在 $[0, 2 \pi]$ 上等距 $\dfrac{\pi}{2}$ 划分为 $4$ 个区间，则：

$$
\forall x \in \left[ 0, \frac{\pi}{2} \right], \cos x = -\cos (\pi - x) = -\cos (\pi + x) = \cos (2 \pi - x)
$$

- 依此将积分重组为 $\displaystyle\int_0^{\frac{\pi}{2}} (f(x) - f(\pi - x) - f(\pi + x) + f(2 \pi - x)) \cos x \text{d} x$，而由凸函数的几何性质可知：

$$
k_{x, \pi - x} \leq k_{\pi + x, 2 \pi - x} \Rightarrow f(x) - f(\pi - x) - f(\pi + x) + f(2 \pi - x) \geq 0
$$

- 故该积分逐点非负，则积分值非负，遂得证。

### 积分不等式

#### 连续版的 Jensen 不等式

内容：已知 $f \in R[0, 1], m = \displaystyle\inf_{x \in [0, 1]} f(x), M = \sup_{x \in [0, 1]} f(x)$，且 $\varphi \in C[m, M]$ 下凸，则：

$$
\varphi \left( \int_0^1 f(x) \text{d} x \right) \leq \int_0^1 \varphi(f(x)) \text{d} x
$$

证明：

- 由 $f \in R[0, 1], \varphi \in C[m, M]$ 可知 $\varphi \circ f \in R[0, 1]$。
- 对 $[0, 1]$ 进行 $n$ 等分，由离散版的 Jensen 不等式可知：

$$
\varphi \left( \frac{1}{n} \sum_{i = 1}^n f \left( \frac{i}{n} \right) \right) \leq \frac{1}{n} \sum_{i = 1}^n \varphi \left( f \left( \frac{i}{n} \right) \right)
$$

- 两边对 $n \to +\infty$ 取极限，由 $\varphi$ 的连续性即得：

$$
\varphi \left( \int_0^1 f(x) \text{d} x \right) \leq \int_0^1 \varphi(f(x)) \text{d} x
$$

#### 连续版的 Cauchy 不等式

内容：已知 $f, g \in R[a, b]$，则：

$$
\left| \int_a^b f(x) g(x) \text{d} x \right| \leq \sqrt{\int_a^b f^2(x) \text{d} x} \cdot \sqrt{\int_a^b g^2(x) \text{d} x}
$$

证明：

- 令 $A = \displaystyle\int_a^b |f(x)| |g(x)| \text{d} x \geq 0$，首先有 $\left| \displaystyle\int_a^b f(x) g(x) \text{d} x \right| \leq A \ (1)$。
- 令 $B = \displaystyle\int_a^b f^2(x) \text{d} x, C = \int_a^b g^2(x) \text{d} x$，则有：

$$
\begin{aligned}
\forall t \in \mathbb{R}, & \int_a^b (t|f(x)| - |g(x)|)^2 \text{d} x \geq 0 \\
\Rightarrow & Bt^2 - 2At + C \geq 0 \\
\Rightarrow & P = (-2A)^2 - 4BC \leq 0 \\
\Rightarrow & A \leq \sqrt{BC} &\quad (2)
\end{aligned}
$$

- 联立 (1)(2) 即得：

$$
\left| \int_a^b f(x) g(x) \text{d} x \right| \leq \sqrt{\int_a^b f^2(x) \text{d} x} \cdot \sqrt{\int_a^b g^2(x) \text{d} x}
$$

[例 23] 已知 $f \in R[a, b]$，求证：

$$
\left( \int_a^b f(x) \sin x \text{d} x \right)^2 + \left( \int_a^b f(x) \cos x \text{d} x \right)^2 \leq (b - a) \int_a^b f^2(x) \text{d} x
$$

证明：

- 由 Cauchy 不等式可知：

$$
\begin{aligned}
\text{LHS} &\leq \int_a^b f^2(x) \text{d} x \cdot \int_a^b \sin^2 x \text{d} x + \int_a^b f^2(x) \text{d} x \cdot \int_a^b \cos^2 x \text{d} x \\
&= \int_a^b f^2(x) \text{d} x \cdot \int_a^b (\sin^2 x+ \cos^2 x) \text{d} x \\
&= (b - a) \int_a^b f^2(x) \text{d} x
\end{aligned}
$$

[例 24] 已知 $f \in R[a, b]$ 非负，求证：

$$
\left( \int_a^b f(x) \sin x \text{d} x \right)^2 + \left( \int_a^b f(x) \cos x \text{d} x \right)^2 \leq \left( \int_a^b f(x) \text{d} x \right)^2
$$

证明：

- 由 Cauchy 不等式可知：

$$
\begin{aligned}
\text{LHS} &= \left( \int_a^b \sqrt{f(x)} \cdot \sqrt{f(x)} \sin x \text{d} x \right)^2 + \left( \int_a^b \sqrt{f(x)} \cdot \sqrt{f(x)} \cos x \text{d} x \right)^2 \\
&\leq \int_a^b f(x) \text{d} x \cdot \int_a^b f(x) \sin^2 x \text{d} x + \int_a^b f(x) \text{d} x \cdot \int_a^b f(x) \cos^2 x \text{d} x \\
&= \int_a^b f(x) \text{d} x \cdot \int_a^b f(x) (\sin^2 x+ \cos^2 x) \text{d} x \\
&= \left( \int_a^b f(x) \text{d} x \right)^2
\end{aligned}
$$

#### 连续版的 Hölder 不等式

内容：已知 $f, g \in R[a, b]$ 非负，$p, q > 1$ 且 $\dfrac{1}{p} + \dfrac{1}{q} = 1$，则：

$$
\int_a^b f(x) g(x) \text{d} x \leq \left( \int_a^b f^p(x) \text{d} x \right)^{\frac{1}{p}} \cdot \left( \int_a^b g^q(x) \text{d} x \right)^{\frac{1}{q}}
$$

证明：

- 法一：同上 Jensen 不等式的证明转化为 Riemann 和，再对其求极限。
- 法二：首先特判 $\displaystyle\int_a^b f^p(x) \text{d} x = 0$ 或 $\displaystyle\int_a^b g^q(x) \text{d} x = 0$ 的情况。
- 否则，通过给分别 $f, g$ 乘上系数，转化为 $\displaystyle\int_a^b f^p(x) \text{d} x = \int_a^b g^q(x) \text{d} x = 1$ 的情况。
- 由 Young 不等式即得：

$$
\begin{aligned}
\int_a^b f(x) g(x) \text{d} x &\leq \int_a^b \left( \frac{1}{p} f^p(x) + \frac{1}{q} g^q(x) \right) \text{d} x \\
&= \frac{1}{p} \int_a^b f^p(x) \text{d} x + \frac{1}{q} \int_a^b g^q(x) \text{d} x \\
&= 1
\end{aligned}
$$

#### 连续版的 Minkowski 不等式

内容：已知 $f, g \in R[a, b]$ 非负，$p > 1$，则：

$$
\left( \int_a^b (f(x) + g(x))^p \text{d} x \right)^{\frac{1}{p}} \leq \left( \int_a^b f^p(x) \text{d} x \right)^{\frac{1}{p}} + \left( \int_a^b g^p(x) \text{d} x \right)^{\frac{1}{p}}
$$

证明：

$$
\begin{aligned}
\text{LHS} &= \int_a^b f(x) (f(x) + g(x))^{p - 1} \text{d} x + \int_a^b g(x) (f(x) + g(x))^{p - 1} \text{d} x \\
&\leq \left( \left( \int_a^b f^p(x) \text{d} x \right)^{\frac{1}{p}} + \left( \int_a^b g^p(x) \text{d} x \right)^{\frac{1}{p}} \right) \cdot \left( \int_a^b \left( (f(x) + g(x))^{p - 1} \right)^q \text{d} x \right)^{\frac{1}{q}} &\quad \left( \frac{1}{p} + \frac{1}{q} = 1; \text{Hölder} \right) \\
&= \left( \left( \int_a^b f^p(x) \text{d} x \right)^{\frac{1}{p}} + \left( \int_a^b g^p(x) \text{d} x \right)^{\frac{1}{p}} \right) \cdot \left( \int_a^b (f(x) + g(x))^p \text{d} x \right)^{\frac{1}{q}}
\end{aligned}
$$

- 不等式左右同时除以 $\left( \displaystyle\int_a^b (f(x) + g(x))^p \text{d} x \right)^{\frac{1}{q}}$ 即得：

$$
\left( \int_a^b (f(x) + g(x))^p \text{d} x \right)^{\frac{1}{p}} \leq \left( \int_a^b f^p(x) \text{d} x \right)^{\frac{1}{p}} + \left( \int_a^b g^p(x) \text{d} x \right)^{\frac{1}{p}}
$$

#### Gronwall 不等式

内容：已知 $f, g \in C[0, A]$ 且函数值非负，若 $f(x) \leq K + \displaystyle\int_0^x f(t)g(t) \text{d} t \ (K \geq 0)$，则：$f(A) \leq K \exp \left( \displaystyle\int_0^A g(t) \text{d} t \right)$。

证明：

- _Motivation：$f$ 在条件不等式中“自引用” $\Rightarrow$ 构造辅助函数“解耦”。_
- 令 $H(x) = \displaystyle\int_0^x f(t) g(t) \text{d} t \geq 0$，则 $H'(x) = f(x)g(x)$，且条件不等式等价于：

$$
f(x) \leq K + H(x)
$$

- _Motivation：我们可以通过控制 $H'$ 来控制 $f$。_
- 将 $H'(x)$ 的表达式代入条件不等式，可知：

$$
H'(x) \leq (K + H(x)) \cdot g(x)
$$

- _Motivation：为了造出 $g$ 的积分形式，将其独立于不等号一侧。_
- (1) 当 $K > 0$，两边同时除以 $K + H(x)$ 后积分：

$$
\begin{array}{l}
& \dfrac{H'(x)}{K + H(x)} \leq g(x) \\
\Rightarrow & \displaystyle\int_0^A \frac{H'(x)}{K + H(x)} \text{d} x \leq \int_0^A g(x) \text{d} x \\
\Leftrightarrow & \left. \ln(K + H(x)) \right|_0^A \leq \displaystyle\int_0^A g(x) \text{d} x \\
\Leftrightarrow & K + H(A) \leq K \exp \left( \displaystyle\int_0^A g(x) \text{d} x \right) \\
\Rightarrow & f(A) \leq K \exp \left( \displaystyle\int_0^A g(x) \text{d} x \right)
\end{array}
$$

- (2) 当 $K = 0$，对 $K \to 0^+$ 的结果取极限即得。

#### Young 不等式

内容：已知 $y = f(x) \in C[0, +\infty)$ 严格单调递增，$f(0) = 0$，记其反函数为 $x = f^{-1}(y)$，则：

$$
\forall a, b > 0, ab \leq \int_0^a f(x) \text{d} x + \int_0^b f^{-1}(y) \text{d} y
$$

- 感性理解：当 $b = f(a)$，曲线 $y = f(x)$ 与 $y = 0, x = a$ 和 $x = 0, y = b$ 所夹的两部分拼起来刚好是面积为 $ab$ 的矩形。

证明：

- 不妨设 $f(a) \leq b$，$f(a) > b$ 的情况是同理的。

$$
\begin{aligned}
\text{RHS} &\geq \int_0^a f(x) \text{d} x + \int_0^{f(a)} f^{-1}(y) \text{d} y + \int_{f(a)}^b a \text{d} y &\quad (f^{-1} \text{ 单调递增}) \\
&= \int_0^a f(x) \text{d} x + \int_0^a x \text{d} f(x) + a(b - f(a)) &\quad (\text{换元 } y = f(x)) \\
&= \left. xf(x) \right|_0^a + a(b - f(a)) \\
&= ab
\end{aligned}
$$

- 但这里有一个问题：$f$ 未必在 $[0, a]$ 上可导，这将导致上面的换元无法进行。
- 考虑回到 Riemann 积分的定义。

$$
\forall \epsilon > 0, \exists P : 0 = x_0 < \cdots < x_n = a, \text{s.t. } \left| S_f(P, \{x_{i - 1}\}_{i = 1}^n) - \int_0^a f(x) \text{d} x \right| < \epsilon
$$

- 由于 $f$ 在 $[0, +\infty)$ 上严格单调递增，构造 $[0, f(a)]$ 的分割 $P' : 0 = f(x_0) < \cdots < f(x_n) = f(a)$，则：

$$
\begin{aligned}
S_{f^{-1}}(P', \{f(x_i)\}_{i = 1}^n) &= \sum_{i = 1}^n f^{-1} (f(x_i)) (f(x_i) - f(x_{i - 1})) \\
&= \sum_{i = 1}^n x_i (f(x_i) - f(x_{i - 1})) \\
&= a f(a) - \sum_{i = 1}^n f(x_{i - 1}) (x_i - x_{i - 1}) \\
&= a f(a) - S_f(P, \{x_{i - 1}\}_{i = 1}^n)
\end{aligned}
$$

- 故有：

$$
\left| S_{f^{-1}}(P', \{f(x_i)\}_{i = 1}^n) - \int_0^{f(a)} f^{-1}(y) \text{d} y \right| < \epsilon
$$

- 因此 $\displaystyle\int_0^{f(a)} f^{-1}(y) \text{d} y = a f(a) - \int_0^a f(x) \text{d} x$，这就解决了前面因 $f$ 未必可导而无法换元的问题。

取 $f(x) = x^{p - 1} \ (p > 1)$ 即可得到经典形式的 Young 不等式。

### 杂题

#### 值域对称的处理

[例 25] 求 $I = \displaystyle\int_{-\frac{\pi}{4}}^{\frac{\pi}{4}} \frac{\cos^2 x}{1 + e^x} \text{d} x$。

解：
$$
\begin{aligned}
I &= \int_0^{\frac{\pi}{4}} \frac{\cos^2 x}{1 + e^x} \text{d} x + \int_0^{\frac{\pi}{4}} \frac{\cos^2 x}{1 + e^{-x}} \text{d} x \\
&= \int_0^{\frac{\pi}{4}} \cos^2 x \text{d} x \\
&= \left. \left( \frac{1}{2}x + \frac{1}{4} \sin 2x \right) \right|_0^{\frac{\pi}{4}} \\
&= \frac{\pi}{8} + \frac{1}{4}
\end{aligned}
$$

[例 26]

- (1) 已知 $f \in C[0, \pi]$，求证：

$$
\int_0^\pi x f(\sin x) \text{d} x = \frac{\pi}{2} \int_0^\pi f(\sin x) \text{d} x
$$

- (2) 求证：

$$
\int_0^\pi \frac{x \sin^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x = \frac{\pi^2}{4} \quad (n \in \mathbb{N})
$$

证明：

- (1)

$$
\begin{aligned}
\text{LHS} &= \frac{1}{2} \left( \int_0^\pi x f(\sin x) \text{d} x + \int_0^\pi x f(\sin(\pi - x)) \text{d} x \right) \\
&= \frac{1}{2} \left( \int_0^\pi x f(\sin x) \text{d} x + \int_0^\pi (\pi - x) f(\sin x) \text{d} x \right) \\
&= \frac{\pi}{2} \int_0^\pi f(\sin x) \text{d} x \\
&= \text{RHS}
\end{aligned}
$$

- (2)

$$
\begin{aligned}
\text{LHS} &\stackrel{(1)}{=} \frac{\pi}{2} \int_0^\pi \frac{\sin^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x \\
&= \frac{\pi}{2} \left( \int_0^{\frac{\pi}{2}} \frac{\sin^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x + \int_{\frac{\pi}{2}}^\pi \frac{\sin^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x \right) \\
&= \frac{\pi}{2} \left( \int_0^{\frac{\pi}{2}} \frac{\sin^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x + \int_0^{\frac{\pi}{2}} \frac{\cos^{2n} x}{\sin^{2n} x + \cos^{2n} x} \text{d} x \right) \\
&= \frac{\pi}{2} \cdot \frac{\pi}{2} \\
&= \frac{\pi^2}{4}
\end{aligned}
$$

#### 边界点的特殊处理

[例 27] 求 $\displaystyle\lim_{x \to 0} \frac{1}{x^2} \int_0^x \left( \frac{1}{u} - \cot u \right) \text{d} u$。

解：

- 首先考察被积函数在 $0$ 附近的有界性（即求极限）：

$$
\begin{aligned}
\lim_{u \to 0} \left( \frac{1}{u} - \cot u \right) &= \lim_{u \to 0} \frac{\sin u - u \cos u}{u \sin u} \\
&= \lim_{u \to 0} \frac{u \sin u}{u \cos u + \sin u} &\quad (洛必达) \\
&= \lim_{u \to 0} \frac{\sin u}{\cos u + \frac{\sin u}{u}} \\
&= 0
\end{aligned}
$$

- 即 $\exists \delta_0 > 0, \text{s.t. } \forall u \in \check{U}(0, \delta_0), \left| \dfrac{1}{u} - \cot u \right| < 1$，则有：

$$
\begin{aligned}
& \forall \epsilon > 0, \text{let } \delta = \min\{\epsilon, \delta_0\}, \forall x \in U(0, \delta), \left| \int_0^x \left( \frac{1}{u} - \cot u \right) \text{d} u \right| < \epsilon \\
\Rightarrow & \lim_{x \to 0} \int_0^x \left( \frac{1}{u} - \cot u \right) \text{d} u = 0
\end{aligned}
$$

- 故有：

$$
\begin{aligned}
\lim_{x \to 0} \frac{1}{x^2} \int_0^x \left( \frac{1}{u} - \cot u \right) \text{d} u &= \lim_{x \to 0} \frac{\frac{1}{x} - \cot x}{2x} &\quad (洛必达) \\
&= \lim_{x \to 0} \frac{-\frac{1}{x^2} + \frac{1}{\sin^2 x}}{2} &\quad (洛必达) \\
&= \frac{1}{2} \lim_{x \to 0} \frac{x^2 - \sin^2 x}{x^2 \sin^2 x} \\
&= \frac{1}{2} \lim_{x \to 0} \frac{\frac{1}{3} x^4 + o(x^4)}{x^4 + o(x^4)} \\
&= \frac{1}{6}
\end{aligned}
$$

[例 28] 求 $I = \displaystyle\int_0^{\frac{\pi}{2}} \sin x \ln(\sin x) \text{d} x$。

解：

- _Motivation：看到 $\sin, \ln$ 堆叠，且并非几种常规类型的积分，考虑换元 / 分部积分。_
- _Motivation：若换元 $t = \sin x$ 或 $t = \cos x$，会引入关于 $t$ 的含根号项。因此考虑分部积分。_

$$
I = \left. -\cos x \ln (\sin x) \right|_0^{\frac{\pi}{2}} - \int_0^{\frac{\pi}{2}} (-\cos x) \cdot \frac{\cos x}{\sin x} \text{d} x
$$

- 这个式子看上去非常有道理，但注意到两部分在 $x \to 0$ 时都是发散的 /ll
- 但注意到：

$$
\begin{aligned}
\lim_{x \to 0} \sin x \ln(\sin x) &= \lim_{x \to 0} \frac{\ln(\sin x)}{x^{-1}} \\
&= \lim_{x \to 0} \frac{\frac{\cos x}{\sin x}}{-x^{-2}} \\
&= -\lim_{x \to 0} x \\
&= 0
\end{aligned}
$$

- 则同前一例可证 $\displaystyle\lim_{\epsilon \to 0^+} \int_\epsilon^{\frac{\pi}{2}} \sin x \ln(\sin x) \text{d} x = 0$，故有：

$$
\begin{aligned}
I &= \lim_{\epsilon \to 0^+} \int_\epsilon^{\frac{\pi}{2}} \sin x \ln(\sin x) \text{d} x \\
&= \lim_{\epsilon \to 0^+} \left( \cos \epsilon \ln (\sin \epsilon) + \left. \left( \cos x + \ln \left( \tan \frac{x}{2} \right) \right) \right|_\epsilon^{\frac{\pi}{2}}\right) \\
&= \lim_{\epsilon \to 0^+} \left( \cos \epsilon \cdot \left( \ln 2 - 1 - \ln \left( 1 + \tan^2 \frac{\epsilon}{2} \right) \right) - (\cos \epsilon - 1) \ln \left( \tan \frac{\epsilon}{2} \right) \right) \\
&= (\ln 2 - 1) + \lim_{\epsilon \to 0^+} \frac{1}{2} \epsilon^2 \cdot \ln \left( \tan \frac{\epsilon}{2} \right) \\
&= (\ln 2 - 1) + 2 \lim_{\epsilon \to 0^+} \frac{\ln (\tan \frac{\epsilon}{2})}{\epsilon^{-2}} \\
&= (\ln 2 - 1) - \lim_{\epsilon \to 0^+} \frac{\epsilon^3}{\sin \frac{\epsilon}{2} \cos \frac{\epsilon}{2}} &\quad (洛必达) \\
&= \ln 2 - 1
\end{aligned}
$$

#### 未分类

[例 29] 已知 $f \in C[a, b]$，且 $\forall k \in \{0, \cdots, n\}, \displaystyle\int_a^b f(x) x^k \text{d} x = 0$，求证：$f$ 在 $[a, b]$ 上至少有 $n + 1$ 个零点。

证明：

- _Motivation：$\displaystyle\int_a^b f(x) x^k \text{d} x$ 可以作为“基”，“张成”所有次数不高于 $n$ 的多项式与 $f$ 相乘的积分。_
- 假设 $f$ 在 $[a, b]$ 上只有 $m \leq n$ 个零点，其中包含 $k \leq m$ 个**变号**零点 $x_1, \cdots, x_k$。
- 由 $f \in C[a, b]$ 可知：在变号零点分隔开的各个区间中，$f$ **不变号**。
- 令 $P(x) = \displaystyle\prod_{i = 1}^k (x - x_i)$，一方面由 $\deg P = k \leq n$ 可知 $\displaystyle\int_a^b f(x) P(x) \text{d} x = 0$。
- 另一方面，$f(x) P(x)$ 在 $[a, b]$ 上不变号，且只有 $m$ 个零点，则 $\displaystyle\int_a^b f(x) P(x) \text{d} x \neq 0$，矛盾！
- 故假设不成立，即 $f$ 在 $[a, b]$ 上至少有 $n + 1$ 个零点。
