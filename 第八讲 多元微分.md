### 偏导数与全微分

#### 定义：多元函数的偏导数和偏导函数

设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，若 $\displaystyle\lim_{\Delta x_i \to 0} \frac{f(\mathbf{x}_0 + \Delta x_i \mathbf{e}_i) - f(\mathbf{x}_0)}{\Delta x_i}$ 存在，则称此极限为 $f$ 在 $\mathbf{x}_0$ 处关于 $x_i$ 的偏导数，也称 $f$ 在 $\mathbf{x}_0$ 处对 $x_i$ 可导，记作 $\left. \dfrac{\partial f}{\partial x_i} \right|_{\mathbf{x}_0}, \dfrac{\partial f(\mathbf{x}_0)}{\partial x_i}, f_{x_i}'(\mathbf{x}_0)$。

若 $f: S \to \mathbb{R}$ 在区域 $D \subset S$ 上每一点都有 $\dfrac{\partial f}{\partial x_i}$，则称 $f_{x_i}' : D \to \mathbb{R}$ 为 $f$ 在 $D$ 上关于 $x_i$ 的偏导函数，简称 $f$ 关于 $x_i$ 的偏导数。

[例 1] 计算 $f(x, y) = \arctan \dfrac{x + y}{1 - xy}$ 在 $(0, 0)$ 处的两个偏导数。

解：

$$
\begin{cases}
f_x'(0, 0) = \displaystyle\lim_{x \to 0} \frac{\arctan x}{x} = 1 \\
f_y'(0, 0) = \displaystyle\lim_{y \to 0} \frac{\arctan y}{y} = 1 \\
\end{cases}
$$

[例 2] 计算 $f(x, y, z) = x^y \sin z \ (x > 0)$ 在定义域内的偏导函数。

解：

$$
\begin{cases}
\dfrac{\partial f}{\partial x} = y x^{y - 1} \sin z \\
\dfrac{\partial f}{\partial y} = x^y \ln x \sin z \\
\dfrac{\partial f}{\partial z} = x^y \cos z
\end{cases}
$$

------

事实上，偏导数存在是一个相当弱的条件，它甚至不蕴含连续性。下为一例。

[例 3] 设 $f(x, y) = \begin{cases} \dfrac{xy}{x^2 + y^2}, &\quad xy \neq 0 \\ 0, &\quad xy = 0 \end{cases}$，我们在第七讲例 4 中已经证明其在 $(0, 0)$ 处不连续，求证：$f$ 在 $(0, 0)$ 处对 $x, y$ 可导。

证明：

$$
\begin{cases}
f_x'(0, 0) = \displaystyle\lim_{x \to 0} 0 = 0 \\
f_y'(0, 0) = \displaystyle\lim_{y \to 0} 0 = 0 \\
\end{cases}
$$

#### 定义：多元函数的全微分 / 导数

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$\mathbf{x}_0 \in D$，若 $\exists \mathbf{A} \in \mathbb{R}^n, \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) - f(\mathbf{x}_0) = \langle \mathbf{A}, \Delta \mathbf{x} \rangle + o(||\Delta \mathbf{x}||) \ (||\Delta \mathbf{x}|| \to 0)$，则称 $f$ 在 $\mathbf{x}_0$ 处可微。

此时称增量的线性主部 $\langle \mathbf{A}, \Delta \mathbf{x} \rangle$ 为 $f$ 在 $\mathbf{x}_0$ 处的全微分，记作 $\text{d} f(\mathbf{x}_0)$；称 $\mathbf{A}$ 为 $f$ 在 $\mathbf{x}_0$ 处的导数，记作 $f'(\mathbf{x}_0)$。

#### 命题：可微蕴含连续

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $f$ 在 $\mathbf{x}_0$ 处连续。

#### 命题：可微蕴含可偏导

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，$\text{d} f(\mathbf{x}_0) = \langle \mathbf{A}, \Delta \mathbf{x} \rangle$，则 $\forall 1 \leq i \leq n$，$f$ 在 $\mathbf{x}_0$ 处对 $x_i$ 可导，且 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = \mathbf{A}_i$。

推论：若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $\text{d} f(\mathbf{x}_0) = \displaystyle\sum_{i = 1}^n \frac{\partial f(\mathbf{x}_0)}{\partial x_i} \text{d} x_i$。

------

下面尝试给可偏导加一些条件，使之蕴含可微。

#### 定义：$C^1$ 函数类

内容：设 $D \subset \mathbb{R}^n$ 为区域，称 $f : D \to \mathbb{R}$ 在 $D$ 上是 $C^1$ 的，若 $\forall \mathbf{x}_0 \in D, \forall 1 \leq i \leq n$，$f$ 在 $\mathbf{x}_0 \in D$ 处对 $x_i$ 可导，且 $\dfrac{\partial f}{\partial x_i}$ 在 $\mathbf{x}_0$ 处连续，记作 $f \in C^1(D)$。

#### 命题：偏导连续蕴含可微

内容：设 $D \subset \mathbb{R}^n$ 为区域，若 $f \in C^1(D)$，则 $f$ 在 $D$ 上可微。

证明：由连续性容易推知可控制余项 $\displaystyle\sum_{i = 1}^n \left( \frac{\partial f}{\partial x_i}(\mathbf{x}_0 + \Delta \mathbf{x}) - \frac{\partial f}{\partial x_i}(\mathbf{x}_0) \right) \Delta x_i$ 充分小。

[例 4] 求 $z = \arctan \dfrac{y}{x}$ 在 $x \neq 0$ 处的全微分。

解：由 $\dfrac{\partial z}{\partial x} = \dfrac{-y}{x^2 + y^2}, \dfrac{\partial z}{\partial y} = \dfrac{x}{x^2 + y^2}$ 可知 $\text{d} z = \dfrac{-y \text{d} x + x \text{d} y}{x^2 + y^2}$。

不过需要注意的是，存在偏导数不连续但可微的情况。下为一例。

[例 5] 设 $f(x, y) = \begin{cases} (x^2 + y^2) \sin \dfrac{1}{x^2 + y^2}, &\quad x^2 + y^2 \neq 0 \\ 0, &\quad x^2 + y^2 = 0 \end{cases}$，求证：$f$ 在 $(0, 0)$ 处可微，但 $f$ 在 $(0, 0)$ 处对 $x, y$ 的偏导数不连续。

证明：

- 首先易知：

$$
\begin{cases}
\text{d} f(0, 0) = (0, 0) \\
\dfrac{\partial f(0, 0)}{\partial x} = \displaystyle\lim_{x \to 0} x \sin \frac{1}{x^2} = 0 \\
\forall x \neq 0, \dfrac{\partial f(x, 0)}{\partial x} = 2x \sin \dfrac{1}{x^2} - \dfrac{2}{x} \cos \dfrac{1}{x^2}
\end{cases}
$$

- 令 $x_k = \dfrac{1}{\sqrt{k \pi}}$，则 $\dfrac{\partial f(x_k, 0)}{\partial x_k} = 2 (-1)^{k - 1} \sqrt{k \pi}$，故 $\displaystyle\lim_{k \to +\infty} x_k = 0$ 但 $\displaystyle\lim_{k \to +\infty} \frac{\partial f(x_k, 0)}{\partial x_k}$ 不存在。
- 因此 $f$ 在 $(0, 0)$ 处对 $x$ 的偏导数不连续，同理可知 $f$ 在 $(0, 0)$ 处对 $y$ 的偏导数不连续。

### 方向导数与梯度

#### 定义：方向导数

设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，$||\mathbf{u}|| = 1$，若 $\displaystyle\lim_{h \to 0^+} \frac{f(\mathbf{x}_0 + h \mathbf{u}) - f(\mathbf{x}_0)}{h}$ 存在，则称此极限为 $f$ 于 $\mathbf{x}$ 处在 $\mathbf{u}$ 方向上的方向导数，记作 $\left. \dfrac{\partial f}{\partial \mathbf{u}} \right|_{\mathbf{x}_0}, \dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{u}}$。

#### 命题：方向导数与偏导数的关系

内容：设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，则偏导数 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i}$ 存在当且仅当方向导数 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i}, \dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$ 存在有限、且 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i} = -\dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$，此时有 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = \dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i} = -\dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$。

------

事实上方向导数存在也不是一个很强的条件。下为两例。

[例 6] 设 $f(x, y) = \begin{cases} 1, &\quad y = x^2, x \neq 0 \\ 0, &\quad \text{otherwise} \end{cases}$，则对 xOy 平面中任意方向向量 $\mathbf{u}$，都有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{u}} = 0$，但 $f$ 在 $(0, 0)$ 处不连续。

[例 7] 构造函数 $f : \mathbb{R}^2 \rightarrow \mathbb{R}$，使得下列三个条件成立：

- (1) $f$ 在 $(0,0)$ 处的两个偏导数存在。
- (2) $f$ 在 $(0,0)$ 处各方向导数存在。
- (3) $f$ 在 $(0,0)$ 处不可微。

解：

- 构造：

$$
f(x, y) = \begin{cases}
\dfrac{x^2 y}{x^4 + y^2}, &\quad (x, y) \neq (0, 0) \\
0, &\quad (x, y) = (0, 0)
\end{cases}
$$

- (2) 设从 $x = r \cos \theta, y = r \sin \theta \ (r > 0)$ 趋近 $(0, 0)$，有：

$$
\lim_{r \to 0^+} \frac{f(r \cos \theta, r \sin \theta) - f(0, 0)}{r} = \lim_{r \to 0^+} \frac{r^2 \cos^2 \theta \sin \theta}{r^4 \cos^4 \theta + r^2 \sin^2 \theta} = \begin{cases}
\dfrac{\cos^2 \theta}{\sin \theta}, &\quad \sin \theta \neq 0 \\
0, &\quad \sin \theta = 0
\end{cases}
$$

- 故 $f$ 在 $(0, 0)$ 处的各方向导数存在。
- (1) 当 $\theta = 0, \pi$，方向导数均为 $0$，故 $f$ 在 $(0, 0)$ 处关于 $x$ 的偏导数存在且为 $0$。
- 当 $\theta = \dfrac{\pi}{2}, \dfrac{3 \pi}{2}$，方向导数均为 $0$，故 $f$ 在 $(0, 0)$ 处关于 $y$ 的偏导数存在且为 $0$。
- (3) 由 $\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ y = x^2}} \frac{x^2 y}{x^4 + y^2} = \dfrac{1}{2} \neq 0$ 可知 $f$ 在 $(0, 0)$ 处不连续，故 $f$ 在 $(0,0)$ 处不可微。

#### 定理：可微蕴含方向可导

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $f$ 于 $\mathbf{x}_0$ 在方向 $\mathbf{u}$ 上的方向导数为 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{u}} = \langle \text{d} f(\mathbf{x}_0), \mathbf{u} \rangle$。

- 也即：方向导数就是导数在方向向量上的投影。

#### 定义：梯度

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，记 $f'(\mathbf{x}_0)$ 为 $\text{grad } f(\mathbf{x}_0), \nabla f(\mathbf{x}_0)$，称为 $f$ 在 $\mathbf{x}_0$ 处的梯度。我们约定梯度用**行向量**表示。

容易算得 $f$ 在梯度 $\boldsymbol{\ell} = \nabla f(\mathbf{x}_0)$ 方向上的方向导数为 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \boldsymbol{\ell}_0} = ||\boldsymbol{\ell}||$，其中 $\boldsymbol{\ell}_0 = \dfrac{\boldsymbol{\ell}}{||\boldsymbol{\ell}||}$。由 Cauchy 不等式可见这正是函数值增加最快的方向。

### 多元函数求导

[例 8] 设 $k \in \mathbb{N}_{\geq 2}$，$f(x, y)$ 在极坐标 $(r, \theta)$ 下有表达式 $f(x, y) = \begin{cases} r \sin k \theta, &\quad (x, y) \neq (0, 0) \\ 0, &\quad (x, y) = (0, 0) \end{cases}$。

- (1) 求 $f$ 在 $(0, 0)$ 处各方向上的方向导数。
- (2) $f$ 在 $(0, 0)$ 处是否连续？
- (3) $f$ 在 $(0, 0)$ 处是否可微？

解：

- (1) $\forall \theta \in [0, 2 \pi)$，令 $\mathbf{v}(\theta) = (\cos \theta, \sin \theta)$，则算得 $f$ 在方向 $\mathbf{v}(\theta)$ 上的方向导数为 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$。
- (2) $\forall (x, y) \neq (0, 0), 0 \leq |f(x, y)| = r |\sin k \theta| \leq r$，由夹挤原理即得 $\displaystyle\lim_{(x, y) \to (0, 0)} f(x, y) = 0 = f(0, 0)$，故 $f$ 在 $(0, 0)$ 处连续。
- (3) 首先有 $f_x'(0, 0) = 0, f_y'(0, 0) = \sin \dfrac{k \pi}{2}$。
- 假设 $f$ 在 $(0, 0)$ 处可微，则 $\forall \theta \in [0, 2 \pi), \dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = f_x'(0, 0) \cdot \cos \theta + f_y'(0, 0) \cdot \sin \theta = \sin \dfrac{k \pi}{2} \sin \theta$。
- (I) 当 $k$ 为偶数，应有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)}$ 恒为 $0$，与 (1) 中 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$ 不恒为 $0$ 矛盾。
- (II) 当 $k$ 为奇数，应有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \pm \sin \theta$，取 $\theta \in \left( 0, \dfrac{\pi}{2k} \right)$ 可得 $|\pm \sin \theta| < \sin k \theta$，与 (1) 中 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$ 矛盾。
- 综上，假设不成立，即 $f$ 在 $(0, 0)$ 处不可微。

#### 定理：多元函数导数的四则运算

内容：

- 设 $D \subset \mathbb{R}^n$ 为区域，$f, g : D \to \mathbb{R}$，若 $f, g$ 在 $\mathbf{x} \in D$ 处可微，则：
- (1) $\forall \alpha, \beta \in \mathbb{R}, (\alpha f(\mathbf{x}) + \beta g(\mathbf{x}))' = \alpha f'(\mathbf{x}) + \beta g'(\mathbf{x})$。
- (2) $(f(\mathbf{x}) g(\mathbf{x}))' = f(\mathbf{x}) g'(\mathbf{x}) + g(\mathbf{x}) f'(\mathbf{x})$。
- (3) 当 $g(\mathbf{x}) \neq 0$，$\left( \dfrac{f(\mathbf{x})}{g(\mathbf{x})} \right)' = \dfrac{g(\mathbf{x}) f'(\mathbf{x}) - f(\mathbf{x}) g'(\mathbf{x})}{g^2(\mathbf{x})}$。

#### 定理：复合多元函数偏导数

- 这里给出二元函数的情形。

内容：设 $u = f(x, y), x = g(s, t), y = h(s, t)$ 构成复合函数，且 $\dfrac{\partial x}{\partial s}, \dfrac{\partial x}{\partial t}, \dfrac{\partial y}{\partial s}, \dfrac{\partial y}{\partial t}$ 在 $(s, t)$ 处存在、$f$ 在相应位置可微，则复合函数 $u = f(g(s, t), h(s, t))$ 在 $(s, t)$ 处的两个偏导数存在，且有 $\dfrac{\partial u}{\partial s} = \dfrac{\partial u}{\partial x} \cdot \dfrac{\partial x}{\partial s} + \dfrac{\partial u}{\partial y} \cdot \dfrac{\partial y}{\partial s}, \dfrac{\partial u}{\partial t} = \dfrac{\partial u}{\partial x} \cdot \dfrac{\partial x}{\partial t} + \dfrac{\partial u}{\partial y} \cdot \dfrac{\partial y}{\partial t}$。

下面的题目中假定所涉及的函数都有相应的可微性或偏导数。

[例 9] 给定关于 $x, y$ 的函数 $u = f \left( xy, \dfrac{y}{x} \right)$，求 $\dfrac{\partial u}{\partial x}, \dfrac{\partial u}{\partial y}$。

解：

- 令 $s = xy, t = \dfrac{y}{x}$，则 $u = f(s, t)$，故：

$$
\begin{cases}
\dfrac{\partial u}{\partial x} = \dfrac{\partial f}{\partial s} \cdot y - \dfrac{\partial f}{\partial t} \cdot \dfrac{y}{x^2} \\
\dfrac{\partial u}{\partial y} = \dfrac{\partial f}{\partial s} \cdot x + \dfrac{\partial f}{\partial t} \cdot \dfrac{1}{x}
\end{cases}
$$

[例 10] 设 $u = f(x, y, z), z = \varphi(x, y)$，求 $\dfrac{\partial u}{\partial x}, \dfrac{\partial u}{\partial y}$。

解：

$$
\begin{cases}
\dfrac{\partial u}{\partial x} = \dfrac{\partial f}{\partial x} + \dfrac{\partial f}{\partial z} \cdot \dfrac{\partial z}{\partial x} \\
\dfrac{\partial u}{\partial y} = \dfrac{\partial f}{\partial y} + \dfrac{\partial f}{\partial z} \cdot \dfrac{\partial z}{\partial y}
\end{cases}
$$

[例 11] 求 $H(t) = |a_{i, j}(t)|_{1 \leq i, j \leq n}$ 的导数。

解：

- 视 $\det(a_{i, j})$ 为 $n^2$ 元函数，按行展开可知：

$$
\frac{\partial \det(a_{i, j})}{\partial a_{i, j}} = \sum_{k = 1}^n \frac{\partial (a_{i, k} A_{i, k})}{\partial a_{i, j}} = A_{i, j}
$$

- 其中 $A_{i, j}$ 为矩阵 $(a_{i, j})_{1 \leq i, j \leq n}$ 的代数余子式。再由链式法则即得：

$$
H'(t) = \sum_{i = 1}^n \sum_{j = 1}^n A_{i, j} a'_{i, j}(t)
$$

- 事实上，注意到 $\displaystyle\sum_{j = 1}^n A_{i, j} a'_{i, j}(t)$ 正是将原矩阵第 $i$ 行替换为其对 $t$ 的导数所得的行列式，因此还能给出一个更便于计算的形式。

[例 12] 设 $F \in C^1(\mathbb{R}^2)$ 在直角坐标系下可写作 $F(x, y) = f(x) g(y)$，在极坐标系下可写作 $F(r \cos \theta, r \sin \theta) = h(r)$，若 $F$ 无零点，求 $F(x,y)$。

解：

- 由于 $F$ **与极角无关**，故有 $\dfrac{\partial F}{\partial \theta} = 0$。再由链式法则即得：

$$
\begin{aligned}
\frac{\partial F}{\partial \theta} &= \frac{\partial F}{\partial x} \cdot \frac{\partial x}{\partial \theta} + \frac{\partial F}{\partial y} \cdot \frac{\partial y}{\partial \theta} \\
&= f'(x) g(y) \cdot (-r \sin \theta) + f(x) g'(y) \cdot r \cos \theta \\
&= -y f'(x) g(y) + xf(x) g'(y)
\end{aligned}
$$

- 由 $F$ 无零点可知 $f(x), g(y) \neq 0$，分离变量可得：

$$
\frac{f'(x)}{xf(x)} = \frac{g'(y)}{yg(y)} \equiv C \neq 0
$$

- 也即 $(\ln |f(x)|)' = Cx$，积分即得 $\ln |f(x)| = \dfrac{Cx^2}{2} + C'$。
- 由 $f$ 连续且无零点可知其定号，则 $f(x) = C'' e^{\frac{Cx^2}{2}}$。
- 同理可知 $g(y) = C''' e^{\frac{Cy^2}{2}}$，故有：

$$
F(x, y) = \lambda e^{\frac{C(x^2 + y^2)}{2}} \ (\lambda \neq 0)
$$

[例 13] 设 $\mathbf{A} = (a_{i, j})_{1 \leq i, j \leq n} \in M_n(\mathbb{R})$ 为正交矩阵，$f \in C^2(\mathbb{R}^n)$，令 $g(\mathbf{x}) = f(\mathbf{y})$，当 $\mathbf{y} = A \mathbf{x}$，求证：

- (1) $\displaystyle\sum_{i = 1}^n \left( \frac{\partial f}{\partial y_i} \right)^2 = \sum_{i = 1}^n \left( \frac{\partial g}{\partial x_i} \right)^2$。
- (2) $\displaystyle\sum_{i = 1}^n \frac{\partial^2 f}{\partial y_i^2} = \sum_{i = 1}^n \frac{\partial^2 g}{\partial x_i^2}$。

证明：

- (1) 由链式法则可知 $\nabla_x g = (\nabla_y f) \mathbf{A}$，故有：

$$
\text{RHS} = (\nabla_x g) (\nabla_x g)^T = (\nabla_y f) \mathbf{A} \mathbf{A}^T (\nabla_y f)^T = (\nabla_y f) (\nabla_y f)^T = \text{LHS}
$$

- (2) 将 $g$ 对 $x_i$ 求二阶导可知：

$$
\begin{aligned}
\frac{\partial^2 g}{\partial x_i^2} &= \frac{\partial}{\partial x_i} \left( \sum_{j = 1}^n \frac{\partial f}{\partial y_j} a_{j, i} \right) \\
&= \sum_{j = 1}^n \left( \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \cdot \frac{\partial y_k}{\partial x_i} \right) a_{j, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} a_{j, i} a_{k, i}
\end{aligned}
$$

- 对 $i$ 求和可知：

$$
\begin{aligned}
\text{RHS} &= \sum_{i = 1}^n \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} a_{j, i} a_{k, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \sum_{i = 1}^n a_{j, i} a_{k, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \cdot \delta_{j, k} \\
&= \sum_{j = 1}^n \frac{\partial^2 f}{\partial y_j^2} \\
&= \text{LHS}
\end{aligned}
$$

#### 齐次多元函数的欧拉定理

内容：设 $f : \mathbb{R}^n \to \mathbb{R}$ 可微，且 $f$ 为 $k$ 次齐次函数，则 $\mathbf{x} \cdot f'(\mathbf{x}) = kf(\mathbf{x})$。

证明：

- 由齐次性可见 $f(t \mathbf{x}) = t^k f(\mathbf{x})$，等式两边同时对 $t$ 求导可知：

$$
\mathbf{x} \cdot f'(t \mathbf{x}) = kt^{k - 1} f(\mathbf{x})
$$

- 令 $t = 1$ 即可得证。

[例 14] 设 $u(x_1, \cdots, x_n) = \begin{vmatrix} 1 & 1 & \cdots & 1 \\ x_1 & x_2 & \cdots & x_n \\ \vdots & \vdots & & \vdots \\ x_1^{n - 1} & x_2^{n - 1} & \cdots & x_n^{n - 1} \end{vmatrix}$，求证：

- (1) $\displaystyle\sum_{i = 1}^n \frac{\partial u}{\partial x_i} = 0$。
- (2) $\displaystyle\sum_{i = 1}^n x_i \frac{\partial u}{\partial x_i} = \frac{n(n - 1)}{2} u$。

证明：

- (1) 由 van der Monde 行列式的结论可知：

$$
u(x_1, \cdots, x_n) = \prod_{1 \leq i < j \leq n} (x_j - x_i)
$$

- 令 $\tilde{x}_i(t) = x_i + t$，则 $u(\tilde{x}_1(t), \cdots, \tilde{x}_n(t))$ 为常数，据链式法则即得：

$$
0 = \frac{\partial u}{\partial t} = \sum_{i = 1}^n \frac{\partial u}{\partial \tilde{x}_i} \cdot \frac{\partial \tilde{x}_i}{\partial x_i} = \sum_{i = 1}^n \frac{\partial u}{\partial \tilde{x}_i}
$$

- 令 $t = 0$ 即得：

$$
\sum_{i = 1}^n \frac{\partial u}{\partial x_i} = 0
$$

- (2) $u$ 关于 $x_1, \cdots, x_n$ 为 $\dfrac{n(n - 1)}{2}$ 次齐次函数，故由欧拉公式即得。

#### 定义：等值面、切平面

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$ 在 $D$ 上可微，定义其等值面：

$$
S = \{x \in D \mid f(\mathbf{x}) = c\}
$$

取 $\mathbf{x}_0 \in S$，令 $\mathbf{v}$ 为 $S$ 在 $\mathbf{x}_0$ 处的一个**切向量**，其由 $S$ 上的一条 $C^1$ 曲线 $\boldsymbol{\gamma} : (-\epsilon, \epsilon) \to S$ 给出，满足 $\boldsymbol{\gamma}(0) = \mathbf{x}_0, \boldsymbol{\gamma}'(0) = \mathbf{v}$。

将 $f(\boldsymbol{\gamma}(t)) = c$ 两边同时对 $t$ 求导，由多元函数偏导数的运算法则可算得 $\langle \nabla f(\mathbf{x}_0), \mathbf{v} \rangle = 0$，即梯度向量与切向量总是正交。故当梯度非零，其为等值面在 $\mathbf{x}_0$ 处的法向量。

特别地，对于三维空间中的曲面 $z = f(x, y)$，将其改写为三元函数等值面的形式 $F(x, y, z) \triangleq f(x, y) - z = 0$，则曲面在 $(x_0, y_0, z_0)$ 处的法向量可以取为：

$$
\nabla F(x_0, y_0, z_0) = (f_x'(x_0, y_0), f_y'(x_0, y_0), -1)
$$

因此切平面方程为：

$$
z - z_0 = f_x'(x_0, y_0) (x - x_0) + f_y'(x_0, y_0) (y - y_0)
$$

### 向量值函数的可微性

#### 定义：向量值函数的微分

设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x}_0 \in D$，若 $\exists \mathbf{A} \in M_{m \times n}(\mathbb{R}), \text{s.t. } \mathbf{f}(\mathbf{x}_0 + \Delta \mathbf{x}) - \mathbf{f}(\mathbf{x}_0) = \mathbf{A} \Delta \mathbf{x} + \mathbf{r}(\Delta \mathbf{x})$，其中 $\displaystyle\lim_{\Delta \mathbf{x} \to \mathbf{0}} \frac{||\mathbf{r}(\Delta \mathbf{x})||}{||\Delta \mathbf{x}||} = 0$，则称 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，又称可导。

此时称 $\mathbf{A}$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的（Fréchet）导数，记作 $\mathbf{f}'(\mathbf{x}_0)$ 或 $D \mathbf{f}(\mathbf{x}_0)$；称 $\mathbf{A} \Delta \mathbf{x}$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的全微分，记作 $\text{d} \mathbf{f}(\mathbf{x}_0)$。

#### 定理 & 定义：向量值函数可微的充要条件、Jacobi 矩阵

内容：设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x}_0 \in D$，设 $\mathbf{f}$ 的分量形式为 $\mathbf{f}(\mathbf{x}) = (f_1(\mathbf{x}), \cdots, f_m(\mathbf{x}))^T$，则 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微的充要条件为 $\forall 1 \leq i \leq m$，$f_i$ 在 $\mathbf{x}_0$ 处均可微，此时 $\mathbf{f}'(\mathbf{x}_0) = J \mathbf{f}(\mathbf{x}_0)$，其中：

$$
J \mathbf{f}(\mathbf{x}_0) \triangleq \left( \frac{\partial f_i(\mathbf{x}_0)}{\partial x_j} \right)_{1 \leq i \leq m, 1 \leq j \leq n} = (\nabla f_1(\mathbf{x}_0), \cdots, \nabla f_m(\mathbf{x}_0))^T
$$

此时称 $J \mathbf{f}(\mathbf{x}_0)$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的 Jacobi 矩阵。

推论：

- (1) 若 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，则 $\forall 1 \leq i \leq m, 1 \leq j \leq n$，$\dfrac{\partial f_i(\mathbf{x}_0)}{\partial x_j}$ 存在。
- (2) 若 $\forall 1 \leq i \leq m, 1 \leq j \leq n$，$\dfrac{\partial f_i(\mathbf{x}_0)}{\partial x_j}$ 存在且连续，则 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微。

#### 定义：Jacobi 行列式

特别地，当 $m = n$，Jacobi 矩阵为 $n$ 阶方阵，称此时 $J \mathbf{f}(\mathbf{x}_0)$ 的行列式为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的 Jacobi 行列式，可以记作：

$$
|J \mathbf{f}(\mathbf{x}_0)|, \quad |\mathbf{f}'(\mathbf{x}_0)|, \quad |D \mathbf{f}(\mathbf{x}_0)|, \quad \det(\mathbf{f}'(\mathbf{x}_0)), \quad \left| \frac{\partial(f_1, f_2, \cdots, f_n)}{\partial(x_1, x_2, \cdots, x_n)} \right|_{\mathbf{x}_0}
$$

#### 定理：向量值函数的数乘、加法、点乘的导数

内容：

- 设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f}, \mathbf{g} : D \to \mathbb{R}^m$，若 $\mathbf{f}, \mathbf{g}$ 在 $\mathbf{x} \in D$ 处可微，则：
- (1) 数乘与加法：$\forall \alpha, \beta \in \mathbb{R}, (\alpha \mathbf{f}(\mathbf{x}) + \beta \mathbf{g}(\mathbf{x}))' = \alpha \mathbf{f}'(\mathbf{x}) + \beta \mathbf{g}'(\mathbf{x})$。
- (2) 点乘：$(\mathbf{f}(\mathbf{x}) \cdot \mathbf{g}(\mathbf{x}))' = \mathbf{f}(\mathbf{x})^T \mathbf{g}'(\mathbf{x}) + \mathbf{g}(\mathbf{x})^T \mathbf{f}'(\mathbf{x})$。

证明：展开为多元函数的情形即可。

#### 定理：复合向量值函数的导数

内容：设 $D \subset \mathbb{R}^n, \Omega \subset \mathbb{R}^m$ 为区域，$\mathbf{f} : D \to \Omega, \mathbf{g} : \Omega \to \mathbb{R}^p$，若 $\mathbf{f}$ 在 $\mathbf{x}_0 \in D$ 处可微，$\mathbf{g}$ 在 $\mathbf{y}_0 = \mathbf{f}(\mathbf{x}_0)$ 处可微，则复合映射 $\mathbf{h} = \mathbf{g} \circ \mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，且 $\mathbf{h}'(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0)$。

证明：

$$
\begin{aligned}
\mathbf{h}(\mathbf{x}) - \mathbf{h}(\mathbf{x}_0) &= \mathbf{g}(\mathbf{f}(\mathbf{x})) - \mathbf{g}(\mathbf{f}(\mathbf{x}_0)) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0))(\mathbf{f}(\mathbf{x}) - \mathbf{f}(\mathbf{x}_0)) + o(||\mathbf{f}(\mathbf{x}) - \mathbf{f}(\mathbf{x}_0)||) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0))(\mathbf{f}'(\mathbf{x}_0) (\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)) + o(||\mathbf{f}'(\mathbf{x}_0)(\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)||) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0)) \mathbf{f}'(\mathbf{x}_0) (\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)
\end{aligned}
$$

- 故 $\mathbf{h}$ 在 $\mathbf{x}_0$ 处可微，且 $\mathbf{h}'(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0)$。

注：

- (1) 写成微分形式则可见**一阶微分形式不变性**：

$$
\text{d} \mathbf{h}(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0) \text{d} \mathbf{x}_0 = \mathbf{g}'(\mathbf{y}_0) \text{d} \mathbf{y}_0
$$

- (2) 写成分量形式则可见**链式法则**：

$$
\frac{\partial h_i(\mathbf{x}_0)}{\partial x_j} = \sum_{k = 1}^m \frac{\partial g_i(\mathbf{y}_0)}{\partial y_k} \cdot \frac{\partial f_k(\mathbf{x}_0)}{\partial x_j}
$$

#### 定理：反函数求导

内容：设 $D, \Omega \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \Omega$ 为 $C^1$ 的同胚映射（即 $\mathbf{f}, \mathbf{f}^{-1}$ 均连续可微），则两者的 Jacobi 矩阵互为逆矩阵，即 $\mathbf{f}'(\mathbf{x}) (\mathbf{f}^{-1})'(\mathbf{y}) = (\mathbf{f}^{-1})'(\mathbf{y}) \mathbf{f}'(\mathbf{x}) = I$；反映在 Jacobi 行列式上则有：

$$
\left| \frac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right| \left| \frac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right| = \left| \frac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right| \left| \frac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right| = 1
$$

证明：由 $\mathbf{f} \circ \mathbf{f}^{-1} = \mathbf{f}^{-1} \circ \mathbf{f} = \text{id}$，据复合向量值函数的求导法则即得。

推论：Jacobi 行列式 $\left| \dfrac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right|, \left| \dfrac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right|$ 分别在 $\Omega, D$ 上非零。

[例 15] 求 $f(\mathbf{x}) = ||\mathbf{x}||$ 在 $\mathbf{x} \in \mathbb{R}^n \backslash \{\mathbf{0}\}$ 处的导数。

解：

- 由复合函数求导法则可知：

$$
\frac{\partial f}{\partial x_i} = \frac{1}{2 ||\mathbf{x}||} \cdot 2x_i = \frac{x_i}{||\mathbf{x}||}
$$

- 拼起来即得 $f'(\mathbf{x}) = \dfrac{\mathbf{x}^T}{||\mathbf{x}||}$。
- **注意：梯度为行向量！**

### 向量值函数的微分中值定理

- _Motivation：将多元转化为一元的情形 $\Rightarrow$ 投影！_

#### 向量值函数的微分中值定理（投影 ver.）

内容：设 $D \subset \mathbb{R}^n$ 为凸区域，$\mathbf{f} : D \to \mathbb{R}^m$ 可微，则：

$$
\forall \mathbf{a}, \mathbf{b} \in D, \forall \boldsymbol{\ell} \in \mathbb{R}^m \backslash \{\mathbf{0}\}, \exists \theta \in (0, 1), \text{s.t. } (\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a}) - (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a}))) \cdot \boldsymbol{\ell} = 0
$$

证明：

- 令 $\varphi(t) = \mathbf{f}(\mathbf{a} + t(\mathbf{b} - \mathbf{a})) \cdot \boldsymbol{\ell}$，则 $\varphi(0) = \mathbf{f}(\mathbf{a}) \cdot \boldsymbol{\ell}, \varphi(1) = \mathbf{f}(\mathbf{b}) \cdot \boldsymbol{\ell}$。
- 同时，据复合函数求导法则可知 $\varphi$ 在 $[0, 1]$ 上可导，且有：

$$
\begin{aligned}
\varphi'(t) &= (\mathbf{f}(\mathbf{a} + t(\mathbf{b} - \mathbf{a})))'_t \cdot \boldsymbol{\ell} \\
&= (\mathbf{f}'(\mathbf{a} + t(\mathbf{b} - \mathbf{a})) (\mathbf{b} - \mathbf{a})) \cdot \boldsymbol{\ell}
\end{aligned}
$$

- 对 $\varphi$ 施以 Lagrange 中值定理可知：

$$
\exists \theta \in (0, 1), \text{s.t. } \varphi'(\theta) = \varphi(1) - \varphi(0)
$$

- 移项整理即得：

$$
(\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a}) - (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a}))) \cdot \boldsymbol{\ell} = 0
$$

#### 拟微分中值定理

内容：设 $D \subset \mathbb{R}^n$ 为凸区域，$\mathbf{f} : D \to \mathbb{R}^m$ 可微，则：

$$
\forall \mathbf{a}, \mathbf{b} \in D, \exists \theta \in (0, 1), \text{s.t. } ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})|| \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))|| \cdot ||\mathbf{b} - \mathbf{a}||
$$

证明：

- (1) 当 $\mathbf{f}(\mathbf{a}) = \mathbf{f}(\mathbf{b})$，定理平凡地成立。
- (2) 当 $\mathbf{f}(\mathbf{a}) \neq \mathbf{f}(\mathbf{b})$，在上一定理中取 $\boldsymbol{\ell} = \mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})$，代入并移项整理可得：

$$
\exists \theta \in (0, 1), \text{s.t. } (\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a})) \cdot (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})) = ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||^2
$$

- 由 Cauchy 不等式可知：

$$
||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||^2 \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a})) (\mathbf{b} - \mathbf{a})|| \cdot ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||
$$

- 两边同除 $||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||$ 即得：

$$
||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})|| \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a})) (\mathbf{b} - \mathbf{a})|| \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))|| \cdot ||\mathbf{b} - \mathbf{a}||
$$

- 最后一步依赖于范数不等式 $\forall A \in M_{n \times m}(\mathbb{R}), \mathbf{b} \in \mathbb{R}^m, ||A \mathbf{b}|| \leq ||A|| \cdot ||\mathbf{b}||$，这由 Cauchy 不等式易证。

### 高阶偏导数与高阶全微分

#### 定义：高阶偏导数

对于二元函数 $f(x, y)$，当其一阶偏导数对某个变量可求偏导，可定义其二阶偏导数：

$$
\begin{cases}
f_{xx}'', \dfrac{\partial^2 f}{\partial x^2} \triangleq \dfrac{\partial}{\partial x} \left( \dfrac{\partial f}{\partial x} \right) \\
f_{xy}'', \dfrac{\partial^2 f}{\partial x \partial y} \triangleq \dfrac{\partial}{\partial x} \left( \dfrac{\partial f}{\partial y} \right) \\
f_{yx}'', \dfrac{\partial^2 f}{\partial y \partial x} \triangleq \dfrac{\partial}{\partial y} \left( \dfrac{\partial f}{\partial x} \right) \\
f_{yy}'', \dfrac{\partial^2 f}{\partial y^2} \triangleq \dfrac{\partial}{\partial y} \left( \dfrac{\partial f}{\partial y} \right)
\end{cases}
$$

特别地，称其中 $f_{xy}'', f_{yx}''$ 为 $f$ 的二阶混合偏导数。

多元函数的更多元偏导数可以类似定义。

记 $C^k(D)$ 为区域 $D \subset \mathbb{R}^n$ 上具有 $1 \sim k$ 阶连续偏导数的函数的构成集合，并可类似定义 $C^k(\bar{D})$。

[例 16] 设 $f(x, y) = \begin{cases} xy \cdot \dfrac{x^2 - y^2}{x^2 + y^2}, &\quad (x, y) \neq (0, 0) \\ 0, &\quad (x, y) = (0, 0) \end{cases}$，求其二阶偏导数。

解：

- (1) 当 $(x, y) \neq (0, 0)$，有：

$$
\begin{cases}
\dfrac{\partial f}{\partial x} = \dfrac{y(x^2 - y^2)}{x^2 + y^2} + \dfrac{4x^2 y^3}{(x^2 + y^2)^2} \\
\dfrac{\partial f}{\partial y} = \dfrac{x(x^2 - y^2)}{x^2 + y^2} - \dfrac{4x^3 y^2}{(x^2 + y^2)^2}
\end{cases}
$$

- 且由定义可以算得 $\dfrac{\partial f}{\partial x}(0, 0) = \dfrac{\partial f}{\partial y}(0, 0) = 0$。
- (2) 当 $(x, y) \neq (0, 0)$，有：

$$
\begin{cases}
\dfrac{\partial^2 f}{\partial x^2} = \dfrac{4xy^3}{(x^2 + y^2)^2} - \dfrac{8xy^3(x^2 - y^2)}{(x^2 + y^2)^3} \\
\dfrac{\partial^2 f}{\partial x \partial y} = \dfrac{\partial^2 f}{\partial y \partial x} = \dfrac{x^4 - 4x^2 y^2 - y^4}{(x^2 + y^2)^2} + \dfrac{12x^4 y^2 - 4x^2 y^4}{(x^2 + y^2)^3} \\
\dfrac{\partial^2 f}{\partial y^2} = -\dfrac{4x^3 y}{(x^2 + y^2)^2} + \dfrac{8x^3 y(x^2 - y^2)}{(x^2 + y^2)^3}
\end{cases}
$$

- 且由定义可以算得 $\dfrac{\partial^2 f}{\partial x^2}(0, 0) = 0, \dfrac{\partial^2 f}{\partial x \partial y}(0, 0) = 1, \dfrac{\partial^2 f}{\partial y \partial x}(0, 0) = -1, \dfrac{\partial^2 f}{\partial y^2}(0, 0) = 0$。
- _可以发现在原点之外的地方，$f$ 的两个二阶混合偏导数一致；但在原点处则不然。_

[例 17] 设 $f(x, y) = \begin{cases} (x^2 + y^2)^2 \sin \dfrac{1}{\sqrt{x^2 + y^2}}, &\quad x^2 + y^2 \neq 0 \\ 0, &\quad x^2 + y^2 = 0 \end{cases}$，求其二阶偏导数。

解：

- 令 $r = \sqrt{x^2 + y^2}$。
- (1) 当 $r > 0$，有 $f(x, y) = r^4 \sin \dfrac{1}{r}$，由 $r'_x = \dfrac{x}{r}, r'_y = \dfrac{y}{r}$ 可知：

$$
\begin{cases}
f_x' = f'_r r'_x = x \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right) \\
f_y' = f'_r r'_y = y \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right)
\end{cases}
$$

- 且由定义可以算得 $f_x'(0) = f_y'(0) = 0$。
- (2) 当 $r > 0$，有：

$$
\begin{cases}
f_{xx}'' = \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right) + \dfrac{x^2}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right) \\
f_{xy}'' = f_{yx}'' = \dfrac{xy}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right) \\
f_{yy}'' = \dfrac{y^2}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right)
\end{cases}
$$

- 且由定义可以算得 $f_{xx}''(0, 0) = f_{xy}''(0, 0) = f_{yx}''(0, 0) = f_{yy}''(0, 0) = 0$。
- _可以发现 $f$ 的两个二阶混合偏导数总是一致。_

[例 18] 设 $z = f(x, y)$，$f \in C^{\infty}(\mathbb{R}^2)$，当 $x, y$ 用参数 $t$ 表出为 $\begin{cases} x = x_0 + t \cos \alpha \\ y = y_0 + t \sin \alpha \end{cases}$，求 $\dfrac{\text{d}^n z}{\text{d} t^n}$。

解：

$$
\dfrac{\text{d} z}{\text{d} t} = \frac{\partial z}{\partial x} \cdot \frac{\text{d} x}{\text{d} t} + \frac{\partial z}{\partial y} \cdot \frac{\text{d} y}{\text{d} t} = \cos \alpha \frac{\partial z}{\partial x} + \sin \alpha \frac{\partial z}{\partial y}
$$

- 使用**算子记法**可以写作：

$$
\dfrac{\text{d} z}{\text{d} t} = \left( \cos \alpha \frac{\partial}{\partial x} + \sin \alpha \frac{\partial}{\partial y} \right) z
$$

- 约定算子的乘法为**映射复合**，则归纳可知：

$$
\dfrac{\text{d}^n z}{\text{d} t^n} = \left( \cos \alpha \frac{\partial}{\partial x} + \sin \alpha \frac{\partial}{\partial y} \right)^n z
$$

------

在例 16、例 17 中，我们观察到函数在某些位置的两个二阶混合偏导数相等，下面对二元函数的情形给出几个充分条件。

#### 两个混合偏导数相等的充分条件 I：$C^2$

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，则 $\forall (x_0, y_0) \in D, f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

证明：

- 考虑 $f$ 在 $(x_0, y_0)$ 附近的二阶增量：

$$
d_{\Delta x, \Delta y} \triangleq f(x_0 + \Delta x, y_0 + \Delta y) - f(x_0 + \Delta x, y_0) - f(x_0, y_0 + \Delta y) + f(x_0, y_0), \quad \Delta x, \Delta y \neq 0
$$

- 固定 $\Delta x$，由 Lagrange 中值定理可知：

$$
\exists \xi \text{ 在 } y_0, y_0 + \Delta y \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = (f_y'(x_0 + \Delta x, \xi) - f_y'(x_0, \xi)) \Delta y \ (*)
$$

- 再由 Lagrange 中值定理可知：

$$
\exists \eta \text{ 在 } x_0, x_0 + \Delta x \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = f_{xy}''(\eta, \xi) \Delta x \Delta y
$$

- 先固定 $\Delta y$ 重复上面的步骤，同理可知：

$$
\exists \tilde{\eta} \text{ 在 } x_0, x_0 + \Delta x \text{ 之间}, \tilde{\xi} \text{ 在 } y_0, y_0 + \Delta y \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = f_{yx}''(\tilde{\eta}, \tilde{\xi}) \Delta x \Delta y \ (**)
$$

- 故有 $f_{xy}''(\eta, \xi) = f_{yx}''(\tilde{\eta}, \tilde{\xi})$。
- 由 $f \in C^2(D)$ 可知 $f_{xy}'', f_{yx}''$ 在 $D$ 上连续，令 $(\Delta x, \Delta y) \to (0, 0)$ 即得：

$$
f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)
$$

#### 两个混合偏导数相等的充分条件 II：Schwarz 定理

内容：设 $f_x', f_y', f_{xy}''$（或 $f_{yx}''$）在 $(x_0, y_0)$ 附近存在，$f_{xy}''$（或 $f_{yx}''$）在 $(x_0, y_0)$ 处连续，则 $f_{yx}''$（或 $f_{xy}''$）存在，且有 $f_{yx}''(x_0, y_0) = f_{xy}''(x_0, y_0)$。

证明（以括号外为例）：

- 同 $C^2$ 的情形得到 $(**)$ 式，则有：

$$
\begin{aligned}
f_{yx}''(x_0, y_0) &= \lim_{\Delta x \to 0} \lim_{\Delta y \to 0} \frac{f_{xy}''(\eta, \xi) \Delta x \Delta y}{\Delta x \Delta y} \\
&= \lim_{\Delta x \to 0} \lim_{\Delta y \to 0} f_{xy}''(\eta, \xi) \\
&= \lim_{(\Delta x, \Delta y) \to (0, 0)} f_{xy}''(\eta, \xi) &\quad (\text{重极限存在，内层极限存在为 } f_y'(x + \Delta x, y_0)) \\
&= f_{xy}''(x_0, y_0)
\end{aligned}
$$

#### 两个混合偏导数相等的充分条件 III：Young 定理

内容：设 $f_x', f_y'$ 在 $(x_0, y_0)$ 处可微，则 $f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

证明：

- 同 $C^2$ 的情形得到 $(*)$ 式，则有：

$$
\begin{aligned}
d_{\Delta x, \Delta y} &= ((f_{xy}''(x_0, y_0) \Delta x + f_{yy}''(x_0, y_0) (\xi - y_0) + o(\sqrt{(\Delta x)^2 + (\xi - y_0)^2})) - (f_{yy}''(x_0, y_0) (\xi - y_0) + o(\sqrt{(\Delta x)^2 + (\xi - y_0)^2}))) \Delta y \\
&= f_{xy}''(x_0, y_0) \Delta x \Delta y + o(\sqrt{(\Delta x)^2 + (\Delta y)^2}) \Delta y
\end{aligned}
$$

- 先固定 $\Delta y$ 重复上面的步骤，同理可知道：

$$
d_{\Delta x, \Delta y} = f_{yx}''(x_0, y_0) \Delta x \Delta y + o(\sqrt{(\Delta x)^2 + (\Delta y)^2}) \Delta x
$$

- 取 $\Delta x = \Delta y$，则有：

$$
f_{xy}''(x_0, y_0) - f_{yx}''(x_0, y_0) = o(1)
$$

- 令 $\Delta x = \Delta y \to 0$ 即得 $f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

[例 19] 设 $f$ 在区域 $D \subset \mathbb{R}^2$ 上二阶可微且为一次齐次函数，求证：$f_{xx}'' f_{yy}'' = (f_{xy}'')^2$。

证明：

- 由欧拉定理可知 $xf_x' + yf_y' = f$，分别关于 $x, y$ 求偏导可知：

$$
\begin{cases}
xf_{xx}'' + yf_{yx}'' = 0 \\
xf_{xy}'' + yf_{yy}'' = 0
\end{cases}
$$

- 此乃齐次线性方程组 $\begin{bmatrix} f_{xx}'' & f_{yx}'' \\ f_{xy}'' & f_{yy}'' \end{bmatrix} \begin{bmatrix} x \\ y \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \end{bmatrix}$。
- (1) 当 $(0, 0) \not\in D$，$\forall (x, y) \in D$，其给出上述齐次线性方程组的非零解，故：

$$
\begin{vmatrix}
f_{xx}'' & f_{yx}'' \\
f_{xy}'' & f_{yy}''
\end{vmatrix} = f_{xx}'' f_{yy}'' - f_{xy}'' f_{yx}'' = 0
$$

- 由 Young 定理可知 $f_{xy}'' = f_{yx}''$，代入整理即得 $f_{xx}'' f_{yy}'' = (f_{xy}'')^2$。
- (2) 当 $(0, 0) \in D$，则 $f(0, 0) = 0$。设 $f'(0, 0) = (a, b)$，则：

$$
\forall (0, 0) \neq (x, y) \in D, f(tx, ty) = t(ax + by) + o(t) \ (t \to 0)
$$

- 又由齐次性可知 $f(tx, ty) = tf(x, y)$，故有：

$$
f(x, y) = ax + by + o(1) \ (t \to 0)
$$

- 令 $t \to 0$ 即得 $f(x, y) = ax + by$，故 $f$ 在 $D$ 上的四个偏导数全为 $0$，等式显然成立。

#### 变量变换在偏微分方程中的应用

[例 20] 设 $u(x, y)$ 满足偏微分方程 $y \dfrac{\partial u}{\partial x} - x \dfrac{\partial u}{\partial y} = 0$，在极坐标变换 $\begin{cases} x = r \cos \theta \\ y = r \sin \theta \end{cases}$ 下求解该方程。

解：

$$
\frac{\partial u}{\partial \theta} = \frac{\partial u}{\partial x} \cdot \frac{\partial x}{\partial \theta} + \frac{\partial u}{\partial y} \cdot \frac{\partial y}{\partial \theta} = \frac{\partial u}{\partial x} (-r \sin \theta) + \frac{\partial u}{\partial y} (r \cos \theta) = -y \frac{\partial u}{\partial x} + x \frac{\partial u}{\partial y}
$$

- 据条件可知 $\dfrac{\partial u}{\partial \theta} = 0$，故 $u$ 只由 $r$ 决定。
- 再考虑原点处的情况，可知该方程的通解为 $u = f(r)$，其中 $f \in D[0, +\infty), f'_+(0) = 0$。

[例 21] 定义在 $\mathbb{R}^3 \backslash \{\mathbf{0}\}$ 上的函数 $u(x, y, z)$ 满足 Laplace 方程 $\Delta u \triangleq \dfrac{\partial^2 u}{\partial x^2} + \dfrac{\partial^2 u}{\partial y^2} + \dfrac{\partial^2 u}{\partial z^2} = 0$，求其球对称解（即只与到原点的距离 $r = \sqrt{x^2 + y^2 + z^2}$ 有关的解 $u = f(r)$）。

解：

- (1) 首先计算一阶偏导数：

$$
\dfrac{\partial u}{\partial x} = f'(r) \dfrac{x}{r}, \dfrac{\partial u}{\partial y} = f'(r) \dfrac{y}{r}, \dfrac{\partial u}{\partial z} = f'(r) \dfrac{z}{r}
$$

- (2) 然后计算二阶偏导数：

$$
\begin{cases}
\dfrac{\partial^2 u}{\partial x^2} = f''(r) \dfrac{x^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{x^2}{r^3} \right) \\
\dfrac{\partial^2 u}{\partial y^2} = f''(r) \dfrac{y^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{y^2}{r^3} \right) \\
\dfrac{\partial^2 u}{\partial z^2} = f''(r) \dfrac{z^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{z^2}{r^3} \right)
\end{cases}
$$

- (3) 写出 Laplace 方程，有 $f''(r) + \dfrac{2}{r} f'(r) = 0$。
- 故 $r^2 f'(r) \equiv \tilde{C}_1$，则 $f'(r) = \dfrac{\tilde{C}_1}{r^2}$，进而有 $f(r) = -\dfrac{\tilde{C}_1}{r} + C_2 \triangleq \dfrac{C_1}{r} + C_2$。
- _注：Laplace 方程描述的正是静电场、引力场等物理势场在无源区域的平滑分布。_

#### 定义：高阶全微分

设 $D \subset \mathbb{R}^n$ 为区域，$f$ 在 $D$ 上可微，设 $\mathbf{x} \in D$，则：

$$
\text{d} f(\mathbf{x}) = \sum_{i = 1}^n \frac{\partial f(\mathbf{x})}{\partial x_i} \text{d} x_i
$$

若 $\forall 1 \leq i \leq n$，$f_i'$ 仍在 $\mathbf{x}$ 处可微，则称：

$$
\text{d}^2 f(\mathbf{x}) \triangleq \sum_{i = 1}^n \left( \sum_{j = 1}^n \frac{\partial^2 f(\mathbf{x})}{\partial x_j \partial x_i} \text{d} x_j \right) \text{d} x_i
$$

为 $f$ 在 $\mathbf{x}$ 处的二阶微分。依此类推，还可以定义 $k$ 阶微分。若仿照例 18 引入算子记号：

$$
\text{d} f(\mathbf{x}) = \left( \sum_{i = 1}^n \text{d} x_i \frac{\partial}{\partial x_i} \right) f(\mathbf{x})
$$

则当 $f$ 在 $D$ 上 $k$ 阶可微，可以写出其 $k$ 阶微分：

$$
\text{d}^k f(\mathbf{x}) = \left( \sum_{i = 1}^n \text{d} x_i \frac{\partial}{\partial x_i} \right)^k f(\mathbf{x})
$$

这一形式可以通过多项式定理展开。

### 多元函数 Taylor 公式

#### 带 Lagrange 余项的多元函数 Taylor 公式

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^{k + 1}(D)$，$B_r(\mathbf{x}_0) \subset D$，则：

$$
\forall ||\Delta \mathbf{x}|| < r, \exists \theta \in (0, 1), \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^i f(\mathbf{x}_0) + \frac{1}{(k + 1)!} (\Delta \mathbf{x} \cdot \nabla)^{k + 1} f(\mathbf{x}_0 + \theta \Delta \mathbf{x})
$$

证明：

- 设 $\varphi(t) = f(\mathbf{x}_0 + t \Delta \mathbf{x}), t \in [0, 1]$，则 $\varphi \in C^{k + 1}[0, 1]$，且 $\varphi(0) = f(\mathbf{x}_0), \varphi(1) = f(\mathbf{x}_0 + \Delta \mathbf{x})$。
- 算得 $\varphi'(t) = \nabla f(\mathbf{x}_0 + t \Delta \mathbf{x}) \Delta \mathbf{x} = (\Delta \mathbf{x} \cdot \nabla) f(\mathbf{x}_0 + t \Delta \mathbf{x})$，进而归纳可知 $\varphi^{(i)}(t) = (\Delta \mathbf{x} \cdot \nabla)^i f(\mathbf{x}_0 + t \Delta \mathbf{x})$。
- 由 $\varphi$ 的带 Lagrange 余项的一元函数 Taylor 公式可知：

$$
\begin{aligned}
\exists \theta \in (0, 1), \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) &= \varphi(1) \\
&= \varphi(0) + \sum_{i = 1}^k \frac{\varphi^{(i)}(0)}{i!} + \frac{\varphi^{(k + 1)}(\theta)}{(k + 1)!} \\
&= f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^i f(\mathbf{x}_0) + \frac{1}{(k + 1)!} (\Delta \mathbf{x} \cdot \nabla)^{k + 1} f(\mathbf{x}_0 + \theta \Delta \mathbf{x})
\end{aligned}
$$

#### 带 Peano 余项的多元函数 Taylor 公式

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^k(D)$，$B_r(\mathbf{x}_0) \subset D$，则：

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^i f(\mathbf{x}_0) + o(||\Delta \mathbf{x}||^k) \ (\Delta \mathbf{x} \to \mathbf{0})
$$

证明：

- 代入相应的带 Lagrange 余项的多元函数 Taylor 公式，将 $(\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0 + \theta \Delta \mathbf{x}) - (\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0)$ 用多项式定理展开。
- 展开式中的系数由 $C^k$ 可知其在以 $\mathbf{x}_0$ 为球心的闭球上有一致的界，借此放缩即得：当 $\Delta \mathbf{x} \to \mathbf{0}$，其是 $o(||\Delta \mathbf{x}||^k)$ 的。

设指数组 $(\alpha_1, \cdots, \alpha_n)$ 满足 $\alpha_i \geq 0$ 且 $\displaystyle\sum_{i = 1}^n \alpha_i = s$，其中 $1 \leq s \leq k$，则可见展开式中 $\displaystyle\prod_{i = 1}^n \Delta x_i^{\alpha_i}$ 项的系数为 $\dfrac{1}{\prod_{i = 1}^n \alpha_i!} \cdot \dfrac{\partial^s f}{\partial x_1^{\alpha_1} \cdots \partial x_n^{\alpha_n}}$。

与一元的情形一致，可以证明只要

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \sum_{\alpha_j \geq 0; \sum \alpha_j = s} c_{\alpha} \prod_{j = 1}^n \Delta x_j^{\alpha_j} + o(||\Delta \mathbf{x}||^k) \quad (\Delta \mathbf{x} \to \mathbf{0})
$$

成立，就有 $c_{\alpha} = \dfrac{1}{\prod_{j = 1}^n \alpha_j!} \cdot \dfrac{\partial^s f}{\partial x_1^{\alpha_1} \cdots \partial x_n^{\alpha_n}}$。这一性质可以为我们计算高阶偏导数提供便利。

[例 22] 求 $f(x, y) = xe^{x + y}$ 在 $(0, 0)$ 处的所有四阶偏导数。

解：

$$
f(x, y) = x \left( 1 + x + \frac{x^2}{2} + \frac{x^3}{6} + o(x^3) \right) \left( 1 + y + \frac{y^2}{2} + \frac{y^3}{6} + \frac{y^4}{24} + o(y^4) \right) \quad ((x, y) \to (0, 0))
$$

- (1) 提取 $x^4$ 的系数可知 $\dfrac{1}{24} \cdot \dfrac{\partial^4 f}{\partial x^4} = \dfrac{1}{6}$，故 $\dfrac{\partial^4 f}{\partial x^4} = 4$。
- (2) 提取 $x^3 y$ 的系数可知 $\dfrac{1}{6} \cdot \dfrac{\partial^4 f}{\partial x^3 \partial y} = \dfrac{1}{2}$，故 $\dfrac{\partial^4 f}{\partial x^3 \partial y} = 3$。
- (3) 提取 $x^2 y^2$ 的系数可知 $\dfrac{1}{4} \cdot \dfrac{\partial^4 f}{\partial x^2 \partial y^2} = \dfrac{1}{2}$，故 $\dfrac{\partial^4 f}{\partial x^2 \partial y^2} = 2$。
- (4) 提取 $xy^3$ 的系数可知 $\dfrac{1}{6} \cdot \dfrac{\partial^4 f}{\partial x \partial y^3} = \dfrac{1}{6}$，故 $\dfrac{\partial^4 f}{\partial x \partial y^3} = 1$。
- (5) 提取 $y^4$ 的系数可知 $\dfrac{1}{24} \cdot \dfrac{\partial^4 f}{\partial y^4} = 0$，故 $\dfrac{\partial^4 f}{\partial y^4} = 0$。

#### 定义：Hesse 阵

特别地，在带 Peano 余项的多元函数 Taylor 公式中取 $k = 2$，可得：

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \nabla f(\mathbf{x}_0) \Delta \mathbf{x} + \frac{1}{2} (\Delta \mathbf{x})^T H_f(\mathbf{x}_0) \Delta \mathbf{x} + o(||\Delta \mathbf{x}||^2)
$$

其中 $H_f(\mathbf{x}_0) = \left( \dfrac{\partial^2 f}{\partial x_i \partial x_j} \right)_{1 \leq i, j \leq n}$ 称为 $f$ 在 $\mathbf{x}_0$ 处的 Hesse 阵。

#### 定义：临界点

设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x} \in D$，$\mathbf{f}$ 在 $\mathbf{x}$ 处可微，称 $\mathbf{x}$ 为 $\mathbf{f}$ 的一个临界点，若 $\mathbf{f}'(\mathbf{x})$ 不满秩，此时称 $\mathbf{f}(\mathbf{x})$ 为 $\mathbf{f}$ 的一个临界值。

特别地：

- (1) 当 $m = 1$ 即 $n$ 元函数的情形，$\mathbf{x}$ 为临界点当且仅当 $f'(\mathbf{x}) = 0$。
- (2) 当 $m = n$，$\mathbf{x}$ 为临界点当且仅当 $\det(\mathbf{f}'(\mathbf{x})) = 0$。

[例 23] 设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，$\mathbf{x}_0 \in D$ 为 $f$ 的临界点，$\det(H_f(\mathbf{x}_0)) \neq 0$，求证：临界点 $\mathbf{x}_0$ 孤立，即 $\exists r > 0, \text{s.t. } \mathring{B}_r(\mathbf{x}_0)$ 内不存在 $f$ 的临界点。

证明：

- $f'(\mathbf{x}_0 + \Delta \mathbf{x})^T = H_f(\mathbf{x}_0) \Delta \mathbf{x} + \mathbf{r}(\Delta \mathbf{x})$，其中 $\displaystyle\lim_{\Delta \mathbf{x} \to \mathbf{0}} \dfrac{||\mathbf{r}(\Delta \mathbf{x})||}{||\Delta \mathbf{x}||} = 0$。
- 由 $\det(H_f(\mathbf{x}_0)) \neq 0$ 可知 $\exists \lambda > 0, \text{s.t. } ||H_f(\mathbf{x}_0) \Delta \mathbf{x}|| \geq \lambda ||\Delta \mathbf{x}||$。
- 取 $r > 0$ 使得 $B_r(\mathbf{x}_0) \subset D$ 且 $\forall ||\Delta \mathbf{x}|| < r, ||\mathbf{r}(\Delta \mathbf{x})|| < \dfrac{\lambda}{2} ||\Delta \mathbf{x}||$，则有：

$$
\forall \mathbf{x} = \mathbf{x}_0 + \Delta \mathbf{x} \in \mathring{B}_r(\mathbf{x}_0), ||f'(\mathbf{x}_0 + \Delta \mathbf{x})|| \geq ||H_f(\mathbf{x}_0) \Delta \mathbf{x}|| - ||\mathbf{r}(\Delta \mathbf{x})|| > \frac{\lambda}{2} ||\Delta \mathbf{x}|| > 0
$$

- 故 $\mathring{B}_r(\mathbf{x}_0)$ 内不存在 $f$ 的临界点。

### 隐函数存在定理

#### 定义：压缩

设 $D \subset \mathbb{R}^n$ 为区域，$\Omega = D \times U(y_0, \delta)$，$\Phi(\mathbf{x}, y) \in C(\Omega)$，$\Phi(\Omega) \subset U(y_0, \delta)$，称 $\Phi$ 在 $\Omega$ 上关于 $y$ 为一个压缩，若：

$$
\exists L \in (0, 1), \text{s.t. } \forall \mathbf{x} \in D, y_1, y_2 \in U(y_0, \delta), |\Phi(\mathbf{x}, y_1) - \Phi(\mathbf{x}, y_2)| \leq L|y_1 - y_2|
$$

即 $\{\Phi(\mathbf{x}, \cdot)\} \subset C(U(y_0, \delta))$ 有**一致**的 Lipschitz 常数 $L \in (0, 1)$。

#### 引理：迭代压缩函数列一致收敛

内容：

- 设 $\Phi$ 在 $D \times U(y_0, \delta)$ 上关于 $y$ 为一个压缩，则函数列

$$
\begin{cases}
y_1(\mathbf{x}) \in C(D), &\quad y_1(D) \subset U(y_0, \delta) \\
y_{k + 1}(\mathbf{x}) = \Phi(\mathbf{x}, y_k(\mathbf{x}))
\end{cases}
$$

- 在 $D$ 上**内闭**一致收敛，即在 $D$ 的任意一个**紧子集**上一致收敛。
- 记 $f(\mathbf{x}) = \displaystyle\lim_{k \to +\infty} y_k(\mathbf{x})$，则 $f \in C(D)$，且 $f$ 为**唯一**满足 $y(\mathbf{x}) = \Phi(\mathbf{x}, y(\mathbf{x}))$ 的函数 $y$。

证明：

- 由 $\Phi$ 关于 $y$ 的一致压缩性易见 $y_n(\mathbf{x})$ 在 $D$ 上内闭一致收敛，又由 $y_n(\mathbf{x}) \in C(D)$ 可知 $f \in C(D)$。
- 同压缩映射定理的唯一性证明可知满足条件的 $y$ 唯一。

#### 隐函数存在定理

内容：

- (1) 设 $D \subset \mathbb{R}^n$ 为区域，$\Omega = D \times U(y_0, \delta)$，$F(\mathbf{x}, y) \in C(\Omega)$，且满足：
- (I) $F(\mathbf{x}_0, y_0) = 0$。
- (II) $F_y'(\mathbf{x}, y)$ 在 $\Omega$ 上存在且连续。
- (III) $F_y'(\mathbf{x}_0, y_0) \neq 0$。
- 则：

$$
\exists r > 0, \text{s.t. } \exists ! f \in C(B_r(\mathbf{x}_0)), \text{s.t. } \begin{cases}
f(\mathbf{x}_0) = y_0 \\
\forall \mathbf{x} \in B_r(\mathbf{x}_0), F(\mathbf{x}, f(\mathbf{x})) = 0
\end{cases}
$$

- (2) 在 (1) 的基础上，若 $F \in C^1(\Omega)$，则 $f \in C^1(B_r(\mathbf{x}_0))$，且有：

$$
\forall \mathbf{x} \in B_r(\mathbf{x}_0), f'(\mathbf{x}) = -\frac{F_{\mathbf{x}}'(\mathbf{x}, f(\mathbf{x}))}{F_y'(\mathbf{x}, f(\mathbf{x}))}
$$

证明：

- (1) 令 $\Phi(\mathbf{x}, y) = y - kF(\mathbf{x}, y)$，其中 $k = \dfrac{1}{F_y'(\mathbf{x}_0, y_0)}$，则 $\dfrac{\partial \Phi}{\partial y} = 1 - k \dfrac{\partial F}{\partial y}$，特别地有 $\Phi_y'(\mathbf{x}_0, y_0) = 0$。
- 由 $F_y' \in C(\Omega)$ 可知 $\Phi_y' \in C(\Omega)$，故有：

$$
\exists \tilde{r} > 0, \delta' \in (0, \delta), \text{s.t. } \forall \mathbf{x} \in B_{\tilde{r}}(\mathbf{x}_0), y \in U(y_0, \delta'), |\Phi_y'(\mathbf{x}, y)| < \dfrac{1}{2}
$$

- 进而有：

$$
\exists r \in (0, \tilde{r}), \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), |\Phi(\mathbf{x}, y_0) - y_0| = |\Phi(\mathbf{x}, y_0) - \Phi(\mathbf{x}_0, y_0)| < \dfrac{1}{2} \delta'
$$

- 令 $\Omega' = B_r(\mathbf{x}_0) \times U(y_0, \delta')$，则有：

$$
\begin{cases}
\forall (\mathbf{x}, y) \in \Omega', |\Phi(\mathbf{x}, y) - y_0| \leq |\Phi(\mathbf{x}, y) - \Phi(\mathbf{x}, y_0)| + |\Phi(\mathbf{x}, y_0) - y_0| < \dfrac{1}{2} |y - y_0| + \dfrac{1}{2} \delta' < \delta' \Rightarrow \Phi(\mathbf{x}, y) \in U(y_0, \delta') \\
\forall \mathbf{x} \in B_r(\mathbf{x}_0), y_1, y_2 \in U(y_0, \delta'), |\Phi(\mathbf{x}, y_1) - \Phi(\mathbf{x}, y_2)| < \dfrac{1}{2} |y_1 - y_2|
\end{cases}
$$

- 其中 $\dfrac{1}{2}$ 的系数来自 Lagrange 中值定理。故 $\Phi$ 在 $\Omega'$ 上关于 $y$ 为一个压缩。
- 由引理即得 $\exists ! f \in C(B_r(\mathbf{x}_0)), \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), f(\mathbf{x}) = \Phi(\mathbf{x}, f(\mathbf{x})) \Leftrightarrow F(\mathbf{x}, f(\mathbf{x})) = 0$。
- (2) 考察增量 $F(\mathbf{x} + \mathbf{h}, f(\mathbf{x} + \mathbf{h})) - F(\mathbf{x}, f(\mathbf{x})) = 0$，用可微性展开可知：

$$
F_{\mathbf{x}}'(\mathbf{x}, f(\mathbf{x})) \cdot \mathbf{h} + F_y'(\mathbf{x}, f(\mathbf{x})) \cdot (f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})) + o(||\mathbf{h}|| + |f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})|) = 0
$$

- 由 $F \in C^1(\Omega)$ 可知 $\Phi \in C^1(\Omega)$，则 $\Phi$ 关于 $\mathbf{x}$ 的 Jacobi 矩阵的范数局部有界（设之为 $L > 0$），则：

$$
\begin{array}{l}
& |f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})| \\
= & |\Phi(\mathbf{x} + \mathbf{h}, f(\mathbf{x} + \mathbf{h})) - \Phi(\mathbf{x}, f(\mathbf{x}))| \\
\leq & |\Phi(\mathbf{x} + \mathbf{h}, f(\mathbf{x} + \mathbf{h})) - \Phi(\mathbf{x}, f(\mathbf{x} + \mathbf{h}))| + |\Phi(\mathbf{x}, f(\mathbf{x} + \mathbf{h})) - \Phi(\mathbf{x}, f(\mathbf{x}))| \\
\leq & L ||\mathbf{h}|| + \dfrac{1}{2} |f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})| &\quad (\text{拟微分中值定理；压缩性})
\end{array}
$$

- 则 $|f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})| \leq 2L ||\mathbf{h}||$，故 $o(||\mathbf{h}|| + |f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x})|) = o(||\mathbf{h}||)$，因此有：

$$
f(\mathbf{x} + \mathbf{h}) - f(\mathbf{x}) = -\frac{F_{\mathbf{x}}'(\mathbf{x}, f(\mathbf{x}))}{F_y'(\mathbf{x}, f(\mathbf{x}))} \cdot \mathbf{h} + o(||\mathbf{h}||)
$$

- 故 $f$ 在 $\mathbf{x}$ 处可微，且 $f'(\mathbf{x}) = -\dfrac{F_{\mathbf{x}}'(\mathbf{x}, f(\mathbf{x}))}{F_y'(\mathbf{x}, f(\mathbf{x}))}$，可见 $f'$ 在 $B_r(\mathbf{x}_0)$ 上连续。因此 $f \in C^1(B_r(\mathbf{x}_0))$。

[例 24] 设区域 $D \subset \mathbb{R}^3$，$F \in C^1(D)$，$F_x' F_y' F_z' \neq 0$，则由隐函数存在定理可知对于方程 $F(x, y, z) = 0$，在这一点附近三者之一可**唯一**表示为另外两者的函数，求证：$\dfrac{\partial z}{\partial x} \cdot \dfrac{\partial x}{\partial y} \cdot \dfrac{\partial y}{\partial z} = -1$。

证明：

- 视 $z$ 为因变量，对 $x$ 求偏导数可知 $F'_x + F'_z \cdot \dfrac{\partial z}{\partial x} = 0$，故 $\dfrac{\partial z}{\partial x} = -\dfrac{F_x'}{F_z'}$。
- 同理可得 $\dfrac{\partial x}{\partial y} = -\dfrac{F_y'}{F_x'}, \dfrac{\partial y}{\partial z} = -\dfrac{F_z'}{F_y'}$，相乘即得 $\dfrac{\partial z}{\partial x} \cdot \dfrac{\partial x}{\partial y} \cdot \dfrac{\partial y}{\partial z} = -1$。

[例 25] 设区域 $D \subset \mathbb{R}^2$，$f \in C^2(D)$，且满足方程 $\dfrac{\partial^2 f}{\partial x^2} \cdot \dfrac{\partial^2 f}{\partial y^2} = \left( \dfrac{\partial^2 f}{\partial x \partial y} \right)^2$，若 $\dfrac{\partial f}{\partial y} \neq 0$，求证：$z = f(x, y)$ 所确定的函数 $y = g(x, z)$ 满足 $\dfrac{\partial^2 g}{\partial x^2} \cdot \dfrac{\partial^2 g}{\partial z^2} = \left( \dfrac{\partial^2 g}{\partial x \partial z} \right)^2$。

证明：

- (1) $z = f(x, y)$ 对应于隐函数方程 $f(x, y) - z = 0$。
- 对 $x$ 求偏导数可知 $\dfrac{\partial f}{\partial x} + \dfrac{\partial f}{\partial y} \cdot \dfrac{\partial g}{\partial x} = 0$，对 $z$ 求偏导数可知 $\dfrac{\partial f}{\partial y} \cdot \dfrac{\partial g}{\partial z} - 1 = 0$。
- 故而解得 $\dfrac{\partial g}{\partial x} = -\dfrac{f_x'}{f_y'}, \dfrac{\partial g}{\partial z} = \dfrac{1}{f_y'}$。
- (2) 直接计算可得：

$$
\begin{cases}
\dfrac{\partial^2 g}{\partial x^2} = \dfrac{\partial}{\partial x} \left( -\dfrac{f_x'}{f_y'} \right) = -\dfrac{f_y' (f_{xx}'' + f_{yx}'' \frac{\partial g}{\partial x}) - f_x'(f_{xy}'' + f_{yy}'' \frac{\partial g}{\partial x})}{(f_y')^2} = -\dfrac{f_{xx}'' (f_y')^2 - 2f_x' f_{xy}'' f_y' + (f_x')^2 f_{yy}''}{(f_y')^3} \\
\dfrac{\partial^2 g}{\partial x \partial z} = \dfrac{\partial}{\partial x} \left( \dfrac{1}{f_y'} \right) = -\dfrac{f_{xy}'' + f_{yy}'' \frac{\partial g}{\partial x}}{(f_y')^2} = \dfrac{f_x' f_{yy}'' - f_{xy}'' f_y'}{(f_y')^3} \\
\dfrac{\partial^2 g}{\partial z^2} = \dfrac{\partial}{\partial z} \left( \dfrac{1}{f_y'} \right) = -\dfrac{f_{yy}'' \cdot \frac{\partial g}{\partial z}}{(f_y')^2} = -\dfrac{f_{yy}''}{(f'_y)^3}
\end{cases}
$$

- 利用题给方程化简即得目标等式。

------

接下来，容易将上述**多元函数**的情形推广到**向量值函数**的情形。这里的证明与上面的思路完全一致。

- 另一个视角是，从单个函数方程 $F(\mathbf{x}, y) = 0$ 的情形扩展到函数方程组 $F_i(\mathbf{x}, \mathbf{y}) = 0, \forall 1 \leq i \leq m$ 的情形。

#### 定义：压缩（ex.）

设 $D \subset \mathbb{R}^n, E \subset \mathbb{R}^m$ 为区域，$\Omega = D \times E$，$\boldsymbol{\Phi}(\mathbf{x}, \mathbf{y})$ 在 $\Omega$ 上连续，$\boldsymbol{\Phi}(\Omega) \subset E$，称 $\boldsymbol{\Phi}$ 在 $\Omega$ 上关于 $\mathbf{y}$ 为一个压缩，若：

$$
\exists L \in (0, 1), \text{s.t. } \forall \mathbf{x} \in D, \mathbf{y}_1, \mathbf{y}_2 \in E, ||\boldsymbol{\Phi}(\mathbf{x}, \mathbf{y}_1) - \boldsymbol{\Phi}(\mathbf{x}, \mathbf{y}_2)|| \leq L ||\mathbf{y}_1 - \mathbf{y}_2||
$$

#### 引理：迭代压缩函数列一致收敛（ex.）

内容：

- 设 $\Phi$ 在 $D \times E$ 上关于 $\mathbf{y}$ 为一个压缩，则函数列

$$
\begin{cases}
\mathbf{y}_1(\mathbf{x}) \text{ 在 } D \text{ 上连续}, &\quad \mathbf{y}_1(D) \subset E \\
\mathbf{y}_{k + 1}(\mathbf{x}) = \boldsymbol{\Phi}(\mathbf{x}, \mathbf{y}_k(\mathbf{x}))
\end{cases}
$$

- 在 $D$ 上**内闭**一致收敛。
- 记 $\mathbf{f}(\mathbf{x}) = \displaystyle\lim_{k \to +\infty} \mathbf{y}_k(\mathbf{x})$，则 $\mathbf{f}$ 在 $D$ 上连续，且 $\mathbf{f}$ 为**唯一**满足 $\mathbf{y}(\mathbf{x}) = \boldsymbol{\Phi}(\mathbf{x}, \mathbf{y}(\mathbf{x}))$ 的函数 $y$。

#### 隐映射定理

内容：

- (1) 设 $D \subset \mathbb{R}^n, E \subset \mathbb{R}^m$ 为区域，$\Omega = D \times E$，$\mathbf{F} : \mathbb{R}^n \times \mathbb{R}^m \to \mathbb{R}^m$，$\mathbf{F}(\mathbf{x}, \mathbf{y})$ 在 $\Omega$ 上连续，且满足：
- (I) $\mathbf{F}(\mathbf{x}_0, \mathbf{y}_0) = \mathbf{0}$。
- (II) $\mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{y})$ 在 $\Omega$ 上存在且连续。
- (III) $\mathbf{F}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{y}_0)$ 可逆。
- 则：

$$
\exists r, s > 0, \text{s.t. } \exists ! \mathbf{f} : B_r(\mathbf{x}_0) \to B_s(\mathbf{y}_0) \text{ 连续}, \text{s.t. } \begin{cases}
\mathbf{f}(\mathbf{x}_0) = \mathbf{y}_0 \\
\forall \mathbf{x} \in B_r(\mathbf{x}_0), \mathbf{F}(\mathbf{x}, \mathbf{f}(\mathbf{x})) = \mathbf{0}
\end{cases}
$$

- (2) 在 (1) 的基础上，若 $\mathbf{F}$ 为 $\Omega$ 上的 $C^1$ 映射，则 $\mathbf{f}$ 为 $B_r(\mathbf{x}_0)$ 上的 $C^1$ 映射，且有：

$$
\forall \mathbf{x} \in B_r(\mathbf{x}_0), \mathbf{f}'(\mathbf{x}) = -(\mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})))^{-1} \mathbf{F}_{\mathbf{x}}'(\mathbf{x}, \mathbf{f}(\mathbf{x}))
$$

证明：

- (1) 令 $\boldsymbol{\Phi}(\mathbf{x}, \mathbf{y}) = \mathbf{y} - P \mathbf{F}(\mathbf{x}, \mathbf{y})$，其中 $P = (\mathbf{F}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{y}_0))^{-1}$，则 $\boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}, \mathbf{y}) = I_m - P \mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{y})$，特别地有 $\boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{y}_0) = O_m$。
- 由 $\mathbf{F}_{\mathbf{y}}'$ 在 $\Omega$ 上连续可知 $\boldsymbol{\Phi}_{\mathbf{y}}'$ 在 $\Omega$ 上连续，故有：

$$
\exists \tilde{r}, s > 0, \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), \mathbf{y} \in B_s(\mathbf{y}_0), ||\boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}, \mathbf{y})|| < \frac{1}{2}
$$

进而有：

$$
\exists r \in (0, \tilde{r}), \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), ||\Phi(\mathbf{x}, \mathbf{y}_0) - \mathbf{y}_0|| = ||\Phi(\mathbf{x}, \mathbf{y}_0) - \Phi(\mathbf{x}_0, \mathbf{y}_0)|| < \dfrac{1}{2} s
$$

- 令 $\Omega' = B_r(\mathbf{x}_0) \times B_s(\mathbf{y}_0)$，则有：

$$
\begin{cases}
\forall (\mathbf{x}, \mathbf{y}) \in \Omega', ||\Phi(\mathbf{x}, \mathbf{y}) - \mathbf{y}_0|| \leq ||\Phi(\mathbf{x}, \mathbf{y}) - \Phi(\mathbf{x}, \mathbf{y}_0)|| + ||\Phi(\mathbf{x}, \mathbf{y}_0) - \mathbf{y}_0|| < \dfrac{1}{2} ||\mathbf{y} - \mathbf{y}_0|| + \dfrac{1}{2} s < s \Rightarrow \Phi(\mathbf{x}, \mathbf{y}) \in E \\
\forall \mathbf{x} \in B_r(\mathbf{x}_0), \mathbf{y}_1, \mathbf{y}_2 \in B_s(\mathbf{y}_0), ||\Phi(\mathbf{x}, \mathbf{y}_1) - \Phi(\mathbf{x}, \mathbf{y}_2)|| < \dfrac{1}{2} ||\mathbf{y}_1 - \mathbf{y}_2||
\end{cases}
$$

- 其中 $\dfrac{1}{2}$ 的系数来自拟微分中值定理。故 $\boldsymbol{\Phi}$ 在 $\Omega'$ 上关于 $y$ 为一个压缩。
- (2) 考察增量 $\mathbf{F}(\mathbf{x} + \mathbf{h}, \mathbf{f}(\mathbf{x} + \mathbf{h})) - \mathbf{F}(\mathbf{x}, \mathbf{f}(\mathbf{x})) = \mathbf{0}$，用可微性展开可知：

$$
\mathbf{F}_{\mathbf{x}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})) \cdot \mathbf{h} + \mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})) \cdot (\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})) + o(||\mathbf{h}|| + ||\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})||) = 0
$$

- 由 $F$ 为 $C^1$ 映射可知 $\Phi$ 为 $C^1$ 映射，则 $\Phi$ 关于 $\mathbf{x}$ 的 Jacobi 矩阵的范数局部有界（设之为 $L > 0$），则：

$$
\begin{array}{l}
& ||\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})|| \\
= & ||\boldsymbol{\Phi}(\mathbf{x} + \mathbf{h}, \mathbf{f}(\mathbf{x} + \mathbf{h})) - \boldsymbol{\Phi}(\mathbf{x}, \mathbf{f}(\mathbf{x}))|| \\
\leq & ||\boldsymbol{\Phi}(\mathbf{x} + \mathbf{h}, \mathbf{f}(\mathbf{x} + \mathbf{h})) - \boldsymbol{\Phi}(\mathbf{x}, \mathbf{f}(\mathbf{x} + \mathbf{h}))|| + ||\boldsymbol{\Phi}(\mathbf{x}, \mathbf{f}(\mathbf{x} + \mathbf{h})) - \boldsymbol{\Phi}(\mathbf{x}, \mathbf{f}(\mathbf{x}))|| \\
\leq & L ||\mathbf{h}|| + \dfrac{1}{2} ||\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})|| &\quad (\text{拟微分中值定理；压缩性})
\end{array}
$$

- 则 $||\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})|| \leq 2L ||\mathbf{h}||$，故 $o(||\mathbf{h}|| + ||\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x})||) = o(||\mathbf{h}||)$。
- 由于 $\mathbf{F}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{f}(\mathbf{x}_0)) = \mathbf{F}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{y}_0)$ 可逆，在以 $(\mathbf{x}_0, \mathbf{y}_0)$ 为球心的一个开球中 $\mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{f}(\mathbf{x}))$ 也可逆，此时有：

$$
\mathbf{f}(\mathbf{x} + \mathbf{h}) - \mathbf{f}(\mathbf{x}) = -(\mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})))^{-1} \mathbf{F}_{\mathbf{x}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})) \cdot \mathbf{h} + o(||\mathbf{h}||)
$$

- 故 $\mathbf{f}$ 在 $\mathbf{x}$ 处可微，且 $\mathbf{f}'(\mathbf{x}) = -(\mathbf{F}_{\mathbf{y}}'(\mathbf{x}, \mathbf{f}(\mathbf{x})))^{-1} \mathbf{F}_{\mathbf{x}}'(\mathbf{x}, \mathbf{f}(\mathbf{x}))$，可见 $\mathbf{f}'$ 在某个 $r > 0$ 对应的 $B_r(\mathbf{x}_0)$ 上连续。因此 $\mathbf{f}$ 为 $B_r(\mathbf{x}_0)$ 上的 $C^1$ 映射。

[例 26] 设 $\begin{cases} x^2 + y^2 + z^2 = a^2 > 0 \\ x + y + z = 0 \end{cases}$ 决定 $y, z$ 是 $x$ 的函数，求 $\dfrac{\text{d} y}{\text{d} x}, \dfrac{\text{d} z}{\text{d} x}, \dfrac{\text{d}^2 y}{\text{d} x^2}, \dfrac{\text{d}^2 z}{\text{d} x^2}$。

解：

- 题给即函数方程 $\mathbf{F}(x, (y, z)) \triangleq (x^2 + y^2 + z^2 - a^2, x + y + z) = 0$，则有：

$$
\mathbf{F}_x'(x, (y, z)) = \begin{bmatrix} 2x \\ 1 \end{bmatrix}, \mathbf{F}_{(y, z)}'(x, (y, z)) = \begin{bmatrix} 2y & 2z \\ 1 & 1 \end{bmatrix}
$$

- 设 $\mathbf{f}(x) = (y, z)$，则当 $\det \mathbf{F}_{(y, z)}'(x, (y, z)) \neq 0$ 即 $y \neq z$，有：

$$
\mathbf{f}'(x) = -(\mathbf{F}_{(y, z)}'(x, (y, z)))^{-1} \mathbf{F}_x'(x, (y, z)) = \frac{1}{2(y - z)} \begin{bmatrix} 2z - 2x \\ 2x - 2y \end{bmatrix}
$$

- 即 $\dfrac{\text{d} y}{\text{d} x} = \dfrac{z - x}{y - z}, \dfrac{\text{d} z}{\text{d} x} = \dfrac{x - y}{y - z}$。
- 直接计算 $\dfrac{\text{d}}{\text{d} x} \left( \dfrac{\text{d} y}{\text{d} x} \right), \dfrac{\text{d}}{\text{d} x} \left( \dfrac{\text{d} z}{\text{d} x} \right)$ 并代入 $x^2 + y^2 + z^2 = a^2$，求得 $\dfrac{\text{d}^2 y}{\text{d} x^2} = -\dfrac{3a^2}{(y - z)^3}, \dfrac{\text{d}^2 z}{\text{d} x^2} = \dfrac{3a^2}{(y - z)^3}$。

#### 逆映射定理

内容：设 $D, \Omega \subset \mathbb{R}^n$ 为区域，$\mathbf{y} = \mathbf{f}(\mathbf{x})$ 是从 $D$ 到 $\Omega$ 的 $C^1$ 映射，$\mathbf{x}_0 \in D$，$\mathbf{y}_0 = \mathbf{f}(\mathbf{x}_0)$，且 $\mathbf{f}'(\mathbf{x}_0)$ 可逆，则 $\exists r > 0, \text{s.t. } \mathbf{y} = \mathbf{f}(\mathbf{x})$ 为从 $B_r(\mathbf{x}_0) \subset D$ 到 $\mathbf{f}(B_r(\mathbf{x}_0)) \subset \Omega$ 的 $C^1$ 同胚，且逆映射 $\mathbf{x} = \mathbf{g}(\mathbf{y})$ 满足 $\mathbf{g}'(\mathbf{y}) = (\mathbf{f}'(\mathbf{g}(\mathbf{y})))^{-1}$。

证明：

- 考虑方程 $\mathbf{F}(\mathbf{x}, \mathbf{y}) = \mathbf{f}(\mathbf{x}) - \mathbf{y} = \mathbf{0}$，下面验证其满足隐映射定理的条件：
- (I) $\mathbf{F}(\mathbf{x}_0, \mathbf{y}_0) = \mathbf{f}(\mathbf{x}_0) - \mathbf{y}_0 = \mathbf{0}$。
- (II) 由 $\mathbf{f}$ 为 $C^1$ 映射可知 $\mathbf{F}$ 为 $C^1$ 映射。
- (III) 由 $\mathbf{F}_{\mathbf{x}}'(\mathbf{x}_0, \mathbf{y}_0) = \mathbf{f}'(\mathbf{x}_0)$ 可知其可逆。
- 故 $\exists r, s > 0$，$\text{s.t. } \exists ! \mathbf{g}$ 为从 $B_s(\mathbf{y}_0)$ 到 $B_r(\mathbf{x}_0)$ 的 $C^1$ 映射，$\text{s.t. } \mathbf{g}(\mathbf{f}(\mathbf{x})) = \mathbf{x}$，即在此区域内 $\mathbf{g} = \mathbf{f}^{-1}$ 成立。
- 对 $\mathbf{f}(\mathbf{g}(\mathbf{y})) = \mathbf{y}$ 两边求导即得：

$$
\mathbf{f}'(\mathbf{g}(\mathbf{y})) \cdot \mathbf{g}'(\mathbf{y}) = I_n \Rightarrow \mathbf{g}'(\mathbf{y}) = (\mathbf{f}'(\mathbf{g}(\mathbf{y})))^{-1}
$$

事实上隐映射定理（$C^1$ 情形）也可以反过来用逆映射定理来证明：

- 构造映射 $\Phi$：

$$
\begin{aligned}
\boldsymbol{\Phi} : D \times E &\to D \times \mathbb{R}^m \\
(\mathbf{x}, \mathbf{y}) &\mapsto (\mathbf{x}, \mathbf{F}(\mathbf{x}, \mathbf{y}))
\end{aligned}
$$

- 则 Jacobi 矩阵

$$
\boldsymbol{\Phi}'(\mathbf{x}_0, \mathbf{y}_0) = \begin{bmatrix}
I_n & O \\
\mathbf{F}_{\mathbf{x}}'(\mathbf{x}_0, \mathbf{y}_0) & \mathbf{F}_{\mathbf{y}}'(\mathbf{x}_0, \mathbf{y}_0)
\end{bmatrix}
$$

- 可逆。故其在 $(\mathbf{x}_0, \mathbf{y}_0)$ 的邻域 $V$ 中存在 $C^1$ 的逆映射 $\boldsymbol{\Psi} : B_{r_1}(\mathbf{x}_0) \times B_{r_2}(\mathbf{y}_0) \to D \times E$。
- $\forall \mathbf{x} \in B_{r_1}(\mathbf{x}_0)$，令 $\boldsymbol{\Psi}(\mathbf{x}, \mathbf{0}) = (\mathbf{x}, \mathbf{f}(\mathbf{x}_0))$，易见这一 $\mathbf{f}$ 即满足条件。

### 多元函数的极值

#### 定义：（严格）极（大、小）值（点）

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$\mathbf{x}_0 \in D$。

若 $\exists \delta > 0, \text{s.t. } \forall \mathbf{x} \in \mathring{B}_\delta(\mathbf{x}_0), f(\mathbf{x}) \leq f(\mathbf{x}_0)$，则称 $f$ 在 $\mathbf{x}_0$ 处取得极大值，此时称 $\mathbf{x}_0$ 为 $f$ 的极大值点。

将不等式替换为 $f(\mathbf{x}) < f(\mathbf{x}_0)$，即得严格极大值（点）的定义。

同理可以定义极小值（点）。极大值（点）与极小值（点）统称极值（点）。

- 与一元函数的情形一致，我们在定义中去除了边界点的情况（**区域**），但边界点可能是**最值**点。

------

与一元函数的情形一致，我们首先给出极值点的必要条件。

#### 定理：极值点的一阶必要条件

内容：

- 设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$\mathbf{x}_0 \in D$，若 $f$ 在 $\mathbf{x}_0$ 处取得极值，且 $f$ 在 $\mathbf{x}_0$ 处的各个偏导数存在，则 $\forall 1 \leq i \leq n, \dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = 0$。
- 特别地，若 $f$ 在极值点 $\mathbf{x}_0$ 处可微，则 $f'(\mathbf{x}_0) = \mathbf{0}$。

证明：

- 将 $f(\mathbf{x})$ 中除 $x_i$ 外的所有分量固定在 $\mathbf{x}_0$ 处的值，则 $g(x) \triangleq f((\mathbf{x}_0)_1, \cdots, (\mathbf{x}_0)_{i - 1}, x, (\mathbf{x}_0)_{i + 1}, \cdots, (\mathbf{x}_0)_n)$ 在 $t = x_i^0$ 处取得极值。
- 由 Fermat 引理可知 $g'((\mathbf{x}_0)_i) = 0$，即 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = 0$。
- 若 $f$ 在 $\mathbf{x}_0$ 处可微，则 $f'(\mathbf{x}_0) = \left( \dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} \right)_{i = 1}^n$ 为零向量。

但需要注意的是，这一条件并不充分，如 $f(x, y) = xy$ 在 $(0, 0)$ 处的导数为零向量，但 $(0, 0)$ 是 $f$ 的鞍点而非极值点。

接下来考虑 $f$ 更光滑的情况。当 $f$ 在临界点 $\mathbf{x}_0$ 附近是 $C^2$ 函数，取带 Peano 余项的二阶 Taylor 公式，可知：

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) - f(\mathbf{x}_0) = \frac{1}{2} (\Delta \mathbf{x})^T H_f(\mathbf{x}_0) \Delta \mathbf{x} + o(||\Delta \mathbf{x}||^2) \quad (\Delta \mathbf{x} \to \mathbf{0})
$$

可见 Hesse 阵可能决定了极值点的特性。下面给出具体的陈述。

#### 定理：极值点的二阶必要条件

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f$ 在 $D$ 上可微，$\mathbf{x}_0 \in D$，$f'$ 在 $\mathbf{x}_0$ 处可微，若 $f$ 在 $\mathbf{x}_0$ 处取得极小 / 大值，则 $H_f(\mathbf{x}_0)$ 半正 / 负定。

证明：

- 下面只证明极小值的情况，极大值是同理的。
- $\forall ||\mathbf{e}|| = 1$，由带 Peano 余项的二阶 Taylor 公式可知：

$$
f(\mathbf{x}_0 + t \mathbf{e}) = f(\mathbf{x}_0) + \frac{1}{2} (t \mathbf{e})^T H_f(\mathbf{x}_0) (t \mathbf{e}) + o(||t \mathbf{e}||^2)
$$

- 故有：

$$
\mathbf{e}^T H_f(\mathbf{x}_0) \mathbf{e} = 2 \lim_{t \to 0} \frac{f(\mathbf{x}_0 + t \mathbf{e}) - f(\mathbf{x}_0)}{t^2} \geq 0
$$

- 即 $H_f(\mathbf{x}_0)$ 半正定。

------

接下来考虑充分条件。

#### 定理：极值的一阶充分条件

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，$\mathbf{x}_0 \in D$ 为 $f$ 的临界点，若 $H_f(\mathbf{x}_0)$ **满秩**，此时其必为正定、负定、不定三者之一，且有：

- (1) 当 $H_f(\mathbf{x}_0)$ 正定，$f(\mathbf{x}_0)$ 为严格极小值。
- (2) 当 $H_f(\mathbf{x}_0)$ 负定，$f(\mathbf{x}_0)$ 为严格极大值。
- (3) 当 $H_f(\mathbf{x}_0)$ 不定，$f(\mathbf{x}_0)$ 不是极值。

证明：

- (1) 当 $H_f(\mathbf{x}_0)$ 正定，同第七讲例 16 可知：

$$
\exists \lambda > 0, \text{s.t. } \forall \Delta \mathbf{x} \in \mathbb{R}^n, (\Delta \mathbf{x})^T H_f(\mathbf{x}_0) \Delta \mathbf{x} \geq \lambda ||\Delta \mathbf{x}||^2
$$

- 取 $r > 0$ 使得 $\forall 0 < ||\Delta \mathbf{x}|| < r$，$o(||\Delta \mathbf{x}||^2)$ 的 Peano 余项的绝对值 $< \dfrac{\lambda}{4} ||\Delta \mathbf{x}||^2$，此时有：

$$
\begin{array}{l}
& f(\mathbf{x}_0 + \Delta \mathbf{x}) - f(\mathbf{x}_0) \\
> & \dfrac{1}{2} \cdot \lambda ||\Delta \mathbf{x}||^2 - \dfrac{\lambda}{4} ||\Delta \mathbf{x}||^2 \\
= & \dfrac{\lambda}{4} ||\Delta \mathbf{x}||^2 \\
> & 0
\end{array}
$$

- 故 $f(\mathbf{x}_0)$ 为严格极小值。
- (2) 当 $H_f(\mathbf{x}_0)$ 负定，考虑 $-f$ 即化为情形 (1)。
- (3) 当 $H_f(\mathbf{x}_0)$ 不定，有：

$$
\exists \mathbf{v}_1, \mathbf{v}_2 \in \mathbb{R}^n \backslash \{\mathbf{0}\}, \text{s.t. } \mathbf{v}_1^T H_f(\mathbf{x}_0) \mathbf{v}_1 > 0, \mathbf{v}_2^T H_f(\mathbf{x}_0) \mathbf{v}_2 < 0
$$

- 当 $t$ 充分小，$f(\mathbf{x}_0 + t \mathbf{v}_1) = f(\mathbf{x}_0) + (\mathbf{v}_1^T H_f(\mathbf{x}_0) \mathbf{v}_1) t^2 + o(t^2) > f(\mathbf{x}_0)$，$f(\mathbf{x}_0 + t \mathbf{v}_2) = f(\mathbf{x}_0) + (\mathbf{v}_2^T H_f(\mathbf{x}_0) \mathbf{v}_2) t^2 + o(t^2) < f(\mathbf{x}_0)$，故 $f$ 在 $\mathbf{x}_0$ 处不取极值。

#### 定理：极值的二阶充分条件

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，$\mathbf{x}_0 \in D$ 为 $f$ 的临界点，则：

- (1) 若 $\exists r > 0, \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0),  H_f(\mathbf{x})$ 半正定，$f(\mathbf{x}_0)$ 为极小值。
- (2) 若 $\exists r > 0, \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0),  H_f(\mathbf{x})$ 半负定，$f(\mathbf{x}_0)$ 为极大值。

证明 (1)：

- 由带 Lagrange 余项的二阶 Taylor 公式可知：

$$
\forall 0 < ||\Delta \mathbf{x}|| < r, \exists \theta \in (0, 1), \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \frac{1}{2} (\Delta \mathbf{x})^T H_f(\mathbf{x}_0 + \theta \Delta \mathbf{x}) (\Delta \mathbf{x}) \geq f(\mathbf{x}_0)
$$

- 故 $f(\mathbf{x}_0)$ 为极小值。

------

与一元函数的情形一致，求极值时把临界点和偏导数不全存在的点抓出来讨论即可。

[例 27] 求 $z = x^4 + y^4 - x^2 - 2xy - y^2$ 的极值。

解：

- (1) 求临界点：

$$
\begin{cases}
z_x' = 4x^3 - 2x - 2y = 0 &\quad (1) \\
z_y' = 4y^3 - 2x - 2y = 0 &\quad (2)
\end{cases}
$$

- $(1) - (2)$ 得 $4(x^3 - y^3) = 0 \Rightarrow x = y$。
- 代回 $(1)$ 得 $4x^3 - 4x = 0 \Rightarrow 4x(x^2 - 1) = 0 \Rightarrow x = 0, \pm 1$。
- 故有三个临界点：$(0, 0), (1, 1), (-1, -1)$。
- (2) 求 Hesse 阵：

$$
H_z = \begin{bmatrix}
12x^2 - 2 & -2 \\
-2 & 12y^2 - 2
\end{bmatrix}
$$

- I. $(0, 0)$：$H_z = \begin{bmatrix} -2 & -2 \\ -2 & -2 \end{bmatrix} \Rightarrow \det H_z = 0$，则 $H_z$ 不满秩。
- 沿射线 $y = x$ 有 $z(x, x) = 2x^4 - 4x^2 = 2x^2(x^2 - 2) < 0$（当 $0 < x < \sqrt{2}$），但沿射线 $y = -x$ 有 $z(x, -x) = 2x^4 > 0$。故 $(0, 0)$ 不为极值。
- II. $(1, 1), (-1, -1)$：$H_z = \begin{bmatrix} 10 & -2 \\ -2 & 10 \end{bmatrix} \Rightarrow \det H_z = 96 \neq 0$，$H_z$ 正定，则取得严格极小值。
- 综上，原函数 $z = z(x, y)$ 有严格极小值 $z(1, 1) = z(-1, -1) = -2$。

#### 多元函数的最值

设 $f$ 定义在 $D \subset \mathbb{R}$ 上，统称 $f$ 在 $D$ 上的最大值和最小值统为最值。最值若存在，则包含在两类点中：

- (1) $\mathring{D}$ 中的极值点。
- (2) $\partial D$ 上的点。

[例 28] 一半径为 $R$ 的球，从内部切出一个长方体，如何切所得体积最大？

解：

- 设长方体棱长分别为 $2x, 2y, 2z$，中心置于球心，顶点在球面上，则体积 $V = 8xyz$，约束条件为 $x^2 + y^2 + z^2 = R^2$。
- 消元 $z = \sqrt{R^2 - x^2 - y^2}$，则有：

$$
V^2 = 64x^2 y^2 (R^2 - x^2 - y^2)
$$

- 令 $u = x^2, v = y^2$，则 $f(u, v) \triangleq V^2 = 64 uv(R^2 - u - v)$，欲求 $f$ 在区域 $D = \{u + v < R^2 \mid u, v > 0\}$ 内的最大值。
- 令 $f'(u, v) = (0, 0)$，解方程：

$$
\begin{cases}
\dfrac{\partial f}{\partial u} = 64v(R^2 - u - v) - 64uv = 64v(R^2 - 2u - v) = 0 \\[8pt]
\dfrac{\partial f}{\partial v} = 64u(R^2 - u - v) - 64uv = 64u(R^2 - u - 2v) = 0
\end{cases}
$$

- 解得 $u = v = \dfrac{R^2}{3}$，可见 $f(u, v)$ 在 $\left( \dfrac{R^2}{3}, \dfrac{R^2}{3} \right)$ 取得极大值。
- 由 $f$ 在紧集 $\bar{D}$ 上连续可知 $f$ 在 $\bar{D}$ 上取得最大值，而在 $\partial D$ 上有 $f = 0$（即退化为长方形），故 $f$ 在 $\bar{D}$ 上的最大值在 $D$ 中取得，正是这个唯一的极大值。
- 此时有 $x = y = z = \dfrac{R}{\sqrt{3}}$，故体积最大值为 $V = \dfrac{8R^3}{3 \sqrt{3}}$。

#### 线性经验公式的获得——最小二乘法

设经验告诉我们，变量 $x$ 与 $y$ 成线性关系 $y = kx + b$，其中 $k, b$ 为待定参数。现经测量得到 $n$ 组数据 $\{(x_i, y_i)\}_{i = 1}^n$，求与实验数据最接近的直线 $y = kx + b$。

不妨设 $x_i$ 并非两两相同，即数据不全在同一竖直直线上。

定义误差为**逐点误差的平方和**：

$$
L(k, b) = \sum_{i = 1}^n (kx_i + b - y_i)^2
$$

则 $L$ 是关于 $k, b$ 的二元二次函数，问题转化为求 $L$ 的最小值。

首先求临界点：

$$
\begin{cases}
\dfrac{\partial L}{\partial k} = 2 \displaystyle\sum_{i = 1}^n x_i (kx_i + b - y_i) = 0 \\
\dfrac{\partial L}{\partial b} = 2 \displaystyle\sum_{i = 1}^n (kx_i + b - y_i) = 0
\end{cases}
$$

移项整理得：

$$
\begin{cases}
\displaystyle\left( \sum_{i = 1}^n x_i^2 \right) k + \left( \sum_{i = 1}^n x_i \right) b = \sum_{i = 1}^n x_i y_i &\quad (1) \\
\displaystyle\left( \sum_{i = 1}^n x_i \right) k + n b = \sum_{i = 1}^n y_i &\quad (2)
\end{cases}
$$

记 $\bar{x} = \dfrac{1}{n} \displaystyle\sum_{i=1}^n x_i$，$\bar{y} = \dfrac{1}{n} \displaystyle\sum_{i=1}^n y_i$，由 $(2)$ 可知：

$$
b = \bar{y} - k \bar{x}
$$

代入 $(1)$ 解得：

$$
k = \frac{\sum_{i = 1}^n x_i y_i - n \bar{x} \bar{y}}{\sum_{i = 1}^n x_i^2 - n \bar{x}^2} = \frac{\sum_{i = 1}^n (x_i - \bar{x})(y_i - \bar{y})}{\sum_{i = 1}^n (x_i - \bar{x})^2}
$$

可以验证，此临界点确为 $L$ 的极小值点，进而是其最小值点。事实上，$L$ 为开口向上的抛物面。

上述过程称为**线性拟合**，所用方法称为**最小二乘法**。

注：

- (1) 拟合直线必过平均值点 $(\bar{x}, \bar{y})$，因此只需求出斜率 $k$，将直线写成点斜式 $y - \bar{y} = k(x - \bar{x})$ 即可。
- (2) 在 $k$ 的表达式中，分母为样本 $\{x_i\}$ 的**方差**的 $n$ 倍，分子为样本 $\{x_i\}$ 与 $\{y_i\}$ 的**协方差**的 $n$ 倍。
- (3) 若 $\forall 1 \leq i \leq n, x_i \neq \bar{x}$，$k$ 的表达式可以改写为 $\displaystyle\sum_{i = 1}^n \dfrac{(x_i - \bar{x})^2}{\sum_{j = 1}^n (x_j - \bar{x})^2} \cdot \dfrac{y_i - \bar{y}}{x_i - \bar{x}}$，即拟合直线的斜率是各点与平均值点连线斜率的加权平均值。
- (4) 在实际应用中，还有一种常用的经验公式 $y = b e^{-kx} \ (b > 0)$，取对数得到 $\ln y = \ln b - kx$，即转化为线性拟合问题。

### 条件极值与 Lagrange 乘数法

在实际问题中，我们求解的往往不是自变量完全自由时的**无条件极值**，而且要求其满足某些条件的**条件极值**。考虑将这类问题建模为“当 $\varphi = 0$，求解 $f$ 的极值”，下面来进行讨论。

#### 定义：条件极值

设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^1(D)$，$\boldsymbol{\Phi} : D \to \mathbb{R}^m \ (m < n)$ 为 $C^1$ 映射，记：

$$
\begin{cases}
\boldsymbol{\Phi} = (\varphi_1, \cdots, \varphi_m) \\
\Sigma \triangleq \boldsymbol{\Phi}^{-1}(\mathbf{0}) = \{\mathbf{x} \in U \mid \forall 1 \leq i \leq m, \varphi_i(\mathbf{x}) = \mathbf{0}\}
\end{cases}
$$

称方程 $\boldsymbol{\Phi}(\mathbf{x}) = \mathbf{0}$ 为约束条件，$f$ 在 $\Sigma$ 上的极值为条件极值。

#### 定理：Lagrange 乘数法 / 条件极值的必要条件

内容：

- 设 $D, f, \boldsymbol{\Phi}$ 如上定义，若 $\mathbf{x}_0 \in D$ 为条件极值点，且 $\boldsymbol{\Phi}'(\mathbf{x}_0)$ 满秩，则：

$$
\exists \boldsymbol{\lambda} \in \mathbb{R}^m, \text{s.t. } \begin{cases}
\boldsymbol{\Phi}(\mathbf{x}_0) = \mathbf{0} \\
f'(\mathbf{x}_0) - \boldsymbol{\lambda}^T \boldsymbol{\Phi}'(\mathbf{x}_0) = \mathbf{0}
\end{cases}
$$

- 称其中的 $\boldsymbol{\lambda}$ 为 Lagrange 乘数。换言之，构造 Lagrange 函数：

$$
\begin{aligned}
F : \mathbb{R}^n \times \mathbb{R}^m &\to \mathbb{R} \\
(\mathbf{x}, \boldsymbol{\lambda}) &\mapsto f(\mathbf{x}) - \boldsymbol{\lambda} \cdot \boldsymbol{\Phi}(\mathbf{x})
\end{aligned}
$$

- 则 $F'(\mathbf{x}_0, \boldsymbol{\lambda}) = \mathbf{0}$。

证明：

- 设 $\mathbf{x}_0 = (x_0^{(i)})_{i = 1}^n$，记 $\mathbf{y}_0 = (x_0^{(i)})_{i = 1}^m \in \mathbb{R}^m, \mathbf{z}_0 = (x_0^{(i)})_{i = m + 1}^n \in \mathbb{R}^{n - m}$，由隐映射定理可知：
- 存在 $\mathbf{z}_0$ 的开邻域 $E$ 和唯一的 $C^1$ 映射 $\boldsymbol{\psi} : E \to \mathbb{R}^m$，使得：

$$
\begin{cases}
\boldsymbol{\psi}(\mathbf{z}_0) = \mathbf{y}_0 \\
\forall \mathbf{z} \in E, \boldsymbol{\Phi}(\boldsymbol{\psi}(\mathbf{z}), \mathbf{z}) = \mathbf{0} \\
\forall \mathbf{z} \in E, \boldsymbol{\psi}'(\mathbf{z}) = -(\boldsymbol{\Phi}_{\mathbf{y}}'(\boldsymbol{\psi}(\mathbf{z}), \mathbf{z}))^{-1} \boldsymbol{\Phi}_{\mathbf{z}}'(\boldsymbol{\psi}(\mathbf{z}), \mathbf{z}) &\quad (1)
\end{cases}
$$

- 在 $\mathbf{z}_0$ 处 $(1)$ 即为：

$$
\boldsymbol{\psi}'(\mathbf{z}_0) = -(\boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}_0))^{-1} \boldsymbol{\Phi}_{\mathbf{z}}'(\mathbf{x}_0)
$$

- 由于 $\mathbf{x}_0$ 为 $f$ 的条件极值点，$\mathbf{z}_0$ 也为 $f(\boldsymbol{\psi}(\mathbf{z}_0), \mathbf{z}_0)$ 的极值点，在 $\mathbf{z}_0$ 处求导可知：

$$
f_{\mathbf{y}}'(\mathbf{x}_0) \boldsymbol{\psi}'(\mathbf{z}_0) + f_{\mathbf{z}}'(\mathbf{z}_0) = 0 \quad (2)
$$

- 令 $\boldsymbol{\lambda} = f_{\mathbf{y}}'(\mathbf{x}_0) (\boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}_0))^{-1}$，则 $(2)$ 可以改写为：

$$
f_{\mathbf{z}}'(\mathbf{x}_0) = \boldsymbol{\lambda} \boldsymbol{\Phi}_{\mathbf{z}}'(\mathbf{x}_0)
$$

- 同时，$\boldsymbol{\lambda}$ 的定义可以改写为：

$$
f_{\mathbf{y}}'(\mathbf{x}_0) = \boldsymbol{\lambda} \boldsymbol{\Phi}_{\mathbf{y}}'(\mathbf{x}_0)
$$

- 将两者拼起来并移项即得：

$$
f'(\mathbf{x}_0) - \boldsymbol{\lambda}^T \boldsymbol{\Phi}'(\mathbf{x}_0) = \mathbf{0}
$$

[例 29] （加权算术-几何平均值不等式）

- 设 $\forall 1 \leq i \leq n, \alpha_i > 0, x_i > 0$，求证：

$$
\prod_{i = 1}^n x_i^{\alpha_i} \leq \left( \frac{\sum_{i = 1}^n \alpha_i x_i}{\sum_{i = 1}^n \alpha_i} \right)^{\sum_{i = 1}^n \alpha_i}
$$

- 且等号成立当且仅当 $x_1 = \cdots = x_n$。

证明：

- 设 $D = \left\{ (x_i)_{i = 1}^n \mid \forall 1 \leq i \leq n, x_i > 0; \displaystyle\sum_{i = 1}^n \alpha_i x_i = c \right\} \subset \mathbb{R}^n$ 为区域，欲求 $D$ 上的 $C^1$ 函数 $f(x_1, \cdots, x_n) = \displaystyle\sum_{i = 1}^n \alpha_i \ln x_i$ 在条件 $\displaystyle\sum_{i = 1}^n \alpha_i x_i = c > 0$ 下的条件极值。
- 设出 Lagrange 函数：

$$
F(\mathbf{x}, \lambda) = \sum_{i = 1}^n \alpha_i \ln x_i - \lambda \left( \sum_{i = 1}^n \alpha_i x_i - c \right)
$$

- 接下来求临界点，令：

$$
\begin{cases}
F_{\mathbf{x}}'(\mathbf{x}, \lambda) = \left( \dfrac{\alpha_i}{x_i} - \lambda \alpha_i \right)_{i = 1}^n = \mathbf{0} \\
F_{\lambda}'(\mathbf{x}, \lambda) = \displaystyle\sum_{i = 1}^n \alpha_i x_i - c = 0
\end{cases}
$$

- 解得：

$$
\forall 1 \leq i \leq n, x_i = \dfrac{c}{\sum_{j = 1}^n \alpha_j}
$$

- 由于在 $\partial D$ 上 $f$ 取值为 $-\infty$，$f$ 在 $D$ 中取得最大值，上述**唯一**临界点即为**唯一**最大值点，故有：

$$
\sum_{i = 1}^n \alpha_i \ln x_i \leq \sum_{i = 1}^n \alpha_i \ln \left( \dfrac{c}{\sum_{j = 1}^n \alpha_j} \right)
$$

- 对不等式两边同时取 $\exp$，整理即得：

$$
\prod_{i = 1}^n x_i^{\alpha_i} \leq \left( \frac{\sum_{i = 1}^n \alpha_i x_i}{\sum_{i = 1}^n \alpha_i} \right)^{\sum_{i = 1}^n \alpha_i}
$$

上面“断言内部唯一临界点为极大值”这一步可以更严格地写为：

- 设极大值为 $A$，令 $K = \{\mathbf{x} \in D \mid f(\mathbf{x}) \geq A - 1\} \subset D$。
- (1) 一方面，由 $D$ 有界可知 $K$ 有界。
- (2) 另一方面，设 $\{\mathbf{x}^{(k)}\}_{k = 1}^{+\infty} \subset K$ 为 Cauchy 列，其在 $\mathbb{R}^n$ 上收敛到 $\mathbf{x}^*$，则 $\mathbf{x}^* \in \bar{K} \subset \bar{D}$。
- 若 $\mathbf{x}^* \subset \partial D$，则 $\exists 1 \leq i \leq n, \text{s.t. } x^*_i = 0$，故 $\displaystyle\lim_{k \to +\infty} f(\mathbf{x}^{(k)}) = -\infty$，与 $\forall k \in \mathbb{N}_+, f(\mathbf{x}^{(k)}) \geq A - 1$ 矛盾！
- 故 $\mathbf{x}^* \in D$，则 $f(\mathbf{x}^*) = \displaystyle\lim_{k \to +\infty} f(\mathbf{x}^{(k)}) \geq A - 1$，因而 $\mathbf{x}^* \in K$。
- 由 Cauchy 列的任意性可知 $K$ 闭。
- 综上 (1)(2)，$K$ 为紧集，则 $f$ 在 $K$ 上取得最大值。
- 同时易见 $f$ 在 $\partial K$ 上取值为 $A - 1$，唯一临界值点在其内部，故 $f$ 于 $K$ 上在该唯一临界值点处取得最大值为 $A$，推及 $D$ 上亦然。

下面来讨论如何判定临界点到底是不是极值点、如果是的话又是哪一类极值点。

#### 二元函数的条件极值

在求函数 $z = f(x, y)$ 在约束条件 $\varphi(x, y) = 0$ 下的极值时，令 Lagrange 函数：

$$
F(x, y, \lambda) = f(x, y) + \lambda \varphi(x, y)
$$

若 $(x_0, y_0, \lambda_0)$ 满足：

$$
\begin{cases}
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial x} = 0 \\
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial y} = 0 \\
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial \lambda} = 0
\end{cases}
$$

记：

$$
\begin{cases}
A = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial x^2} \\
B = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial x \partial y} \\
C = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial y^2}
\end{cases}
$$

则由正 / 负定阵的判定条件可知：

- (1) 若 $A > 0,\; AC - B^2 > 0$，则 $(x_0, y_0)$ 是条件极值问题的极小值点。
- (2) 若 $A < 0,\; AC - B^2 > 0$，则 $(x_0, y_0)$ 是条件极值问题的极大值点。
- (3) 若 $AC - B^2 < 0$，则 $(x_0, y_0)$ 是否是条件极值问题的极值点待定。

[例 30] 求 $z = x^2 + y^2$ 在条件 $y = x^2 + 1$ 下的条件极值。

解：

- 令 $F(x, y, \lambda) = (x^2 + y^2) + \lambda (y - x^2 - 1)$，设 $(x_0, y_0, \lambda_0)$ 满足：

$$
\begin{cases}
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial x} = 2(1 - \lambda_0) x_0 = 0 \\
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial y} = 2y_0 + \lambda_0 = 0 \\
\dfrac{\partial F(x_0, y_0, \lambda_0)}{\partial \lambda} = y_0 - x_0^2 - 1 = 0
\end{cases}
$$

- 解得 $x_0 = 0, y_0 = 1, \lambda_0 = -2$，故唯一临界点为 $(0, 1)$。
- 记：

$$
\begin{cases}
A = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial x^2} = 2(1 - \lambda_0) = 6 \\
B = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial x \partial y} = 0 \\
C = \dfrac{\partial^2 F(x_0, y_0, \lambda_0)}{\partial y^2} = 2
\end{cases}
$$

- 由 $A > 0, AC - B^2 = 12 > 0$ 可知 $(0, 1)$ 为该条件极值问题的极小值点，且极小值为 $1$。
- 由于当 $x \to +\infty, y = x^2 + 1 \to +\infty$，有 $z \to +\infty$，故该条件极值问题无极大值。

#### 定理：条件极值的充分条件

内容：设 $(\mathbf{x}_0, \boldsymbol{\lambda}_0)$ 为 Lagrange 函数 $F(\mathbf{x}, \boldsymbol{\lambda}) = f(\mathbf{x}) - \boldsymbol{\lambda} \cdot \boldsymbol{\Phi}(\mathbf{x})$ 的临界点，考虑 Hesse 阵：

$$
H_{F, \mathbf{x}_0}(\mathbf{x}_0, \boldsymbol{\lambda}_0) = \left( \frac{\partial^2 F(\mathbf{x}_0, \boldsymbol{\lambda}_0)}{\partial x_i \partial x_j} \right)_{i, j}
$$

- (1) 若 $H_{F, \mathbf{x}_0}(\mathbf{x}_0, \boldsymbol{\lambda}_0)$ 正定，则 $\mathbf{x}_0$ 为条件极值问题的极小值点。
- (2) 若 $H_{F, \mathbf{x}_0}(\mathbf{x}_0, \boldsymbol{\lambda}_0)$ 负定，则 $\mathbf{x}_0$ 为条件极值问题的极大值点。

[例 31] 求内接于椭球面 $16x^2 + 4y^2 + 9z^2 = 144$ 的长方体的最大体积。

解：

- 设长方体在第一卦限的顶点为 $(x, y, z)$，则体积 $V = 8xyz$。
- 令 $F(x, y, z, \lambda) = 8xyz + \lambda (16x^2 + 4y^2 + 9z^2 - 144)$，设 $(x_0, y_0, \lambda_0)$ 满足：

$$
\begin{cases}
\dfrac{\partial F(x_0, y_0, z_0, \lambda_0)}{\partial x} = 8y_0 z_0 + 32 \lambda_0 x_0 = 0 \\
\dfrac{\partial F(x_0, y_0, z_0, \lambda_0)}{\partial y} = 8x_0 z_0 + 8 \lambda_0 y_0 = 0 \\
\dfrac{\partial F(x_0, y_0, z_0, \lambda_0)}{\partial z} = 8x_0 y_0 + 18 \lambda_0 z_0 = 0 \\
\dfrac{\partial F(x_0, y_0, z_0, \lambda_0)}{\partial \lambda} = 16x_0^2 + 4y_0^2 + 9z_0^2 - 144 = 0
\end{cases}
$$

- 解得唯一临界点为 $x_0 = \sqrt{3}, y_0 = 2 \sqrt{3}, z_0 = \dfrac{4}{\sqrt{3}}$。
- 设 $D = \{(x, y, z) \mid x, y, z \geq 0, 16x^2 + 4y^2 + 9z^2 = 144\}$，则 $D$ 为紧集，故 $V$ 在 $D$ 上取得最大值。
- 但 $V$ 在 $\partial D$ 上为 $0$、在 $\mathring{D}$ 上 $> 0$，故 $V$ 在 $\mathring{D}$ 上取得最大值，而 $\mathring{D}$ 中有唯一临界点，故最大值在该临界点上取得，为 $V_{\max} = 64 \sqrt{3}$。

[例 32] 用条件极值推导 $\mathbb{R}^n$ 中点到平面的距离公式。

解：

- 设平面为 $\mathbf{a} \cdot \mathbf{x} = b \ (\mathbf{a} \neq \mathbf{0})$，给定点 $\mathbf{x}_0 \in \mathbb{R}^n$，求 $\mathbf{x}_0$ 到平面的距离。
- 即求 $||\mathbf{x} - \mathbf{x}_0||^2$ 在条件 $\mathbf{a} \cdot \mathbf{x} = b$ 下的最小值。设出 Lagrange 函数：

$$
F(\mathbf{x}, \lambda) = ||\mathbf{x} - \mathbf{x}_0||^2 + \lambda (\mathbf{a} \cdot \mathbf{x} - b)
$$

- 令：

$$
\begin{cases}
F_{\mathbf{x}}' = 2(\mathbf{x} - \mathbf{x}_0) + \lambda \mathbf{a} = \mathbf{0} \\
F_{\lambda}' = \mathbf{a} \cdot \mathbf{x} - b = 0
\end{cases}
$$

- 解得 $\lambda = \dfrac{2(\mathbf{a} \cdot \mathbf{x}_0 - b)}{||\mathbf{a}||^2}$，故有：

$$
||\mathbf{x} - \mathbf{x}_0|| = \left| \frac{\lambda}{2} \right| \cdot ||\mathbf{a}|| = \dfrac{\mathbf{a} \cdot \mathbf{x}_0 - b}{||\mathbf{a}||}
$$

[例 33] 求 $f(x, y, z) = x^3 + y^3 + z^3 - 3xyz$ 在紧集 $D = \{(x, y, z) \in \mathbb{R}^3 \mid x^2 + y^2 + z^2 \leq 1\}$ 上的最值。

解：

- 由于 $D$ 为紧集，$f \in C(D)$，$f$ 在 $D$ 上取得最值。
- (1) 对于在 $\mathring{D}$ 中的情形，解得临界点 $(x, y, z)$ 皆形如 $x = y = z \in \left( 0, \dfrac{1}{\sqrt{3}} \right)$，此时 $f(x, y, z) = 0$。
- (2) 对于在 $\partial D$ 上即有条件 $x^2 + y^2 + z^2 = 1$ 的情形，设出 Lagrange 函数：

$$
F(x, y, z, \lambda) = (x^3 + y^3 + z^3 - 3xyz) - \lambda (x^2 + y^2 + z^2 - 1)
$$

- 欲解其临界点，得到方程组：

$$
\begin{cases}
3x^2 - 3yz - 2 \lambda x = 0 &\quad (1) \\
3y^2 - 3zx - 2 \lambda y = 0 &\quad (2) \\
3z^2 - 3xy - 2 \lambda z = 0 \\
x^2 + y^2 + z^2 - 1 = 0
\end{cases}
$$

- I. 若 $x, y, z$ 互不相同， 则：

$$
\begin{array}{l}
& (1) - (2) \\
\Leftrightarrow & 3(x - y)(x + y) + 3z(x - y) - 2 \lambda (x - y) = 0 \\
\Leftrightarrow & 3(x + y + z) = 2 \lambda
\end{array}
$$

- 代入 $(1)$ 得到 $xy + yz + zx = 0$，故有：

$$
(x + y + z)^2 = (x^2 + y^2 + z^2) + 2(xy + yz + zx) = 1 \Rightarrow x + y + z = \pm 1
$$

- 此时 $f(x, y, z) = (x + y + z)((x^2 + y^2 + z^2) - (xy + yz + zx)) = \pm 1$。
- II. 若 $x, y, z$ 中有两者相等，不妨令 $x = y$。
- 当 $x = y = 0$，有 $z = \pm 1$，此时 $f(x, y, z) = \pm 1$。
- 当 $x \neq 0$，将 $(1)$ 两边同时除以 $x$ 得到 $3(x - z) = 2 \lambda$，代入 $(2)$ 解得 $x = z$ 或 $x + 2z = 0$。
- 若 $x = z$，解得 $x = y = z = \pm \dfrac{1}{\sqrt{3}}$，此时 $f(x, y, z) = 0$。
- 若 $x + 2z = 0$，解得 $x = y = \pm \dfrac{2}{3}, z = \mp \dfrac{1}{2}$，此时 $f(x, y, z) = \pm 1$。
- 综上，$f$ 在 $D$ 上取得最大值为 $1$、最小值为 $-1$。

### 多元微分学在几何上的应用

#### 定义：$\mathbb{R}^n$ 中的曲线

$\mathbb{R}^n$ 中的一条曲线 $\Gamma$ 是指 $[\alpha, \beta] \to \mathbb{R}^n$ 的一个连续映射：

$$
\Gamma: \mathbf{h}(t) = (x_1(t), x_2(t), \cdots, x_n(t))
$$

若 $\forall t_1, t_2 \in [\alpha, \beta] \land t_1 \neq t_2, \mathbf{h}(t_1) \neq \mathbf{h}(t_2)$，则称 $\Gamma$ 为一条简单曲线。

 $\forall t_1, t_2 \in [\alpha, \beta) \land t_1 \neq t_2, \mathbf{h}(t_1) \neq \mathbf{h}(t_2)$ 但 $\mathbf{h}(\alpha) = \mathbf{h}(\beta)$，则称 $\Gamma$ 为一条简单闭曲线。特别地，$n = 2$ 时简单闭曲线也称 Jordan 曲线。

本小节主要讨论 $n = 3$ 的情形。

#### 参数式曲线的切线和法平面

设空间连续曲线 $\Gamma$ 由参数式方程 $\begin{cases} x = x(t) \\ y = y(t) \\ z = z(t) \end{cases}, \quad t \in (\alpha, \beta)$ 给出，其中 $x(t), y(t), z(t)$ 可导，且 $x'(t)^2 + y'(t)^2 + z'(t)^2 \neq 0$。

考虑曲线上一定点 $M_0(x(t_0), y(t_0), z(t_0))$ 和动点 $P(x(t), y(t), z(t))$，其中 $t_0, t \in (\alpha, \beta)$。此时 $M_0, P$ 两点所成的**割线**的一个方向向量为：

$$
(x(t) - x(t_0), y(t) - y(t_0), z(t) - z(t_0))
$$

当 $t \neq t_0$，此亦为其方向向量：

$$
\left( \frac{x(t) - x(t_0)}{t - t_0}, \frac{y(t) - y(t_0)}{t - t_0}, \frac{z(t) - z(t_0)}{t - t_0} \right)
$$

令 $t \to t_0$，由 $x(t), y(t), z(t)$ 可导可知：

$$
\lim_{t \to t_0} \left( \frac{x(t) - x(t_0)}{t - t_0}, \frac{y(t) - y(t_0)}{t - t_0}, \frac{z(t) - z(t_0)}{t - t_0} \right) = (x'(t_0), y'(t_0), z'(t_0)).
$$

因此，从几何上讲，**切线是割线的极限位置**，这引出了下面的定义。

##### 定义：切向量、切线与法平面

称 $\boldsymbol{\tau} = (x'(t_0), y'(t_0), z'(t_0))$ 为参数式曲线 $\Gamma$ 在 $M_0$ 点的**切线方向**，亦称**切向量**。

称过 $M_0$ 点以 $\boldsymbol{\tau}$ 为方向矢量的直线为曲线 $L$ 在 $M_0$ 的**切线**，称过 $M_0$ 点以 $\boldsymbol{\tau}$ 为法向量的平面为曲线 $L$ 在 $M_0$ 的**法平面**。

曲线在 $M_0$ 点的切线方程为：

$$
\frac{x - x_0}{x'(t_0)} = \frac{y - y_0}{y'(t_0)} = \frac{z - z_0}{z'(t_0)}
$$

- 当 $x'(t_0) = 0, y'(t_0), z'(t_0) \neq 0$，约定上式表示 $\begin{cases} x = x_0 \\ \dfrac{y - y_0}{y'(t_0)} = \dfrac{z - z_0}{z'(t_0)} \end{cases}$。
- 对于其他包含 $0$ 的情况有类似的约束，不再赘述。

曲线在 $M_0$ 点的法平面方程为：

$$
x'(t_0)(x - x_0) + y'(t_0)(y - y_0) + z'(t_0)(z - z_0) = 0
$$

#### 二面式曲线的切线和法平面

设曲线 $\Gamma$ 由方程组

$$
\begin{cases}
F(x, y, z) = 0 \\
G(x, y, z) = 0
\end{cases}
$$

隐式给出（即二面式曲线），其中 $F, G$ 都是 $C^1$ 函数，且 Jacobi 矩阵

$$
\frac{\partial(F, G)}{\partial(x, y, z)} = \begin{bmatrix}
F_x' & F_y' & F_z' \\
G_x' & G_y' & G_z'
\end{bmatrix}
$$

的三个二阶子式

$$
\begin{cases}
A = \left| \dfrac{\partial(F, G)}{\partial(y, z)} \right| \\
B = \left| \dfrac{\partial(F, G)}{\partial(z, x)} \right| \\
C = \left| \dfrac{\partial(F, G)}{\partial(x, y)} \right|
\end{cases}
$$

不同时为零。

设 $M_0(x_0, y_0, z_0)$ 为此曲线上一点，若 $A \neq 0$，则由隐函数存在定理可知：曲线在 $M_0(x_0, y_0, z_0)$ 附近可以表示为**参数式**，即在 $\delta > 0$ 给出的邻域中可表示为：

$$
\begin{cases}
y = y(x) \\
z = z(x)
\end{cases}, \quad x \in U(x_0, \delta)
$$

由定义及隐可知 $\Gamma$ 在 $M_0$ 点处的切向量为 $\left(1, \dfrac{\text{d} y(x_0)}{\text{d} x}, \dfrac{\text{d} z(x_0)}{\text{d} x} \right)$。而隐函数存在定理还指出：

$$
\begin{cases}
\dfrac{\text{d} y(x_0)}{\text{d} x} = \dfrac{B}{A} \\
\dfrac{\text{d} z(x_0)}{\text{d} x} = \dfrac{C}{A}
\end{cases}
$$

故在忽略向量长度、只考虑方向的意义下，$\Gamma$ 在 $M_0$ 点的切向量为即为：

$$
\boldsymbol{\tau} = (A, B, C)
$$

当 $B \neq 0$ 或 $C \neq 0$，同理可得同一结果。

故曲线 $L$ 于 $M_0$ 点的切线方程为：

$$
\frac{x - x_0}{A} = \frac{y - y_0}{B} = \frac{z - z_0}{C}
$$

曲线 $L$ 于 $M_0$ 点的法平面方程为：

$$
A(x - x_0) + B(y - y_0) + C(z - z_0) = 0
$$

[例 34] 求曲线 $\begin{cases} x^2 - y^2 + 2z^2 = 2 \\ x + y + z = 3 \end{cases}$ 在 $(1, 1, 1)$ 处的切线方程与法平面方程。

解：

- 令 $\begin{cases} F(x, y, z) = x^2 - y^2 + 2z^2 - 2 \\ G(x, y, z) = x + y + z - 3 \end{cases}$，则：

$$
\begin{cases}
A = \left| \dfrac{\partial(F, G)}{\partial(y, z)} \right| = -2y - 4z \\
B = \left| \dfrac{\partial(F, G)}{\partial(z, x)} \right| = 4z - 2x \\
C = \left| \dfrac{\partial(F, G)}{\partial(x, y)} \right| = 2x + 2y
\end{cases}
$$

- 在 $(1, 1, 1)$ 处有 $A = -6, B = 2, C = 4$，故切线方程为：

$$
\frac{x - 1}{-6} = \frac{y - 1}{2} = \frac{z - 1}{4} \Leftrightarrow \frac{x - 1}{3} = \frac{y - 1}{-1} = \frac{z - 1}{-2}
$$

- 法平面方程为：

$$
-6(x - 1) + 2(y - 1) + 4(z - 1) = 0 \Leftrightarrow 3x - y - 2z = 0
$$

[例 35] 证明曲面 $S: x^2 + y^2 + z^2 = 4$ 与曲面族 $S_t: x^2 + ty^2 - (1 + t)z^2 = 1 \ (t \in \mathbb{R})$ 在 $(1, 1, 1)$ 处正交（即它们的法向量相互垂直），并求出两个曲面的交线在 $(1, 1, 1)$ 处的切线方程。

解：

- 对于 $S$，$F(x, y, z) = x^2 + y^2 + z^2 - 4$，则 $F'(1, 1, 1) = (2, 2, 2)$。
- 对于 $S_t$，$G(x, y, z) = x^2 + ty^2 - (1 + t)z^2 - 1$，则 $G'(1, 1, 1) = (2, 2t, -2(1 + t))$。
- 则 $F'(1, 1, 1) \cdot G'(1, 1, 1) = 0$，故 $S, S_t$ 正交。
- 二者的交线（即二面式曲线）在 $(1, 1, 1)$ 处的切向量为：

$$
\boldsymbol{\tau} = F'(1, 1, 1) \times G'(1, 1, 1) = (-8t - 4, 4t + 8, 4t - 4)
$$

- 故切线方程为：

$$
\frac{x - 1}{-8t - 4} = \frac{y - 1}{4t + 8} = \frac{z - 1}{4t - 4} \Leftrightarrow \frac{x - 1}{2t + 1} = \frac{y - 1}{-t - 2} = \frac{z - 1}{-t + 1}
$$

#### 定义：曲面的切平面、法向量与法线

设 $S$ 是 $\mathbb{R}^3$ 中的一个曲面，$M_0(x_0, y_0, z_0) \in S$。

若 $S$ 上过点 $M_0$ 且有切线的所有曲线在 $M_0$ 点的切线都在一个平面 $\Sigma$ 内，则称 $\Sigma$ 为 $S$ 在 $M_0$ 处的**切平面**。

称 $\Sigma$ 的法向量为曲面 $S$ 在 $M_0$ 处的**法向量**，称过 $M_0$ 且垂直于 $\Sigma$ 的直线为 $S$ 在 $M_0$ 处的**法线**。

#### 一个方程所表示的曲面的切平面和法线

设曲面 $S$ 由方程 $F(x, y, z) = 0$ 给出，$M_0(x_0, y_0, z_0)$ 为曲面上一点，$F_x'(x_0, y_0, z_0), F_y'(x_0, y_0, z_0), F_z'(x_0, y_0, z_0)$ 不全为零。

$S$ 作为三元函数 $u = F(x, y, z)$ 的等值面，其在 $M_0$ 处的法线方向即为：

$$
F'(x_0, y_0, z_0) = (F_x'(x_0, y_0, z_0), F_y'(x_0, y_0, z_0), F_z'(x_0, y_0, z_0))
$$

故 $S$ 在 $M_0$ 处的切平面方程为：

$$
F_x(x_0, y_0, z_0)(x - x_0) + F_y(x_0, y_0, z_0)(y - y_0) + F_z(x_0, y_0, z_0)(z - z_0) = 0
$$

$S$ 在 $M_0$ 处的法线方程为

$$
\frac{x - x_0}{F_x(x_0, y_0, z_0)} = \frac{y - y_0}{F_y(x_0, y_0, z_0)} = \frac{z - z_0}{F_z(x_0, y_0, z_0)}
$$

而对显函数 $z = f(x, y)$ 而言，考虑 $F(x, y, z) = f(x, y) - z$ 的情形即可。

#### 参数式曲面的切平面和法线

设曲面 $S$ 由参数式方程 $\begin{cases} x = x(u, v) \\ y = y(u, v) \\ z = z(u, v) \end{cases}, \quad (u, v) \in D$ 给出，$M_0(x_0, y_0, z_0) = (x(u_0, v_0), y(u_0, v_0), z(u_0, v_0)) \ ((u_0, v_0) \in D)$。

设 $x(u, v), y(u, v), z(u, v) \in C^1(D)$，且 Jacobi 矩阵


$$
\frac{\partial(x, y, z)}{\partial(u, v)} =
\begin{bmatrix}
x_u & x_v \\
y_u & y_v \\
z_u & z_v
\end{bmatrix}
$$

的三个二阶子式

$$
\begin{cases}
A = \left| \dfrac{\partial(y, z)}{\partial(u, v)} \right| \\
B = \left| \dfrac{\partial(z, x)}{\partial(u, v)} \right| \\
C = \left| \dfrac{\partial(x, y)}{\partial(u, v)} \right|
\end{cases}
$$

不同时为零。

在 $S$ 上取两条曲线（“折痕”）：

$$
\begin{aligned}
\Gamma_1 : \begin{cases} x = x(u, v_0) \\ y = y(u, v_0) \\ z = z(u, v_0) \end{cases}, \quad (u, v_0) \in D \\
\Gamma_2 : \begin{cases} x = x(u_0, v) \\ y = y(u_0, v) \\ z = z(u_0, v) \end{cases}, \quad (u_0, v) \in D
\end{aligned}
$$

设 $\Gamma_1, \Gamma_2$ 在 $M_0$ 处的切向量分别为：

$$
\begin{cases}
\boldsymbol{\tau}_1 = (x_u'(u_0, v_0), y_u'(u_0, v_0), z_u'(u_0, v_0)) \\
\boldsymbol{\tau}_2 = (x_v'(u_0, v_0), y_v'(u_0, v_0), z_v'(u_0, v_0))
\end{cases}
$$

当二者不相平行，则可张成切平面，此时切平面的法线方向为：

$$
\mathbf{n} = \boldsymbol{\tau}_1 \times \boldsymbol{\tau}_2 = A \mathbf{i} + B \mathbf{j} + C \mathbf{k}
$$

故 $S$ 在 $M_0$ 处的切平面方程为：

$$
A(x - x_0) + B(y - y_0) + C(z - z_0) = 0
$$

$S$ 在 $M_0$ 处的法线方程为

$$
\frac{x - x_0}{A} = \frac{y - y_0}{B} = \frac{z - z_0}{C}
$$

其中 $A, B, C$ 中若有零者，按前述约定处理。

[例 36] 求曲面 $\begin{cases} x = u + v \\ y = u^2 + v^2 \\ z = u^3 + v^3 \end{cases}$ 在 $u_0 = 1, v_0 = 2$ 对应点 $M_0$ 处的切平面方程。

解：

- 计算切平面的法线方向：

$$
\mathbf{n} = (1, 2u, 3u^2)|_{u = 1} \times (1, 2v, 3v^2)|_{v = 2} = (1, 2, 3) \times (1, 4, 12) = (12, -9, 2)
$$

- $M_0$ 点坐标为 $(3, 5, 9)$，故切平面方程为：

$$
12(x - 3) - 9(y - 5) + 2(z - 9) = 0
$$

### 多元凸函数

#### 定义：多元凸函数

设 $D \subset \mathbb{R}^n$ 是凸域，$f : D \to \mathbb{R}$。若：

$$
\forall \mathbf{x}_1, \mathbf{x}_2 \in D, \forall t \in (0, 1), f(t\mathbf{x}_1 + (1 - t)\mathbf{x}_2) \leqslant t f(\mathbf{x}_1) + (1 - t) f(\mathbf{x}_2)
$$

则称 $f(\mathbf{x})$ 为 $D$ 上的凸函数。

进一步地，若当 $\mathbf{x}_1 \neq \mathbf{x}_2$，严格不等号总是成立，称 $f(\mathbf{x})$ 为 $D$ 上的严格凸函数。

与一元凸函数的情形一致，Jensen 不等式对多元凸函数也成立。

#### 定理：$C^2$ 函数凸性的等价条件

内容：设 $D \subset \mathbb{R}^n$ 是凸域，$f(\mathbf{x}) \in C^2(D)$，则下列命题等价：

- (1) $f(\mathbf{x})$ 是 $D$ 上的凸函数。
- (2) 切平面总在曲面的下方：$\forall \mathbf{x}_0, \mathbf{x} \in D, f(\mathbf{x}) \geqslant f(\mathbf{x}_0) + f'(\mathbf{x}_0)(\mathbf{x} - \mathbf{x}_0)$。
- (3) $\forall \mathbf{x}_0 \in D$，$H_f(\mathbf{x}_0)$ 半正定。

证明思路：

- $(1) \Rightarrow (2)$：$\forall ||\mathbf{e}|| = 1$，$\forall \mathbf{x}_0 \in D, d > 0$，有：

$$
\begin{aligned}
\forall \epsilon > 0, f(\mathbf{x}_0) &\leq \frac{\epsilon}{d + \epsilon} f(\mathbf{x}_0 + d \mathbf{e}) + \frac{d}{d + \epsilon} f(\mathbf{x}_0 - \epsilon \mathbf{e}) \\
&= \frac{\epsilon}{d + \epsilon} f(\mathbf{x}_0 + d \mathbf{e}) + \frac{d}{d + \epsilon} (f(\mathbf{x}_0) - \epsilon f'(\mathbf{x}_0) \mathbf{e} + o(\epsilon)) &\quad (\epsilon \to 0)
\end{aligned}
$$

- 令 $\epsilon \to 0$ 并整理即得 $f(\mathbf{x}_0 + de) \geq f(\mathbf{x}_0) + f'(\mathbf{x}_0)(d \mathbf{e})$。
- 由方向 $\mathbf{e}$ 的任意性即得 $\forall \mathbf{x}_0, \mathbf{x} \in D, f(\mathbf{x}) \geqslant f(\mathbf{x}_0) + f'(\mathbf{x}_0)(\mathbf{x} - \mathbf{x}_0)$。
- $(2) \Rightarrow (3)$：$\forall \mathbf{x}_0 \in D$，$\forall ||\mathbf{e}|| = 1, d > 0$，由带 Peano 余项的 Taylor 公式可知：

$$
f(\mathbf{x}_0) + d f'(\mathbf{x}_0) \mathbf{e} + \frac{1}{2} d^2 \mathbf{e}^T H_f(\mathbf{x}_0) \mathbf{e} + o(d^2) = f(\mathbf{x}_0 + d \mathbf{e}) \geq f(\mathbf{x}_0) + d f'(\mathbf{x}_0) \mathbf{e} \quad (d \to 0)
$$

- 整理得 $\mathbf{e}^T H_f(\mathbf{x}_0) \mathbf{e} + o(1) \geq 0 \ (d \to 0)$，故 $\mathbf{e}^T H_f(\mathbf{x}_0) \mathbf{e} \geq 0$，再由 $\mathbf{e}$ 的任意性即得 $H_f(\mathbf{x}_0)$ 半正定。
- $(3) \Rightarrow (1)$：$\forall \mathbf{x}_1, \mathbf{x}_2 \in D, t \in (0, 1)$，令 $\mathbf{x}_0 = t \mathbf{x}_1 + (1 - t) \mathbf{x}_2$，由带 Lagrange 余项的 Taylor 公式可知：

$$
\begin{cases}
f(\mathbf{x}_1) = f(\mathbf{x}_0) + f'(\mathbf{x}_0)(\mathbf{x}_1 - \mathbf{x}_0) + \dfrac{1}{2} (\mathbf{x}_1 - \mathbf{x}_0)^T H_f(\boldsymbol{\xi}_1) (\mathbf{x}_1 - \mathbf{x}_0), &\quad \boldsymbol{\xi}_1 \text{ 在 } \mathbf{x}_0, \mathbf{x}_1 \text{ 之间} &\quad (*_1) \\
f(\mathbf{x}_2) = f(\mathbf{x}_0) + f'(\mathbf{x}_0)(\mathbf{x}_2 - \mathbf{x}_0) + \dfrac{1}{2} (\mathbf{x}_2 - \mathbf{x}_0)^T H_f(\boldsymbol{\xi}_2) (\mathbf{x}_2 - \mathbf{x}_0), &\quad \boldsymbol{\xi}_2 \text{ 在 } \mathbf{x}_0, \mathbf{x}_2 \text{ 之间} &\quad (*_2)
\end{cases}
$$

- 写出 $t (*_1) + (1 - t) (*_2)$ 即得：

$$
\begin{array}{l}
& tf(\mathbf{x}_1) + (1 - t) f(\mathbf{x}_2) \\
= & f(\mathbf{x}_0) + \dfrac{t}{2} (\mathbf{x}_1 - \mathbf{x}_0)^T H_f(\boldsymbol{\xi}_1) (\mathbf{x}_1 - \mathbf{x}_0) + \dfrac{1 - t}{2} (\mathbf{x}_2 - \mathbf{x}_0)^T H_f(\boldsymbol{\xi}_2) (\mathbf{x}_2 - \mathbf{x}_0) \\
\geq & f(\mathbf{x}_0) &\quad (\text{半正定})
\end{array}
$$

[例 37] 设一元凸函数 $g \in C^2(a, b)$，求证：$f(\mathbf{x}) = \displaystyle\sum_{i = 1}^n g(x_i)$ 是凸域 $(a, b)^n$ 上的凸函数。

证明：

- 由 $g$ 为 $C^2$ 的凸函数可知 $\forall t \in (a, b), g''(t) \geq 0$。
- 写出 $f$ 的各个二阶偏导数：

$$
\frac{\partial^2 f}{\partial x_i \partial x_j} = \begin{cases}
g''(x_i), &\quad i = j \\
0, &\quad i \neq j
\end{cases}
$$

- 故 $H_f(\mathbf{x})$ 为对角阵 $\text{diag}(g''(x_1), \cdots, g''(x_n))$，其各对角元非负，则半正定，故 $f$ 为凸函数。
