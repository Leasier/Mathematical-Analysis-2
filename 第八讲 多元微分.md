### 偏导数与全微分

#### 定义：多元函数的偏导数和偏导函数

设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，若 $\displaystyle\lim_{\Delta x_i \to 0} \frac{f(\mathbf{x}_0 + \Delta x_i \mathbf{e}_i) - f(\mathbf{x}_0)}{\Delta x_i}$ 存在，则称此极限为 $f$ 在 $\mathbf{x}_0$ 处关于 $x_i$ 的偏导数，也称 $f$ 在 $\mathbf{x}_0$ 处对 $x_i$ 可导，记作 $\left. \dfrac{\partial f}{\partial x_i} \right|_{\mathbf{x}_0}, \dfrac{\partial f(\mathbf{x}_0)}{\partial x_i}, f_{x_i}'(\mathbf{x}_0)$。

若 $f: S \to \mathbb{R}$ 在区域 $D \subset S$ 上每一点都有 $\dfrac{\partial f}{\partial x_i}$，则称 $f_{x_i}' : D \to \mathbb{R}$ 为 $f$ 在 $D$ 上关于 $x_i$ 的偏导函数，简称 $f$ 关于 $x_i$ 的偏导数。

[例 1] 计算 $f(x, y) = \arctan \dfrac{x + y}{1 - xy}$ 在 $(0, 0)$ 处的两个偏导数。

解：

$$
\begin{cases}
f_x'(0, 0) = \displaystyle\lim_{x \to 0} \frac{\arctan x}{x} = 1 \\
f_y'(0, 0) = \displaystyle\lim_{y \to 0} \frac{\arctan y}{y} = 1 \\
\end{cases}
$$

[例 2] 计算 $f(x, y, z) = x^y \sin z \ (x > 0)$ 在定义域内的偏导函数。

解：

$$
\begin{cases}
\dfrac{\partial f}{\partial x} = y x^{y - 1} \sin z \\
\dfrac{\partial f}{\partial y} = x^y \ln x \sin z \\
\dfrac{\partial f}{\partial z} = x^y \cos z
\end{cases}
$$

------

事实上，偏导数存在是一个相当弱的条件，它甚至不蕴含连续性。下为一例。

[例 3] 设 $f(x, y) = \begin{cases} \dfrac{xy}{x^2 + y^2}, &\quad xy \neq 0 \\ 0, &\quad xy = 0 \end{cases}$，我们在第七讲例 4 中已经证明其在 $(0, 0)$ 处不连续，求证：$f$ 在 $(0, 0)$ 处对 $x, y$ 可导。

证明：

$$
\begin{cases}
f_x'(0, 0) = \displaystyle\lim_{x \to 0} 0 = 0 \\
f_y'(0, 0) = \displaystyle\lim_{y \to 0} 0 = 0 \\
\end{cases}
$$

#### 定义：多元函数的全微分 / 导数

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$\mathbf{x}_0 \in D$，若 $\exists \mathbf{A} \in \mathbb{R}^n, \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) - f(\mathbf{x}_0) = \langle \mathbf{A}, \Delta \mathbf{x} \rangle + o(||\Delta \mathbf{x}||) \ (||\Delta \mathbf{x}|| \to 0)$，则称 $f$ 在 $\mathbf{x}_0$ 处可微。

此时称增量的线性主部 $\langle \mathbf{A}, \Delta \mathbf{x} \rangle$ 为 $f$ 在 $\mathbf{x}_0$ 处的全微分，记作 $\text{d} f(\mathbf{x}_0)$；称 $\mathbf{A}$ 为 $f$ 在 $\mathbf{x}_0$ 处的导数，记作 $f'(\mathbf{x}_0)$。

#### 命题：可微蕴含连续

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，$f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $f$ 在 $\mathbf{x}_0$ 处连续。

#### 命题：可微蕴含可偏导

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，$\text{d} f(\mathbf{x}_0) = \langle \mathbf{A}, \Delta \mathbf{x} \rangle$，则 $\forall 1 \leq i \leq n$，$f$ 在 $\mathbf{x}_0$ 处对 $x_i$ 可导，且 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = \mathbf{A}_i$。

推论：若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $\text{d} f(\mathbf{x}_0) = \displaystyle\sum_{i = 1}^n \frac{\partial f(\mathbf{x}_0)}{\partial x_i} \text{d} x_i$。

------

下面尝试给可偏导加一些条件，使之蕴含可微。

#### 定义：$C^1$ 函数类

内容：设 $D \subset \mathbb{R}^n$ 为区域，称 $f : D \to \mathbb{R}$ 在 $D$ 上是 $C^1$ 的，若 $\forall \mathbf{x}_0 \in D, \forall 1 \leq i \leq n$，$f$ 在 $\mathbf{x}_0 \in D$ 处对 $x_i$ 可导，且 $\dfrac{\partial f}{\partial x_i}$ 在 $\mathbf{x}_0$ 附近存在、且在 $\mathbf{x}_0$ 处连续，记作 $f \in C^1(D)$。

#### 命题：偏导连续蕴含可微

内容：设 $D \subset \mathbb{R}^n$ 为区域，若 $f \in C^1(D)$，则 $f$ 在 $D$ 上可微。

证明：由连续性容易推知可控制余项 $\displaystyle\sum_{i = 1}^n \left( \frac{\partial f}{\partial x_i}(\mathbf{x}_0 + \Delta \mathbf{x}) - \frac{\partial f}{\partial x_i}(\mathbf{x}_0) \right) \Delta x_i$ 充分小。

[例 4] 求 $z = \arctan \dfrac{y}{x}$ 在 $x \neq 0$ 处的全微分。

解：由 $\dfrac{\partial z}{\partial x} = \dfrac{-y}{x^2 + y^2}, \dfrac{\partial z}{\partial y} = \dfrac{x}{x^2 + y^2}$ 可知 $\text{d} z = \dfrac{-y \text{d} x + x \text{d} y}{x^2 + y^2}$。

不过需要注意的是，存在偏导数不连续但可微的情况。下为一例。

[例 5] 设 $f(x, y) = \begin{cases} (x^2 + y^2) \sin \dfrac{1}{x^2 + y^2}, &\quad x^2 + y^2 \neq 0 \\ 0, &\quad x^2 + y^2 = 0 \end{cases}$，求证：$f$ 在 $(0, 0)$ 处可微，但 $f$ 在 $(0, 0)$ 处对 $x, y$ 的偏导数不连续。

证明：

- 首先易知：

$$
\begin{cases}
\text{d} f(0, 0) = (0, 0) \\
\dfrac{\partial f(0, 0)}{\partial x} = \displaystyle\lim_{x \to 0} x \sin \frac{1}{x^2} = 0 \\
\forall x \neq 0, \dfrac{\partial f(x, 0)}{\partial x} = 2x \sin \dfrac{1}{x^2} - \dfrac{2}{x} \cos \dfrac{1}{x^2}
\end{cases}
$$

- 令 $x_k = \dfrac{1}{\sqrt{k \pi}}$，则 $\dfrac{\partial f(x_k, 0)}{\partial x_k} = 2 (-1)^{k - 1} \sqrt{k \pi}$，故 $\displaystyle\lim_{k \to +\infty} x_k = 0$ 但 $\displaystyle\lim_{k \to +\infty} \frac{\partial f(x_k, 0)}{\partial x_k}$ 不存在。
- 因此 $f$ 在 $(0, 0)$ 处对 $x$ 的偏导数不连续，同理可知 $f$ 在 $(0, 0)$ 处对 $y$ 的偏导数不连续。

### 方向导数与梯度

#### 定义：方向导数

设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，$||\mathbf{u}|| = 1$，若 $\displaystyle\lim_{h \to 0^+} \frac{f(\mathbf{x}_0 + h \mathbf{u}) - f(\mathbf{x}_0)}{h}$ 存在，则称此极限为 $f$ 于 $\mathbf{x}$ 处在 $\mathbf{u}$ 方向上的方向导数，记作 $\left. \dfrac{\partial f}{\partial \mathbf{u}} \right|_{\mathbf{x}_0}, \dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{u}}$。

#### 命题：方向导数与偏导数的关系

内容：设 $S \subset \mathbb{R}^n$，$f : S \to \mathbb{R}$ 在 $B(\mathbf{x}_0)$ 内有定义，则偏导数 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i}$ 存在当且仅当方向导数 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i}, \dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$ 存在有限、且 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i} = -\dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$，此时有 $\dfrac{\partial f(\mathbf{x}_0)}{\partial x_i} = \dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{e}_i} = -\dfrac{\partial f(\mathbf{x}_0)}{\partial (-\mathbf{e}_i)}$。

------

事实上方向导数存在也不是一个很强的条件。下为两例。

[例 6] 设 $f(x, y) = \begin{cases} 1, &\quad y = x^2, x \neq 0 \\ 0, &\quad \text{otherwise} \end{cases}$，则对 xOy 平面中任意方向向量 $\mathbf{u}$，都有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{u}} = 0$，但 $f$ 在 $(0, 0)$ 处不连续。

[例 7] 构造函数 $f : \mathbb{R}^2 \rightarrow \mathbb{R}$，使得下列三个条件成立：

- (1) $f$ 在 $(0,0)$ 处的两个偏导数存在。
- (2) $f$ 在 $(0,0)$ 处各方向导数存在。
- (3) $f$ 在 $(0,0)$ 处不可微。

解：

- 构造：

$$
f(x, y) = \begin{cases}
\dfrac{x^2 y}{x^4 + y^2}, &\quad (x, y) \neq (0, 0) \\
0, &\quad (x, y) = (0, 0)
\end{cases}
$$

- (2) 设从 $x = r \cos \theta, y = r \sin \theta \ (r > 0)$ 趋近 $(0, 0)$，有：

$$
\lim_{r \to 0^+} \frac{f(r \cos \theta, r \sin \theta) - f(0, 0)}{r} = \lim_{r \to 0^+} \frac{r^2 \cos^2 \theta \sin \theta}{r^4 \cos^4 \theta + r^2 \sin^2 \theta} = \begin{cases}
\dfrac{\cos^2 \theta}{\sin \theta}, &\quad \sin \theta \neq 0 \\
0, &\quad \sin \theta = 0
\end{cases}
$$

- 故 $f$ 在 $(0, 0)$ 处的各方向导数存在。
- (1) 当 $\theta = 0, \pi$，方向导数均为 $0$，故 $f$ 在 $(0, 0)$ 处关于 $x$ 的偏导数存在且为 $0$。
- 当 $\theta = \dfrac{\pi}{2}, \dfrac{3 \pi}{2}$，方向导数均为 $0$，故 $f$ 在 $(0, 0)$ 处关于 $y$ 的偏导数存在且为 $0$。
- (3) 由 $\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ y = x^2}} \frac{x^2 y}{x^4 + y^2} = \dfrac{1}{2} \neq 0$ 可知 $f$ 在 $(0, 0)$ 处不连续，故 $f$ 在 $(0,0)$ 处不可微。

#### 定理：可微蕴含方向可导

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，则 $f$ 于 $\mathbf{x}_0$ 在方向 $\mathbf{u}$ 上的方向导数为 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \mathbf{u}} = \langle \text{d} f(\mathbf{x}_0), \mathbf{u} \rangle$。

- 也即：方向导数就是导数在方向向量上的投影。

#### 定义：梯度

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$，若 $f$ 在 $\mathbf{x}_0 \in D$ 处可微，记 $f'(\mathbf{x}_0)$ 为 $\text{grad } f(\mathbf{x}_0), \nabla f(\mathbf{x}_0)$，称为 $f$ 在 $\mathbf{x}_0$ 处的梯度。我们约定梯度用**行向量**表示。

容易算得 $f$ 在梯度 $\boldsymbol{\ell} = \nabla f(\mathbf{x}_0)$ 方向上的方向导数为 $\dfrac{\partial f(\mathbf{x}_0)}{\partial \boldsymbol{\ell}_0} = ||\boldsymbol{\ell}||$，其中 $\boldsymbol{\ell}_0 = \dfrac{\boldsymbol{\ell}}{||\boldsymbol{\ell}||}$。由 Cauchy 不等式可见这正是函数值增加最快的方向。

### 多元函数求导

[例 8] 设 $k \in \mathbb{N}_{\geq 2}$，$f(x, y)$ 在极坐标 $(r, \theta)$ 下有表达式 $f(x, y) = \begin{cases} r \sin k \theta, &\quad (x, y) \neq (0, 0) \\ 0, &\quad (x, y) = (0, 0) \end{cases}$。

- (1) 求 $f$ 在 $(0, 0)$ 处各方向上的方向导数。
- (2) $f$ 在 $(0, 0)$ 处是否连续？
- (3) $f$ 在 $(0, 0)$ 处是否可微？

解：

- (1) $\forall \theta \in [0, 2 \pi)$，令 $\mathbf{v}(\theta) = (\cos \theta, \sin \theta)$，则算得 $f$ 在方向 $\mathbf{v}(\theta)$ 上的方向导数为 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$。
- (2) $\forall (x, y) \neq (0, 0), 0 \leq |f(x, y)| = r |\sin k \theta| \leq r$，由夹挤原理即得 $\displaystyle\lim_{(x, y) \to (0, 0)} f(x, y) = 0 = f(0, 0)$，故 $f$ 在 $(0, 0)$ 处连续。
- (3) 首先有 $f_x'(0, 0) = 0, f_y'(0, 0) = \sin \dfrac{k \pi}{2}$。
- 假设 $f$ 在 $(0, 0)$ 处可微，则 $\forall \theta \in [0, 2 \pi), \dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = f_x'(0, 0) \cdot \cos \theta + f_y'(0, 0) \cdot \sin \theta = \sin \dfrac{k \pi}{2} \sin \theta$。
- (I) 当 $k$ 为偶数，应有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)}$ 恒为 $0$，与 (1) 中 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$ 不恒为 $0$ 矛盾。
- (II) 当 $k$ 为奇数，应有 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \pm \sin \theta$，取 $\theta \in \left( 0, \dfrac{\pi}{2k} \right)$ 可得 $|\pm \sin \theta| < \sin k \theta$，与 (1) 中 $\dfrac{\partial f(0, 0)}{\partial \mathbf{v}(\theta)} = \sin k \theta$ 矛盾。
- 综上，假设不成立，即 $f$ 在 $(0, 0)$ 处不可微。

#### 定理：多元函数导数的四则运算

内容：

- 设 $D \subset \mathbb{R}^n$ 为区域，$f, g : D \to \mathbb{R}$，若 $f, g$ 在 $\mathbf{x} \in D$ 处可微，则：
- (1) $\forall \alpha, \beta \in \mathbb{R}, (\alpha f(\mathbf{x}) + \beta g(\mathbf{x}))' = \alpha f'(\mathbf{x}) + \beta g'(\mathbf{x})$。
- (2) $(f(\mathbf{x}) g(\mathbf{x}))' = f(\mathbf{x}) g'(\mathbf{x}) + g(\mathbf{x}) f'(\mathbf{x})$。
- (3) 当 $g(\mathbf{x}) \neq 0$，$\left( \dfrac{f(\mathbf{x})}{g(\mathbf{x})} \right)' = \dfrac{g(\mathbf{x}) f'(\mathbf{x}) - f(\mathbf{x}) g'(\mathbf{x})}{g^2(\mathbf{x})}$。

#### 定理：复合多元函数偏导数

- 这里给出二元函数的情形。

内容：设 $u = f(x, y), x = g(s, t), y = h(s, t)$ 构成复合函数，且 $\dfrac{\partial x}{\partial s}, \dfrac{\partial x}{\partial t}, \dfrac{\partial y}{\partial s}, \dfrac{\partial y}{\partial t}$ 在 $(s, t)$ 处存在、$f$ 在相应位置可微，则复合函数 $u = f(g(s, t), h(s, t))$ 在 $(s, t)$ 处的两个偏导数存在，且有 $\dfrac{\partial u}{\partial s} = \dfrac{\partial u}{\partial x} \cdot \dfrac{\partial x}{\partial s} + \dfrac{\partial u}{\partial y} \cdot \dfrac{\partial y}{\partial s}, \dfrac{\partial u}{\partial t} = \dfrac{\partial u}{\partial x} \cdot \dfrac{\partial x}{\partial t} + \dfrac{\partial u}{\partial y} \cdot \dfrac{\partial y}{\partial t}$。

下面的题目中假定所涉及的函数都有相应的可微性或偏导数。

[例 9] 给定关于 $x, y$ 的函数 $u = f \left( xy, \dfrac{y}{x} \right)$，求 $\dfrac{\partial u}{\partial x}, \dfrac{\partial u}{\partial y}$。

解：

- 令 $s = xy, t = \dfrac{y}{x}$，则 $u = f(s, t)$，故：

$$
\begin{cases}
\dfrac{\partial u}{\partial x} = \dfrac{\partial f}{\partial s} \cdot y - \dfrac{\partial f}{\partial t} \cdot \dfrac{y}{x^2} \\
\dfrac{\partial u}{\partial y} = \dfrac{\partial f}{\partial s} \cdot x + \dfrac{\partial f}{\partial t} \cdot \dfrac{1}{x}
\end{cases}
$$

[例 10] 设 $u = f(x, y, z), z = \varphi(x, y)$，求 $\dfrac{\partial u}{\partial x}, \dfrac{\partial u}{\partial y}$。

解：

$$
\begin{cases}
\dfrac{\partial u}{\partial x} = \dfrac{\partial f}{\partial x} + \dfrac{\partial f}{\partial z} \cdot \dfrac{\partial z}{\partial x} \\
\dfrac{\partial u}{\partial y} = \dfrac{\partial f}{\partial y} + \dfrac{\partial f}{\partial z} \cdot \dfrac{\partial z}{\partial y}
\end{cases}
$$

[例 11] 求 $H(t) = |a_{i, j}(t)|_{1 \leq i, j \leq n}$ 的导数。

解：

- 视 $\det(a_{i, j})$ 为 $n^2$ 元函数，按行展开可知：

$$
\frac{\partial \det(a_{i, j})}{\partial a_{i, j}} = \sum_{k = 1}^n \frac{\partial (a_{i, k} A_{i, k})}{\partial a_{i, j}} = A_{i, j}
$$

- 其中 $A_{i, j}$ 为矩阵 $(a_{i, j})_{1 \leq i, j \leq n}$ 的代数余子式。再由链式法则即得：

$$
H'(t) = \sum_{i = 1}^n \sum_{j = 1}^n A_{i, j} a'_{i, j}(t)
$$

- 事实上，注意到 $\displaystyle\sum_{j = 1}^n A_{i, j} a'_{i, j}(t)$ 正是将原矩阵第 $i$ 行替换为其对 $t$ 的导数所得的行列式，因此还能给出一个更便于计算的形式。

[例 12] 设 $F \in C^1(\mathbb{R}^2)$ 在直角坐标系下可写作 $F(x, y) = f(x) g(y)$，在极坐标系下可写作 $F(r \cos \theta, r \sin \theta) = h(r)$，若 $F$ 无零点，求 $F(x,y)$。

解：

- 由于 $F$ **与极角无关**，故有 $\dfrac{\partial F}{\partial \theta} = 0$。再由链式法则即得：

$$
\begin{aligned}
\frac{\partial F}{\partial \theta} &= \frac{\partial F}{\partial x} \cdot \frac{\partial x}{\partial \theta} + \frac{\partial F}{\partial y} \cdot \frac{\partial y}{\partial \theta} \\
&= f'(x) g(y) \cdot (-r \sin \theta) + f(x) g'(y) \cdot r \cos \theta \\
&= -y f'(x) g(y) + xf(x) g'(y)
\end{aligned}
$$

- 由 $F$ 无零点可知 $f(x), g(y) \neq 0$，分离变量可得：

$$
\frac{f'(x)}{xf(x)} = \frac{g'(y)}{yg(y)} \equiv C \neq 0
$$

- 也即 $(\ln |f(x)|)' = Cx$，积分即得 $\ln |f(x)| = \dfrac{Cx^2}{2} + C'$。
- 由 $f$ 连续且无零点可知其定号，则 $f(x) = C'' e^{\frac{Cx^2}{2}}$。
- 同理可知 $g(y) = C''' e^{\frac{Cy^2}{2}}$，故有：

$$
F(x, y) = \lambda e^{\frac{C(x^2 + y^2)}{2}} \ (\lambda \neq 0)
$$

[例 13] 设 $\mathbf{A} = (a_{i, j})_{1 \leq i, j \leq n} \in M_n(\mathbb{R})$ 为正交矩阵，$f \in C^2(\mathbb{R}^n)$，令 $g(\mathbf{x}) = f(\mathbf{y})$，当 $\mathbf{y} = A \mathbf{x}$，求证：

- (1) $\displaystyle\sum_{i = 1}^n \left( \frac{\partial f}{\partial y_i} \right)^2 = \sum_{i = 1}^n \left( \frac{\partial g}{\partial x_i} \right)^2$。
- (2) $\displaystyle\sum_{i = 1}^n \frac{\partial^2 f}{\partial y_i^2} = \sum_{i = 1}^n \frac{\partial^2 g}{\partial x_i^2}$。

证明：

- (1) 由链式法则可知 $\nabla_x g = (\nabla_y f) \mathbf{A}$，故有：

$$
\text{RHS} = (\nabla_x g) (\nabla_x g)^T = (\nabla_y f) \mathbf{A} \mathbf{A}^T (\nabla_y f)^T = (\nabla_y f) (\nabla_y f)^T = \text{LHS}
$$

- (2) 将 $g$ 对 $x_i$ 求二阶导可知：

$$
\begin{aligned}
\frac{\partial^2 g}{\partial x_i^2} &= \frac{\partial}{\partial x_i} \left( \sum_{j = 1}^n \frac{\partial f}{\partial y_j} a_{j, i} \right) \\
&= \sum_{j = 1}^n \left( \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \cdot \frac{\partial y_k}{\partial x_i} \right) a_{j, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} a_{j, i} a_{k, i}
\end{aligned}
$$

- 对 $i$ 求和可知：

$$
\begin{aligned}
\text{RHS} &= \sum_{i = 1}^n \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} a_{j, i} a_{k, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \sum_{i = 1}^n a_{j, i} a_{k, i} \\
&= \sum_{j = 1}^n \sum_{k = 1}^n \frac{\partial^2 f}{\partial y_j \partial y_k} \cdot \delta_{j, k} \\
&= \sum_{j = 1}^n \frac{\partial^2 f}{\partial y_j^2} \\
&= \text{LHS}
\end{aligned}
$$

#### 齐次多元函数的欧拉定理

内容：设 $f : \mathbb{R}^n \to \mathbb{R}$ 可微，且 $f$ 为 $k$ 次齐次函数，则 $\mathbf{x} \cdot f'(\mathbf{x}) = kf(\mathbf{x})$。

证明：

- 由齐次性可见 $f(t \mathbf{x}) = t^k f(\mathbf{x})$，等式两边同时对 $t$ 求导可知：

$$
\mathbf{x} \cdot f'(t \mathbf{x}) = kt^{k - 1} f(\mathbf{x})
$$

- 令 $t = 1$ 即可得证。

[例 14] 设 $u(x_1, \cdots, x_n) = \begin{vmatrix} 1 & 1 & \cdots & 1 \\ x_1 & x_2 & \cdots & x_n \\ \vdots & \vdots & & \vdots \\ x_1^{n - 1} & x_2^{n - 1} & \cdots & x_n^{n - 1} \end{vmatrix}$，求证：

- (1) $\displaystyle\sum_{i = 1}^n \frac{\partial u}{\partial x_i} = 0$。
- (2) $\displaystyle\sum_{i = 1}^n x_i \frac{\partial u}{\partial x_i} = \frac{n(n - 1)}{2} u$。

证明：

- (1) 由 van der Monde 行列式的结论可知：

$$
u(x_1, \cdots, x_n) = \prod_{1 \leq i < j \leq n} (x_j - x_i)
$$

- 令 $\tilde{x}_i(t) = x_i + t$，则 $u(\tilde{x}_1(t), \cdots, \tilde{x}_n(t))$ 为常数，据链式法则即得：

$$
0 = \frac{\partial u}{\partial t} = \sum_{i = 1}^n \frac{\partial u}{\partial \tilde{x}_i} \cdot \frac{\partial \tilde{x}_i}{\partial x_i} = \sum_{i = 1}^n \frac{\partial u}{\partial \tilde{x}_i}
$$

- 令 $t = 0$ 即得：

$$
\sum_{i = 1}^n \frac{\partial u}{\partial x_i} = 0
$$

- (2) $u$ 关于 $x_1, \cdots, x_n$ 为 $\dfrac{n(n - 1)}{2}$ 次齐次函数，故由欧拉公式即得。

#### 定义：等值面、切平面

设 $D \subset \mathbb{R}^n$ 为区域，$f : D \to \mathbb{R}$ 在 $D$ 上可微，定义其等值面：

$$
S = \{x \in D \mid f(\mathbf{x}) = c\}
$$

取 $\mathbf{x}_0 \in S$，令 $\mathbf{v}$ 为 $S$ 在 $\mathbf{x}_0$ 处的一个**切向量**，其由 $S$ 上的一条 $C^1$ 曲线 $\boldsymbol{\gamma} : (-\epsilon, \epsilon) \to S$ 给出，满足 $\boldsymbol{\gamma}(0) = \mathbf{x}_0, \boldsymbol{\gamma}'(0) = \mathbf{v}$。

将 $f(\boldsymbol{\gamma}(t)) = c$ 两边同时对 $t$ 求导，由多元函数偏导数的运算法则可算得 $\langle \nabla f(\mathbf{x}_0), \mathbf{v} \rangle = 0$，即梯度向量与切向量总是正交。故当梯度非零，其为等值面在 $\mathbf{x}_0$ 处的法向量。

特别地，对于三维空间中的曲面 $z = f(x, y)$，将其改写为三元函数等值面的形式 $F(x, y, z) \triangleq f(x, y) - z = 0$，则曲面在 $(x_0, y_0, z_0)$ 处的法向量可以取为：

$$
\nabla F(x_0, y_0, z_0) = (f_x'(x_0, y_0), f_y'(x_0, y_0), -1)
$$

因此切平面方程为：

$$
z - z_0 = f_x'(x_0, y_0) (x - x_0) + f_y'(x_0, y_0) (y - y_0)
$$

### 向量值函数的可微性

#### 定义：向量值函数的微分

设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x}_0 \in D$，若 $\exists \mathbf{A} \in M_{m \times n}(\mathbb{R}), \text{s.t. } \mathbf{f}(\mathbf{x}_0 + \Delta \mathbf{x}) - \mathbf{f}(\mathbf{x}_0) = \mathbf{A} \Delta \mathbf{x} + \mathbf{r}(\Delta \mathbf{x})$，其中 $\displaystyle\lim_{\Delta \mathbf{x} \to \mathbf{0}} \frac{||\mathbf{r}(\Delta \mathbf{x})||}{||\Delta \mathbf{x}||} = 0$，则称 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，又称可导。

此时称 $\mathbf{A}$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的（Fréchet）导数，记作 $\mathbf{f}'(\mathbf{x}_0)$ 或 $D \mathbf{f}(\mathbf{x}_0)$；称 $\mathbf{A} \Delta \mathbf{x}$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的全微分，记作 $\text{d} \mathbf{f}(\mathbf{x}_0)$。

#### 定理 & 定义：向量值函数可微的充要条件、Jacobi 矩阵

内容：设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x}_0 \in D$，设 $\mathbf{f}$ 的分量形式为 $\mathbf{f}(\mathbf{x}) = (f_1(\mathbf{x}), \cdots, f_m(\mathbf{x}))^T$，则 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微的充要条件为 $\forall 1 \leq i \leq m$，$f_i$ 在 $\mathbf{x}_0$ 处均可微，此时 $\mathbf{f}'(\mathbf{x}_0) = J \mathbf{f}(\mathbf{x}_0)$，其中：

$$
J \mathbf{f}(\mathbf{x}_0) \triangleq \left( \frac{\partial f_i(\mathbf{x}_0)}{\partial x_j} \right)_{1 \leq i \leq m, 1 \leq j \leq n} = (\nabla f_1(\mathbf{x}_0), \cdots, \nabla f_m(\mathbf{x}_0))^T
$$

此时称 $J \mathbf{f}(\mathbf{x}_0)$ 为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的 Jacobi 矩阵。

推论：

- (1) 若 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，则 $\forall 1 \leq i \leq m, 1 \leq j \leq n$，$\dfrac{\partial f_i(\mathbf{x}_0)}{\partial x_j}$ 存在。
- (2) 若 $\forall 1 \leq i \leq m, 1 \leq j \leq n$，$\dfrac{\partial f_i(\mathbf{x}_0)}{\partial x_j}$ 存在且连续，则 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处可微。

#### 定义：Jacobi 行列式

特别地，当 $m = n$，Jacobi 矩阵为 $n$ 阶方阵，称此时 $J \mathbf{f}(\mathbf{x}_0)$ 的行列式为 $\mathbf{f}$ 在 $\mathbf{x}_0$ 处的 Jacobi 行列式，可以记作：

$$
|J \mathbf{f}(\mathbf{x}_0)|, \quad |\mathbf{f}'(\mathbf{x}_0)|, \quad |D \mathbf{f}(\mathbf{x}_0)|, \quad \det(\mathbf{f}'(\mathbf{x}_0)), \quad \left| \frac{\partial(f_1, f_2, \cdots, f_n)}{\partial(x_1, x_2, \cdots, x_n)} \right|_{\mathbf{x}_0}
$$

#### 定理：向量值函数的数乘、加法、点乘的导数

内容：

- 设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f}, \mathbf{g} : D \to \mathbb{R}^m$，若 $\mathbf{f}, \mathbf{g}$ 在 $\mathbf{x} \in D$ 处可微，则：
- (1) 数乘与加法：$\forall \alpha, \beta \in \mathbb{R}, (\alpha \mathbf{f}(\mathbf{x}) + \beta \mathbf{g}(\mathbf{x}))' = \alpha \mathbf{f}'(\mathbf{x}) + \beta \mathbf{g}'(\mathbf{x})$。
- (2) 点乘：$(\mathbf{f}(\mathbf{x}) \cdot \mathbf{g}(\mathbf{x}))' = \mathbf{f}(\mathbf{x})^T \mathbf{g}'(\mathbf{x}) + \mathbf{g}(\mathbf{x})^T \mathbf{f}'(\mathbf{x})$。

证明：展开为多元函数的情形即可。

#### 定理：复合向量值函数的导数

内容：设 $D \subset \mathbb{R}^n, \Omega \subset \mathbb{R}^m$ 为区域，$\mathbf{f} : D \to \Omega, \mathbf{g} : \Omega \to \mathbb{R}^p$，若 $\mathbf{f}$ 在 $\mathbf{x}_0 \in D$ 处可微，$\mathbf{g}$ 在 $\mathbf{y}_0 = \mathbf{f}(\mathbf{x}_0)$ 处可微，则复合映射 $\mathbf{h} = \mathbf{g} \circ \mathbf{f}$ 在 $\mathbf{x}_0$ 处可微，且 $\mathbf{h}'(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0)$。

证明：

$$
\begin{aligned}
\mathbf{h}(\mathbf{x}) - \mathbf{h}(\mathbf{x}_0) &= \mathbf{g}(\mathbf{f}(\mathbf{x})) - \mathbf{g}(\mathbf{f}(\mathbf{x}_0)) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0))(\mathbf{f}(\mathbf{x}) - \mathbf{f}(\mathbf{x}_0)) + o(||\mathbf{f}(\mathbf{x}_0) - \mathbf{f}(\mathbf{x}_0)||) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0))(\mathbf{f}'(\mathbf{x}_0) (\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)) + o(||\mathbf{f}'(\mathbf{x}_0)(\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)||) \\
&= \mathbf{g}'(\mathbf{f}(\mathbf{x}_0)) \mathbf{f}'(\mathbf{x}_0) (\mathbf{x} - \mathbf{x}_0) + o(||\mathbf{x} - \mathbf{x}_0||)
\end{aligned}
$$

- 故 $\mathbf{h}$ 在 $\mathbf{x}_0$ 处可微，且 $\mathbf{h}'(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0)$。

注：

- (1) 写成微分形式则可见**一阶微分形式不变性**：

$$
\text{d} \mathbf{h}(\mathbf{x}_0) = \mathbf{g}'(\mathbf{y}_0) \mathbf{f}'(\mathbf{x}_0) \text{d} \mathbf{x}_0 = \mathbf{g}'(\mathbf{y}_0) \text{d} \mathbf{y}_0
$$

- (2) 写成分量形式则可见**链式法则**：

$$
\frac{\partial h_i(\mathbf{x}_0)}{\partial x_j} = \sum_{k = 1}^m \frac{\partial g_i(\mathbf{y}_0)}{\partial y_k} \cdot \frac{\partial f_k(\mathbf{x}_0)}{\partial x_j}
$$

#### 定理：反函数求导

内容：设 $D, \Omega \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \Omega$ 为 $C^1$ 的同胚映射（即 $\mathbf{f}, \mathbf{f}^{-1}$ 均连续可微），则两者的 Jacobi 矩阵互为逆矩阵，即 $\mathbf{f}'(\mathbf{x}) (\mathbf{f}^{-1})'(\mathbf{y}) = (\mathbf{f}^{-1})'(\mathbf{y}) \mathbf{f}'(\mathbf{x}) = I$；反映在 Jacobi 行列式上则有：

$$
\left| \frac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right| \left| \frac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right| = \left| \frac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right| \left| \frac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right| = 1
$$

证明：由 $\mathbf{f} \circ \mathbf{f}^{-1} = \mathbf{f}^{-1} \circ \mathbf{f} = \text{id}$，据复合向量值函数的求导法则即得。

推论：Jacobi 行列式 $\left| \dfrac{\partial(y_1, \cdots, y_n)}{\partial(x_1, \cdots, x_n)} \right|, \left| \dfrac{\partial(x_1, \cdots, x_n)}{\partial(y_1, \cdots, y_n)} \right|$ 分别在 $\Omega, D$ 上非零。

[例 15] 求 $f(\mathbf{x}) = ||\mathbf{x}||$ 在 $\mathbf{x} \in \mathbb{R}^n \backslash \{\mathbf{0}\}$ 处的导数。

解：

- 由复合函数求导法则可知：

$$
\frac{\partial f}{\partial x_i} = \frac{1}{2 ||\mathbf{x}||} \cdot 2x_i = \frac{x_i}{||\mathbf{x}||}
$$

- 拼起来即得 $f'(\mathbf{x}) = \dfrac{\mathbf{x}^T}{||\mathbf{x}||}$。
- **注意：梯度为行向量！**

### 向量值函数的微分中值定理

- _Motivation：将多元转化为一元的情形 $\Rightarrow$ 投影！_

#### 向量值函数的微分中值定理（投影 ver.）

内容：设 $D \subset \mathbb{R}^n$ 为凸区域，$\mathbf{f} : D \to \mathbb{R}^m$ 可微，则：

$$
\forall \mathbf{a}, \mathbf{b} \in D, \forall \boldsymbol{\ell} \in \mathbb{R}^m \backslash \{\mathbf{0}\}, \exists \theta \in (0, 1), \text{s.t. } (\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a}) - (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a}))) \cdot \boldsymbol{\ell} = 0
$$

证明：

- 令 $\varphi(t) = \mathbf{f}(\mathbf{a} + t(\mathbf{b} - \mathbf{a})) \cdot \boldsymbol{\ell}$，则 $\varphi(0) = \mathbf{f}(\mathbf{a}) \cdot \boldsymbol{\ell}, \varphi(1) = \mathbf{f}(\mathbf{b}) \cdot \boldsymbol{\ell}$。
- 同时，据复合函数求导法则可知 $\varphi$ 在 $[0, 1]$ 上可导，且有：

$$
\begin{aligned}
\varphi'(t) &= (\mathbf{f}(\mathbf{a} + t(\mathbf{b} - \mathbf{a})))'_t \cdot \boldsymbol{\ell} \\
&= (\mathbf{f}'(\mathbf{a} + t(\mathbf{b} - \mathbf{a})) (\mathbf{b} - \mathbf{a})) \cdot \boldsymbol{\ell}
\end{aligned}
$$

- 对 $\varphi$ 施以 Lagrange 中值定理可知：

$$
\exists \theta \in (0, 1), \text{s.t. } \varphi'(\theta) = \varphi(1) - \varphi(0)
$$

- 移项整理即得：

$$
(\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a}) - (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a}))) \cdot \boldsymbol{\ell} = 0
$$

#### 拟微分中值定理

内容：设 $D \subset \mathbb{R}^n$ 为凸区域，$\mathbf{f} : D \to \mathbb{R}^m$ 可微，则：

$$
\forall \mathbf{a}, \mathbf{b} \in D, \exists \theta \in (0, 1), \text{s.t. } ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})|| \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))||
$$

证明：

- (1) 当 $\mathbf{f}(\mathbf{a}) = \mathbf{f}(\mathbf{b})$，定理平凡地成立。
- (2) 当 $\mathbf{f}(\mathbf{a}) \neq \mathbf{f}(\mathbf{b})$，在上一定理中取 $\boldsymbol{\ell} = \mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})$，代入并移项整理可得：

$$
\exists \theta \in (0, 1), \text{s.t. } (\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))(\mathbf{b} - \mathbf{a})) \cdot (\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})) = ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||^2
$$

- 由 Cauchy 不等式可知：

$$
||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||^2 \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))|| \cdot ||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||
$$

- 两边同除 $||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})||$ 即得：

$$
||\mathbf{f}(\mathbf{b}) - \mathbf{f}(\mathbf{a})|| \leq ||\mathbf{f}'(\mathbf{a} + \theta(\mathbf{b} - \mathbf{a}))||
$$

### 高阶偏导数与高阶全微分

#### 定义：高阶偏导数

对于二元函数 $f(x, y)$，当其一阶偏导数对某个变量可求偏导，可定义其二阶偏导数：

$$
\begin{cases}
f_{xx}'', \dfrac{\partial^2 f}{\partial x^2} \triangleq \dfrac{\partial}{\partial x} \left( \dfrac{\partial f}{\partial x} \right) \\
f_{xy}'', \dfrac{\partial^2 f}{\partial x \partial y} \triangleq \dfrac{\partial}{\partial x} \left( \dfrac{\partial f}{\partial y} \right) \\
f_{yx}'', \dfrac{\partial^2 f}{\partial y \partial x} \triangleq \dfrac{\partial}{\partial y} \left( \dfrac{\partial f}{\partial x} \right) \\
f_{yy}'', \dfrac{\partial^2 f}{\partial y^2} \triangleq \dfrac{\partial}{\partial y} \left( \dfrac{\partial f}{\partial y} \right)
\end{cases}
$$

特别地，称其中 $f_{xy}'', f_{yx}''$ 为 $f$ 的二阶混合偏导数。

多元函数的更多元偏导数可以类似定义。

记 $C^k(D)$ 为区域 $D \subset \mathbb{R}^n$ 上具有 $1 \sim k$ 阶连续偏导数的函数的构成集合，并可类似定义 $C^k(\bar{D})$。

[例 16] 设 $f(x, y) = \begin{cases} xy \cdot \dfrac{x^2 - y^2}{x^2 + y^2}, &\quad (x, y) \neq (0, 0) \\ 0, &\quad (x, y) = (0, 0) \end{cases}$，求其二阶偏导数。

解：

- (1) 当 $(x, y) \neq (0, 0)$，有：

$$
\begin{cases}
\dfrac{\partial f}{\partial x} = \dfrac{y(x^2 - y^2)}{x^2 + y^2} + \dfrac{4x^2 y^3}{(x^2 + y^2)^2} \\
\dfrac{\partial f}{\partial y} = \dfrac{x(x^2 - y^2)}{x^2 + y^2} - \dfrac{4x^3 y^2}{(x^2 + y^2)^2}
\end{cases}
$$

- 且由定义可以算得 $\dfrac{\partial f}{\partial x}(0, 0) = \dfrac{\partial f}{\partial y}(0, 0) = 0$。
- (2) 当 $(x, y) \neq (0, 0)$，有：

$$
\begin{cases}
\dfrac{\partial^2 f}{\partial x^2} = \dfrac{4xy^3}{(x^2 + y^2)^2} - \dfrac{8xy^3(x^2 - y^2)}{(x^2 + y^2)^3} \\
\dfrac{\partial^2 f}{\partial x \partial y} = \dfrac{\partial^2 f}{\partial y \partial x} = \dfrac{x^4 - 4x^2 y^2 - y^4}{(x^2 + y^2)^2} + \dfrac{12x^4 y^2 - 4x^2 y^4}{(x^2 + y^2)^3} \\
\dfrac{\partial^2 f}{\partial y^2} = -\dfrac{4x^3 y}{(x^2 + y^2)^2} + \dfrac{8x^3 y(x^2 - y^2)}{(x^2 + y^2)^3}
\end{cases}
$$

- 且由定义可以算得 $\dfrac{\partial^2 f}{\partial x^2}(0, 0) = 0, \dfrac{\partial^2 f}{\partial x \partial y}(0, 0) = 1, \dfrac{\partial^2 f}{\partial y \partial x}(0, 0) = -1, \dfrac{\partial^2 f}{\partial y^2}(0, 0) = 0$。
- _可以发现在原点之外的地方，$f$ 的两个二阶混合偏导数一致；但在原点处则不然。_

[例 17] 设 $f(x, y) = \begin{cases} (x^2 + y^2)^2 \sin \dfrac{1}{\sqrt{x^2 + y^2}}, &\quad x^2 + y^2 \neq 0 \\ 0, &\quad x^2 + y^2 = 0 \end{cases}$，求其二阶偏导数。

解：

- 令 $r = \sqrt{x^2 + y^2}$。
- (1) 当 $r > 0$，有 $f(x, y) = r^4 \sin \dfrac{1}{r}$，由 $r'_x = \dfrac{x}{r}, r'_y = \dfrac{y}{r}$ 可知：

$$
\begin{cases}
f_x' = f'_r r'_x = x \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right) \\
f_y' = f'_r r'_y = y \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right)
\end{cases}
$$

- 且由定义可以算得 $f_x'(0) = f_y'(0) = 0$。
- (2) 当 $r > 0$，有：

$$
\begin{cases}
f_{xx}'' = \left( 4r^2 \sin \dfrac{1}{r} - r \cos \dfrac{1}{r} \right) + \dfrac{x^2}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right) \\
f_{xy}'' = f_{yx}'' = \dfrac{xy}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right) \\
f_{yy}'' = \dfrac{y^2}{r} \left( 8r \sin \dfrac{1}{r} - 5 \cos \dfrac{1}{r} - \dfrac{1}{r} \sin \dfrac{1}{r} \right)
\end{cases}
$$

- 且由定义可以算得 $f_{xx}''(0, 0) = f_{xy}''(0, 0) = f_{yx}''(0, 0) = f_{yy}''(0, 0) = 0$。
- _可以发现 $f$ 的两个二阶混合偏导数总是一致。_

[例 18] 设 $z = f(x, y)$，$f \in C^{\infty}(\mathbb{R}^2)$，当 $x, y$ 用参数 $t$ 表出为 $\begin{cases} x = x_0 + t \cos \alpha \\ y = y_0 + t \sin \alpha \end{cases}$，求 $\dfrac{\text{d}^n z}{\text{d} t^n}$。

解：

$$
\dfrac{\text{d} z}{\text{d} t} = \frac{\partial z}{\partial x} \cdot \frac{\text{d} x}{\text{d} t} + \frac{\partial z}{\partial y} \cdot \frac{\text{d} y}{\text{d} t} = \cos \alpha \frac{\partial z}{\partial x} + \sin \alpha \frac{\partial z}{\partial y}
$$

- 使用**算子记法**可以写作：

$$
\dfrac{\text{d} z}{\text{d} t} = \left( \cos \alpha \frac{\partial}{\partial x} + \sin \alpha \frac{\partial}{\partial y} \right) z
$$

- 约定算子的乘法为**映射复合**，则归纳可知：

$$
\dfrac{\text{d}^n z}{\text{d} t^n} = \left( \cos \alpha \frac{\partial}{\partial x} + \sin \alpha \frac{\partial}{\partial y} \right)^n z
$$

------

在例 16、例 17 中，我们观察到函数在某些位置的两个二阶混合偏导数相等，下面对二元函数的情形给出几个充分条件。

#### 两个混合偏导数相等的充分条件 I：$C^2$

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，则 $\forall (x_0, y_0) \in D, f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

证明：

- 考虑 $f$ 在 $(x_0, y_0)$ 附近的二阶增量：

$$
d_{\Delta x, \Delta y} \triangleq f(x_0 + \Delta x, y_0 + \Delta y) - f(x_0 + \Delta x, y_0) - f(x_0, y_0 + \Delta y) + f(x_0, y_0), \quad \Delta x, \Delta y \neq 0
$$

- 固定 $\Delta x$，由 Lagrange 中值定理可知：

$$
\exists \xi \text{ 在 } y_0, y_0 + \Delta y \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = (f_y'(x_0 + \Delta x, \xi) - f_y'(x_0, \xi)) \Delta y \ (*)
$$

- 再由 Lagrange 中值定理可知：

$$
\exists \eta \text{ 在 } x_0, x_0 + \Delta x \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = f_{xy}''(\eta, \xi) \Delta x \Delta y
$$

- 先固定 $\Delta y$ 重复上面的步骤，同理可知：

$$
\exists \tilde{\eta} \text{ 在 } x_0, x_0 + \Delta x \text{ 之间}, \tilde{\xi} \text{ 在 } y_0, y_0 + \Delta y \text{ 之间}, \text{s.t. } d_{\Delta x, \Delta y} = f_{yx}''(\tilde{\eta}, \tilde{\xi}) \Delta x \Delta y \ (**)
$$

- 故有 $f_{xy}''(\eta, \xi) = f_{yx}''(\tilde{\eta}, \tilde{\xi})$。
- 由 $f \in C^2(D)$ 可知 $f_{xy}'', f_{yx}''$ 在 $D$ 上连续，令 $(\Delta x, \Delta y) \to (0, 0)$ 即得：

$$
f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)
$$

#### 两个混合偏导数相等的充分条件 II：Schwarz 定理

内容：设 $f_x', f_y', f_{xy}''$（或 $f_{yx}''$）在 $(x_0, y_0)$ 附近存在，$f_{xy}''$（或 $f_{yx}''$）在 $(x_0, y_0)$ 处连续，则 $f_{yx}''$（或 $f_{xy}''$）存在，且有 $f_{yx}''(x_0, y_0) = f_{xy}''(x_0, y_0)$。

证明（以括号外为例）：

- 同 $C^2$ 的情形得到 $(**)$ 式，则有：

$$
\begin{aligned}
f_{yx}''(x_0, y_0) &= \lim_{\Delta x \to 0} \lim_{\Delta y \to 0} \frac{f_{xy}''(\eta, \xi) \Delta x \Delta y}{\Delta x \Delta y} \\
&= \lim_{\Delta x \to 0} \lim_{\Delta y \to 0} f_{xy}''(\eta, \xi) \\
&= \lim_{(\Delta x, \Delta y) \to (0, 0)} f_{xy}''(\eta, \xi) &\quad (\text{重极限存在，内层极限存在为 } f_y'(x + \Delta x, y_0)) \\
&= f_{xy}''(x_0, y_0)
\end{aligned}
$$

#### 两个混合偏导数相等的充分条件 III：Young 定理

内容：设 $f_x', f_y'$ 在 $(x_0, y_0)$ 处可微，则 $f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

证明：

- 同 $C^2$ 的情形得到 $(*)$ 式，则有：

$$
\begin{aligned}
d_{\Delta x, \Delta y} &= ((f_{xy}''(x_0, y_0) \Delta x + f_{yy}''(x_0, y_0) (\xi - y_0) + o(\sqrt{(\Delta x)^2 + (\xi - y_0)^2})) - (f_{yy}''(x_0, y_0) (\xi - y_0) + o(\sqrt{(\Delta x)^2 + (\xi - y_0)^2}))) \Delta y \\
&= f_{xy}''(x_0, y_0) \Delta x \Delta y + o(\sqrt{(\Delta x)^2 + (\Delta y)^2}) \Delta y
\end{aligned}
$$

- 先固定 $\Delta y$ 重复上面的步骤，同理可知道：

$$
d_{\Delta x, \Delta y} = f_{yx}''(x_0, y_0) \Delta x \Delta y + o(\sqrt{(\Delta x)^2 + (\Delta y)^2}) \Delta x
$$

- 取 $\Delta x = \Delta y$，则有：

$$
f_{xy}''(x_0, y_0) - f_{yx}''(x_0, y_0) = o(1)
$$

- 令 $\Delta x = \Delta y \to 0$ 即得 $f_{xy}''(x_0, y_0) = f_{yx}''(x_0, y_0)$。

[例 19] 设 $f$ 在区域 $D \subset \mathbb{R}^2$ 上二阶可微且为一次齐次函数，求证：$f_{xx}'' f_{yy}'' = (f_{xy}'')^2$。

证明：

- 由欧拉定理可知 $xf_x' + yf_y' = f$，分别关于 $x, y$ 求偏导可知：

$$
\begin{cases}
xf_{xx}'' + yf_{yx}'' = 0 \\
xf_{xy}'' + yf_{yy}' = 0
\end{cases}
$$

- 此乃齐次线性方程组 $\begin{bmatrix} f_{xx}'' & f_{yx}'' \\ f_{xy}'' & f_{yy}'' \end{bmatrix} \begin{bmatrix} x \\ y \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \end{bmatrix}$。
- (1) 当 $(0, 0) \not\in D$，$\forall (x, y) \in D$，其给出上述齐次线性方程组的非零解，故：

$$
\begin{vmatrix}
f_{xx}'' & f_{yx}'' \\
f_{xy}'' & f_{yy}''
\end{vmatrix} = f_{xx}'' f_{yy}'' - f_{xy}'' f_{yx}'' = 0
$$

- 由 Young 定理可知 $f_{xy}'' = f_{yx}''$，代入整理即得 $f_{xx}'' f_{yy}'' = (f_{xy}'')^2$。
- (2) 当 $(0, 0) \in D$，则 $f(0, 0) = 0$。设 $f'(0, 0) = (a, b)$，则：

$$
\forall (0, 0) \neq (x, y) \in D, f(tx, ty) = t(ax + by) + o(t) \ (t \to 0)
$$

- 又由齐次性可知 $f(tx, ty) = tf(x, y)$，故有：

$$
f(x, y) = ax + by + o(1) \ (t \to 0)
$$

- 令 $t \to 0$ 即得 $f(x, y) = ax + by$，故 $f$ 在 $D$ 上的四个偏导数全为 $0$，等式显然成立。

#### 变量变换在偏微分方程中的应用

[例 20] 设 $u(x, y)$ 满足偏微分方程 $y \dfrac{\partial u}{\partial x} - x \dfrac{\partial u}{\partial y} = 0$，在极坐标变换 $\begin{cases} x = r \cos \theta \\ y = r \sin \theta \end{cases}$ 下求解该方程。

解：

$$
\frac{\partial u}{\partial \theta} = \frac{\partial u}{\partial x} \cdot \frac{\partial x}{\partial \theta} + \frac{\partial u}{\partial y} \cdot \frac{\partial y}{\partial \theta} = \frac{\partial u}{\partial x} (-r \sin \theta) + \frac{\partial u}{\partial y} (r \cos \theta) = -y \frac{\partial u}{\partial x} + x \frac{\partial u}{\partial y}
$$

- 据条件可知 $\dfrac{\partial u}{\partial \theta} = 0$，故 $u$ 只由 $r$ 决定。
- 再考虑原点处的情况，可知该方程的通解为 $u = f(r)$，其中 $f \in D[0, +\infty), f'_+(0) = 0$。

[例 21] 定义在 $\mathbb{R}^3 \backslash \{\mathbf{0}\}$ 上的函数 $u(x, y, z)$ 满足 Laplace 方程 $\Delta u \triangleq \dfrac{\partial^2 u}{\partial x^2} + \dfrac{\partial^2 u}{\partial y^2} + \dfrac{\partial^2 u}{\partial z^2} = 0$，求其球对称解（即只与到原点的距离 $r = \sqrt{x^2 + y^2 + z^2}$ 有关的解 $u = f(r)$）。

解：

- (1) 首先计算一阶偏导数：

$$
\dfrac{\partial u}{\partial x} = f'(r) \dfrac{x}{r}, \dfrac{\partial u}{\partial y} = f'(r) \dfrac{y}{r}, \dfrac{\partial u}{\partial z} = f'(r) \dfrac{z}{r}
$$

- (2) 然后计算二阶偏导数：

$$
\begin{cases}
\dfrac{\partial^2 u}{\partial x^2} = f''(r) \dfrac{x^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{x^2}{r^3} \right) \\
\dfrac{\partial^2 u}{\partial y^2} = f''(r) \dfrac{y^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{y^2}{r^3} \right) \\
\dfrac{\partial^2 u}{\partial z^2} = f''(r) \dfrac{z^2}{r^2} + f'(r) \left( \dfrac{1}{r} - \dfrac{z^2}{r^3} \right)
\end{cases}
$$

- (3) 写出 Laplace 方程，有 $f''(r) + \dfrac{2}{r} f'(r) = 0$。
- 故 $r^2 f'(r) \equiv \tilde{C}_1$，则 $f'(r) = \dfrac{\tilde{C}_1}{r^2}$，进而有 $f(r) = -\dfrac{\tilde{C}_1}{r} + C_2 \triangleq \dfrac{C_1}{r} + C_2$。
- _注：Laplace 方程描述的正是静电场、引力场等物理势场在无源区域的平滑分布。_

#### 定义：高阶全微分

设 $D \subset \mathbb{R}^n$ 为区域，$f$ 在 $D$ 上可微，设 $\mathbf{x} \in D$，则：

$$
\text{d} f(\mathbf{x}) = \sum_{i = 1}^n \frac{\partial f(\mathbf{x})}{\partial x_i} \text{d} x_i
$$

若 $\forall 1 \leq i \leq n$，$f_i'$ 仍在 $\mathbf{x}$ 处可微，则称：

$$
\text{d}^2 f(\mathbf{x}) \triangleq \sum_{i = 1}^n \left( \sum_{j = 1}^n \frac{\partial^2 f(\mathbf{x})}{\partial x_j \partial x_i} \text{d} x_j \right) \text{d} x_i
$$

为 $f$ 在 $\mathbf{x}$ 处的二阶微分。依此类推，还可以定义 $k$ 阶微分。若仿照例 18 引入算子记号：

$$
\text{d} f(\mathbf{x}) = \left( \sum_{i = 1}^n \text{d} x_i \frac{\partial}{\partial x_i} \right) f(\mathbf{x})
$$

则当 $f$ 在 $D$ 上 $k$ 阶可微，可以写出其 $k$ 阶微分：

$$
\text{d}^k f(\mathbf{x}) = \left( \sum_{i = 1}^n \text{d} x_i \frac{\partial}{\partial x_i} \right)^k f(\mathbf{x})
$$

这一形式可以通过多项式定理展开。

### 多元函数 Taylor 公式

#### 带 Lagrange 余项的多元函数 Taylor 公式

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^{k + 1}(D)$，$B_r(\mathbf{x}_0) \subset D$，则：

$$
\forall ||\Delta \mathbf{x}|| < r, \exists \theta \in (0, 1), \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0) + \frac{1}{(k + 1)!} (\Delta \mathbf{x} \cdot \nabla)^{k + 1} f(\mathbf{x}_0 + \theta \Delta \mathbf{x})
$$

证明：

- 设 $\varphi(t) = f(\mathbf{x}_0 + t \Delta \mathbf{x}), t \in [0, 1]$，则 $\varphi \in C^{k + 1}[0, 1]$，且 $\varphi(0) = f(\mathbf{x}_0), \varphi(1) = f(\mathbf{x} + t \Delta \mathbf{x})$。
- 算得 $\varphi'(t) = \nabla f(\mathbf{x}_0 + t \Delta \mathbf{x}) \Delta \mathbf{x} = (\Delta \mathbf{x} \cdot \nabla) f(\mathbf{x}_0 + t \Delta \mathbf{x})$，进而归纳可知 $\varphi^{(i)}(t) = (\Delta \mathbf{x} \cdot \nabla)^i f(\mathbf{x}_0 + t \Delta \mathbf{x})$。
- 由 $\varphi$ 的带 Lagrange 余项的多元函数 Taylor 公式可知：

$$
\begin{aligned}
\exists \theta \in (0, 1), \text{s.t. } f(\mathbf{x}_0 + \Delta \mathbf{x}) &= \varphi(1) \\
&= \varphi(0) + \sum_{i = 1}^k \frac{\varphi^{(i)}(0)}{i!} + \frac{\varphi^{(i)}(\theta)}{(k + 1)!} \\
&= f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0) + \frac{1}{(k + 1)!} (\Delta \mathbf{x} \cdot \nabla)^{k + 1} f(\mathbf{x}_0 + \theta \Delta \mathbf{x})
\end{aligned}
$$

#### 带 Peano 余项的多元函数 Taylor 公式

内容：设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^k(D)$，$B_r(\mathbf{x}_0) \subset D$，则：

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \dfrac{1}{i!} (\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0) + o(||\Delta \mathbf{x}||^k) \ (\Delta \mathbf{x} \to \mathbf{0})
$$

证明：

- 代入相应的带 Lagrange 余项的多元函数 Taylor 公式，将 $(\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0 + \theta \Delta \mathbf{x}) - (\Delta \mathbf{x} \cdot \nabla)^k f(\mathbf{x}_0)$ 用多项式定理展开。
- 展开式中的系数由 $C^k$ 可知其在以 $\mathbf{x}_0$ 为球心的闭球上有一致的界，借此放缩即得：当 $\Delta \mathbf{x} \to \mathbf{0}$，其是 $o(||\Delta \mathbf{x}||^k)$ 的。

设指数组 $(\alpha_1, \cdots, \alpha_n)$ 满足 $\alpha_i \geq 0$ 且 $\displaystyle\sum_{i = 1}^n \alpha_i = s$，其中 $1 \leq s \leq k$，则可见展开式中 $\displaystyle\prod_{i = 1}^n \Delta x_i^{\alpha_i}$ 项的系数为 $\dfrac{1}{\prod_{i = 1}^n \alpha_i} \cdot \dfrac{\partial^s f}{\partial x_1^{\alpha_1} \cdots \partial x_n^{\alpha_n}}$。

与一元的情形一致，可以证明只要

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \sum_{i = 1}^k \sum_{\alpha_j \geq 0; \sum \alpha_j = s} c_{\alpha} \prod_{j = 1}^n \Delta x_j^{\alpha_j} + o(||\Delta \mathbf{x}||^k) \quad (\Delta \mathbf{x} \to \mathbf{0})
$$

成立，就有 $c_{\alpha} = \dfrac{1}{\prod_{j = 1}^n \alpha_j!} \cdot \dfrac{\partial^s f}{\partial x_1^{\alpha_1} \cdots \partial x_n^{\alpha_n}}$。这一性质可以为我们计算高阶偏导数提供便利。

[例 22] 求 $f(x, y) = xe^{x + y}$ 在 $(0, 0)$ 处的所有四阶偏导数。

解：

$$
f(x, y) = x \left( 1 + x + \frac{x^2}{2} + \frac{x^3}{6} + o(x^3) \right) \left( 1 + y + \frac{y^2}{2} + \frac{y^3}{6} + \frac{y^4}{24} + o(y^4) \right) \quad ((x, y) \to (0, 0))
$$

- (1) 提取 $x^4$ 的系数可知 $\dfrac{1}{24} \cdot \dfrac{\partial^4 f}{\partial x^4} = \dfrac{1}{6}$，故 $\dfrac{\partial^4 f}{\partial x^4} = 4$。
- (2) 提取 $x^3 y$ 的系数可知 $\dfrac{1}{6} \cdot \dfrac{\partial^4 f}{\partial x^3 \partial y} = \dfrac{1}{2}$，故 $\dfrac{\partial^4 f}{\partial x^3 \partial y} = 3$。
- (3) 提取 $x^2 y^2$ 的系数可知 $\dfrac{1}{4} \cdot \dfrac{\partial^4 f}{\partial x^3 \partial y} = \dfrac{1}{2}$，故 $\dfrac{\partial^4 f}{\partial x^2 \partial y^2} = 2$。
- (4) 提取 $xy^3$ 的系数可知 $\dfrac{1}{6} \cdot \dfrac{\partial^4 f}{\partial x^3 \partial y} = \dfrac{1}{6}$，故 $\dfrac{\partial^4 f}{\partial x^3 \partial y} = 1$。
- (5) 提取 $y^4$ 的系数可知 $\dfrac{1}{24} \cdot \dfrac{\partial^4 f}{\partial y^4} = 0$，故 $\dfrac{\partial^4 f}{\partial y^4} = 0$。

#### 定义：Hesse 阵

特别地，在带 Peano 余项的多元函数 Taylor 公式中取 $k = 2$，可得：

$$
f(\mathbf{x}_0 + \Delta \mathbf{x}) = f(\mathbf{x}_0) + \nabla f(\mathbf{x}_0) \Delta \mathbf{x} + \frac{1}{2} (\Delta \mathbf{x})^T H_f(\mathbf{x}_0) \Delta \mathbf{x} + o(||\Delta \mathbf{x}||^2)
$$

其中 $H_f(\mathbf{x}_0) = \left( \dfrac{\partial^2 f}{\partial x_i \partial x_j} \right)_{1 \leq i, j \leq n}$ 称为 $f$ 在 $\mathbf{x}_0$ 处的 Hesse 阵。

#### 定义：临界点

设 $D \subset \mathbb{R}^n$ 为区域，$\mathbf{f} : D \to \mathbb{R}^m$，$\mathbf{x} \in D$，$\mathbf{f}$ 在 $\mathbf{x}$ 处可微，称 $\mathbf{x}$ 为 $\mathbf{f}$ 的一个临界点，若 $\text{rank}(\mathbf{f}'(\mathbf{x})) < \min(n, m)$，此时称 $\mathbf{f}(\mathbf{x})$ 为 $\mathbf{f}$ 的一个临界值。

特别地：

- (1) 当 $m = 1$ 即 $n$ 元函数的情形，$\mathbf{x}$ 为临界点当且仅当 $f'(\mathbf{x}) = 0$。
- (2) 当 $m = n$，$\mathbf{x}$ 为临界点当且仅当 $\det(\mathbf{f}'(\mathbf{x})) = 0$。

[例 23] 设 $D \subset \mathbb{R}^n$ 为区域，$f \in C^2(D)$，$\mathbf{x}_0 \in D$ 为 $f$ 的临界点，$\det(H_f(\mathbf{x}_0)) \neq 0$，求证：临界点 $\mathbf{x}_0$ 孤立，即 $\exists r > 0, \text{s.t. } \mathring{B}_r(\mathbf{x}_0)$ 内不存在 $f$ 的临界点。

证明：

- $f'(\mathbf{x}_0 + \Delta \mathbf{x})^T = H_f(\mathbf{x}_0) \Delta \mathbf{x} + \mathbf{r}(\Delta \mathbf{x})$，其中 $\displaystyle\lim_{\Delta \mathbf{x} \to \mathbf{0}} \dfrac{||\mathbf{r}(\Delta \mathbf{x})||}{||\Delta \mathbf{x}||} = 0$。
- 由 $\det(H_f(\mathbf{x}_0)) \neq 0$ 可知 $\exists \lambda > 0, \text{s.t. } ||H_f(\mathbf{x}_0) \Delta \mathbf{x}|| \geq \lambda ||\Delta \mathbf{x}||$。
- 取 $r > 0$ 使得 $B_r(\mathbf{x}_0) \subset D$ 且 $\forall ||\Delta \mathbf{x}|| < r, ||\mathbf{r}(\Delta \mathbf{x})|| < \dfrac{\lambda}{2} ||\Delta \mathbf{x}||$，则有：

$$
\forall \mathbf{x} = \mathbf{x}_0 + \Delta \mathbf{x} \in \mathring{B}_r(\mathbf{x}_0), ||f'(\mathbf{x}_0 + \Delta \mathbf{x})|| \geq ||H_f(\mathbf{x}_0) \Delta \mathbf{x}|| - ||\mathbf{r}(\Delta \mathbf{x})|| > \frac{\lambda}{2} ||\Delta \mathbf{x}|| > 0
$$

- 故 $\mathring{B}_r(\mathbf{x}_0)$ 内不存在 $f$ 的临界点。

### 隐函数存在定理

#### 定义：压缩

设 $D \subset \mathbb{R}^n$ 为区域，$\Omega = D \times U(y_0, \delta)$，$\Phi(\mathbf{x}, y) \in C(\Omega)$，$\Phi(\Omega) \subset U(y_0, \delta)$，称 $\Phi$ 在 $\Omega$ 上关于 $y$ 为一个压缩，若：

$$
\exists L \in (0, 1), \text{s.t. } \forall \mathbf{x} \in D, y_1, y_2 \in U(y_0, \delta), |\Phi(\mathbf{x}, y_1) - \Phi(\mathbf{x}, y_2)| \leq L|y_1 - y_2|
$$

即 $\{\Phi(\mathbf{x}, \cdot)\} \subset C(U(y_0, \delta))$ 有**一致**的 Lipschitz 常数 $L \in (0, 1)$。

#### 引理：迭代压缩函数列一致收敛

内容：

- 设 $\Phi$ 在 $D \times U(y_0, \delta)$ 上关于 $y$ 为一个压缩，则函数列

$$
\begin{cases}
y_1(\mathbf{x}) \in C(D), &\quad y_1(D) \subset U(y_0, \delta) \\
y_{n + 1}(\mathbf{x}) = \Phi(\mathbf{x}, y_n(\mathbf{x}))
\end{cases}
$$

- 在 $D$ 上一致收敛。
- 记 $y_n(\mathbf{x}) \rightrightarrows f(\mathbf{x}), \mathbf{x} \in D$，则 $f \in C(D)$，且 $f$ 为**唯一**满足 $y(\mathbf{x}) = \Phi(\mathbf{x}, y(\mathbf{x}))$ 的函数 $y$。

证明：

- 由 $\Phi$ 关于 $y$ 的一致压缩性易见 $y_n(\mathbf{x}) \rightrightarrows f(\mathbf{x}), \mathbf{x} \in D$，又由 $y_n(\mathbf{x}) \in C(D)$ 可知 $f \in C(D)$。
- 同压缩映射定理的唯一性证明可知满足条件的 $y$ 唯一。

#### 隐函数存在定理

内容：

- (1) 设 $D \subset \mathbb{R}^n$ 为区域，$\Omega = D \times U(y_0, \delta)$，$F(\mathbf{x}, y) \in C(\Omega)$，且满足：
- (I) $F(\mathbf{x}_0, y_0) = 0$。
- (II) $F_y'(\mathbf{x}, y)$ 在 $\Omega$ 上存在且连续。
- (III) $F_y'(\mathbf{x}_0, y_0) \neq 0$。
- 则：

$$
\exists r > 0, \text{s.t. } \exists ! f \in C(D), \text{s.t. } \begin{cases}
y_0 = f(\mathbf{x}_0) \\
\forall \mathbf{x} \in D, F(\mathbf{x}, f(\mathbf{x})) = 0
\end{cases}
$$

- (2) 在 (1) 的基础上，若 $F \in C^1(\Omega)$，则 $f \in C^1(D)$，且有：

$$
\forall \mathbf{x} \in D, f'(\mathbf{x}) = -\frac{F_{\mathbf{x}}'(\mathbf{x}, f(\mathbf{x}))}{F_y'(\mathbf{x}, f(\mathbf{x}))}
$$

证明：

- (1) 令 $\Phi(\mathbf{x}, y) = y - kF(\mathbf{x}, y)$，其中 $k = \dfrac{1}{2F_y'(\mathbf{x}_0, y_0)}$，则 $\dfrac{\partial \Phi}{\partial y} = 1 - k \dfrac{\partial F}{\partial y}$。
- 由 $F_y' \in C(\Omega)$ 可知：

$$
\exists r > 0, \delta' \in (0, \delta), \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), y \in U(y_0, \delta'), |F_y'(\mathbf{x}, y) - F_y'(\mathbf{x}_0, y_0)| < \frac{1}{2} |F_y'(\mathbf{x}_0, y_0)|
$$

- 令 $\Omega' = B_r(\mathbf{x}_0) \times U(y_0, \delta')$，则在 $\Omega'$ 上有 $\dfrac{\partial \Phi}{\partial y} \in \left( \dfrac{1}{4}, \dfrac{3}{4} \right)$，故 $\Phi$ 在 $\Omega'$ 上关于 $y$ 为一个压缩。
- 由引理即得 $\exists ! f \in C(B_r(\mathbf{x}_0)), \text{s.t. } \forall \mathbf{x} \in B_r(\mathbf{x}_0), f(\mathbf{x}) = \Phi(\mathbf{x}, f(\mathbf{x})) \Leftrightarrow F(\mathbf{x}, f(\mathbf{x})) = 0$。
- (2) 考察增量 $F(\mathbf{x}_0 + \Delta \mathbf{x}, f(\mathbf{x}_0 + \Delta \mathbf{x})) - F(\mathbf{x}_0, f(\mathbf{x}_0)) = 0$，记 $\Delta y = f(\mathbf{x}_0 + \Delta \mathbf{x}) - f(\mathbf{x}_0)$，由微分中值定理可知：

$$
\exists \theta \in (0, 1), \text{s.t. } F_{\mathbf{x}}'(\mathbf{x}_0 + \theta \Delta \mathbf{x}, f(\mathbf{x}_0) + \theta \Delta y) \cdot \Delta \mathbf{x} + F_y'(\mathbf{x}_0 + \theta \Delta \mathbf{x}, f(\mathbf{x}_0) + \theta \Delta y) \cdot \Delta y = 0
$$

- 由 $F_y' \in C(\Omega)$ 和 $F_y'(\mathbf{x}_0, y_0) \neq 0$ 可知，当 $\Delta x$ 充分小时 $F_y'(\mathbf{x}_0 + \theta \Delta \mathbf{x}, f(\mathbf{x}_0) + \theta \Delta y) \neq 0$，故有：

$$
\begin{aligned}
\Delta y &= -\frac{F_{\mathbf{x}}'(\mathbf{x}_0 + \theta \Delta \mathbf{x}, f(\mathbf{x}_0) + \theta \Delta y)}{F_y'(\mathbf{x}_0 + \theta \Delta \mathbf{x}, f(\mathbf{x}_0) + \theta \Delta y)} \Delta \mathbf{x} \\
&= \left( -\frac{F_{\mathbf{x}}'(\mathbf{x}_0, f(\mathbf{x}_0))}{F_y'(\mathbf{x}_0, f(\mathbf{x}_0))} + o(1) \right) \Delta \mathbf{x} &\quad (F \in C^1(\Omega), \Delta \mathbf{x} \to \mathbf{0}) \\
&= -\frac{F_{\mathbf{x}}'(\mathbf{x}_0, f(\mathbf{x}_0))}{F_y'(\mathbf{x}_0, f(\mathbf{x}_0))} \Delta x + o(||\Delta x||)
\end{aligned}
$$

- 即 $f'(\mathbf{x}_0) = -\dfrac{F_{\mathbf{x}}'(\mathbf{x}_0, f(\mathbf{x}_0))}{F_y'(\mathbf{x}_0, f(\mathbf{x}_0))}$，故 $f \in C^1(D)$。

[例 24] 设区域 $D \subset \mathbb{R}^3$，$F \in C^1(D)$，$F_x' F_y' F_z' \neq 0$，则由隐函数存在定理可知对于方程 $F(x, y, z) = 0$，在这一点附近三者之一可**唯一**表示为另外两者的函数，求证：$\dfrac{\partial z}{\partial x} \cdot \dfrac{\partial x}{\partial y} \cdot \dfrac{\partial y}{\partial z} = -1$。

证明：

- 视 $z$ 为因变量，对 $x$ 求偏导数可知 $F'_x + F'_z \cdot \dfrac{\partial z}{\partial x} = 0$，故 $\dfrac{\partial z}{\partial x} = -\dfrac{F_x'}{F_z'}$。
- 同理可得 $\dfrac{\partial x}{\partial y} = -\dfrac{F_y'}{F_x'}, \dfrac{\partial y}{\partial z} = -\dfrac{F_z'}{F_y'}$，相乘即得 $\dfrac{\partial z}{\partial x} \cdot \dfrac{\partial x}{\partial y} \cdot \dfrac{\partial y}{\partial z} = -1$。

[例 25] 设区域 $D \subset \mathbb{R}^2$，$f \in C^2(D)$，且满足方程 $\dfrac{\partial^2 f}{\partial x^2} \cdot \dfrac{\partial^2 f}{\partial y^2} = \left( \dfrac{\partial^2 f}{\partial x \partial y} \right)^2$，若 $\dfrac{\partial f}{\partial y} \neq 0$，求证：$z = f(x, y)$ 所确定的函数 $y = g(x, z)$ 满足 $\dfrac{\partial^2 g}{\partial x^2} \cdot \dfrac{\partial^2 g}{\partial z^2} = \left( \dfrac{\partial^2 g}{\partial x \partial z} \right)^2$。

证明：

- (1) $z = f(x, y)$ 对应于隐函数方程 $f(x, y) - z = 0$。
- 对 $x$ 求偏导数可知 $\dfrac{\partial f}{\partial x} + \dfrac{\partial f}{\partial y} \cdot \dfrac{\partial g}{\partial x} = 0$，对 $z$ 求偏导数可知 $\dfrac{\partial f}{\partial y} \cdot \dfrac{\partial g}{\partial z} - 1 = 0$。
- 故而解得 $\dfrac{\partial g}{\partial x} = -\dfrac{f_x'}{f_y'}, \dfrac{\partial g}{\partial z} = \dfrac{1}{f_y'}$。
- (2) 直接计算可得：

$$
\begin{cases}
\dfrac{\partial^2 g}{\partial x^2} = \dfrac{\partial}{\partial x} \left( -\dfrac{f_x'}{f_y'} \right) = -\dfrac{f_y' (f_{xx}'' + f_{yx}'' \frac{\partial g}{\partial x}) - f_x'(f_{xy}'' + f_{yy}'' \frac{\partial g}{\partial x})}{(f_y')^2} = -\dfrac{f_{xx}'' (f_y')^2 - 2f_x' f_{xy}'' f_y' + (f_x')^2 f_{yy''}}{(f_y')^3} \\
\dfrac{\partial^2 g}{\partial x \partial z} = \dfrac{\partial}{\partial x} \left( \dfrac{1}{f_y'} \right) = -\dfrac{f_{xy}'' + f_{yy}'' \frac{\partial g}{\partial x}}{(f_y')^2} = \dfrac{f_x' f_{yy}'' - f_{xy}'' f_y'}{(f_y')^3} \\
\dfrac{\partial^2 g}{\partial z^2} = \dfrac{\partial}{\partial z} \left( \dfrac{1}{f_y'} \right) = -\dfrac{f_{yy}'' \cdot \frac{\partial g}{\partial z}}{(f_y')^2} = -\dfrac{f_{yy}''}{(f'_y)^3}
\end{cases}
$$

- 利用题给方程化简即得目标等式。
