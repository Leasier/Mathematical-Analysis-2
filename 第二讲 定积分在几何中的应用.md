- _部分内容参见上学期 gy 数分的笔记：[第十九讲](https://github.com/Leasier/Mathematical-Analysis-1/blob/main/%E7%AC%AC%E5%8D%81%E4%B9%9D%E8%AE%B2%20%E5%AE%9A%E7%A7%AF%E5%88%86%E7%9A%84%E8%AE%A1%E7%AE%97%E4%B8%8E%E5%BA%94%E7%94%A8.md)。_

### 平面曲线的长度

#### 定义：平面曲线与连续曲线

参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 可给出一条平面曲线 $\Gamma$。

特别地，若 $x(t), y(t) \in C[\alpha, \beta]$，称 $\Gamma$ 为一条连续曲线。

#### 定义：简单曲线与简单闭曲线

若 $\forall t_1, t_2 \in [\alpha, \beta] \land t_1 \neq t_2, (x(t_1), y(t_1)) \neq (x(t_2), y(t_2))$，则称连续曲线 $\Gamma$ 为一条简单曲线 / 非闭合曲线。

若 $(x(\alpha), y(\alpha)) = (x(\beta), y(\beta))$ 但 $\forall t_1, t_2 \in [\alpha, \beta) \land t_1 \neq t_2, (x(t_1), y(t_1)) \neq (x(t_2), y(t_2))$，则称连续曲线 $\Gamma$ 为一条简单闭曲线 / Jordan 曲线，其中 $(x(\alpha), y(\alpha))$ 称为 $\Gamma$ 的闭合点 / 重点。

#### 定义：曲线的长度

设参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 给出简单曲线 $\Gamma$。

- _Motivation：化曲为直，当直线段“充分密”就几乎得到了曲线。_

在端点 $A(x(\alpha), y(\alpha)), B(x(\beta), y(\beta))$ 之间插入 $n - 1$ 个分点 $M_i(x(t_i), y(t_i))$，其中 $P_t : \alpha = t_0 < t_1 < \cdots < t_n = \beta$，如此构成 $\Gamma$ 的一个分割 $P_{\Gamma} : A = M_0, M_1, \cdots, M_n = B$。

用线段将相邻分点相连形成折线串，记折线串长度为：

$$
\sigma(\Gamma, P_{\Gamma}) = \sum_{i = 1}^n |M_{i - 1} M_i|
$$

若 $\displaystyle\sup_{P_{\Gamma}} \sigma(\Gamma, P_{\Gamma}) < +\infty$，则称 $\Gamma$ 可求长，并定义其长度为 $|\Gamma| = \displaystyle\sup_{P_{\Gamma}} \sigma(\Gamma, P_{\Gamma})$；否则，称 $\Gamma$ 不可求长，并令 $|\Gamma| = +\infty$。

- 注意：并非所有连续曲线都可求长，如第一讲例 10 中的连续但不有界变差的函数。

------

仿照区间分割的模 $||P_t||$ 的概念，定义曲线分割的模 $||P_{\Gamma}|| = \displaystyle\max_{i = 1}^n |M_{i - 1} M_i|$，并称 $(P_{\Gamma}, P_t)$ 为一个分割对。为了通过研究**参数方程**来研究**曲线**，我们有必要考察两者及其模之间的关联，这引出了下面的引理。

#### 引理：分割对的模的关联

内容：设 $\Gamma$ 为简单曲线，记 $(P_{\Gamma}, P_t)$ 为其分割对，则：

- (1) $\forall \delta_{\Gamma} > 0, \exists \delta_t > 0, \text{s.t. } ||P_t|| < \delta_t \Rightarrow ||P_{\Gamma}|| < \delta_{\Gamma}$。
- (2) $\forall \delta_t > 0, \exists \delta_{\Gamma} > 0, \text{s.t. } ||P_{\Gamma}|| < \delta_{\Gamma} \Rightarrow ||P_t|| < \delta_t$。

因此 $||P_{\Gamma}|| \to 0 \Leftrightarrow ||P_t|| \to 0$。进而，让我们通过参数方程的性质，考察怎样的曲线是可求长的。

#### 定理：可求长的充要条件

内容：设参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 给出简单曲线 $\Gamma$，则 $\Gamma$ 可求长当且仅当 $x(t), y(t) \in BV[\alpha, \beta]$。

证明：

- 充分性：

$$
\begin{aligned}
\sigma(\Gamma, P_{\Gamma}) &= \sum_{i = 1}^n \sqrt{(x(t_i) - x(t_{i - 1}))^2 + (y(t_i) - y(t_{i - 1}))^2} \\
&\leq \sum_{i = 1}^n (|x(t_i) - x(t_{i - 1})| + |y(t_i) - y(t_{i - 1})|) \\
&= \sigma(x, P_t) + \sigma(y, P_t) \\
&\leq \bigvee_a^b x(t) + \bigvee_a^b y(t)
\end{aligned}
$$

- 故 $\displaystyle\sup_{P_{\Gamma}} \sigma(\Gamma, P_{\Gamma}) \leq \bigvee_a^b x(t) + \bigvee_a^b y(t) < +\infty$，即 $\Gamma$ 可求长。
- 必要性：

$$
\begin{aligned}
\sigma(\Gamma, P_{\Gamma}) &= \sum_{i = 1}^n \sqrt{(x(t_i) - x(t_{i - 1}))^2 + (y(t_i) - y(t_{i - 1}))^2} \\
&\geq \sum_{i = 1}^n |x(t_i) - x(t_{i - 1})| \\
&= \sigma(x, P_t)
\end{aligned}
$$

- 故 $\displaystyle\bigvee_a^b x(t) = \sup_{P_t} \sigma(x, P_t) \leq \sup_{P_{\Gamma}} \sigma(\Gamma, P_{\Gamma}) = |\Gamma|$，即 $x(t) \in BV[\alpha, \beta]$。同理可证 $y(t) \in BV[\alpha, \beta]$。

------

利用全变差的长得很像 Riemann 积分的定义的等式，我们还可以导出关于曲线长度的一个结论。

#### 定理：另一个长得很像 Riemann 积分的定义的等式

内容：设参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 给出简单曲线 $\Gamma$，则：

$$
|\Gamma| = \lim_{||P_{\Gamma}|| \to 0} \sum_{i = 1}^n |M_{i - 1} M_i|
$$

#### 定义：有闭合点的连续曲线的长度

若平面连续曲线 $\Gamma$ 可以分割为至多可数个简单曲线 $\{\Gamma_i\}_{i \geq 1}$（即 $\Gamma = \displaystyle\bigcup_{i \geq 1} \Gamma_i$），则定义 $\Gamma$ 的长度为所有 $\Gamma_i$ 的长度之和，即：

$$
|\Gamma| = \sum_{i \geq 1} |\Gamma_i|
$$

#### 定义：光滑曲线

称参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 给出的平面曲线 $\Gamma$ 为光滑曲线，若 $x(t), y(t) \in C^1 [\alpha, \beta]$，且 $\forall t \in [\alpha, \beta], (x'(t))^2 + (y'(t))^2 \neq 0$。

#### 定理：光滑曲线的长度

内容：设参数方程 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [\alpha, \beta]$ 给出简单光滑曲线 $\Gamma$，则 $\Gamma$ 可求长，且有：

$$
|\Gamma| = \int_{\alpha}^{\beta} \sqrt{x'(t)^2 + y'(t)^2} \text{d} t
$$

[例 1] 设 $f \in C^1 [0, 1]$，$f(0) = f(1) = 0$，$0 \leq x \leq 1, \forall x \in [0, 1]$，$f'$ 单调递减，求证：曲线 $y = f(x)$ 的 $x \in [0, 1]$ 段弧长不超过 $3$，且 $3$ 无法改进。

证明：

- _Motivation：弧长“比较长”的情况就是“贴着” $(0, [0, 1]), ([0, 1], 1), (1, [0, 1])$ 走，此时在 $x$ 接近 $0, 1$ 的一小段内 $|f'(x)|$ “比较大”。_

$$
\begin{aligned}
L &= \int_0^1 \sqrt{f'(x)^2 + 1} \text{d} x \\
&\leq \int_0^1 (|f'(x)| + 1) \text{d} x \\
&= \int_0^1 |f'(x)| \text{d} x + 1
\end{aligned}
$$

- 令 $x_0 = \inf \{x \in [0, 1] \mid f'(x) = 0\}$，则由 $f'$ 的单调性可知：

$$
\begin{aligned}
L &= \int_0^{x_0} f'(x) \text{d} x + \int_{x_0}^1 (-f'(x)) \text{d} x + 1 \\
&= \left. f(x) \right|_0^{x_0} + \left. (-f(x)) \right|_{x_0}^1 + 1 \\
&= 2f(x) + 1 \\
&\leq 3
\end{aligned}
$$

- 令 $g(x) = (1 - x)^2$，对于 $\delta \in \left( 0, \dfrac{1}{2} \right)$，构造：

$$
f(x) = \begin{cases}
g \left( \dfrac{\delta - x}{\delta} \right), &\quad x \in [0, \delta] \\
1, &\quad x \in (\delta, 1 - \delta) \\
g \left( \dfrac{x - (1 - \delta)}{\delta} \right), &\quad x \in [1 - \delta, 1]
\end{cases}
$$

- 可算得只需 $\delta$ 充分小，即可使 $L$ 任意接近 $3$，故 $3$ 不可改进。

### 平面图形的面积

#### 曲边梯形的面积

设 x 轴（y 轴同理）上曲边梯形之曲边的参数方程为 $\begin{cases} x = \varphi(t) \\ y = \psi(t) \end{cases}, t \in [\min\{\alpha, \beta\}, \max\{\alpha, \beta\}]$，其中 $\varphi(\alpha) = a, \varphi(\beta) = b \ (a < b)$，$\varphi(t)$ 严格单调，$\varphi'(t), \psi'(t) \in R[\min\{\alpha, \beta\}, \max\{\alpha, \beta\}]$，则有：

$$
\begin{aligned}
S &= \int_a^b y \text{d} x \\
&= \int_{\alpha}^{\beta} \psi(t) \text{d} \varphi(t) &\quad (\text{换元 } x = \varphi(t)) \\
&= \int_{\alpha}^{\beta} \psi(t) \varphi'(t) \text{d} t
\end{aligned}
$$

需要注意的是，$t$ 的积分区间应当与 $x$ 的积分区间相对应。

#### 曲边扇形的面积

设曲边梯形由射线 $\theta = \alpha, \theta = \beta \ (\alpha < \beta)$ 和参数式方程描述的光滑曲边 $\begin{cases} x = x(t) \\ y = y(t) \end{cases}, t \in [a, b]$ 围成，其中 $(x(a), y(a)), (x(b), y(b))$ 分别在射线 $\theta = \alpha, \theta = \beta$ 上，曲边被夹在两射线之间。

$$
\begin{array}{l}
& \begin{cases}
x = r \cos \theta \\
y = r \sin \theta
\end{cases} \\
\Rightarrow & \begin{cases}
\text{d} x = \cos \theta \text{d} r - r \sin \theta \text{d} \theta \\
\text{d} y = \sin \theta \text{d} r + r \cos \theta \text{d} \theta
\end{cases} \\
\Rightarrow & x \text{d} y - y \text{d} x = r^2 \text{d} \theta
\end{array}
$$

故有：

$$
\begin{aligned}
S &= \int_{\alpha}^{\beta} \frac{1}{2} r^2(\theta) \text{d} \theta \\
&= \int_a^b \frac{1}{2} (x(t) y'(t) - y(t) x'(t)) \text{d} t &\quad (\text{换元 } \theta \to t)
\end{aligned}
$$

- _注：上面这段论证并不严格，因为这里不能直接使用换元法（除非 $\theta = \theta(t)$ 严格单调）。真正严谨的证明需要用到 Green 公式。_
