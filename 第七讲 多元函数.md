如无特殊说明，下面默认采取 $2$-范数。

### $\mathbb{R}^n$ 中的极限

#### 定义：邻域

设 $\mathbf{x}_0 \in \mathbb{R}^n, r > 0$，称集合 $B_r(\mathbf{x}_0) = \{\mathbf{x} \in \mathbb{R}^n \mid ||\mathbf{x} - \mathbf{x}_0|| < r\}$ 为 $\mathbf{x}_0$ 的 $r$-（球形）邻域，称 $\mathring{B}_r(\mathbf{x}_0) = B_r(\mathbf{x}_0) \backslash \{\mathbf{x}_0\}$ 为 $\mathbf{x}_0$ 的去心 $r$-（球形）邻域。

除球形邻域外，方形邻域也是常用的。称集合 $N_r(\mathbf{x}_0) = \{\mathbf{x} \in \mathbb{R}^n \mid ||\mathbf{x} - \mathbf{x}_0||_{\infty} < r\}$ 为 $\mathbf{x}_0$ 的 $r$-方形邻域，称 $\mathring{N}_r(\mathbf{x}_0) = N_r(\mathbf{x}_0) \backslash \{\mathbf{x}_0\}$ 为 $\mathbf{x}_0$ 的去心 $r$-方形邻域。

由于 $B_r(\mathbf{x}_0) \subset N_r(\mathbf{x}_0) \subset B_{\sqrt{n} r}(\mathbf{x}_0)$，使用二者中任何一个进行讨论常常是等价的。

#### 定义：极限

设点列 $\{\mathbf{x}_k\}_{k = 1}^{+\infty} \subset \mathbb{R}^n$，若 $\exists \mathbf{x}_0 \in \mathbb{R}^n, \text{s.t. } \displaystyle\lim_{k \to +\infty} ||\mathbf{x}_k - \mathbf{x}_0|| = 0$，则称 $\{\mathbf{x}_k\}$ 收敛于 $\mathbf{x}_0$，$\mathbf{x}_0$ 为 $\{\mathbf{x}_k\}$ 的极限（点），记作 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$。

称存在极限的点列为收敛点列，不存在极限的点列为发散点列。

#### 定义：有界

称集合 $E \subset \mathbb{R}^n$ 有界，若 $\exists M > 0, \text{s.t. } \forall \mathbf{x} \in E, ||\mathbf{x}|| \leq M$。

称点列 $\{x_k\} \subset \mathbb{R}^n$ 有界，若 $\exists M > 0, \text{s.t. } \forall k \in \mathbb{N}_+, ||\mathbf{x}_k|| \leq M$。

#### 定理：收敛的充要条件

内容：设点列 $\{\mathbf{x}_k\} \subset \mathbb{R}^n$，$\mathbf{x}_0 \in \mathbb{R}^n$，记 $\mathbf{x}_k = (x_k^{(i)})_{i = 1}^n$，则 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$ 当且仅当 $\forall 1 \leq i \leq n, \displaystyle\lim_{k \to +\infty} x_k^{(i)} = x_0^{(i)}$。

------

进而，与 $\mathbb{R}$ 上的情况一致地，容易给出极限的如下性质：

- (1) 收敛点列的极限唯一。
- (2) 收敛点列有界。
- (3) 若点列 $\{\mathbf{x}_k\}, \{\mathbf{y}_k\} \subset \mathbb{R}^n$ 满足 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0, \lim_{k \to +\infty} \mathbf{y}_k = \mathbf{y}_0$，则 $\forall \alpha, \beta \in \mathbb{R}, \displaystyle\lim_{k \to +\infty} (\alpha \mathbf{x}_k + \beta \mathbf{y}_k) = \alpha \mathbf{x}_0 + \beta \mathbf{y}_0$。

#### $\mathbb{R}^n$ 的点集拓扑

#### 定义：聚点

设 $\varnothing \subsetneq E \subset \mathbb{R}^n$，称 $\mathbf{x} \in \mathbb{R}^n$ 为 $E$ 的聚点，若 $\forall r > 0, \mathring{B}_r(\mathbf{x}) \cap E \neq \varnothing$。

#### 定理：聚点 $\Leftrightarrow$ 收敛子列

内容：设 $\varnothing \subsetneq E \subset \mathbb{R}^n$，则 $\mathbf{x}_0$ 为 $E$ 的聚点的充要条件为 $\exists \{\mathbf{x}_k\} \subset E \backslash \{\mathbf{x}_0\}, \text{s.t. } \displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$。

#### 定义：孤立点

设 $\varnothing \subsetneq E \subset \mathbb{R}^n$，称 $\mathbf{x} \in E$ 为 $E$ 的孤立点，若 $\mathbf{x}$ 不为 $E$ 的聚点，也即 $\exists r > 0, \text{s.t. } \mathring{B}_r(\mathbf{x}) \cap E = \varnothing$。

#### 定义：内点、内部、外点、余集、边界点、边界

设 $E \subset \mathbb{R}^n$：

- 称 $\mathbf{x} \in \mathbb{R}^n$ 为 $E$ 的内点，若 $\exists r > 0, \text{s.t. } B_r(\mathbf{x}) \subset E$。显然内点必为聚点。
- 称 $E$ 的所有内点构成的集合为 $E$ 的内部，记作 $\mathring{E}$。
- 称 $\mathbf{x} \in \mathbb{R}^n$ 为 $E$ 的外点，若 $\exists r > 0, \text{s.t. } B_r(\mathbf{x}) \cap E = \varnothing$。
- 称 $\mathbb{R}^n \backslash E$ 为 $E$ 的余集，记作 $E^c$。
- 称 $\mathbf{x} \in \mathbb{R}^n$ 为 $E$ 的边界点，若 $\forall r > 0, B_r(\mathbf{x}) \cap E \neq \varnothing, B_r(\mathbf{x}) \cap E^c \neq \varnothing$。
- 称 $E$ 的所有边界点构成的集合为 $E$ 的边界，记作 $\partial E$。

#### 定义：开集、导集、闭包、稠密、闭集

设 $E \subset \mathbb{R}^n$：

- 称 $E$ 为开集，若 $\mathring{E} = E$。显然开集只包含其内点。
- 称 $E$ 的所有聚点构成的集合为 $E$ 的导集，记作 $E'$。
- 称 $E$ 及其所有聚点构成的集合为 $E$ 的闭包，记作 $\bar{E} \triangleq E \cup E'$。
- 设 $F \subset E$ 且 $E, F$ 为无限集，称 $F$ 在 $E$ 中稠密，若 $E \subset \bar{F}$。
- 称 $E$ 为闭集，若 $\bar{E} = E$。

只有 $\varnothing, \mathbb{R}^n$ 既是开集也是闭集。

#### 命题：开、闭集的交、并

内容：

- (1) 有限个开集的交集仍为开集，任意多个开集的并仍为开集。
- (2) 有限个闭集的并仍为闭集，任意多个闭集的交仍为闭集。

证明：

- 下面只讨论 (1)，(2) 是同理的。
- (I) 设 $E_1, \cdots, E_n \subset \mathbb{R}^n$ 均为开集，令 $E = \displaystyle\bigcap_{i = 1}^n E_i$。
- $\forall \mathbf{x} \in E, \forall 1 \leq i \leq n, \exists r_i > 0, \text{s.t. } B_{r_i}(\mathbf{x}) \subset E_i$。
- 令 $r = \displaystyle\min_{i = 1}^n r_i > 0$，则 $B_r(\mathbf{x}) \subset E$，故 $E$ 为开集。
- (II) 设 $\{E_{\lambda}\}_{\lambda \in \Lambda}$ 为 $\mathbb{R}^n$ 中的开集族，令 $E = \displaystyle\bigcup_{\lambda \in \Lambda} E_{\lambda}$。
- $\forall \mathbf{x} \in E, \exists \lambda \in \Lambda, \text{s.t. } \mathbf{x} \in E_{\lambda}$，则 $\exists r > 0, \text{s.t. } B_r(\mathbf{x}) \subset E_{\lambda} \subset E$，故 $E$ 为开集。

#### 命题：补集与原集开闭性相反

内容：$E \subset \mathbb{R}^n$ 为开集 $\Leftrightarrow E^c$ 为闭集。

证明：

- (1) $\Rightarrow$：若 $\mathbf{x} \in E$，则 $\exists r > 0, \text{s.t. } B_r(\mathbf{x}) \subset E \Rightarrow B_r(\mathbf{x}) \cap E^c = \varnothing$，于是 $\mathbf{x} \not\in (E^c)'$。
- 因此 $E \cap (E^c)' = \varnothing$，则 $(E^c)' \subset E^c$，即 $E^c$ 为闭集。
- (2) $\Leftarrow$：考虑反证法，假设 $E$ 不为开集，则 $\exists \mathbf{x} \in E, \text{s.t. } \forall r > 0, B_r(\mathbf{x}) \not\subset E \Rightarrow B_r(\mathbf{x}) \cap E^c \neq \varnothing$，于是 $\mathbf{x} \in (E^c)'$。
- 因此 $E \cap (E^c)' \neq \varnothing$，则 $(E^c)' \not\subset E^c$，即 $E^c$ 不为闭集，矛盾！
- 故假设不成立，即 $E$ 为开集。

#### 定义：连续曲线

设 $\forall 1 \leq i \leq n, x_i \in C[a, b]$，令 $\mathbf{x}(t) = (x_i(t))_{i = 1}^n$，称 $S = \{\mathbf{x}(t) \mid t \in [a, b]\}$ 为 $\mathbb{R}^n$ 中连接 $\mathbf{x}(a), \mathbf{x}(b)$ 的连续曲线。

特别地：

- 称 $S$ 为闭曲线，若 $\mathbf{x}(a) = \mathbf{x}(b)$。
- 进一步地，称 $S$ 为简单闭曲线，若还有 $\forall t_1, t_2 \in [a, b) \land t_1 \neq t_2, \mathbf{x}(t_1) \neq \mathbf{x}(t_2)$。

#### 定义：道路连通、区域、凸集

称 $E \subset \mathbb{R}^n$ 为道路连通集，若 $E$ 中任意两点间都能通过属于 $E$ 的连续曲线相连。

称 $E$ 为区域，若 $E$ 为道路连通的开集，并称 $\bar{E}$ 为闭区域。

称 $E$ 为凸集，若 $E$ 中任意两点间的直线段都属于 $E$。特别地，称凸的区域为凸区域。

#### 定理：De Morgan 公式

内容：设 $\{E_{\lambda}\}_{\lambda \in \Lambda}$ 为 $\mathbb{R}^n$ 中的集族，则：

- (1) $\displaystyle\left( \bigcup_{\lambda \in \Lambda} E_{\lambda} \right)^c = \bigcap_{\lambda \in \Lambda} E_{\lambda}^c$。
- (2) $\displaystyle\left( \bigcap_{\lambda \in \Lambda} E_{\lambda} \right)^c = \bigcup_{\lambda \in \Lambda} E_{\lambda}^c$。

### $\mathbb{R}^n$ 中的基本定理

#### 定义：Cauchy 列

称点列 $\{\mathbf{x}_k\} \subset \mathbb{R}^n$ 为 Cauchy 列，若 $\forall \epsilon > 0, \exists N \in \mathbb{N}, \text{s.t. } \forall n, m > N, ||\mathbf{x}_n - \mathbf{x}_m|| < \epsilon$。

#### Cauchy 收敛原理

内容：点列 $\{\mathbf{x}_k\} \subset \mathbb{R}^n$ 有极限当且仅当 $\{\mathbf{x}_k\}$ 为 Cauchy 列。

#### 定义：直径

定义 $\varnothing \subsetneq E \subset \mathbb{R}^n$ 的直径为 $\text{diam}(E) \triangleq \displaystyle\sup_{\mathbf{x}_1, \mathbf{x}_2 \in E} ||\mathbf{x}_1 - \mathbf{x}_2||$。

#### 闭集套定理

内容：

- 若非空闭集列 $\{F_k\} \subset \mathbb{R}^n$ 满足：
- (1) $\forall k \in \mathbb{N}_+, F_k \supset F_{k + 1}$。
- (2) $\displaystyle\lim_{k \to +\infty} \text{diam}(F_k) = 0$。
- 则 $\exists ! \mathbf{x}_0 \in \mathbb{R}^n, \text{s.t. } \forall k \in \mathbb{N}_+, \mathbf{x}_0 \in F_k$，即 $\displaystyle\bigcap_{k = 1}^{+\infty} F_k = \{\mathbf{x}_0\}$。

证明：

- (1) 存在性：取点列 $\{\mathbf{x}_k\}$ 使得 $\mathbf{x}_k \in F_k$，则 $\forall 1 \leq k_1 < k_2, ||\mathbf{x}_{k_1} - \mathbf{x}_{k_2}|| \leq \text{diam}(F_{k_1}) \to 0 \ (k_1 \to +\infty)$。
- 由 Cauchy 收敛原理可知 $\{\mathbf{x}_k\}$ 收敛，设 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$。
- $\forall k \in \mathbb{N}_+$，由 $F_k$ 为闭集可知 $\displaystyle\lim_{k' \geq k, k' \to +\infty} \mathbf{x}_k = \mathbf{x}_0 \in F_k$，故 $\mathbf{x}_0 \in \displaystyle\bigcap_{k = 1}^{+\infty} F_k$。
- (2) 唯一性：若 $\mathbf{x}_0' \in \mathbb{R}^n$ 也满足条件，由 $\mathbf{x}_0, \mathbf{x}_0' \in F_k$ 可知 $||\mathbf{x}_0 - \mathbf{x}_0'|| \leq \text{diam}(F_k)$。
- 令 $k \to +\infty$ 即得 $||\mathbf{x}_0 - \mathbf{x}_0'|| = 0$，故 $\mathbf{x}_0 = \mathbf{x}_0'$，即 $\mathbf{x}_0$ 唯一。

#### 定义：完备

对于距离空间 $(X, d)$，若 Cauchy 收敛原理在其上成立，则称 $X$ 完备。$\mathbb{R}, \mathbb{R}^n$ 均是完备的。

#### Bolzano 定理

内容：若点列 $\{\mathbf{x}_k\} \subset \mathbb{R}^n$ 有界，则其有收敛子列。

证明：

- 记 $\mathbf{x}_k = (x_k^{(i)})_{i = 1}^n$，则 $\{x_k^{(1)}\} \subset \mathbb{R}$ 有界，由 $\mathbb{R}$ 上的 Bolzano 定理可知其有收敛子列（不妨设为其本身）。
- 同理，$\{x_k^{(2)}\} \subset \mathbb{R}$ 有界，由 $\mathbb{R}$ 上的 Bolzano 定理可知其有收敛子列（仍不妨设为其本身）。
- 依此类推，得到逐维收敛的点列 $\{\mathbf{x}_k\}$，则点列自身也收敛，此即所求之收敛子列。

#### 聚点原理

内容：设 $E \subset \mathbb{R}^n$ 为有界无穷集，则其有聚点。

证明：仿照 $\mathbb{R}$ 上的情形构造闭超立方体套与 $E$ 之交即可。

#### 定义：开覆盖

称开集族 $\{G_{\lambda}\}_{\lambda \in \Lambda}$ 为 $E$ 的一个开覆盖，若 $E \subset \displaystyle\bigcup_{\lambda \in \Lambda} G_{\lambda}$。

#### Heine-Borel 定理

内容：设 $\varnothing \subsetneq E \subset \mathbb{R}^n$ 为有界闭集，$\{G_{\lambda}\}_{\lambda \in \Lambda}$ 为 $E$ 的一个开覆盖，则 $\exists$ 有限集 $I \subset \Lambda$，$\text{s.t. } \{G_{\lambda}\}_{\lambda \in I}$ 为 $E$ 的一个有限子覆盖。

证明：

- 考虑反证法，假设 $E$ 没有有限子覆盖。
- 构造闭超立方体套 $\{F_k\}$ 与 $E$ 之交，可以说明 $\forall k \in \mathbb{N}_+, F_k \cap E$ 没有有限子覆盖。
- 由开覆盖可知 $\exists \lambda_0 \in \Lambda, \text{s.t. } \mathbf{x}_0 \in G_{\lambda_0}$，由 $G_{\lambda}$ 为开集可知 $\exists r > 0, \text{s.t. } B_r(\mathbf{x}_0) \subset G_{\lambda_0}$。
- 取闭超立方体套中直径 $< r$ 的闭超立方体 $F_{k_0}$，可见 $F_{k_0} \cap E \subset B_r(\mathbf{x}_0) \subset G_{\lambda_0}$，则其有有限子覆盖 $\{G_{\lambda_0}\}$，矛盾！
- 故假设不成立，则 $E$ 有有限子覆盖。

#### 定义：紧集

称 $\varnothing \subsetneq E \subset \mathbb{R}^n$ 为紧集，若 $E$ 的每个开覆盖都有有限子覆盖。

#### 定理：紧集的充要条件

内容：设 $\varnothing \subsetneq E \subset \mathbb{R}^n$，则 $E$ 为紧集当且仅当 $E$ 为有界闭集。

证明：

- (1) $\Leftarrow$：此即 Heine-Borel 定理。
- (2) $\Rightarrow$：首先构造开覆盖 $\{B_n(\mathbf{0})\}_{n \in \mathbb{N}_+}$，取其有限子覆盖即得 $E$ 有界。
- 假设 $\exists E \not\ni \mathbf{x}_0 \in E'$，考虑 $E$ 的开覆盖 $\{B_{\frac{||\mathbf{x} - \mathbf{x}_0||}{2}}(\mathbf{x}) \mid \mathbf{x} \in E\}$，取其有限子覆盖 $\{B_{\frac{||\mathbf{x_i} - \mathbf{x}_0||}{2}}(\mathbf{x}_i) \mid 1 \leq i \leq m\}$。
- 令 $r = \dfrac{1}{2} \displaystyle\min_{i = 1}^m ||\mathbf{x}_i - \mathbf{x}_0||$，则 $B_r(\mathbf{x}_0) \cap \left( \displaystyle\bigcup_{i = 1}^m B_{\frac{||\mathbf{x_i} - \mathbf{x}_0||}{2}}(\mathbf{x}_i) \right) = \varnothing$。
- 由 $\mathbf{x}_0 \in E'$ 可知 $B_r(\mathbf{x}_0) \cap E \neq \varnothing$，则 $\displaystyle\bigcup_{i = 1}^m B_{\frac{||\mathbf{x_i} - \mathbf{x}_0||}{2}}(\mathbf{x}_i) \subset E$ 不成立，与开覆盖的性质矛盾！
- 故假设不成立，则 $E' \subset E$，即 $E$ 为闭集。

[例 1] 设 $\varnothing \subsetneq F \subset E \subset \mathbb{R}^n$，$F$ 为紧集，$E$ 为开集，求证：$\exists$ 开集 $O \subset \mathbb{R}^n$，$\text{s.t. } F \subset O \subset \bar{O} \subset E$。

证明：

- 由 $E$ 为开集可知 $\forall \mathbf{x} \in F, \exists r_{\mathbf{x}} > 0, \text{s.t. } B_{r_{\mathbf{x}}}(\mathbf{x}) \subset E$。
- 则 $F \subset \displaystyle\bigcup_{\mathbf{x} \in F} B_{\frac{r_{\mathbf{x}}}{2}}(\mathbf{x}) \subset \bigcup_{\mathbf{x} \in F} B_{r_{\mathbf{x}}}(\mathbf{x}) \subset E$。
- 由 $F$ 为紧集可知 $\exists \mathbf{x}_1, \cdots, \mathbf{x}_m \in F, \text{s.t. } F \subset O \triangleq \displaystyle\bigcup_{i = 1}^m B_{\frac{r_{\mathbf{x}_i}}{2}}(\mathbf{x}_i)$。
- 故 $F \subset O \subset \bar{O} = \displaystyle\bigcup_{i = 1}^m \overline{B_{\frac{r_{\mathbf{x}_i}}{2}}(\mathbf{x}_i)} \subset \bigcup_{i = 1}^m B_{r_{\mathbf{x}_i}}(\mathbf{x}_i) \subset E$，即所构造的 $O$ 满足条件。

这里最后一步用到了**有限个集合的并的闭包等于闭包的并**，证明只需双向包含。

### 多元函数及其极限

#### 定义：多元函数

设 $X \subset \mathbb{R}^n, Y \subset \mathbb{R}$，称 $f : X \to Y$ 为 $X$ 上的一个 $n$ 元函数。

#### 定义：等高值集

对于固定的 $c \in \mathbb{R}$，当 $C = \{(x, y) \in \mathbb{R}^2 \mid f(x, y) = c\}$ 非空，称之为 $z = f(x, y)$ 的一个等高值集。

特别地，若 $C$ 为一条或若干条平面曲线，称之为 $z = f(x, y)$ 的等位 / 高线。

可见 $C$ 为 $\{(x, y, c) \in \mathbb{R}^3 \mid c = f(x, y)\}$ 在 $xOy$ 平面上的投影。

[例 2] $f(x, y) = \sqrt{1 - x^2 - y^2}$ 的等高线族为 $1 - x^2 - y^2 = c, c \in (0, 1)$。

#### 定义：多元函数的极限

设 $E \subset \mathbb{R}^n$，$f : E \to \mathbb{R}$，$\mathbf{x}_0$ 为 $E$ 的一个聚点，若 $I \in \mathbb{R}$ 使得：

$$
\forall \epsilon > 0, \exists r > 0, \text{s.t. } f(\mathring{B}_r(\mathbf{x}_0)) \subset \check{U}(I, \epsilon)
$$

则称 $\mathbf{x}$ 趋于 $\mathbf{x}_0$ 时，$f(\mathbf{x})$ 以 $I$ 为极限，记作 $\displaystyle\lim_{E \ni \mathbf{x} \to \mathbf{x}_0} f(\mathbf{x}) = I$。一般可以省略这里的 $E \ni$，默认在函数定义域内进行。

这里将球形邻域换成矩形邻域是等价的。

同样可以仿照一元函数的极限，定义某些维度趋于 $+\infty$ 时的极限。

- 上述定义扩展了一元函数的极限的定义，例如：
- (I) $\displaystyle\lim_{x \to 0} \frac{\sin (x \sin \frac{1}{x})}{x \sin \frac{1}{x}}$ 没有意义，因为该函数并不在 $0$ 的某个邻域内每一点都有定义。
- (II) 但 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{\sin ((x^2 + y^2) \sin \frac{1}{x^2 + y^2})}{(x^2 + y^2) \sin \frac{1}{x^2 + y^2}} = 1$ 成立，因为 $(0, 0)$ 是该函数的定义域的一个聚点。

和差积商的极限等于极限的和差积商、夹挤原理等对多元函数的极限仍然适用。

[例 3] 求 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{x^2 y}{x^2 + y^2}$。

解：由于 $0 \leq \left| \dfrac{x^2 y}{x^2 + y^2} \right| = \dfrac{x^2}{x^2 + y^2} \cdot |y| \leq |y|$，且 $\displaystyle\lim_{(x, y) \to (0, 0)} |y| = 0$，据夹挤原理即得 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{x^2 y}{x^2 + y^2} = 0$。

[例 4] 讨论 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{xy}{x^2 + y^2}$ 的存在性。

解：

- 设 $k \neq 0$，考虑 $y = kx$ 的**路径**，有：

$$
\lim_{\substack{(x, y) \to (0, 0) \\ y = kx}} \frac{xy}{x^2 + y^2} = \lim_{\substack{(x, y) \to (0, 0) \\ y = kx}} \frac{k}{k^2 + 1} = \frac{k}{k^2 + 1}
$$

- 故结果依赖于 $k$，则原极限不存在。

[例 5] 求 $I = \displaystyle\lim_{\substack{x \to \infty \\ y \to a}} \left( 1 + \frac{1}{x} \right)^{\frac{x^2}{x + y}} \ (a \in \mathbb{R})$。

解：

$$
\begin{aligned}
I &= \lim_{\substack{x \to \infty \\ y \to a}} \left( 1 + \frac{1}{x} \right)^{\frac{x^2}{x + y}} \\
&= \left( \lim_{\substack{x \to \infty \\ y \to a}} \left( 1 + \frac{1}{x} \right)^x \right)^{\lim_{\substack{x \to \infty \\ y \to a}} \frac{x}{x + y}} \\
&= e^1 \\
&= e
\end{aligned}
$$

[例 6] 求 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{\sin(x^3 + y^3)}{x^2 + y^2}$。

解：由于 $0 \leq \left| \dfrac{\sin(x^3 + y^3)}{x^2 + y^2} \right| \leq \dfrac{|x^3 + y^3|}{x^2 + y^2} \leq \dfrac{|x|^3 + |y|^3}{x^2 + y^2} \leq |x| + |y|$，且 $\displaystyle\lim_{(x, y) \to (0, 0)} (|x| + |y|) = 0$，据夹挤原理即得 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{\sin(x^3 + y^3)}{x^2 + y^2} = 0$。

[例 7] 讨论 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{\sin(x^3 + y^3)}{x^2 + y}$ 的存在性。

解：

- 一方面，$\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ y = -x^2 + x^3}} \frac{\sin(x^3 + y^3)}{x^2 + y^2} = \lim_{x \to 0} \frac{x^3 + (-x^2 + x^3)^3}{x^3} = 1$。
- 另一方面，$\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ y = -x^2 + x}} \frac{\sin(x^3 + y^3)}{x^2 + y^2} = \lim_{x \to 0} \frac{x^3 + (-x^2 + x)^3}{x} = 0$。
- 故两条路径 $y = -x^2 + x^3, y = -x^2 + x$ 给出的极限不同，则原极限不存在。

#### 命题：无交并的极限

内容：设 $E \subset \mathbb{R}^n$，$f : E \to \mathbb{R}$，$\mathbf{x}_0$ 为 $E$ 的一个聚点，若 $\exists r > 0, \text{s.t. } E \cap \mathring{B}_r(\mathbf{x}_0) = E_1 \cup E_2, E_1 \cap E_2 = \varnothing$，且 $\displaystyle\lim_{E_1 \ni \mathbf{x} \to \mathbf{x}_0} f(\mathbf{x}) = \lim_{E_2 \ni \mathbf{x} \to \mathbf{x}_0} f(\mathbf{x}) = I$，则 $\displaystyle\lim_{E \ni \mathbf{x} \to \mathbf{x}_0} f(\mathbf{x}) = I$。

#### 定义：二元函数的累次极限

设 $f$ 在 $\mathring{N}_r(x_0, y_0)$ 上有定义，若 $\forall y \in \check{U}(y_0, r), \displaystyle\lim_{x \to x_0} f(x, y) = \varphi(y)$ 存在，且 $\displaystyle\lim_{y \to y_0} \varphi(y) = A$ 存在，则称 $I$ 为 $f$ 的一个累次极限，记作：

$$
\lim_{y \to y_0} \lim_{x \to x_0} f(x, y) = \lim_{y \to y_0} \varphi(y) = A
$$

同理可以定义另一顺序的累次极限：

$$
\lim_{x \to x_0} \lim_{y \to y_0} f(x, y) = \lim_{x \to x_0} \psi(x) = B
$$

并可以定义单侧的累次极限，不再赘述。

[例 8] 讨论 $\displaystyle\lim_{y \to 0} \lim_{x \to 0} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2}, \lim_{x \to 0} \lim_{y \to 0} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2}, \lim_{(x, y) \to (0, 0)} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2}$ 的存在性。

解：

- (1) $\displaystyle\lim_{y \to 0} \lim_{x \to 0} \frac{x^2 y^2}{x^2 y^2 + (x-y)^2} = \lim_{y \to 0} 0 = 0$。
- (2) $\displaystyle\lim_{x \to 0} \lim_{y \to 0} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2} = \lim_{x \to 0} 0 = 0$。
- (3) 一方面，$\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ x = 0}} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2} = 0$。
- 另一方面，$\displaystyle\lim_{\substack{(x, y) \to (0, 0) \\ y = x}} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2} = 1$。
- 故两条路径 $x = 0, y = x$ 给出的极限不同，则 $\displaystyle\lim_{(x, y) \to (0, 0)} \frac{x^2 y^2}{x^2 y^2 + (x - y)^2}$ 不存在。

[例 9] $\forall x \in \mathbb{R}, \displaystyle\lim_{y \to 0} (x + y) \sin \frac{1}{x} \sin \frac{1}{y}$ 不存在，但 $\displaystyle\lim_{(x, y) \to (0, 0)} (x + y) \sin \frac{1}{x} \sin \frac{1}{y} = 0$。

由上二例可见多元函数的极限和累次极限并非等价。但在满足某些额外条件的情况下，我们仍可以说明两者相等。

#### 定理：极限与累次极限相等的充分条件

内容：设 $f$ 在 $\mathring{N}_r(x_0, y_0)$ 上有定义，若 $\displaystyle\lim_{(x, y) \to (x_0, y_0)} f(x, y) = A \in \bar{\mathbb{R}}$，则：

- (1) 若 $\forall y \in \check{U}(y_0, r), \displaystyle\lim_{x \to x_0} f(x, y) = \varphi(y)$，则 $\displaystyle\lim_{y \to y_0} \lim_{x \to x_0} f(x, y) = \lim_{y \to y_0} \varphi(y) = A$。
- (2) 若 $\forall x \in \check{U}(x_0, r), \displaystyle\lim_{y \to y_0} f(x, y) = \psi(x)$，则 $\displaystyle\lim_{x \to x_0} \lim_{y \to y_0} f(x, y) = \lim_{x \to x_0} \psi(x) = A$。

推论：

- 累次极限可交换顺序的充分条件：若 $\displaystyle\lim_{(x, y) \to (x_0, y_0)} f(x, y)$、$\forall y \in \check{U}(y_0, r), \displaystyle\lim_{x \to x_0} f(x, y)$ 和 $\forall x \in \check{U}(x_0, r), \displaystyle\lim_{y \to y_0} f(x, y)$ 均存在，则：

$$
\displaystyle\displaystyle\lim_{(x, y) \to (x_0, y_0)} f(x, y) = \lim_{y \to y_0} \lim_{x \to x_0} f(x, y) = \lim_{x \to x_0} \lim_{y \to y_0} f(x, y)
$$

上述定理也可用于证明极限不存在，即：若两个累次极限存在但不相等，则极限不存在。

### 向量值函数及其极限

#### 定义：向量值函数

设 $X \subset \mathbb{R}^n, Y \subset \mathbb{R}^m \ (m > 1)$，称 $\mathbf{f} : X \to Y$ 为 $X$ 上的一个 $n$ 元 $m$ 维向量值函数。

#### 定义：向量值函数的极限

设 $E \subset \mathbb{R}^n$，$\mathbf{f} : E \to \mathbb{R}^m$，$\mathbf{x}_0$ 为 $E$ 的一个聚点，若 $\mathbf{y} \in \mathbb{R}^m$ 使得：

$$
\forall \epsilon > 0, \exists r > 0, \text{s.t. } \mathbf{f}(\mathring{B}_r(\mathbf{x}_0)) \subset B_{\epsilon}(\mathbf{y})
$$

则称 $\mathbf{x}$ 趋于 $\mathbf{x}_0$ 时，$\mathbf{f}(\mathbf{x})$ 以 $\mathbf{y}$ 为极限，记作 $\displaystyle\lim_{E \ni \mathbf{x} \to \mathbf{x}_0} \mathbf{f}(\mathbf{x}) = \mathbf{y}$。

其余说明同多元函数的极限，不再赘述。

#### 命题：向量值函数极限存在 $\Leftrightarrow$ 逐维极限存在

内容：设 $\mathbf{f}$ 在 $E \subset \mathbb{R}^n$ 上有定义，$\mathbf{f}(\mathbf{x}) = (f_i(\mathbf{x}))_{i = 1}^m \in \mathbb{R}^m$，$\mathbf{x}_0$ 为 $E$ 的一个聚点，则 $\displaystyle\lim_{\mathbf{x} \to \mathbf{x}_0} \mathbf{f}(\mathbf{x}) = \mathbf{a} = (a_i)_{i = 1}^m$ 当且仅当 $\forall 1 \leq i \leq m, \displaystyle\lim_{\mathbf{x} \to \mathbf{x}_0} f_i(\mathbf{x}) = a_i$。

[例 10] 设 $D = \{(r, \theta) \mid r > 0, \theta \in [0, 2 \pi)\}, r_0 > 0$，令 $\mathbf{f}(r, \theta) = (r \cos \theta, r \sin \theta)$，求 $\displaystyle\lim_{D \ni (r, \theta) \to (r_0, 0^+)} \mathbf{f}(r, \theta), \lim_{D \ni (r, \theta) \to (r_0, 2 \pi^-)} \mathbf{f}(r, \theta)$。

解：$\displaystyle\lim_{D \ni (r, \theta) \to (r_0, 0^+)} \mathbf{f}(r, \theta) = \lim_{D \ni (r, \theta) \to (r_0, 2 \pi^-)} \mathbf{f}(r, \theta) = (r_0, 0)$。

#### 定理：复合向量值函数的极限

内容：设 $X \subset \mathbb{R}^n, Y \subset \mathbb{R}^m$，$\mathbf{f} : Y \to \mathbb{R}^k$，$\mathbf{g} : X \to \mathbb{R}^m$，$\mathbf{x}_0, \mathbf{t}_0$ 分别为 $X, Y$ 的聚点，若 $\exists r_1, r_2 > 0, \text{s.t. } \mathbf{g} \left( \mathring{B}_{r_1}(\mathbf{x}_0) \cap X \right) \subset \mathring{B}_{r_2}(\mathbf{t}_0) \cap Y$，且 $\displaystyle\lim_{X \ni \mathbf{x} \to \mathbf{x}_0} \mathbf{g}(\mathbf{x}) = \mathbf{t}_0, \lim_{Y \ni \mathbf{t} \to \mathbf{t}_0} \mathbf{f}(\mathbf{t}) = \mathbf{a}$，则 $\displaystyle\lim_{X \ni \mathbf{x} \to \mathbf{x}_0} \mathbf{f}(\mathbf{g}(\mathbf{x})) = \mathbf{a}$。

### 多元函数的连续性

#### 定义：多元函数的连续

设 $E \subset \mathbb{R}^n$，$f : E \to \mathbb{R}$，$\mathbf{x}_0 \in E$，称 $f$ 在 $\mathbf{x}_0$ 处连续，若 $\displaystyle\lim_{E \ni \mathbf{x} \to \mathbf{x}_0} f(\mathbf{x}) = f(\mathbf{x}_0)$。

注意到这里使用了极限，则该定义自然地要求 $\mathbf{x}_0$ 为 $E$ 的聚点，进而将定义域的孤立点排除在外。

若 $f$ 在 $E$ 上的每一点都连续，则称 $f$ 在 $E$ 上连续，记作 $f \in C(E)$。

[例 11] 讨论 $f(x, y) = \begin{cases} x \sin \dfrac{1}{y}, &\quad xy \neq 0 \\ 0, &\quad xy = 0 \end{cases}$ 在 $\mathbb{R}^2$ 中的连续性。

解：

- $f$ 在 $xy \neq 0$ 的区域中的连续性显然，下面讨论其在 $xy = 0$（即坐标轴）上的连续性。
- (1) 对于 $(0, y_0)$，由于 $0 \leq \left| x \sin \dfrac{1}{y} \right| \leq |x|$，且 $\displaystyle\lim_{(x, y) \to (0, y_0)} |x| = 0$，据夹挤原理即得 $\displaystyle\lim_{(x, y) \to (0, y_0)} f(x, y) = 0 = f(0, y_0)$，故 $f$ 在此处连续。
- (2) 对于 $(x_0, 0) \ (x_0 \neq 0)$，由于 $\displaystyle\lim_{\substack{(x, y) \to (x_0, 0) \\ x = x_0}} f(x, y) = \lim_{\substack{(x, y) \to (x_0, 0) \\ x = x_0}} x \sin \frac{1}{y}$ 不存在，则 $\displaystyle\lim_{(x, y) \to (x_0, 0)} f(x, y)$ 不存在，故 $f$ 在此处间断。
- 综上，$f$ 在 $\{(x, y) \mid y \neq 0\}$ 处连续。

#### 定义：多元函数的一致连续

设 $f \in C(E)$，称 $f$ 在 $E$ 上一致连续，若 $\forall \epsilon > 0, \exists r > 0, \text{s.t. } \forall \mathbf{x}_1, \mathbf{x}_2 \in E \land ||\mathbf{x}_1 - \mathbf{x}_2|| < r, |f(\mathbf{x}_1) - f(\mathbf{x}_2)| < \epsilon$。

#### 定义：多元函数的 Lipschitz 连续

设 $f \in C(E)$，称 $f$ 在 $E$ 上 Lipschitz 连续，若 $\exists L > 0, \text{s.t. } \forall \mathbf{x}_1, \mathbf{x}_2 \in E, |f(\mathbf{x}_1) - f(\mathbf{x}_2)| \leq L ||\mathbf{x}_1 - \mathbf{x}_2||$，其中 $L$ 称为 $f$ 在 $E$ 上的 Lipschitz 常数。

------

与极限一致，上述定义可以直接推广到向量值函数上，且“整体 $\Leftrightarrow$ 每维都”仍自然地成立。

### 连续函数的性质

#### 定理：连续函数保持道路连通性

内容：设 $E \subset \mathbb{R}^n$ 为道路连通集，$\mathbf{f} : E \to \mathbb{R}^m$ 在 $E$ 上连续，则 $\mathbf{f}(E) \subset \mathbb{R}^m$ 也为道路连通集。

证明：

- $\forall \mathbf{f}(\mathbf{x}_1), \mathbf{f}(\mathbf{x}_2) \in \mathbf{f}(E)$，由 $E$ 为道路连通集可知 $\exists \boldsymbol{\sigma} \in C[a, b], \text{s.t. } \boldsymbol{\sigma}(a) = \mathbf{x}_1, \boldsymbol{\sigma}(b) = \mathbf{x}_2, \boldsymbol{\sigma}([a, b]) \subset E$。
- 令 $\mathbf{g} = \mathbf{f} \circ \boldsymbol{\sigma} \in C[a, b]$，则 $\mathbf{g}(a) = \mathbf{f}(\mathbf{x}_1), \mathbf{g}(b) = \mathbf{f}(\mathbf{x}_2), \mathbf{g}([a, b]) \subset \mathbf{f}(E)$，故 $\mathbf{f}(E)$ 为道路连通集。

#### 定理：连续函数保持紧性

内容：设 $E \subset \mathbb{R}^n$ 为紧集，$\mathbf{f} : E \to \mathbb{R}^m$ 在 $E$ 上连续，则 $\mathbf{f}(E) \subset \mathbb{R}^m$ 也为紧集。

证明：

- (1) 有界性：若不然，$\mathbf{f}(E)$ 中有无界点列 $\{\mathbf{f}(\mathbf{x}_k)\}$，但由 $\{\mathbf{x}_k\} \subset E$ 可知其有收敛子列（不妨设为自身），设 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$，由连续性可知 $\displaystyle\lim_{k \to +\infty} \mathbf{f}(\mathbf{x}_k) = \mathbf{f}(\mathbf{x}_0)$，与无界矛盾。
- (2) 闭性：设 $\mathbf{y} = \displaystyle\lim_{k \to +\infty} \mathbf{f}(\mathbf{x}_k)$ 为 $\mathbf{f}(E)$ 的聚点，由 $\{\mathbf{x}_k\} \subset E$ 可知其有收敛子列（不妨设为自身），设 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_k = \mathbf{x}_0$，由连续性可知 $\mathbf{y} = \displaystyle\lim_{k \to +\infty} \mathbf{f}(\mathbf{x}_k) = \mathbf{f}(\mathbf{x}_0)$，故 $\mathbf{y} \in \mathbf{f}(E)$，则 $\mathbf{f}(E)$ 为闭集。
- 综上，$\mathbf{f}(E)$ 为紧集。

#### 推论：连续多元函数有界且有最值

内容：设 $E \subset \mathbb{R}^n$ 为紧集，$f \in C(E)$，则 $f$ 在 $E$ 上有界，且能取到最值。

证明：在上面的定理中取 $m = 1$ 即得 $f(E)$ 为有界闭集，再构造点列趋向确界即可。

[例 12] 设 $A \in M_n(\mathbb{R})$ 非奇异，求证：$\exists \lambda > 0, \text{s.t. } \forall \mathbf{x} \in \mathbb{R}^n, ||A \mathbf{x}|| \geq \lambda ||\mathbf{x}||$。

证明：

- $\mathbf{x} = \mathbf{0}$ 不等式恒成立，下面考虑 $\mathbf{x} \neq \mathbf{0}$ 的情况。
- 将 $\mathbf{x}$ 单位化为 $\mathbf{x}_0 = \dfrac{\mathbf{x}}{||\mathbf{x}||}$，则 $||A \mathbf{x}|| \geq \lambda ||\mathbf{x}|| \Leftrightarrow ||A \mathbf{x}|| \geq \lambda$。
- 设 $f(\mathbf{x}) = ||A \mathbf{x}||$，则 $f$ 在 $\mathbb{R} \supset \partial B_1(\mathbf{0})$（即 $n$ 维欧氏单位球面）上连续。
- 故 $f$ 在 $\partial B_1(\mathbf{0})$ 上取得最小值为 $f(\mathbf{x}_0)$，令之为 $\lambda$。
- 由 $A$ 非奇异可知 $A \mathbf{x}_0 \neq \mathbf{0}$，则 $\lambda > 0$，遂得证。

#### 介值定理

内容：设 $E \subset \mathbb{R}^n$ 为紧集，$f \in C(E)$，且 $\exists \mathbf{x}_1, \mathbf{x}_2 \in E, \text{s.t. } f(\mathbf{x}_1) < \mu < f(\mathbf{x}_2)$，则 $\exists \boldsymbol{\xi} \in E, \text{s.t. } f(\boldsymbol{\xi}) = \mu$。

证明：

- 由 $E$ 为道路连通集可知 $\exists \boldsymbol{\sigma} \in C[a, b], \text{s.t. } \boldsymbol{\sigma}(a) = \mathbf{x}_1, \boldsymbol{\sigma}(b) = \mathbf{x}_2, \boldsymbol{\sigma}([a, b]) \subset E$。
- 令 $g = \mathbf{f} \circ \boldsymbol{\sigma} \in C[a, b]$，则 $g(a) = \mathbf{f}(\mathbf{x}_1), g = \mathbf{f}(\mathbf{x}_2)$。
- 由一元连续函数的介值定理可知 $\exists \eta \in (a, b), \text{s.t. } g(\eta) = \mu$，令 $\boldsymbol{\xi} = \boldsymbol{\sigma}(\eta)$ 即可。

#### 定理：连续函数在紧集上一致连续

内容：设 $E \subset \mathbb{R}^n$ 为紧集，$f \in C(E)$，则 $f$ 在 $E$ 上一致连续。

证明：

- $\forall \epsilon > 0$，由 $f \in C(E)$ 可知 $\forall \mathbf{x} \in E, \exists r_{\mathbf{x}} > 0, \text{s.t. } \forall \mathbf{x}' \in E \land ||\mathbf{x}' - \mathbf{x}|| < r_x, |f(\mathbf{x}') - f(\mathbf{x})| < \dfrac{\epsilon}{2}$。
- 构造 $E$ 的开覆盖 $\{B_{\frac{r_{\mathbf{x}}}{2}}(\mathbf{x})\}_{\mathbf{x} \in E}$，由 $E$ 为紧集可取其有限子覆盖 $\{B_{\frac{r_{\mathbf{x}_i}}{2}}(\mathbf{x}_i)\}_{i = 1}^m$。
- 令 $r = \displaystyle\min_{i = 1}^m \frac{r_{\mathbf{x}_i}}{2} > 0$，则 $\forall \mathbf{x}, \mathbf{x}' \in E \land ||\mathbf{x} - \mathbf{x}'|| < r$，设 $\mathbf{x} \in B_{\frac{r_{\mathbf{x}_i}}{2}}(\mathbf{x}_i)$，则 $||\mathbf{x}' - \mathbf{x}_i|| \leq ||\mathbf{x}' - \mathbf{x}|| + ||\mathbf{x} - \mathbf{x}_i|| < r + \dfrac{r_{\mathbf{x}_i}}{2} \leq r_{\mathbf{x}_i}$。
- 因此 $|f(\mathbf{x}_1) - f(\mathbf{x}_2)| < \epsilon$，故 $f$ 在 $E$ 上一致连续。

#### 定理：连续函数复合仍为连续函数

内容：设 $S \subset \mathbb{R}^n$，若 $\mathbf{f} : S \to \mathbb{R}^m, \mathbf{g} : T \to \mathbb{R}^k$ 在定义域上连续，$S \subset \mathbb{R}^n, \mathbf{f}(S) \subset T \subset \mathbb{R}^m$，则 $\mathbf{g} \circ \mathbf{f} : S \to \mathbb{R}^k$ 连续。

### 同胚映射

#### 定义：同胚映射

设 $E \subset \mathbb{R}^n$，$F \subset \mathbb{R}^m$，称 $f : E \to F$ 为从 $E$ 到 $F$ 的同胚映射，若 $f$ 为双射，且 $f \in C(E)$，$f^{-1} \in C(F)$。

[例 13] 求证：不存在从 $\mathbb{R}$ 到 $\mathbb{R}^2$ 的同胚映射。

证明：假设 $f : \mathbb{R} \to \mathbb{R}^2$ 为同胚映射，由于 $\mathbb{R} \backslash \{0\}$ 非道路连通，但 $\mathbb{R}^2 \backslash \{f(0)\}$ 道路连通，故 $f^{-1} \not\in C(\mathbb{R}^2 \backslash \{f(0)\})$，与 $f^{-1} \in C(\mathbb{R}^2)$ 矛盾！

[例 14] 求证：例 10 中定义的映射 $\mathbf{f}$ 为从 $D$ 到 $\Omega = \mathbb{R}^2 \backslash \{(0, 0)\}$ 的同胚映射。

证明：

- 首先证明 $\mathbf{f}$ 为双射，随后写出 $(r, \theta) = \mathbf{f}^{-1}(x, y)$：

$$
r = \sqrt{x^2 + y^2}, \theta = \begin{cases}
\arctan \dfrac{y}{x}, &\quad x > 0 \\
\dfrac{\pi}{2}, &\quad x = 0, y > 0 \\
\dfrac{3 \pi}{2}, &\quad x = 0, y < 0 \\
\arctan \dfrac{y}{x} + \pi, &\quad x < 0
\end{cases}
$$

- 再证明 $\mathbf{f}^{-1}$ 为双射即可。

#### 定理：紧集间的连续单射的逆映射连续

内容：设 $E \subset \mathbb{R}^n$ 为紧集，$\mathbf{f} : E \to \mathbb{R}^m$ 为连续单射，则其逆映射 $\mathbf{f}^{-1} : \mathbf{f}(E) \to E$ 连续。

证明：

- 假设 $\mathbf{f}^{-1}$ 在 $\mathbf{f}(E)$ 上不处处连续，则 $\exists \epsilon_0 > 0, \text{s.t. } \forall r > 0, \exists \mathbf{x}_1, \mathbf{x}_2 \in E \land ||\mathbf{f}(\mathbf{x}_1) - \mathbf{f}(\mathbf{x}_2)|| < r, ||\mathbf{x}_1 - \mathbf{x}_2|| \geq \epsilon_0$。
- 构造点列 $\{\mathbf{x}_{1, k}\}, \{\mathbf{x}_{2, k}\} \subset E$，使点对 $\mathbf{x}_{1, k}, \mathbf{x}_{2, k}$ 满足 $r = \dfrac{1}{k}$ 对应的条件。
- 由 $E$ 为紧集，可取 $\{\mathbf{x}_{1, k}\}$ 的一个收敛子列并只保留 $\{\mathbf{x}_{2, k}\}$ 中对应下标的项（不妨设为其自身），再对 $\{\mathbf{x}_{2, k}\}$ 做同样的操作。
- 设此时 $\displaystyle\lim_{k \to +\infty} \mathbf{x}_{1, k} = \mathbf{a}, \lim_{k \to +\infty} \mathbf{x}_{2, k} = \mathbf{b}$，则对两个不等式取极限 $k \to +\infty$ 可知 $||\mathbf{f}(\mathbf{a}) - \mathbf{f}(\mathbf{b})|| = 0, ||\mathbf{a} - \mathbf{b}|| \geq \epsilon_0$。
- 故 $\mathbf{a} \neq \mathbf{b}$ 但 $f(\mathbf{a}) = f(\mathbf{b})$，与 $\mathbf{f}$ 为单射矛盾！因此假设不成立，即 $\mathbf{f}^{-1}$ 在 $\mathbf{f}(E)$ 上连续。

#### 定义：压缩映射

设 $\varnothing \subsetneq \Omega \subset \mathbb{R}^n$，称 $\mathbf{f} : \Omega \to \mathbb{R}^n$ 为 $\Omega$ 上的压缩映射，若：

- (1) $\mathbf{f}(\Omega) \subset \Omega$。
- (2) $\mathbf{f}$ 在 $\Omega$ 上 Lipschitz 连续，且具有 $< 1$ 的 Lipschitz 常数。

#### 压缩映射原理

内容：设 $\Omega \subset \mathbb{R}^n$ 为闭集，$\mathbf{f}$ 为 $\Omega$ 上的压缩映射，则 $\exists ! \mathbf{x}_0 \in \Omega, \text{s.t. } \mathbf{f}(\mathbf{x}_0) = \mathbf{x}_0$。

证明：

- (1) 存在性：构造迭代列，通过 Cauchy 收敛原理说明其收敛。
- (2) 唯一性：反证法。
