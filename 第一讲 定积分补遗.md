- _部分内容参见上学期的笔记：[第十八讲](https://github.com/Leasier/Mathematical-Analysis-1/blob/main/%E7%AC%AC%E5%8D%81%E5%85%AB%E8%AE%B2%20%E5%AE%9A%E7%A7%AF%E5%88%86%E4%B8%8E%E5%8F%AF%E7%A7%AF%E6%9D%A1%E4%BB%B6.md_)、[第十九讲](https://github.com/Leasier/Mathematical-Analysis-1/blob/main/%E7%AC%AC%E5%8D%81%E4%B9%9D%E8%AE%B2%20%E5%AE%9A%E7%A7%AF%E5%88%86%E7%9A%84%E8%AE%A1%E7%AE%97%E4%B8%8E%E5%BA%94%E7%94%A8.md)，本文将顺着王老师的思路加以补充。_

### 定义：Riemann 积分

设 $f$ 在 $[a, b]$ 上有定义，

- 若 $\exists I \in \mathbb{R}, \text{s.t. }$
- $\forall \Delta : a = x_0 < \cdots < x_n = b$（称之为 $[a, b]$ 的一个**分割**），
- $\forall \{\xi_i \in [x_{i - 1}, x_i]\}_{i = 1}^n$（称之为 $\Delta$ 对应的一个**取点法**），
- 只要 $||\Delta|| / \lambda_{\Delta} = \displaystyle\max_{i = 1}^n \Delta x_i \to 0$（称之为 $\Delta$ 的**模**；其中 $\Delta x_i = x_i - x_{i - 1}$），
- 就有 Riemann 和 $S_f(\Delta, \xi) = \displaystyle\sum_{i = 1}^n f(\xi_i) \Delta x_i \to I$，

则称 $f$ 在 $[a, b]$ 上（Riemann）可积，记为 $f \in R[a, b]$，并称 $I$ 为 $f$ 在 $[a, b]$ 上的（Riemann）积分，记为：

$$
I = \int_a^b f(x) \text{d} x = \lim_{||\Delta|| \to 0} \sum_{i = 1}^n f(\xi_i) \Delta x_i
$$

#### 定义：细化与粗化

对于 $[a, b]$ 的一个分割 $\Delta$，若能向 $\Delta$ 中插入若干分点得到 $\Delta'$，则称 $\Delta'$ 为 $\Delta$ 的一个细化、$\Delta$ 为 $\Delta'$ 的一个粗化。

### 连续函数的可积性

#### 命题 1：Riemann 和的可粗化性

内容：设 $f \in C[a, b]$，任给 $[a, b]$ 的一个分割 $\Delta$、相应的取点法 $\xi$ 和其的一个粗化 $\Delta'$，总存在 $\Delta'$ 对应的取点法 $\xi'$，使得 $S_f(\Delta, \xi) = S_f(\Delta', \xi')$。

证明思路：考虑 $\Delta$ 中相邻分割点 $x_{i - 1}, x_i$ 和 $\Delta'$ 中在 $x_{i - 1}, x_i$ 之间的所有分割点，由连续函数的介质性即得。

#### 引理 1：分割充分细时，同一分割对应的 Riemann 和足够接近

内容：设 $f \in C[a, b]$，则 $\forall \epsilon > 0, \exists \delta > 0, \text{s.t. } \forall [a, b]$ 的分割 $\Delta$，只要 $||\Delta|| < \delta$，就有 $\displaystyle\sum_{i = 1}^n \omega_i \Delta x_i < \epsilon$，其中 $m_i = \displaystyle\min_{x \in [x_{i - 1}, x_i]} f(x), M_i = \max_{x \in [x_{i - 1}, x_i]} f(x), \omega_i = M_i - m_i$，称 $w_i$ 为 $f$ 在 $[x_{i - 1}, x_i]$ 上的振幅。

证明思路：$f$ 在**闭区间** $[a, b]$ 上连续 $\Rightarrow$ 一致连续，故通过控制 $||\Delta||$ 可以使得 $\omega_i$ 任意小。

#### 引理 2：分割充分细时，不同分割对应的 Riemann 和足够接近

内容：设 $f \in C[a, b]$，则 $\forall \epsilon > 0, \exists \delta > 0, \text{s.t. } \forall [a, b]$ 的分割 $\Delta_1, \Delta_2$，只要 $||\Delta_1||, ||\Delta_2|| < \delta$，就有 $\forall \Delta_1$ 对应的取点法 $\xi_1$ 和 $\Delta_2$ 对应的取点法 $\xi_2$，均满足 $|S_f(\Delta_1, \xi_1) - S_f(\Delta_2, \xi_2)| < \epsilon$。

证明思路：

- _Motivation：找一个中介将 $\Delta_1, \Delta_2$ 相关联。_
- 令 $\Delta = \Delta_1 \cup \Delta_2$，任取 $\Delta$ 对应的取点法 $\xi$。
- 由命题 1 可知：$\exists \Delta_1$ 对应的取点法 $\xi_1'$ 和 $\Delta_2$ 对应的取点法 $\xi_2'$，$\text{s.t. } S_f(\Delta, \xi) = S_f(\Delta_1, \xi_1') = S_f(\Delta_2, \xi_2')$。
- 对 $\dfrac{1}{2} \epsilon$ 应用引理 1 得到对应的 $\delta$，则有 $|S_f(\Delta_1, \xi_1) - S_f(\Delta_2, \xi_2)| \leq |S_f(\Delta_1, \xi_1) - S_f(\Delta_1, \xi_1')| + |S_f(\Delta_2, \xi_2) - S_f(\Delta_2, \xi_2')| < \epsilon$，遂得证。

#### 定理 1：连续函数的可积性

内容：$f \in C[a, b] \Rightarrow f \in R[a, b]$。

证明思路：

- _Motivation：仿照 Cauchy 收敛原理的证明，以一个“可以离目标任意近的好 Riemann 和”作为中介。_
- 构造一列 $[a, b]$ 的分割 $\{\Delta_n\}_{n = 1}^{+\infty}$，其中 $\Delta_n$ 选取了 $[a, b]$ 的 $n + 1$ 等分点作为分割点，则 $n \to +\infty, ||\Delta_n|| \to 0$。
- 任取 $\Delta_n$ 对应的取点法 $\xi_n$，则 $\{S_f(\Delta_n, \xi_n)\}$ 有界，进而存在收敛子列 $\{S_f(\Delta_{n_k}, \xi_{n_k})\}$，设其收敛到 $I$。
- $\forall \epsilon > 0$，控制引理 2 中的 $\delta$ 使得任意充分细 Riemann 和与 $S_f(\Delta_{n_k}, \xi_{n_k})$ 的误差小于 $\dfrac{1}{2} \epsilon$，再控制 $k$ 使得 $S_f(\Delta_{n_k}, \xi_{n_k})$ 与 $I$ 的误差小于 $\dfrac{1}{2} \epsilon$，旋即得证。
